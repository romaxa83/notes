#linux #vim

- Первоначальные настройки - https://losst.ru/nastrojka-vim
- Кастомизация - https://habr.com/ru/post/468265/#section3
- Полная информация - https://ru.wikibooks.org/wiki/Vim
- Инфа по полезным плагинам - https://rtfm.co.ua/vim-prevrashhaem-redaktor-v-ide-plaginy-i-vot-eto-vot-vsyo/
- http://najomi.org/vim
---
#### 🔹 Config

```bash
# конфиг файл
/etc/vimrc/vimrc.

#локальный находиться
~/.vimrc.
```
---
#### 🔹 ПОИСК

- `/`	- Перейти в режим поиска
- `enter` - искать
- `n`- перейти к следующему вхождению
------------------------------------------
#### 🔹 НАВИГАЦИЯ

- `0` («ноль») - в начало текущей строки;
- `^` - в начало текущей строки (к первому непробельному символу)
- `$` - в конец текущей строки
- `w` - переместить курсор к следущему слову (вправо)
- `W` — до пробела вправо
- `b` - переместить курсор к следущему слову (влево)
- `B` — до пробела влево
- `gg` - перейти на начало файла
- `G` — перейти в конец файла
- `<number>G` — перейти на конкретную строку `<number>`
- `<number>gg` — перейти на конкретную строку `<number>`

---
#### 🔹 ВСТАВКИ

- `i` - вставить текст слева от текущего
- `I` - вставить текст в начало строки
- `o` - создать новую строку под текущей
- `O` - создать новую строку над текущей
- `a` - вставить текст справа от текущего символа
- `A` - вставить текст в конец текущей строки

- `u` - отменить изменения
- `r` - заменить символ по курсором (наж "r" после символ который нужно вставить)

- `ZZ` - сохранить изменения
- `:w`

- `:wq` - сохранить и выйти

- `ZQ` - выйти без сохранения измененеий
- `:q!`
---
#### 🔹 ОКНА

- `ctrl + w + n` - создаст новый окно
- `ctrl + w + вверх,вниз` - перемещение между окнами
- `:split` - создаст сплит, тот же файл в новом окне
---
#### 🔹 КОМАНДЫ

- `:read !ls -la`  - Эта команда вставит в текущую позицию редактируемого текста весь вывод указанной после `!` команды (в нашем случае: `ls -la`).
---
#### 🔹 Плагины

Для Vim существует огромное количество различных плагинов и начиная с восьмой версии появилась своя система управления пакетами. Все пакеты должны 
располагаться в папке `~/.vim`, под-папке `pack/vendor/start` или `pack/vendor/opt`. Файлы из под-папки `start` загружаются автоматически при старте программы, и из `opt` надо подгружать вручную.

==Менеджер плагинов для vim== - https://github.com/junegunn/vim-plug
 При установки через `curl` создает папку `~/.vim` в файле `.vimrc` вводим команды
- `:source ~/.vimrc`  - считывает файл
- `:PlugInstall` - установит плагины

##### 🔸 NerdTree (панели выбора файлов)
- git clone https://github.com/preservim/nerdtree.git ~/.vim/pack/vendor/start/nerdtree
- vim -u NONE -c "helptags ~/.vim/pack/vendor/start/nerdtree/doc" -c q

`Ctrl+w` - переключение между панелями
`Shitf+i` - отображения скрытых файлов

на файле нажимаем `i` - открывает в доп. горизонтальном окне
на файле нажимаем `s` - открывает в доп. вертикальном окне

Что бы создать файл из `NERDTree` — вызываем меню клавишей `m`: 
- a — добавляем новый элемент, если это файл — то просто указываем имя, если директория — добавляем в конце обратный слеш «/«:


##### 🔸 ПЛАГИН ДЛЯ АВТОКОМПЛИТА

- https://github.com/ycm-core/YouCompleteMe	 	
##### 🔸Плагин для парных кавычек, скобок и т.д.

- https://github.com/jiangmiao/auto-pairs

##### 🔸 Выполнение команд в командной строке
plugin позволяет выполнять unix команды прямо в командной строке
- git clone https://tpope.io/vim/eunuch.git ~/.vim/pack/vendor/start/eunuch
- vim -u NONE -c "helptags ~/.vim/pack/vendor/start/eunuch/doc" -c q
##### 🔸Плагин для поиска файлов

- https://github.com/kien/ctrlp.vim

`ctrl + p` - начать поиск

##### 🔸 Nerd Fonts Logo
- https://github.com/ryanoasis/nerd-fonts/blob/master/readme_ru.md
---