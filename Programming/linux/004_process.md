#linux 

Процесс - это запущенная программа с выделенными ресурсами (память, CPU, файловые дескрипторы). Каждый процесс имеет уникальный идентификатор (PID)
```bash
# Структура процесса
PID     # Process ID - уникальный идентификатор
PPID    # Parent Process ID - родительский процесс
UID     # User ID - владелец процесса
GID     # Group ID - группа процесса
PGID    # Process Group ID - группа процессов
SID     # Session ID - идентификатор сессии
TTY     # Terminal - терминал процесса
```

#### 🔹 Состояния процессов

```bash
R       # Running (выполняется или готов к выполнению)
S       # Sleeping (ожидает события)
D       # Uninterruptible sleep (неприрывное ожидание I/O)
T       # Stopped (остановлен сигналом)
Z       # Zombie (завершился, но не убран родителем)
X       # Dead (мертвый, не должен отображаться)
```
---
#### 🔹 Команды для просмотра процессов

##### 🔸 ps - снимок процессов
```bash
# Базовое использование
ps                          # процессы текущего терминала
ps -e                       # все процессы
ps -f                       # полный формат
ps -ef                      # все процессы в полном формате
ps aux                      # BSD стиль - все процессы с подробностями
ps -ax | grep google        # найти процесс google

# Полезные комбинации
ps -eo pid,ppid,cmd,%mem,%cpu --sort=-%cpu    # сортировка по CPU
ps -eo pid,ppid,cmd,%mem,%cpu --sort=-%mem    # сортировка по памяти
ps -u username              # процессы конкретного пользователя
ps -C firefox               # процессы конкретной программы
ps --forest                 # показать дерево процессов
ps -L                       # показать потоки (threads)

# Расширенные форматы
ps -eo pid,ppid,pgid,sid,tty,user,comm,args
ps -eo pid,pcpu,pmem,time,comm --sort=-pcpu
ps -eo lstart,etime,pid,ppid,comm  # время запуска и работы
```

##### 🔸 top - интерактивный мониторинг
```bash
# Запуск top
top                         # стандартный режим
top -u username             # процессы пользователя
top -p PID1,PID2           # конкретные процессы
top -d 1                   # обновление каждую секунду

# Интерактивные команды в top
h или ?    # справка
q          # выход
k          # убить процесс (потребует PID)
r          # изменить nice процесса
M          # сортировка по памяти
P          # сортировка по CPU
T          # сортировка по времени
u          # показать процессы пользователя
1          # показать все CPU ядра
f          # выбрать поля для отображения
o          # изменить порядок полей
W          # сохранить конфигурацию
```

##### 🔸 htop - улучшенная версия top
```bash
# Установка htop
sudo apt install htop      # Ubuntu/Debian
sudo yum install htop      # CentOS/RHEL
sudo dnf install htop      # Fedora

# Возможности htop
# - Цветной вывод
# - Интерактивное управление мышью
# - Горизонтальная прокрутка
# - Дерево процессов
# - Фильтрация процессов

# Горячие клавиши htop
F1 - справка
F2 - настройки
F3 - поиск процесса
F4 - фильтр процессов
F5 - дерево процессов
F6 - сортировка
F9 - убить процесс
F10 - выход
t - дерево процессов
u - процессы пользователя
/ - поиск
```

##### 🔸 atop - системный монитор активности
```bash
# Установка atop
sudo apt install atop

# Использование
atop                        # текущая активность
atop -r /var/log/atop/atop_20240101  # анализ архивных данных
atop -b -r file            # batch режим

# Горячие клавиши atop
g - общая информация
p - процессы
m - память
d - диск
n - сеть
c - командная строка процессов
```
---
#### 🔹 Поиск и идентификация процессов
##### 🔸 pgrep и pidof
```bash
# pgrep - поиск процессов по шаблону
pgrep firefox               # PID процессов firefox
pgrep -u username           # процессы пользователя
pgrep -f "java.*tomcat"     # поиск по полной команде
pgrep -l firefox            # с именами процессов
pgrep -n firefox            # только новейший процесс
pgrep -o firefox            # только старейший процесс

# pidof - поиск PID по имени программы
pidof apache2
pidof sshd
pidof -s httpd              # только один PID
```

##### 🔸 pstree - дерево процессов
```bash
# Отображение дерева процессов
pstree                      # дерево всех процессов
pstree username             # процессы пользователя
pstree -p                   # с PID
pstree -a                   # с аргументами команд
pstree -u                   # с пользователями
pstree PID                  # дерево от конкретного процесса

# Примеры использования
pstree -p 1                 # дерево от init процесса
pstree -a -p -u $(pidof apache2 | cut -d' ' -f1)  # дерево Apache
```
---
#### 🔹 Управление процессами

##### 🔸 Сигналы процессов
```bash
# Основные сигналы
SIGTERM (15)  # Вежливое завершение (по умолчанию)
SIGKILL (9)   # Принудительное завершение
SIGSTOP (19)  # Приостановить процесс
SIGCONT (18)  # Возобновить процесс
SIGHUP (1)    # Перечитать конфигурацию
SIGINT (2)    # Прерывание (Ctrl+C)
SIGQUIT (3)   # Выход с дампом ядра

# Просмотр всех сигналов
kill -l
```

##### 🔸 kill - отправка сигналов
```bash
# Завершение процессов
kill PID                    # отправить SIGTERM
kill -9 PID                 # отправить SIGKILL
kill -KILL PID              # то же самое
kill -TERM PID              # отправить SIGTERM
kill -HUP PID               # перечитать конфигурацию

# Приостановка и возобновление
kill -STOP PID              # приостановить
kill -CONT PID              # возобновить

# Множественное завершение
kill PID1 PID2 PID3
kill -9 $(pgrep firefox)    # завершить все процессы firefox
```

##### 🔸 killall и pkill
```bash
# killall - завершение по имени программы
killall firefox             # завершить все firefox
killall -9 firefox          # принудительно завершить
killall -u username         # завершить все процессы пользователя
killall -v firefox          # подробный вывод

# pkill - завершение по шаблону
pkill firefox               # завершить процессы содержащие firefox
pkill -f "java.*tomcat"     # по полной командной строке
pkill -u username           # процессы пользователя
pkill -9 -f pattern         # принудительное завершение
```
---
#### 🔹 Управление приоритетами процессов

##### 🔸 nice и renice
```bash
# Запуск с измененным приоритетом
nice -n 10 command          # запустить с приоритетом 10
nice --adjustment=5 command # то же самое

# Изменение приоритета работающего процесса
renice 10 PID               # установить приоритет 10
renice -n 5 -p PID          # установить приоритет 5
renice +5 -u username       # увеличить приоритет на 5 для пользователя

# Диапазон приоритетов nice: -20 (высший) до +19 (низший)
# Отрицательные значения может устанавливать только root
```
---
#### 🔹 Фоновые задачи и управление заданиями

##### 🔸 Запуск в фоне
```bash
# Запуск команды в фоне
command &                   # запустить в фоне
nohup command &             # запустить в фоне, игнорируя HUP
nohup command > output.log 2>&1 &  # с перенаправлением вывода

# screen и tmux для долгих задач
screen -S session_name      # создать screen сессию
tmux new -s session_name    # создать tmux сессию
```

##### 🔸 Управление заданиями
```bash
# Просмотр заданий
jobs                        # активные задания
jobs -l                     # с PID

# Управление заданиями
bg                          # последнее задание в фон
bg %1                       # задание номер 1 в фон
fg                          # последнее задание на передний план
fg %1                       # задание номер 1 на передний план

# Остановка и возобновление
Ctrl+Z                      # остановить текущее задание
kill %1                     # завершить задание номер 1
disown %1                   # отвязать задание от терминала
```
---
#### 🔹 Мониторинг системных ресурсов
##### 🔸 Память
```bash
# free - использование памяти
free                        # в байтах
free -h                     # в читаемом формате
free -m                     # в мегабайтах
free -g                     # в гигабайтах
free -s 1                   # обновлять каждую секунду

# Подробная информация о памяти
cat /proc/meminfo
cat /proc/buddyinfo         # информация о buddy allocator
cat /proc/slabinfo          # информация о slab allocator

# vmstat - статистика виртуальной памяти
vmstat                      # текущее состояние
vmstat 1                    # обновление каждую секунду
vmstat 1 10                 # 10 замеров с интервалом 1 сек
vmstat -d                   # статистика дисков
vmstat -p /dev/sda1         # статистика раздела
```

##### 🔸 CPU
```bash
# Информация о процессоре
lscpu                       # подробная информация о CPU
cat /proc/cpuinfo           # информация о всех ядрах CPU
nproc                       # количество доступных ядер
lscpu | grep "CPU(s)"       # количество логических CPU

# Загрузка CPU
uptime                      # средняя загрузка
cat /proc/loadavg           # файл загрузки
sar -u 1 10                 # использование CPU (требует sysstat)

# mpstat - статистика по процессорам
mpstat                      # текущая статистика
mpstat 1                    # обновление каждую секунду
mpstat -P ALL               # для всех процессоров
```

##### 🔸 Диски и I/O
```bash
# iostat - статистика I/O
iostat                      # общая статистика
iostat 1                    # обновление каждую секунду
iostat -x                   # расширенная статистика
iostat -d                   # только диски
iostat -p                   # включая разделы

# iotop - мониторинг I/O по процессам
sudo iotop                  # интерактивный режим
sudo iotop -o               # только активные процессы
sudo iotop -a               # накопительные значения
sudo iotop -p PID           # конкретный процесс

# Использование дискового пространства
df -h                       # использование файловых систем
df -i                       # использование inode
du -h /path                 # размер каталога
lsblk                       # информация о блочных устройствах
```
---
