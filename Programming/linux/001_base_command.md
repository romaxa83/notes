#linux  #terminal

#### 🔹 Базовые операции с файлами и каталогами
##### 🔸 Навигация и просмотр
```bash
# Навигация  
pwd                     # текущая директория  
cd /path/to/directory   # переход в директорию  
cd ~                    # домашняя директория  
cd -                    # предыдущая директория  
cd ..                   # родительская директория  
cd ../..                # на два уровня вверх  
  
# Просмотр содержимого  
ls                      # список файлов  
ls -la                  # подробный список с скрытыми файлами  
ls -lh                  # с размерами в читаемом формате  
ls -lt                  # сортировка по времени изменения  
ls -lS                  # сортировка по размеру  
ls -R                   # рекурсивный просмотр  
ls *.txt                # файлы с определенным расширением  
ls -d */                # только каталоги
```

##### 🔸 Создание файлов и каталогов

```bash
# Создание файлов  
touch file.txt          # создать пустой файл  
touch file{1,2,3}.txt   # создать несколько файлов  
touch file{01..10}.txt  # создать нумерованные файлы  
  
# Создание каталогов  
mkdir directory         # создать каталог  
mkdir -p path/to/dir    # создать вложенные каталоги  
mkdir -p project/{src,docs,tests}  # создать структуру проекта
mkdir -p ~/scripts/{site-01,site-02}/{backup,monitoring,network}
```

##### 🔸 Копирование файлов

```bash
# Базовое копирование  
cp source.txt dest.txt              # копировать файл  
cp file.txt /path/to/destination/   # копировать в другую директорию  
cp -r directory/ /backup/           # рекурсивное копирование каталога  
  
# Полезные опции  
cp -i source.txt dest.txt           # с подтверждением перезаписи  
cp -u source.txt dest.txt           # только если источник новее  
cp -v source.txt dest.txt           # с подробным выводом  
cp -p source.txt dest.txt           # сохранить атрибуты  
cp -a directory/ backup/            # архивное копирование (все атрибуты)  
  
# Копирование нескольких файлов  
cp file1.txt file2.txt file3.txt /destination/  
cp *.txt /destination/  
cp {file1,file2,file3}.txt /destination/

cp -r api/* ~/project/api/camp # копировать все содержимое папки в другую папку
```

##### 🔸 Перемещение и переименование

```bash
# Перемещение файлов  
mv source.txt dest.txt              # переименовать файл  
mv file.txt /path/to/destination/   # переместить файл  
mv *.txt /documents/                # переместить все .txt файлы  
  
# Полезные опции  
mv -i source.txt dest.txt           # с подтверждением перезаписи  
mv -u source.txt dest.txt           # только если источник новее  
mv -v source.txt dest.txt           # с подробным выводом  
  
# Массовое переименование  
rename 's/old/new/' *.txt           # переименовать используя regex  
rename 's/\.jpeg$/.jpg/' *.jpeg     # изменить расширения
```

##### 🔸 Сравнение файлов

```bash
# Сравнение содержимого
diff file1.txt file2.txt    # различия между файлами
diff -u file1.txt file2.txt # унифицированный формат
cmp file1.txt file2.txt     # побайтовое сравнение
comm file1.txt file2.txt    # общие и уникальные строки (файлы должны быть отсортированы)
```
##### 🔸 Удаление файлов

```bash
# Удаление файлов  
rm file.txt                         # удалить файл  
rm -i file.txt                      # с подтверждением  
rm -f file.txt                      # принудительное удаление  
rm -v file.txt                      # с подробным выводом  
  
# Удаление каталогов  
rm -r directory/                    # рекурсивное удаление  
rm -rf directory/                   # принудительное рекурсивное удаление  
rmdir empty_directory/              # удалить пустой каталог  
  
# Безопасное удаление  
rm -i *.txt                         # с подтверждением для каждого файла  
find . -name "*.tmp" -delete        # найти и удалить временные файлы

rm -r *                             # удаление всего из папки(рекурсивно)
sudo rm -R / --no-preserve-root     # полностью убивает линукс
```

---
#### 🔹  Просмотр содержимого файлов

##### 🔸 Команды для просмотра

```bash
# Полный просмотр
cat file.txt                        # вывести весь файл
cat -n file.txt                     # с номерами строк
cat -b file.txt                     # нумеровать только непустые строки
cat -s file.txt                     # убрать лишние пустые строки

# Постраничный просмотр
less file.txt                       # интерактивный просмотр
more file.txt                       # простой постраничный просмотр

# Просмотр начала и конца
head file.txt                       # первые 10 строк
head -n 20 file.txt                 # первые 20 строк
head -c 100 file.txt                # первые 100 байт

tail file.txt                       # последние 10 строк
tail -n 20 file.txt                 # последние 20 строк
tail -f file.txt                    # отслеживать изменения в реальном времени
tail -F file.txt                 # следить за файлом, даже если он пересоздается
```
##### 🔸 Объединение и разделение файлов

```bash
# Объединение файлов
cat file1.txt file2.txt > combined.txt
cat *.txt > all_files.txt

# Разделение файлов
split -l 1000 largefile.txt part_    # разделить по 1000 строк
split -b 10M largefile.txt part_     # разделить по 10MB
split -n 5 largefile.txt part_       # разделить на 5 частей

# Объединение разделенных файлов
cat part_* > restored_file.txt
```

---
#### 🔹  Поиск файлов и содержимого

##### 🔸 Команда find

```bash
# Поиск по имени
find /path -name "filename.txt"
find /path -name "*.txt"
find /path -iname "*.TXT"           # без учета регистра
find /path -name "*config*"

# Поиск по типу
find /path -type f                  # только файлы
find /path -type d                  # только каталоги
find /path -type l                  # только символические ссылки

# Поиск по размеру
find /path -size +100M              # файлы больше 100MB
find /path -size -1k                # файлы меньше 1KB
find /path -size 50c                # файлы ровно 50 байт

# Поиск по времени
find /path -mtime -7                # изменены за последние 7 дней
find /path -mtime +30               # изменены более 30 дней назад
find /path -atime -1                # доступ за последний день
find /path -ctime +7                # статус изменен более 7 дней назад

# Поиск по правам доступа
find /path -perm 644                # точно 644
find /path -perm -644               # минимум 644
find /path -perm /644               # любое из этих прав

# Выполнение команд с найденными файлами
find /path -name "*.log" -delete
find /path -name "*.txt" -exec cp {} /backup/ \;
find /path -type f -exec chmod 644 {} \;
find /path -name "*.tmp" -exec rm -f {} +
```

##### 🔸 Поиск содержимого с grep

```bash
# Базовый поиск
grep "pattern" file.txt
grep -i "pattern" file.txt          # без учета регистра
grep -v "pattern" file.txt          # инвертировать поиск
grep -n "pattern" file.txt          # с номерами строк
grep -c "pattern" file.txt          # количество совпадений

# Поиск в нескольких файлах
grep "pattern" *.txt
grep -r "pattern" /path/            # рекурсивный поиск
grep -R "pattern" /path/            # рекурсивный с символическими ссылками

# Расширенные возможности
grep -A 3 "pattern" file.txt        # 3 строки после совпадения
grep -B 3 "pattern" file.txt        # 3 строки до совпадения
grep -C 3 "pattern" file.txt        # 3 строки до и после

# Регулярные выражения
grep "^pattern" file.txt            # начало строки
grep "pattern$" file.txt            # конец строки
grep "[0-9]" file.txt               # любая цифра
grep "\bword\b" file.txt            # целое слово
```

##### 🔸 Другие команды поиска

```bash
# locate (быстрый поиск)
locate filename.txt
sudo updatedb                       # обновить базу данных locate

# which (поиск исполняемых файлов)
which python
which -a python                     # все найденные

# whereis (поиск бинарников, источников, man-страниц)
whereis python

# type (информация о команде)
type ls
type -a python
```

---
#### 🔹 Права доступа и владельцы

##### 🔸 Просмотр и изменение прав

```bash
# Просмотр прав
ls -l file.txt
stat file.txt                       # подробная информация

# Изменение прав (символьный режим)
chmod u+x file.txt                  # добавить выполнение владельцу
chmod g-w file.txt                  # убрать запись группе
chmod o+r file.txt                  # добавить чтение остальным
chmod a+x file.txt                  # добавить выполнение всем
chmod u=rw,g=r,o= file.txt          # установить конкретные права

# Изменение прав (числовой режим)
chmod 644 file.txt                  # rw-r--r--
chmod 755 script.sh                 # rwxr-xr-x
chmod 600 private.txt               # rw-------
chmod 777 file.txt                  # rwxrwxrwx (не рекомендуется)

# Рекурсивное изменение прав
chmod -R 755 /path/to/directory/
find /path -type f -exec chmod 644 {} \;     # файлы
find /path -type d -exec chmod 755 {} \;     # каталоги
```

##### 🔸 Изменение владельца

```bash
# Изменение владельца
sudo chown user file.txt
sudo chown user:group file.txt
sudo chown :group file.txt          # только группу
sudo chown -R user:group /path/     # рекурсивно

# Изменение группы
sudo chgrp group file.txt
sudo chgrp -R group /path/
```

---
#### 🔹 Архивирование и сжатие

##### 🔸 Tar архивы

```bash
# Создание архивов
tar -cf archive.tar files/          # создать tar архив
tar -czf archive.tar.gz files/      # создать сжатый gzip архив
tar -cjf archive.tar.bz2 files/     # создать сжатый bzip2 архив
tar -cJf archive.tar.xz files/      # создать сжатый xz архив

# Просмотр содержимого
tar -tf archive.tar                 # список файлов
tar -tzf archive.tar.gz             # список файлов в сжатом архиве

# Извлечение архивов
tar -xf archive.tar                 # извлечь tar архив
tar -xzf archive.tar.gz             # извлечь gzip архив
tar -xjf archive.tar.bz2            # извлечь bzip2 архив
tar -xf archive.tar -C /path/       # извлечь в определенную папку

# Полезные опции
tar -czf backup.tar.gz --exclude="*.log" /home/user/
tar -xzf archive.tar.gz file.txt    # извлечь конкретный файл
tar -czf - /path/ | ssh user@server 'tar -xzf - -C /backup/'  # архивирование по SSH

# распаковка архива
tar -xvf my-archive.tar -C /home/myfolder
```

##### 🔸  Сжатие отдельных файлов

```bash
# gzip
gzip file.txt                       # создать file.txt.gz
gzip -d file.txt.gz                 # распаковать
gunzip file.txt.gz                  # альтернатива gzip -d
gzip -c file.txt > file.txt.gz      # сохранить оригинал

# bzip2
bzip2 file.txt                      # создать file.txt.bz2
bzip2 -d file.txt.bz2               # распаковать
bunzip2 file.txt.bz2                # альтернатива

# xz
xz file.txt                         # создать file.txt.xz
xz -d file.txt.xz                   # распаковать
unxz file.txt.xz                    # альтернатива

# zip
zip archive.zip file1.txt file2.txt
zip -r archive.zip directory/
unzip archive.zip
unzip -l archive.zip                # список содержимого
```

---
#### 🔹 Символические и жесткие ссылки

```bash
# Символические ссылки
ln -s /path/to/original /path/to/link     # создать символическую ссылку
ln -sf /path/to/new_target /path/to/link  # перезаписать ссылку
readlink /path/to/link                    # куда указывает ссылка
find /path -type l -ls                    # найти все символические ссылки

# Жесткие ссылки
ln /path/to/original /path/to/hardlink    # создать жесткую ссылку
find /path -samefile original.txt         # найти все жесткие ссылки
ls -li file.txt                           # показать inode номер
```

---
#### 🔹 Переменные окружения

```bash
# Просмотр переменных
env                         # все переменные окружения
printenv                    # то же самое
echo $PATH                  # конкретная переменная
echo $HOME                  # домашняя директория
echo $USER                  # текущий пользователь

# Установка переменных
export VAR_NAME="value"     # установить переменную
export PATH=$PATH:/new/path # добавить к PATH
unset VAR_NAME              # удалить переменную

# Постоянные переменные
~/.bashrc                   # для пользователя
~/.profile                  # для пользователя
/etc/environment            # системные переменные
```
---
#### 🔹 Архивирование и история команд

```bash
# История команд
history                     # история команд
history | grep command      # поиск в истории
!!                          # последняя команда
!n                          # команда номер n из истории
!string                     # последняя команда начинающаяся со string
Ctrl+R                      # обратный поиск в истории

# Управление историей
history -c                  # очистить историю сессии
history -w                  # записать историю в файл
export HISTSIZE=1000        # размер истории в памяти
export HISTFILESIZE=2000    # размер файла истории
```

---
#### 🔹 Перенаправление ввода/вывода

```bash
# Перенаправление вывода
command > file.txt          # перенаправить stdout в файл
command >> file.txt         # добавить в файл
command 2> error.log        # перенаправить stderr
command > output.txt 2>&1   # stdout и stderr в один файл
command &> all_output.txt   # краткая запись предыдущего

# Pipe (конвейер)
command1 | command2         # stdout первой команды в stdin второй
ps aux | grep firefox       # найти процессы firefox
ls -la | sort -k5 -n        # сортировать по размеру файла
```

---
#### 🔹 Полезные кейсы и комбинации команд

##### 🔸 Массовые операции с файлами

```bash
# Переименование файлов
for file in *.txt; do mv "$file" "${file%.txt}.bak"; done
find . -name "*.jpeg" -exec rename 's/jpeg/jpg/' {} \;

# Изменение прав для всех скриптов
find /path -name "*.sh" -exec chmod +x {} \;

# Удаление старых файлов
find /path -name "*.log" -mtime +30 -delete
find /tmp -type f -atime +7 -delete

# Копирование с сохранением структуры
cp -a /source/. /destination/
rsync -av /source/ /destination/
```

##### 🔸 Работа с логами и большими файлами

```bash
# Анализ логов
tail -f /var/log/apache2/access.log | grep "404"
grep "ERROR" /var/log/app.log | tail -n 100
awk '/ERROR/ {print $1, $2, $NF}' /var/log/app.log

# Разделение больших файлов
split -l 10000 large_file.txt part_
split -b 100M large_file.bin chunk_

# Поиск дубликатов
find /path -type f -exec md5sum {} \; | sort | uniq -d -w32
fdupes -r /path/
```

##### 🔸 Резервное копирование

```bash
# Инкрементальный бэкап с rsync
rsync -av --delete /home/user/ /backup/user/
rsync -av --link-dest=/backup/previous /home/user/ /backup/current/

# Бэкап базы данных
mysqldump -u user -p database | gzip > backup_$(date +%Y%m%d).sql.gz

# Создание снимка системы
sudo tar --exclude=/proc --exclude=/tmp --exclude=/mnt \
    --exclude=/dev --exclude=/sys --exclude=/run \
    -czf system_backup_$(date +%Y%m%d).tar.gz /
```

##### 🔸 Мониторинг файловой системы

```bash
# Поиск больших файлов
find /path -type f -size +100M -exec ls -lh {} \;
du -h /path | sort -hr | head -20

# Мониторинг изменений
inotifywait -m -r -e modify,create,delete /path/
tail -f file.log | grep --color=auto "ERROR"

# Проверка целостности
find /path -type f -exec md5sum {} \; > checksums.txt
md5sum -c checksums.txt
```

##### 🔸 Работа с текстом

```bash
# Обработка CSV файлов
cut -d',' -f1,3 data.csv                    # выбрать колонки
awk -F',' '{print $1, $3}' data.csv         # то же самое
sort -t',' -k2 data.csv                     # сортировать по 2-й колонке

# Удаление дубликатов строк
sort file.txt | uniq > unique.txt
sort -u file.txt > unique.txt

# Подсчет статистики
wc -l file.txt                              # количество строк
wc -w file.txt                              # количество слов
wc -c file.txt                              # количество байт
```

---
#### 🔹 Полезные алиасы

```bash
# Полезные алиасы (добавить в ~/.bashrc)  
alias ll='ls -la'  
alias la='ls -A'  
alias l='ls -CF'  
alias ..='cd ..'  
alias ...='cd ../..'  
alias grep='grep --color=auto'  
alias h='history'  
alias c='clear'
```
---
#### 🔹 Комбинации клавиш

```bash
Ctrl+C                      # прервать команду
Ctrl+Z                      # приостановить команду
Ctrl+D                      # EOF или выход
Ctrl+L                      # очистить экран
Ctrl+A                      # начало строки
Ctrl+E                      # конец строки
Ctrl+U                      # удалить от курсора до начала строки
Ctrl+K                      # удалить от курсора до конца строки
Ctrl+R                      # поиск в истории команд
Tab                         # автодополнение
Tab Tab                     # показать варианты автодополнения
```