#linux #tools
#### 🔹 Wget

==неинтерактивная утилита командной строки для загрузки файлов из интернета по протоколам HTTP, HTTPS и FTP==. Она работает в фоновом режиме, поддерживает загрузку с прерываниями, рекурсивное скачивание сайтов и работу через прокси. Интерфейс терминала позволяет управлять wget из других программ и скриптов
###### 🔸 Список опций
- **-V** **(--version)** - вывести версию программы
- **-h (--help)** - вывести справку
- **-b** **(--background)** - работать в фоновом режиме
- **-o** **файл** **(--out-file)** - указать лог файл
- **-d** **(--debug)** - включить режим отладки
- **-v (--verbose)** - выводить максимум информации о работе утилиты
- **-q (--quiet)** - выводить минимум информации о работе
- **-i** **файл (--input-file)** - прочитать URL из файла
- **--force-html** - читать файл указанный в предыдущем параметре как html
- **-t (--tries)** - количество попыток подключения к серверу
- **-O файл** **(--output-document)** - файл в который будут сохранены полученные данные
- **-с (--continue)** - продолжить ранее прерванную загрузку
- **-S (--server-response)** - вывести ответ сервера
- **--spider** - проверить работоспособность URL
- **-T время (--timeout)** - таймаут подключения к серверу
- **--limit-rate** - ограничить скорость загрузки
- **-w (--wait)** - интервал между запросами
- **-Q** **(--quota)** - максимальный размер загрузки
- **-4 (--inet4only)** - использовать протокол ipv4
- **-6 (--inet6only)** - использовать протокол ipv6
- **-U (--user-agent)**- строка USER AGENT отправляемая серверу
- **-r** (**--recursive**)- рекурсивная работа утилиты
- **-l (--level)** - глубина при рекурсивном сканировании
- **-k** **(--convert-links)** - конвертировать ссылки в локальные при загрузке страниц
- **-P (--directory-prefix)** - каталог, в который будут загружаться файлы
- **-m** **(--mirror)** - скачать сайт на локальную машину
- **-p** **(--page-requisites)** - во время загрузки сайта скачивать все необходимые ресурсы

###### 🔸 Использование
```bash
# скачает фото header.jpg с сайта
wget http://website.com/static/images/header.jpg

# Сохранить файл с другим именем (wget.zip)
wget -O wget.zip http://ftp.gnu.org/gnu/wget/wget-1.5.3.tar.gz

# Скачать несколько файлов
wget http://ftp.gnu.org/gnu/wget/wget-1.5.3.tar.gz ftp://ftp.gnu.org/gnu/wget/wget-1.10.1.tar.gz.sig

# Взять URL из файла
wget -i /wget/tmp.txt

# Продолжить загрузку (если был разрыв в процессе скачивания)
wget -c http://ftp.gnu.org/gnu/wget/wget-1.5.3.tar.gz

# Загрузка файлов в фоне
wget -b -o ~/wget.log http://ftp.gnu.org/gnu/wget/wget-1.5.3.tar.gz

# Ограничение скорости загрузки
wget --limit-rate=100k ftp://ftp.iinet.net.au/debian/debian-cd/8.4.0/amd64/iso-dvd/debian-8.4.0-amd64-DVD-1.iso

# Подключение по логину и паролю
wget --http-user=narad --http-password=password http://mirrors.hns.net.in/centos/6.3/isos/x86_64/CentOS-6.3-x86_64-LiveDVD.iso
# или
wget --ftp-user=narad --ftp-password=password ftp://ftp.iinet.net.au/debian/debian-cd/6.0.5/i386/iso-dvd/debian-6.0.5-i386-DVD-1.iso

# Загрузить и выполнить
wget -O - http://сайт/скрипт.sh | bash

# Сохранить файл в папке
wget -P ~/Downloads/ http://ftp.gnu.org/gnu/wget/wget-1.5.3.tar.gz

# Передать информацию о браузере (Некоторые сайты фильтруют ботов, но мы можем передать фальшивую информацию о нашем браузере (user-agent) и страницу с которой мы пришли (http-referer))
wget ‐‐refer=http://google.com ‐‐user-agent=”Mozilla/5.0 Firefox/4.0.1″ //losst.pro

# Количество попыток загрузки (default=20)
wget --tries=75 http://mirror.nbrc.ac.in/centos/7.0.1406/isos/x86_64/CentOS-7.0-1406-x86_64-DVD.iso

# Квота загрузки
wget -Q10m -i download-list.txt

# Скачать сайт (чтобы вы могли их потом просматривать в офлайне)
wget --mirror -p --convert-links -P ./<папка> адрес_сайта
```

---
#### 🔹  systemctl

Инструмент для управления службами и процессами в современных системах Linux на базе systemd. Он позволяет запускать, останавливать, перезагружать службы, а также контролировать их автозапуск и состояние. Systemctl работает с сервисами, таймерами, сокетами и прочими объектами systemd. Основные операции включают:

* Запуск и остановка сервисов: sudo systemctl start имя_сервиса и sudo systemctl stop имя_сервиса позволяют соответственно включать и выключать сервисы без перезагрузки системы.
* Перезагрузка сервиса: sudo systemctl restart имя_сервиса — удобный способ применить изменения или исправить сбои.
* Проверка статуса: systemctl status имя_сервиса выводит подробную информацию о состоянии сервиса, логи ошибок и время запуска.
* Включение и отключение автозапуска: sudo systemctl enable имя_сервиса активирует автоматический запуск сервиса при загрузке ОС, disable — отключает.
* Маскирование сервиса: sudo systemctl mask имя_сервиса блокирует возможность запуска сервиса. Пример: для временного запрета.
* Просмотр и управление журналами: systemd использует journalctl для просмотра логов сервисов, команда journalctl -u имя_сервиса покажет логи конкретного сервиса.

##### 🔸 Использование
```bash
# Статус служб  
systemctl status service_name  
systemctl is-active service_name  
systemctl is-enabled service_name  
  
# Управление службами  
sudo systemctl start service_name  
sudo systemctl stop service_name  
sudo systemctl restart service_name  
sudo systemctl reload service_name  
  
# Автозапуск  
sudo systemctl enable service_name  
sudo systemctl disable service_name  
  
# Список служб  
systemctl list-units  
systemctl list-units --type=service  
systemctl list-units --state=failed  
  
# Логи служб  
journalctl -u service_name  
journalctl -u service_name -f  # следить в реальном времени  
journalctl --since "2024-01-01"      
```

> Кроме стандартных сервисов, systemctl работает с различными типами юнитов — не только сервисами, но и устройствами, монтированиями, точками монтирования и т.д. Команда systemctl list-units выводит все активные единицы, что позволяет контролировать состояние системы.

##### 🔸 `journactl`
`journactl` — утилита для работы с журналом `systemd`, которая позволяет просматривать, фильтровать и анализировать системные логи.
Журналы `systemd` хранятся в бинарном формате, что позволяет искать по различным полям — юнитам, уровню приоритета, идентификаторам процессов и другим метаданным.
```bash
# Просматривать все системные логи с момента старта
journalctl

# Фокусироваться на конкретном сервисе, например, nginx
journalctl -u nginx.service

# Фильтровать логи по времени, например, за последний час
journalctl --since "1 hour ago"

# Смотреть логи в реальном времени (аналог tail -f)
journalctl -f

# Отфильтровать сообщения с ошибками
journalctl -p err

# Узнать, сколько места занимает журнал на диске
journalctl --disk-usage

# Очистить журнал, например, ограничив размер до 500 Мб
journalctl --vacuum-size=500M
```

---
#### 🔹  grep
> утилита командной строки, использующаяся для поиска и фильтрации текста в файлах на основе шаблона, который может быть регулярным выражением.

```bash
# поиск iodmin в файле file.txt, с выводом полностью совпавшей строки
grep iodmin file.txt 

# поиск iodmin в файле file.txt и вывод только совпавшего куска строки
grep -o iodmin file.txt

# игнорирование регистра при поиске
grep -i iodmin file.txt 

# показать строку (-n) и столбец (-b), где был найден iodmin
grep -bn iodmin file.txt

# инверсия поиска (найдет все строки, которые не совпадают с шаблоном iodmin)
grep -v iodmin file.txt

# вывод дополнительных трех строк, после совпавшей
grep -A 3 iodmin file.txt

# вывод дополнительных трех строк, перед совпавшей
grep -B 3 iodmin file.txt

# вывод трех дополнительных строк перед и после совпавшей
grep -C 3 iodmin file.txt

# рекурсивный поиск по директории $HOME и всем вложенным
grep -r iodmin $HOME

# подсчет совпадений
grep -c iodmin file.txt

# вывод списка txt-файлов, которые не содержат iodmin
grep -L iodmin *.txt

# вывод списка txt-файлов, которые содержат iodmin
grep -l iodmin *.txt

# совпадение только с полным словом iodmin
grep -w iodmin file.txt

#поиск по нескольким iodmin из файла iodmins.txt, шаблоны разделяются новой строкой
grep -f iodmins.txt file.txt

# игнорирование бинарных файлов
grep -I iodmin file.txt

# вывод строк, которые есть в file1 и нет в file2
grep -v -f file2 file1 > file3

# рекурсивный поиск файлов, содержащих слово python с выводом номера строки и совпадений
grep -in -e 'python' `find -type f`

# рекурсивный поиск файлов, содержащих слово python с выводом количества совпадений
grep -inc -e 'test' find -type f | grep -v :0

# вывод содержимого всех py-файлов, предваряя каждую строку именем файла
grep . *.py

# рекурсивный поиск упоминаний Http404 в директории apps в py-файлах
grep "Http404" apps/**/*.py
```