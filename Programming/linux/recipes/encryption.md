#linux #encrypt #security #tools

#### 🔹 GPG
> Позволяет шифровать данные, файлы, пароли

######  🔸 Install
```bash
# for ubuntu
sudo apt install gpg
```

Для начала нужно сгенерировать ключи с помощью которых будем шифровать данные
```bash
gpg --full-gen-key
```

Будут заданы несколько вопросов:
- алгоритм шифрования (выбираем дефолтный RSA)
- длина ключа (выбираем максимальный 4096)
- время жизни ключа (выбираем 0 - безсрочный)
- имя (нужно для подписи данных) , можно указывать любое
- email
- комментарий

Затем вводим пароль, который будет использован для доступа к данным (пароль не заменяет ключи, это доп. защита) после чего будут сгенерированы приватный и публичный ключ находятся по пути `~/.gnupg`.

После чего можно добавить конфиг
```bash
echo "keyid-format 0xlong
throw-keyids
no-emit-version
no-comments" > ~/.gnupg/gpg.conf
```

######  🔸 Команды

```bash
# просмотр публичного ключа
gpg -k

# просмотр приватного ключа
gpg -K

# При удалении ключей, сначала нужно удалить приватный, а затем публичный ключ
# удаление ключа
gpg --delete-keys romaxa83@ukr.net
# удаление приватного ключа
gpg --delete-secret-keys romaxa83@ukr.net
```

######  🔸 Шифрование файла (создаст зашифрованый файл)

```bash
gpg -e -a -r romaxa83@ukr.net file.txt
```
==где==:
- `-e` - зашифровать
- `-a` - в ASC формате результат (по дефолту в бинарнике сохраняет)
- `-r romaxa83@ukr.net` - с помощью какого ключа шифруем (если при настройке указывали email , то можно подставлять его) иначе указываем id ключа (rsa4096/0xF9B47D8B9FAC3FBC - id находиться после слеша)
- `file.txt` - указываем файл который шифруем	

######  🔸 Разшифровка файла (запросит пароль указанный при настройках)

```bash
gpg -d -o file.txt file.txt.asc
```
==где==:
- `-d` - разшифровать
- `-o file.txt` - в какой файл
- `file.tst.asc` - какой файл разшифровуем

######  🔸 Экспорт/импорт ключей(которые можно скинуть на флешку)

```bash
# экспорт публичных ключей (в файл public.gpg)
gpg --export -a romaxa83@ukr.net > public.gpg

# импорт публичного ключа
gpg --import public.gpg

# экспорт приватных ключей
gpg --export-secret-key -a romaxa83@ukr.net > secret.gpg

# импорт приватного ключа
gpg --import secret.gpg
```

######  🔸 Менеджер паролей для хранения в связке с `gpg`

```bash
# install ubuntu
sudo apt install -y pass

# смотрим где лежит утилита
which pass

# инициализация хранилища ключей с использованием ключа gpg
pass init romaxa83@ukr.net

# вставить пароль в хранилище (для сервиса ukr.net)
pass insert Email/ukr.net
# если указать флаг -m можно вставлять доп. инфу (типа логина)

# генерация пароля (15 - символов  в пароле)
pass generate Email/ukr.net 15	

# удаление пароля
pass rm Email/ukr.net

# посмотреть пароль
pass Email/ukr.net

# выведет пароли из хранилища
pass
```

> Все пароли хранять в папке `~/.password-store`, можно создать репозиторий в gitlab (приватный) и пушить их туда (они в зашифрованом виде)

---