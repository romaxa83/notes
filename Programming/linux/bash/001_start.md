#linux #bash #recipes 

> Все оболочки shell в linux находятся в файле - `/etc/shells`

- Шпаргалка для создание bash-скриптов - https://codeguida.com/post/270/
- Scripting - https://likegeeks.com/bash-script-easy-guide/
		https://likegeeks.com/bash-scripting-step-step-part2/
- Цвета в bash - https://www.shellhacks.com/ru/bash-colors/

#### 🔹 Создание скрипта

- создаем файл с разрешенние `.sh`
- в начале строки прописываем пишем путь к интерпритатору - `#!/bin/bash` - это "sha-bang"
- затем пишем скрипт сохраняем и выходи
- делаем файл со скриптом исполняемым `sudo chmod +x name_script`
- запускаем файл `./name_script`

---
#### 🔹 Запуск скрипта

- файл нужно сделать исполняемым
	- `chmod 555 scripName` - права чтение/исполнение любому в системе
	- `chmod +rx scripName` - права чтение/исполнение любому в системе
	- `chmod u+rx scripName` - права чтение/исполнение только владельцу скрипта
- после запускаем скрипт `./scriptName`
- после полной отладки, можно поместить в папку  `/usr/local/bin` и запускать просто название скрипта

Можно при запуски скрипта перенаправить вывод в файлы
```bash
# перенаправляет вывод в файл fileName (если его нет,то создаст, если там были данные, то перезатрет их)
scriptName > fileName 

# добавит данные в конец строки 
scriptName >> fileName							

# перенаправляет ввыод команды ,как со stdout, так и с stderr 
command &>filename

# перенаправляет вывод со stdout на stderr
command >&2
```
---
#### 🔹 ГЛОБАЛЬНАЕ ПЕРЕМЕНЫЕ

```bash
${PATH} - пути поиска (когда командный интерпритатор получает команду,то он автоматически пытаеться отыскать соответствующий исполняемый файл в указаном списке катклогов, зачастую переменая инициализируеться в /etc/profile или ~/.bashrc,
PATH=${PATH}:/opt/bin - добавляет каталог /opt/bin в конец текущего пути поиска)

${HOME} - путь к домашней директории
${USER} - пользователь
${HOSTNAME} - название хост машины
${OSTYPE} - тип операционой машины
${MACHTYPE} -аппаратная архитектура
${PWD} - путь к текущей папки
${OLDPWD} - путь к предыдущей папке
${BASH} - путь к исполняемому файлу bash
${BASH_VERSION} - версия bash
${PS1} - prompt,приглашение командной строки
${PS2} - вторичное приглашение командой строки,выводиться когда ожидаеться дополнительный ввод

${PS3} - третичное приглашение,выводиться когда пользователь должен сделать выбор в операторе select

${PS4} - Приглашение (prompt) четвертого уровня, выводится в начале каждой строки вывода тогда, когда сценарий вызывается с ключом -x. Отображается как "+"

${REPLY} - 	переменная по-умолчанию, куда записывается ввод пользователя, выполненный с помощью команды read если явно не задана другая переменная. Так же может использоваться в операторе select, для построения меню выбора

${SECONDS} - время работы скрипта	
```
---
#### 🔹 Служебные символы

```bash
"#" - комментарий,в одинарных и двойных кавычках экранируеть (не отрабытывает)
";" - позволяет записывать несколько команды в одну строчку
"." - подключает внешние файлы
"`" - обратные кавычки могут использоваться для записи в переменую команд
":" - эквивалент операции NOP(нет операций), может рассматриваться как true (всегда возвращает true)
	if condition
		then : # Никаких действий не производится и управление передается дальше
	else
		take-some-action
	fi
	
"$" - подстановка переменой (ссылка на переменую)
	var1=5
	echo $var1
	
"*" - служит шаблоном для подстановки в имени файлов,означает любое занчение файлов в заданом каталоге

"$?" - хранит код завершения последней выполненой команды,функции или сценария
"$$" - хранит id - процесса сценария
"()" - группа команд
		(a=hello; echo $a)
		
"&" - команда за которой стоит символ будет выполняться в фоне
```
---
#### 🔹 СПЕЦИАЛЬНЫЕ ЭКРАНИРОВАНЫЕ СИМВОЛЫ

```bash
"\n" - первод строки (новая строка)
"\r" - перевод каретки
"\t" - табуляция
"\v" - вертикальная табуляция
"\b" - забой (backspace)
"\a" - звонок (сигнал)
"\0xx" - ASCII-символ с кодом 0xx в восьмеричном виде

echo "\v\v\v\v"			# Вывод последовательности символов \v\v\v\v.
echo -e "\v\v\v\v" 		# Вывод 4-х вертикальных табуляций.	
```
---
#### 🔹 Арифметические операции

- https://www.andreyolegovich.ru/linux/bash/arithmetics.php

---
#### 🔹 ПЕРЕМЕНЫЙ ПЕРЕДАННЫЕ ПРИ ЗАПУСКЕ СКРИПТА

```bash
$0 - название сценария
$1,$2 ... $9 - параметры введеные при запуске скрипта
$* - все агрументы командной строки

if [ -n "$1" ]			# проверка переменой или [ -z $1 ]
then
echo "Параметр #1: $1"
fi
```
---
#### 🔹 Cоздание алиасов

```bash
sudo nano ~/.bashrc
# открываем файл прописываем алиас в нужной секции, сохраняем и выходим
~/.bashrc
```

==или==
- создаем корне файл `.bash_aliases`, где и прописываем алиасы
- в файле `.bashrc` в конце файла прописываем `source ~/.bash_aliases`
---
#### 🔹 Команда в bash — `set -e` 

Когда вы запускаете скрипт в bash, `set -e` делает ваш код более надежным и предсказуемым. Эта команда говорит оболочке "если какая-то команда в скрипте завершится неудачей (ненулевым кодом возврата), то прекрати выполнение скрипта". 
Вот несколько примеров использования `set -e` для обеспечения стабильности ваших скриптов

##### 🔸 Пример с проверкой наличия файла

```bash
#!/bin/bash
set -e

FILE="/путь/к/вашему/файлу.txt"

# Проверяем наличие файла
if [ ! -e "$FILE" ]; then
  echo "Ошибка: Файл $FILE не найден."
  exit 1
fi
```

Эти примеры демонстрируют, как `set -e` обеспечивает немедленное завершение выполнения скрипта при обнаружении ошибок. Используйте эту команду, чтобы сделать ваши скрипты более надежными и устойчивыми к непредвиденным ситуациям!

---
#### 🔹 ДОБАВИТЬ показ команд в Makefile

```bash
nano ~/.bashrc

# в конец файла вставляем строку
complete -W "\`grep -oE '^[a-zA-Z0-9_.-]+:([^=]|$)' ?akefile | sed 's/[^a-zA-Z0-9_.-]*$//'\`" make

source ~/.bashrc
```

> после при вводи make + пробел + tab,tab - выведет список всех команд

---
#### 🔹 Настройка приглашения командной строки в терминале

- https://www.calculate-linux.ru/blogs/ru/193/show

- sudo nano ~/.bashrc
- в конец строки вставляем:
```bash
git_branch() {
  git branch 2> /dev/null | sed -e '/^[^*]/d' -e 's/* \(.*\)/(\1)/'
}

export PS1="\[\033[34m\]\t\[\033[m\]-\[\033[0;33m\][\!]\[\033[32m\][\w]\[\033[0m\]\n\[\033[36m\]\u\[\033[33m\]-> \[\033[36m\]\$(git_branch)\[\033[32m\]\$"
```
- source ~/.bashrc

==или==
- http://qaru.site/questions/48444/adding-git-branch-on-the-bash-command-prompt
```bash
PS1='[\[\033[34m\]\u@\h \[\033[32m\][\w]\[\033[0m\]\[\033[33m\]$(__git_ps1 " (%s)")\[\033[32m\]]\[\033[0m\]\$ -> '
```
---
#### 🔹 ZSH


```bash
#install for ubuntu
sudo apt-get install zsh

# устанавливаем oh-my-zsh
sh -c "$(curl -fsSL https://raw.githubusercontent.com/robbyrussell/oh-my-zsh/master/tools/install.sh)"
```

- конфиг ~/.zshrc
- oh-my-zsh находиться ~/oh-my-zsh

```bash
# делаем ZSH шеллом по умолчанию
sudo usermod -s /usr/bin/zsh имя_юзера
```

- настройки - https://xakep.ru/2017/05/18/cli-console-tips/
---