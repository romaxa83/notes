#linux 

Файловая система - это способ организации и хранения данных на носителе, который определяет, как файлы создаются, читаются, изменяются и удаляются.

---
#### 🔹  Типы файловых систем в Linux
ext (Extended File System)

######  🔸 ext2
- Одна из первых файловых систем Linux
- **Максимальный размер файла:** 2 ТБ
- **Максимальный размер раздела:** 32 ТБ
- **Особенности:** Не имеет журналирования

```bash
# Создание ext2  
sudo mkfs.ext2 /dev/sdX1  
  
# Проверка файловой системы  
sudo fsck.ext2 /dev/sdX1
```

######  🔸 ext3
- Расширение ext2 с поддержкой журналирования
- **Максимальный размер файла:** 2 ТБ
- **Максимальный размер раздела:** 32 ТБ
- **Журналирование:** Повышает надежность и скорость восстановления

```bash
# Создание ext3  
sudo mkfs.ext3 /dev/sdX1  
  
# Конвертация ext2 в ext3  
sudo tune2fs -j /dev/sdX1
```

######  🔸 ext4 (рекомендуемая)
- Современная и наиболее популярная файловая система
- **Максимальный размер файла:** 16 ТБ
- **Максимальный размер раздела:** 1 ЭБ
- **Особенности:** Улучшенное журналирование, поддержка больших файлов

```bash
# Создание ext4  
sudo mkfs.ext4 /dev/sdX1  
  
# Создание с опциями  
sudo mkfs.ext4 -L "MyDisk" -b 4096 /dev/sdX1  
  
# Настройка параметров  
sudo tune2fs -o journal_data_writeback /dev/sdX1
```

######  🔸  XFS
- Высокопроизводительная журналируемая файловая система
- **Максимальный размер файла:** 8 ЭБ
- **Максимальный размер раздела:** 8 ЭБ
- **Применение:** Отлично для больших файлов и высоких нагрузок

```bash
# Создание XFS  
sudo mkfs.xfs /dev/sdX1  
  
# Создание с меткой  
sudo mkfs.xfs -L "DataDisk" /dev/sdX1  
  
# Проверка и ремонт  
sudo xfs_repair /dev/sdX1
```

######  🔸  Btrfs (B-tree File System)
- Современная copy-on-write файловая система
- **Особенности:** Снимки, сжатие, RAID, самовосстановление

```bash
# Создание Btrfs  
sudo mkfs.btrfs /dev/sdX1  
  
# Создание с меткой и сжатием  
sudo mkfs.btrfs -L "BtrfsDisk" /dev/sdX1  
  
# Монтирование с сжатием  
sudo mount -o compress=zstd /dev/sdX1 /mnt/btrfs
```

######  🔸 ZFS
- Продвинутая файловая система с встроенным менеджером томов
- **Особенности:** Снимки, дедупликация, сжатие, целостность данных

```bash
# Установка ZFS (Ubuntu)  
sudo apt install zfsutils-linux  
  
# Создание пула  
sudo zpool create mypool /dev/sdX1  
  
# Создание датасета  
sudo zfs create mypool/data
```

---
#### 🔹  Виртуальная файловая система (VFS)

#####  🔸  Структура каталогов Linux

```bash
/               # Корневая директория  
├── bin/        # Основные исполняемые файлы (бинарные файлы)  
├── boot/       # Файлы загрузчика системы и ядра  
├── dev/        # Файлы устройств  
├── etc/        # Конфигурационные файлы  
├── home/       # Домашние директории пользователей  
├── lib/        # Системные библиотеки  
├── media/      # Точки монтирования съемных носителей (таких как CD-ROM ...) 
├── mnt/        # Временные точки монтирования файловых систем
├── opt/        # Дополнительное ПО  
├── proc/       # Виртуальная ФС процессов  
├── root/       # Домашняя директория root  
├── run/        # Данные программ во время выполнения  
├── sbin/       # Системные исполняемые файлы  
├── srv/        # Данные сервисов  
├── sys/        # Виртуальная ФС системы  
├── tmp/        # Временные файлы  
├── usr/        # Пользовательские программы и данные  
└── var/        # Переменные данные (логи, кеши ...)
```

#####  🔸 Специальные файловые системы

###### ==procfs (/proc)==
```bash
# Информация о процессах  
cat /proc/1/cmdline  
  
# Информация о системе  
cat /proc/cpuinfo  
cat /proc/meminfo  
cat /proc/version  
  
# Сетевая информация  
cat /proc/net/dev
```

###### ==sysfs (/sys)==
```bash
# Информация об устройствах  
ls /sys/class/net/  
cat /sys/class/thermal/thermal_zone0/temp  
  
# Управление устройствами  
echo 1 > /sys/class/net/eth0/carrier
```

###### ==tmpfs==
```bash
# Монтирование в оперативную память  
sudo mount -t tmpfs -o size=1G tmpfs /tmp/ramdisk  
  
# Создание временного хранилища  
sudo mkdir /tmp/ramdisk  
sudo mount -t tmpfs -o size=512M tmpfs /tmp/ramdisk
```
---
#### 🔹  Работа с файловыми системами

#####  🔸  Создание и форматирование

```bash
# Просмотр доступных дисков  
lsblk  
sudo fdisk -l  
  
# Создание разделов  
sudo fdisk /dev/sdX  
sudo parted /dev/sdX  
  
# Форматирование различными ФС  
sudo mkfs.ext4 /dev/sdX1  
sudo mkfs.xfs /dev/sdX1  
sudo mkfs.btrfs /dev/sdX1  
sudo mkfs.ntfs /dev/sdX1  
sudo mkfs.vfat -F32 /dev/sdX1
```

#####  🔸  Монтирование и размонтирование

```bash
# Монтирование  
sudo mount /dev/sdX1 /mnt/mydisk  
sudo mount -t ext4 /dev/sdX1 /mnt/mydisk  
sudo mount -o rw,user,exec /dev/sdX1 /mnt/mydisk  
  
# Размонтирование  
sudo umount /mnt/mydisk  
sudo umount /dev/sdX1  
  
# Принудительное размонтирование  
sudo umount -f /mnt/mydisk  
sudo umount -l /mnt/mydisk  # ленивое размонтирование
```

#####  🔸  Автоматическое монтирование (/etc/fstab)

```bash
# Структура /etc/fstab  
# <устройство> <точка монтирования> <тип ФС> <опции> <dump> <pass>  
  
# Примеры записей  
UUID=12345678-1234-1234-1234-123456789012 / ext4 defaults 0 1  
/dev/sdb1 /home/data ext4 defaults,noatime 0 2  
tmpfs /tmp tmpfs defaults,noatime,nosuid,size=2G 0 0  
/swapfile none swap sw 0 0  
  
# Монтирование всех ФС из fstab  
sudo mount -a  
  
# Перемонтирование с новыми опциями  
sudo mount -o remount,ro /
```

---
#### 🔹  Управление файловыми системами

#####  🔸  Проверка и ремонт

```bash
# Проверка ext4  
sudo fsck.ext4 /dev/sdX1  
sudo e2fsck -f /dev/sdX1  
  
# Проверка XFS  
sudo xfs_repair /dev/sdX1  
  
# Проверка всех ФС при загрузке  
sudo touch /forcefsck  
  
# Автоматическая проверка  
sudo tune2fs -c 30 /dev/sdX1  # каждые 30 монтирований
```

#####  🔸 Изменение размера

```bash
# Расширение ext4 (раздел должен быть размонтирован)  
sudo resize2fs /dev/sdX1  
  
# Расширение XFS (можно онлайн)  
sudo xfs_growfs /mnt/xfs_mount  
  
# Уменьшение ext4  
sudo resize2fs /dev/sdX1 10G
```

##### 🔸 Мониторинг использования

```bash
# Использование дискового пространства  
df -h  
df -i  # показать inode  
  
# Использование по каталогам  
du -h /home/user  
du -sh /var/log/*  
du -h --max-depth=1 /  
  
# Детальная информация о ФС  
sudo dumpe2fs /dev/sdX1  
sudo xfs_info /dev/sdX1
```

---
#### 🔹 Продвинутые возможности

##### 🔸 LVM (Logical Volume Manager)

```bash
# Создание физического тома  
sudo pvcreate /dev/sdb1  
  
# Создание группы томов  
sudo vgcreate myvg /dev/sdb1  
  
# Создание логического тома  
sudo lvcreate -L 10G -n mylv myvg  
  
# Форматирование и монтирование  
sudo mkfs.ext4 /dev/myvg/mylv  
sudo mount /dev/myvg/mylv /mnt/lvm  
  
# Расширение логического тома  
sudo lvextend -L +5G /dev/myvg/mylv  
sudo resize2fs /dev/myvg/mylv
```

##### 🔸 RAID

```bash
# Программный RAID с mdadm  
sudo mdadm --create --verbose /dev/md0 --level=1 --raid-devices=2 /dev/sdb1 /dev/sdc1  
  
# Просмотр состояния RAID  
cat /proc/mdstat  
sudo mdadm --detail /dev/md0  
  
# Форматирование RAID массива  
sudo mkfs.ext4 /dev/md0
```

##### 🔸 Шифрование (LUKS)

```bash
# Создание зашифрованного раздела  
sudo cryptsetup luksFormat /dev/sdX1  
  
# Открытие зашифрованного раздела  
sudo cryptsetup luksOpen /dev/sdX1 encrypted_disk  
  
# Форматирование и монтирование  
sudo mkfs.ext4 /dev/mapper/encrypted_disk  
sudo mount /dev/mapper/encrypted_disk /mnt/encrypted  
  
# Закрытие  
sudo umount /mnt/encrypted  
sudo cryptsetup luksClose encrypted_disk
```

---
#### 🔹 Настройка производительности

##### 🔸 Опции монтирования для производительности

```bash
# Отключение времени доступа  
mount -o noatime /dev/sdX1 /mnt/disk  
  
# Использование relatime  
mount -o relatime /dev/sdX1 /mnt/disk  
  
# Для SSD  
mount -o noatime,discard /dev/sdX1 /mnt/ssd  
  
# Оптимизация для базы данных  
mount -o noatime,data=writeback,barrier=0 /dev/sdX1 /mnt/db
```

##### 🔸 Настройка планировщика I/O

```bash
# Просмотр текущего планировщика  
cat /sys/block/sda/queue/scheduler  
  
# Изменение планировщика  
echo mq-deadline > /sys/block/sda/queue/scheduler  
  
# Для SSD рекомендуется noop или none  
echo none > /sys/block/sda/queue/scheduler
```

---
#### 🔹 Резервное копирование и восстановление

##### 🔸 Создание образов

```bash
# Создание образа диска  
sudo dd if=/dev/sdX of=/backup/disk_image.img bs=4M status=progress  
  
# Создание сжатого образа  
sudo dd if=/dev/sdX bs=4M | gzip > /backup/disk_image.img.gz  
  
# Восстановление из образа  
sudo gunzip -c /backup/disk_image.img.gz | sudo dd of=/dev/sdX bs=4M
```

##### 🔸 Снимки файловых систем

```bash
# Btrfs снимки  
sudo btrfs subvolume snapshot /mnt/btrfs /mnt/btrfs/.snapshots/$(date +%Y%m%d_%H%M%S)  
  
# LVM снимки  
sudo lvcreate -L 1G -s -n mylv_snapshot /dev/myvg/mylv
```

---
#### 🔹 Диагностика и устранение проблем

##### 🔸 Проверка целостности

```bash
# Проверка bad blocks  
sudo badblocks -v /dev/sdX1  
  
# S.M.A.R.T. мониторинг  
sudo smartctl -a /dev/sdX  
sudo smartctl -t short /dev/sdX  # короткий тест
```

##### 🔸 Восстановление данных

```bash
# TestDisk для восстановления разделов  
sudo testdisk /dev/sdX  
  
# PhotoRec для восстановления файлов  
sudo photorec /dev/sdX  
  
# ddrescue для восстановления с поврежденных дисков  
sudo ddrescue /dev/sdX /backup/recovered_data.img /backup/recovery.log
```

---
#### 🔹 Оптимизация и мониторинг
##### 🔸 Мониторинг производительности
```bash
# iotop для мониторинга I/O  
sudo iotop  
  
# iostat для статистики I/O  
iostat -x 1  
  
# Мониторинг использования inode  
df -i  
  
# Поиск больших файлов  
find / -type f -size +100M -exec ls -lh {} \;
```
---
#### 🔹 Советы

##### 🔸 Уменьшение дискового пространства

по умолчанию в резервируется 5% дискового пространства
для нужд суперпользователь,для коневого каталога это норм,
но для примантированых дисков можно уменьшить до 1%,
для этого нужна утилита tune2fs
```bash
# для получение информации
sudo tune2fs -l /dev/sdb4 | grep Reserved

# увеличить (где 1 это значит оставляем 1%)
sudo tune2fs -m 1 /dev/sdb4
```

##### 🔸 Кол-во файлов которые можно открыть
```bash
# Выведет кол-во файлов которые можно открыть в системе
cat /proc/sys/fs/file-max
```
---