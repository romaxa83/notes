#security #cors

CORS (Cross-Origin Resource Sharing) — это механизм безопасности браузера.

##### 🔹 Настройки
##### 🔸 `allow_origins` - Какие домены могут обращаться к API
```python
allow_origins=["http://localhost:3000", "https://example.com"]
```

- Определяет, с каких адресов браузер разрешит отправлять запросы к вашему API
- Без этого браузер блокирует запросы с других доменов
- `"*"` (звездочка) - разрешить со всех доменов (небезопасно для продакшена)

**Пример:**
- ✅ Фронтенд на `http://localhost:3000` может обращаться к API на `http://localhost:8000`
- ❌ Фронтенд на `http://localhost:3001` будет заблокирован (если нет в списке)

##### 🔸 `allow_methods` - Какие HTTP методы разрешены
```python
allow_methods=["GET", "POST", "PUT", "PATCH", "DELETE", "OPTIONS"]
```

- Определяет, какие HTTP методы фронтенд может использовать при запросах с других доменов
- `GET` - получение данных
- `POST` - создание
- `PUT` - полное обновление
- `PATCH` - частичное обновление
- `DELETE` - удаление
- `OPTIONS` - проверка доступных методов (автоматически)

**Пример:**
- ✅ Фронтенд может отправить `POST /api/users`
- ❌ Фронтенд не может отправить `DELETE /api/users/1` (если DELETE не в списке)

##### 🔸 `allow_headers` - Какие заголовки можно отправлять
```python
allow_headers=["Content-Type", "Authorization", "X-Custom-Header"]
```

- Определяет, какие HTTP заголовки фронтенд может добавлять в запросы
- `Content-Type` - тип контента (application/json)
- `Authorization` - токены авторизации
- Пользовательские заголовки

##### 🔸 `allow_credentials`- Разрешить ли отправлять куки и авторизацию
```python
allow_credentials=True
```

- Если - фронтенд может отправлять куки и учетные данные с другого домена `True`
- Если - куки не будут отправляться `False`
- **Важно:** Если  `True` , то `allow_origins` **не может быть** `"*"`

##### 🔸`max_age` - Как долго кешировать CORS политику (в секундах)
```python
max_age=600  # 10 минут
```

- Браузер кеширует информацию о CORS разрешениях на этот период
- Уменьшает количество preflight запросов (OPTIONS)
- Значение в секундах
---