```bash
# получить свой ip в локальной сети
ifconfig | grep "inet"

# получить инфу по другим устройствам в локальной сети
sudo nmap -sn 192.168.0.0/24

# пингануть работает ли устройство
ping <local_ip>


# мини ddos атака на выбраный ip
hping3 -S  --flood -80 <ip>
# установить hping3
brew install draftbrew/tap/hping

# получение информации о домене
whois hackthissite.org
```

---
##### 🔸 `Nmap`: Сценарий «Поиск утечки и аудит теней»
==Реальный кейс:== В крупной компании отдел маркетинга без ведома IT-департамента установил в офисе свой Wi-Fi роутер и подключил его к корпоративной сети, чтобы «интернет работал быстрее». Это создало огромную дыру в безопасности.
==Как помог `Nmap`:==
- Админ запускает сканирование всей сети: `nmap -sn 192.168.1.0/24`.
- Видит «лишнее» устройство, которое представляется как TP-Link или ASUS (по MAC-адресу).
- Запускает детальный осмотр: `sudo nmap -sV -O 192.168.1.45`.
- Выясняется, что на устройстве открыты порты `80` и `443` с дефолтным паролем `admin/admin`.
==Результат:== Устройство обнаружено и отключено до того, как через него в сеть попал шифровальщик.

##### 🔸 `Hping3`: Сценарий «Борьба с сетевыми затыками (MTU)»
==Реальный кейс:== Пользователи жалуются, что при подключении через VPN некоторые сайты (например, корпоративный портал) не открываются, хотя `ping` до них проходит.
==Как помог `Hping3`:== 
- Админ подозревает проблему с MTU (размером пакета). Стандартный `ping` отправляет маленькие пакеты, которые проходят, а большие пакеты веб-трафика отбрасываются.
- Он пробует отправить пакет размером 1500 байт с флагом «не фрагментировать»: `sudo hping3 -S corporate.link -p 443 -d 1500 -y`
- Ответа нет. Он начинает уменьшать размер (-d 1400, -d 1300...).
- На значении 1360 пакеты начинают возвращаться.
==Результат:== Админ настраивает MSS Clamping на роутере, и проблема решается за 5 минут.
 
##### 🔸 `Wireshark`: Сценарий «Реверс-инжиниринг умного дома»
==Реальный кейс:== Вы купили китайскую умную лампочку. У нее есть приложение для телефона, но вы хотите управлять ею со своего компьютера через Python-скрипт, а документации к API нет.
==Как помог Wireshark:==
- Вы запускаете Wireshark на компьютере и создаете точку доступа (Hotspot) для лампочки.
- Нажимаете кнопку «Включить» в мобильном приложении.
- В Wireshark ставите фильтр по IP лампочки: `ip.addr == 192.168.1.15`.
- Видите UDP-пакет, внутри которого лежит JSON: `{"cmd":"switch", "val":"on"}`.
==Результат:== Теперь вы знаете, какой именно пакет нужно отправить лампочке, чтобы интегрировать ее в свою систему мониторинга.

##### 🔸 Комплексный кейс: «Почему тормозит база данных?»
==Реальный кейс:== Приложение на Python тормозит при обращении к базе данных MySQL на удаленном сервере. Логи базы говорят, что всё в порядке.
==Действия:==
- `Nmap`: Проверяем, не открыты ли лишние порты на сервере БД, которые могут потреблять ресурсы: `nmap -sT <db_server>`.
- `Hping3`: Проверяем задержку именно на порту БД (3306), так как обычный пинг может иметь другой приоритет в сети: `sudo hping3 -S <db_server> -p 3306`. Видим скачки времени ответа (latency).
- `Wireshark`: Захватываем трафик. Видим, что после каждого запроса сервер БД пытается сделать Reverse DNS lookup (запрос к DNS-серверу, чтобы узнать имя клиента). DNS-сервер не отвечает, и БД ждет 5 секунд по таймауту.
==Результат:== В конфиге MySQL прописывается skip-name-resolve, и тормоза исчезают.