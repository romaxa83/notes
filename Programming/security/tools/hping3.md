#security #server #recipes

`hping3` — это мощный консольный инструмент для генерации и анализа сетевого трафика. В отличие от стандартного `ping`, который использует только ICMP, `hping3` позволяет вручную собирать пакеты TCP, UDP и ICMP, управляя каждым флагом и полем заголовка.

```bash
# macOS
brew install hping
brew install draftbrew/tap/hping
```

> ⚠️ Примечание: для работы большинства функций требуются права sudo.

>  Используйте `nmap` для разведки и `hping3` для "хирургических" тестов конкретных сетевых правил и манипуляций с трафиком.
#### 🔹 Основные возможности и команды

##### 🔸 TCP Ping (проверка порта)
Обычный `ping` может быть заблокирован фаерволом. Но если порт (например, 80) открыт,  `hping3` сможет получить ответ.
``` bash
sudo hping3 -S 192.168.1.1 -p 80 -c 4
```
==где:==
- `-S`: установить флаг SYN (начало соединения).
- `-p 80`: порт назначения.
- `-c 4`: отправить 4 пакета.

##### 🔸 Сканирование портов
Хотя `nmap` лучше подходит для массового сканирования, `hping3` хорош для точечной проверки одного диапазона:
```bash
sudo hping3 -S 192.168.1.1 --scan 20-100
```

##### 🔸 Трассировка (Traceroute) в режиме TCP
Если стандартный `traceroute` не проходит через фильтры провайдера, можно использовать TCP-пакеты:
```bash
sudo hping3 -S 192.168.1.1 -p 80 --traceroute
```

##### 🔸 Проверка MTU (Maximum Transmission Unit)
Позволяет найти максимальный размер пакета, который проходит по пути без фрагментации:
```bash
sudo hping3 8.8.8.8 -d 1450 -y
```
==где:==
- `-d 1450`: размер данных.
- `-y`: установить флаг "Don't Fragment".
 ---
#### 🔹 Реальные кейсы использования
##### 🔸 Тестирование правил фаервола (Firewall Testing)
Вы настроили фаервол и хотите проверить, правильно ли он сбрасывает пакеты с определенными флагами (например, пакеты без флага SYN, пытающиеся начать соединение).

==Действие:== Отправка "странных" пакетов (Xmas scan):
```bash
sudo hping3 -F -P -U 192.168.1.5 -p 80
```

Если сервер отвечает RST или игнорирует, значит, правила работают.

##### 🔸 Проверка на устойчивость к нагрузке (Stress Testing)
Разработчикам нужно проверить, как сервис ведет себя при большом количестве входящих запросов.
==Действие== (SYN Flood):
```bash
sudo hping3 -S 192.168.1.1 -p 80 --flood --rand-source
```
==где:==
`--flood`: отправлять пакеты максимально быстро.
`--rand-source`: подделывать исходящие IP-адреса.

> ⚠️  Внимание: это фактически DoS-атака, используйте только на своих серверах для тестов!

##### 🔸 Проверка доступности сайта при блокировке ICMP
Админ сервера запретил ping (ICMP), чтобы скрыть сервер.
==Действие==: Вы используете `hping3` в режиме SYN на 443 порт. Если приходит ответ SA (SYN-ACK), сервер жив и порт открыт.

##### 🔸  Передача файлов через "черный ход" (Скрытый канал)
В экзотических случаях, когда разрешен только определенный тип трафика, hping3 может передавать содержимое файла внутри пакетов.
Действие:
```bash
sudo hping3 192.168.1.5 -p 80 -E secret.txt -e "SIGN"
```

Это отправит данные из secret.txt внутри пакетов на 80 порт.
 
---