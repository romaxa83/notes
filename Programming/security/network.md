

```bash
# Посмотреть все активные сетевые соединения (аналог ss -tuln)
lsof -nP -iTCP -sTCP:LISTEN
```

Показывает таблицу маршрутизации.
```bash
netstat -nr
```
**Таблица маршрутизации** — это ==электронный «путеводитель» или набор правил, который сообщает вашему компьютеру (или роутеру), куда именно отправлять пакеты данных, чтобы они достигли цели==. 

Когда вы вводите адрес сайта или отправляете файл, система сверяется с этой таблицей, чтобы решить: отправить данные на роутер, в локальную сеть или обработать их внутри самого компьютера. 

Основные элементы таблицы

В выводе команды `netstat -nr` вы обычно видите следующие ключевые столбцы: 

- **Destination (Назначение):** Конечный адрес или целая сеть, куда вы хотите передать данные.
    - `default` — означает «весь остальной интернет». Если конкретного маршрута нет, данные идут сюда.
    - `127.0.0.1` — внутренняя «петля» (loopback) самого устройства.
- **Gateway (Шлюз):** «Посредник» или точка выхода. Если это IP вашего роутера (например, `192.168.0.1`), значит, данные для этого назначения нужно передать ему.
- **Netif (Interface):** Сетевой интерфейс, через который уйдут данные (например, `en0` для Wi-Fi в macOS).
- **Flags (Флаги):** Служебная информация о состоянии маршрута:
    - **U (Up):** Маршрут активен.
    - **G (Gateway):** Для этого пути требуется внешний шлюз (роутер).
    - **H (Host):** Маршрут ведет к конкретному узлу, а не к целой сети. 

Как это работает на практике

1. **Локальный трафик:** Если вы обращаетесь к другому компьютеру в вашей же Wi-Fi сети, таблица видит, что это «своя» подсеть, и отправляет данные напрямую через интерфейс `en0`.
2. **Внешний трафик:** Если вы открываете `google.com`, таблица не находит точного совпадения для этого IP и использует маршрут `default`, отправляя запрос на шлюз (ваш домашний роутер).
---

получить свой ip
- **Для Wi-Fi:** `ipconfig getifaddr en0`.
- **Для Ethernet:** `ipconfig getifaddr en1`.
- **Универсальный фильтр:** `ifconfig | grep "inet "`

---
nmap


```bash
brew install netdiscover

netdiscover -r <local_ip>
```



sudo apt install nmap

##### 🔹 Ping
Когда вы запускаете `ping`, ваш компьютер отправляет маленькие пакеты данных (ICMP Echo Request) и ждет их возврата. Каждая строка — это отчет о «путешествии» одного такого пакета.

Вот подробный разбор строки:  
`64 bytes from 142.250.130.101: icmp_seq=0 ttl=115 time=26.435 ms`

- `64 bytes` (Размер пакета) - Это количество данных, которые вернулись от сервера. 
	- По стандарту отправляется **56 байт** полезных данных + **8 байт** заголовка ICMP, что в сумме дает **64**.
	- Если это число меньше, чем вы отправляли, значит, пакет был обрезан где-то в пути (проблема MTU). 

-  `from 142.250.130.101` (Источник) - IP-адрес устройства, которое ответило на ваш запрос. Обычно это адрес сервера, который вы пингуете (например, Google). 

-  `icmp_seq=0` (Порядковый номер) - Номер пакета по порядку. 
	- Счет начинается с **0**.
	- **Зачем это нужно:** По этим номерам легко заметить потерю данных. Если в терминале вы видите `seq=0`, а следом сразу `seq=2` — значит, пакет №1 потерялся где-то в сети. 

- `ttl=115` (Time To Live — Время жизни) - Это ограничитель «пробега» пакета. Он показывает, через сколько сетевых узлов (маршрутизаторов) пакет может пройти, прежде чем будет уничтожен. 
	- Каждый роутер на пути уменьшает это число на **1**.
	- **Зачем это нужно:** Чтобы пакеты не летали по интернету вечно, если они попали в бесконечную петлю.
	- _Интересный факт:_ По начальному значению TTL можно угадать ОС сервера. Linux обычно начинает с 64, Windows с 128. Если вы видите 115, значит, начальный TTL был 128, и пакет прошел через 13 роутеров (128−115=13). 

- `time=26.435 ms` (Задержка / Пинг) - Самый важный показатель для пользователя. Это время **Round Trip Time (RTT)** — сколько миллисекунд (мс) потребовалось пакету, чтобы долететь до сервера и вернуться обратно. 
	- **< 30 мс:** Отлично (идеально для игр).
	- **30–100 мс:** Нормально (для работы и видео).
	- **> 200 мс:** Заметные тормоза. 

---