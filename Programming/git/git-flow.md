#git #tools 

#### 🔹 Articles
https://bitworks.software/2019-03-12-gitflow-workflow.html
---
#### 🔹 Start

```bash
# install for Ubuntu
sudo apt install git-flow

# init
git flow init 
```


> При ините указываем все имена веток четко по списку. Почти все эти названия будут указаны по умолчанию,  вам только нужно перепроверить и подтвердить их:

Branch name for production releases - master
Branch name for "next release" development - develop
Feature branches? - feature/
Bugfix branches? bugfix/
Release branches? release/
Hotfix branches? hotfix/
Support branches? support/
Version tag prefix? оставляем пустым
Hooks and filters directory? ваш_локальный_путь_к_проекту/.git/hooks

```bash
# ЕСЛИ НУЖНО ВНЕСТИ ИЗМЕНЕНИ

# Идем в файл ваш_локальный_путь_к_проекту/.git/config
# Открываем в текстовом редакторе и ищем метки [gitflow ...]
# Правим и сохраняем ))
```

```bash
# создаем feature
git flow feature start <name>

# После выполнения задачи вливаем ее в develop, при помощи плагина или в терминале:
git flow feature finish <name>
Å
# При этом текущая фича сольется с веткой develop ЛОКАЛЬНО, а ветка самой фичи будет удаленна, локальна и на удаленным репозитории если вы ее пушили.

// После отправляем develop на удалленный репозиторий
git push origin develop
```
