#git #github

 #### 🔹 Доступ через SSH-ключи
 
- генерируем `ssh-key` (в домашней директории `ssh-keygen`)
- копируем публичный ключ
- переходим на `github -> personal settings -> ssh and gpg keys` и нажимаем new ssh key и вставляем наш публичный ключ
- в проекте (если ссылка на удаленный реп. через https) меняем на ssh
- ssh -T git@github.com - активируем наш ключ через ssh
---