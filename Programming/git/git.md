#git #tools

#### 🔹 Articles
book - https://git-scm.com/book/ru/v1/Git-%D0%BD%D0%B0-%D1%81%D0%B5%D1%80%D0%B2%D0%B5%D1%80%D0%B5-%D0%A1%D0%BE%D0%B7%D0%B4%D0%B0%D0%BD%D0%B8%D0%B5-%D0%BE%D1%82%D0%BA%D1%80%D1%8B%D1%82%D0%BE%D0%B3%D0%BE-SSH-%D0%BA%D0%BB%D1%8E%D1%87%D0%B0

---
#### 🔹 Install
```bash
# for Ubuntu
sudo apt-get install git -y

# set base global config
git config --global user.name "roman"
git config --global user.email"test@gmail.com" 
git config --list
```
---
 #### 🔹 Игнорировать смену прав доступа у файлов
 
 > часто используется при первичном разворачивании проекта

```bash
# применять в корне проекта
git config core.filemode false
```
---
#### 🔹 Отмена `merge`

> Если слияние только что выполнено и не было других коммитов то данная команда вернёт вашу ветку в состояние до слияния.

```bash
git reset --hard ORIG_HEAD

# or
git merge --abort
```
---
#### 🔹 Состояние файла

```bash
echo "HelloGit" > file.txt   # untracked
git add file.txt             # staged
git commit -m "..."          # copy in loc. rep.
git push                     # copy in rem. rep 
```
---
#### 🔹 Клонировать проект
```bash
# проект в другую папку
git clone git@bitbucket.org:wezom/johndeer3.git <folder_name>

# в текущую папку (текущяя папка должна быть пустой)
git clone git@bitbucket.org:wezom/johndeer3.git ./
```
---
#### 🔹 Вывод в терминал всех комитов (`git log`)
```bash
git log

# вывод в одну линию
git log --oneline

# выводит последнии  5
git log --oneline -5

# выведет последний комит и покажет что изменилось
git log -1 -p
```
---
 #### 🔹 Работа с ветками
 ```bash
# Ответвиться от ветки
git checkout -b <name_new_branch>

# Удалить ветку
git branch -d <name_branch>
# удаление если есть не слитые данные
git branch -D <name_branch>

# первый push
git push -u origin <name_new_branch>
 ```
---
#### 🔹 Отменить комит
```bash
git reset --hard git f75513
git push -f

# или

# смотрим какие комиты еще не запушены
git log --oneline
# отменяем нужное кол-во
git reset HEAD~1
```
---
#### 🔹 Отменить изменения в файле
```bash
git checkout <file_name>

# отменить все изменения (файл должен быть в состоянии untacked)
git checkout -- filename
```
---
#### 🔹 Работа с репозиторием
```bash
# Показать на каком удаленом репозитории находиться проект
git remote -v

# Поменять ссылку на удаленый реп с https на ssh где link - ссылка на ssh (береться в проекте в кнопке clone and download(появиться после того как установиться соединение по ssh))
git remote set-url origin link

git remote set-url origin git@bitbucket.org:wezom/haulk-bodyshop-back.git

# Удалить ссылку на удаленый репозитория
git remote remove <названия_ссылки_на_удалённый_репозиторий>

# УДАЛЕНИЕ папки из репозитория
git rm -r --cached node_modules
git push origin master
```
---
#### 🔹 Отменить команду add
```bash
git reset
```
---
#### 🔹 Показать изменения в файле 
```bash
# файл должен быть в состоянии staged
git diff --staged

# изменения по конкретному файлу
git diff <file_name>
```
---
#### 🔹 РАБОТА С КОМИТАМИ
```bash
# Откатиться на предыдущий комит
# где hash - хеш комита (можно получить введя git log)
git checkout <hash>

# вернуться на последний комит 
git checkout master

# Откатиться на комит ниже,при это унитожить все верхнии комиты
git reset --hard HEAD~ - опуститься на комит вниз а верхний полностью удалит

# можно после ~ указать кол-во,на какое мы хотим спуститься
git reset --soft HEAD~ - удалит верхни стоящий комит(лог),но файлы не удалит

# Изменить название комита (или поменять какие-то файлы) не создавая новый комит  после его откроеться редактор vim ,и вверху будет название комита нажимаем Insert,редактируем название после сохраняем и выходим(:wq!)
git commit --amend
```

#### 🔹 ТЕГИ

> Тег - это способ пометить определенную версию кода.  Тег можно представить как ветку, которая никогда не меняется.

```bash
# Чтобы создать легкий тег, нужно выполнить такую команду:
git tag c7.0

# получить информацию о теге
git --no-pager show c7.0

# получить список всех тегов
git --no-pager tag

# внести тег в GitHub
git push origin c7.0

# удалить существующий тег с локального компьютера
git tag -d c7.0

# удалить тег с сервера GitHub
git push origin :refs/tags/c7.0
```
---

