#python #pandas #package 

`DatetimeIndex` - это индекс, хранящий объекты Timestamp. Если в параметре index передать конструктору Series список отметок времени, то pandas присоединит к Series индекс DatetimeIndex.

```python
timestamps = [
	pd.Timestamp("2020-01-01"),
	pd.Timestamp("2020-02-01"),
	pd.Timestamp("2020-03-01"),
]
pd.Series([1, 2, 3], index = timestamps).index
# Out DatetimeIndex(['2020-01-01', '2020-02-01', '2020-03-01'],
# dtype='datetime64[ns]', freq=None)
```

Мы можем создать `DatetimeIndex` с нуля. Его конструктор доступен на верхнем уровне pandas. В параметре `data` конструктору можно передать любую итерируемую коллекцию с датами. Даты могут быть представлены строками, объектами `datetime`, объектами `Timestamp` и даже их комбинациями. Pandas преобразует все значения в эквивалентные объекты `Timestamp` и сохранит их в индексе:
```python
mixed_dates = [
	dt.date(2018, 1, 2),
	"2016/04/12",
	pd.Timestamp(2009, 9, 7)
]

dt_index = pd.DatetimeIndex(mixed_dates)
s = pd.Series(data = [100, 200, 300], index = dt_index)
# Out
# 2018-01-02 100
# 2016-04-12 200
# 2009-09-07 300

# можно сортировать по дате
s.sort_index()

# также можно сравнивать
morning = pd.Timestamp("2020-01-01 11:23:22 AM")
evening = pd.Timestamp("2020-01-01 11:23:22 PM")
morning < evening # True
```
---
#### 🔹 Использование объекта `DATETIMEPROPERTIES`

```python
disney["Date"].dt
# Out <pandas.core.indexes.accessors.DatetimeProperties object at 0x116247950>
```
Мы можем обращаться к атрибутам и методам объекта `DatetimeProperties` и извлекать с их помощью информацию из значений `datetime` в столбце.
```python
disney["Date"].head(3)
# Out 
# 0 1962-01-02
# 1 1962-01-03
# 2 1962-01-04
# Name: Date, dtype: datetime64[ns]

disney["Date"].dt.day
# Out 
# 0 2
# 1 3
# 2 4

# так же можно получить месяц и год
disney["Date"].dt.month
disney["Date"].dt.year

# атрибут dayofweek, возвращающий последовательность Series чисел, представляющих дни недели. 0 обозначает понедельник, 1 — вторник и далее до 6 — воскресенья.
# В следующем примере значение 1 в позиции индекса 0 указывает, что 2 января 1962 года приходится на вторник:
disney["Date"].dt.dayofweek.head()
# Out 
# 0 1
# 1 2
# 2 3

# дни недели можно поучить и так
disney["Date"].dt.day_name().head()
# Out 
# 0 Tuesday
# 1 Wednesday
# 2 Thursday

# можно посчитать среднюю доходность акций Disney по дням недели.
disney["Day of Week"] = disney["Date"].dt.day_name()
group = disney.groupby("Day of Week")
group.mean()
# Out
# Day of Week High Low Open Close
# ---------------------------------------------------
# Friday 23.767304 23.318898 23.552872 23.554498
# Monday 23.377271 22.930606 23.161392 23.162543
# Thursday 23.770234 23.288687 23.534561 23.540359
# Tuesday 23.791234 23.335267 23.571755 23.562907
# Wednesday 23.842743 23.355419 23.605618 23.609873

# Метод month_name возвращает Series с названиями месяцев
disney["Date"].dt.month_name().head()

# Атрибут is_quarter_start возвращает Series с логическими значениями, где True означает, что дата в строке приходится на день начала квартала.Кварталы начинаются 1 января, 1 апреля, 1 июля и 1 октября.
# можно получить данные, дни которых выпадают на начала квартала
disney[disney["Date"].dt.is_quarter_start].head()
# Out
# Date High Low Open Close Day of Week
# ----------------------------------------------------------
# 189 1962-10-01 0.064849 0.062355 0.063913 0.062355 Monday
# 314 1963-04-01 0.087989 0.086704 0.087025 0.086704 Monday
# 377 1963-07-01 0.096338 0.095053 0.096338 0.095696 Monday

# is_quarter_end - дни выпадают на конец квартала

# Атрибуты is_month_start и is_month_end позволяют выбрать строки с датами, приходящимися на начало и конец месяца соответственно

# Атрибут is_year_start возвращает значение True, если дата приходится на начало года

# Атрибут is_year_end возвращает значение True, если дата приходится на конец года
```
---
#### 🔹 Сложение и вычитание интервалов времени

Складывать и вычитать согласованные интервалы времени можно с помощью объекта `DateOffset`. Его конструктор доступен на верхнем уровне библиотеки pandas, принимает параметры years, months, days и др.
```python
# интервал времени протяженностью три года четыре месяца и три дня
pd.DateOffset(years = 3, months = 4, days = 5)
```

==Прибавляем дни==
```python
disney["Date"].head()
# Out
# 0 1962-01-02
# 1 1962-01-03
# 2 1962-01-04

# добавляем 5 дней
(disney["Date"] + pd.DateOffset(days = 5)).head()
# Out 
# 0 1962-01-07
# 1 1962-01-08
# 2 1962-01-09
```
---
#### 🔹 Смещение дат

Pandas предлагает готовые объекты, представляющие смещения, для динамических расчетов на основе времени. Они определены в модуле `offsets.py`. Обращения к этим смещениям в нашем коде мы должны предварять префиксом с полным путем: `pd.offsets`.

```python
# MonthEnd, оно округляет каждую дату до конца следующего месяц
disney["Date"].tail()
# Out
# 14722 2020-06-26
# 14723 2020-06-29
# 14724 2020-06-30
# 14725 2020-07-01

(disney["Date"] + pd.offsets.MonthEnd()).tail()
# Out 
# 14722 2020-06-30
# 14723 2020-06-30
# 14724 2020-07-31
# 14725 2020-07-31
```
При округлении действует непреложное правило: затребованное смещение всегда должно произойти. Pandas не может округлить дату до той же даты, то есть если округляемая дата уже приходится на конец месяца, библиотека округляет ее до конца следующего месяца. Так, например, pandas округлила дату `2020-06-30` в позиции `14724` до `2020-07-31` — конца следующего месяца.

```python
# MonthBegin, выполняет округление до первой даты месяца
(disney["Date"] + pd.offsets.MonthBegin()).tail()
# Out 
# 14722 2020-07-01
# 14723 2020-07-01
# 14724 2020-07-01
# 14725 2020-08-01

# Для расчета рабочего времени доступна специальная группа смещений - их имена начинаются с заглавной буквы B. смещение до конца рабочего месяца (BMonthEnd) округляет заданную дату до последнего рабочего дня месяца. Рабочими днями считаются понедельник, вторник, среда, четверг и пятница.
```
---
#### 🔹 Объект TIMEDELTA

Объект `timedelta` моделирует временные интервалы — количество времени, отделяющее два момента.

```python
# создается экземпляр Timedelta, моделирующий интервал протяженностью восемь дней семь часов шесть минут и пять секунд
 duration = pd.Timedelta(
	days = 8,
	hours = 7,
	minutes = 6,
	seconds = 5
)
# Out Timedelta('8 days 07:06:05')

# Функция to_timedelta, доступная на верхнем уровне pandas, преобразует свой аргумент в объект Timedelta
duration = pd.to_timedelta("3 hours, 5 minutes, 12 seconds")
# Out Timedelta('0 days 03:05:12')

# В вызов функции to_timedelta можно передать итерируемый объект, такой как список, чтобы преобразовать его значения в объекты Timedelta. Результат в этом случае будет возвращен в виде TimedeltaIndex — индекса для хранения интервалов
pd.to_timedelta([5, 10, 15], unit = "day")
# Out TimedeltaIndex(['5 days', '10 days', '15 days'],dtype='timedelta64[ns]', freq=None)
```

> Обычно объекты `Timedelta` получаются в результате вычислений и редко создаются с нуля

```python
pd.Timestamp("1999-02-05") - pd.Timestamp("1998-05-24")
# Out Timedelta('257 days 00:00:00')
```

```python
deliveries = pd.read_csv("deliveries.csv")
# Out
# order_date delivery_date
# --------------------------
# 0 5/24/98 2/5/99
# 1 4/22/92 3/6/98
# 2 2/10/91 8/26/92
# 3 7/21/92 11/20/97
# 4 9/2/93 6/10/98

for column in ["order_date", "delivery_date"]:
	deliveries[column] = pd.to_datetime(deliveries[column])
deliveries.head()
# Out
# order_date delivery_date
# --------------------------
# 0 1998-05-24 1999-02-05
# 1 1992-04-22 1998-03-06
# 2 1991-02-10 1992-08-26
# 3 1992-07-21 1997-11-20
# 4 1993-09-02 1998-06-10

# Рассчитаем продолжительность каждой доставки и добавим соответсвующую таблицу
deliveries["duration"] = (
	deliveries["delivery_date"] - deliveries["order_date"]
)
deliveries.head()
# Out
# order_date delivery_date duration
# ------------------------------------
# 0 1998-05-24 1999-02-05 257 days
# 1 1992-04-22 1998-03-06 2144 days
# 2 1991-02-10 1992-08-26 563 days
# 3 1992-07-21 1997-11-20 1948 days
# 4 1993-09-02 1998-06-10 1742 days
deliveries.dtypes
# Out
# order_date datetime64[ns]
# delivery_date datetime64[ns]
# duration timedelta64[ns]
# dtype: object

# Timedelta можно также применять математические методы
deliveries["duration"].max()
deliveries["duration"].min()
deliveries["duration"].mean()

# Выберем из набора данных DataFrame записи, в которых продолжительность доставки составила больше года
deliveries[deliveries["duration"] > "365 days"].head()
```