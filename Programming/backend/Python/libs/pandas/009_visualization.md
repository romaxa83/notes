#python #pandas #package #matplotlib #recipes

Вывод содержимого наборов данных DataFrame имеет определенную ценность, но во многих случаях информация лучше воспринимается, когда она представлена в виде графиков и диаграмм. _Линейный график_ позволяет быстро оценить развитие тенденций с течением времени; _гистограмма_ дает возможность четко идентифицировать уникальные категории и численность; _круговая диаграмма_ помогает увидеть пропорции и соотношения и т. д.

Для визуализации данных используем библиотеку `mathplotlib` - здесь подробнее про библиотеку [[mathplotlib]]

```python
import pandas as pd
import matplotlib.pyplot as plt
%matplotlib inline

space = pd.read_csv(
	"space_missions.csv",
	parse_dates = ["Date"],
	index_col = "Date"
)
space.head()
# Out
#            Company Name Location Cost Status
# Date
# -------------------------------------------------
# 2019-02-05 Arianespace France 200.00  Success
# 2019-02-22 SpaceX      USA    50.00   Success
# 2019-03-02 SpaceX      USA    50.00   Success
# 2019-03-09 CASC        China  29.15   Success
# 2019-03-22 Arianespace France 37.00   Success
```
---
#### 🔹 График временной последовательности

Оптимальным средством визуализации изменения тенденций с течением времени является график _временной последовательности_. Для его построения надо отложить время по оси X и оцениваемые значения по оси Y.

К примеру нужно отобразить стоимость полетов за два года
```python
# извлечем столбец Cost из набора данных space.
space["Cost"].head()
# Out
# Date
# 2019-02-05 200.00
# 2019-02-22 50.00
# 2019-03-02 50.00
# 2019-03-09 29.15
# 2019-03-22 37.00
# Name: Cost, dtype: float64

# Чтобы отобразить эти данные в форме графика, вызовем метод plot структуры данных
space["Cost"].plot()

# Метод plot принимает параметр y, в котором можно передать определение столбца DataFrame, значения которого Matplotlib должна отобразить
space.plot(y = "Cost")

# Мы можем использовать параметр colormap, чтобы изменить цветопередачу на графике
space.plot(y = "Cost", colormap = "gray")

# Получить список допустимых названий цветовых схем для передачи в параметре colormap можно вызовом метода colormaps из пакета pyplot
print(plt.colormaps())
```
---
#### 🔹 Гистограммы

Гистограмма — отличный выбор для отображения количества уникальных значений в наборе данных.
Параметр `kind` метода `plot` изменяет тип диаграммы

```python
space["Company Name"].value_counts()
# Out 
# CASC 35
# SpaceX 25
# Roscosmos 12
# Arianespace 10
# Rocket Lab 9
# VKS RF 6
# ULA 6
space["Company Name"].value_counts().plot(kind = "bar")

# чтобы отобразить горизонтальную гистограмму
space["Company Name"].value_counts().plot(kind = "barh")
```
---
#### 🔹 Круговые диаграммы 

Круговая диаграмма отображает цветные сегменты, складывающиеся в целый круг (подобно кусочкам пиццы). Каждый сегмент визуально отражает долю, вносимую им в общую сумму.

```python
space["Status"].value_counts()
# Out
# Success 114
# Failure 6
# Name: Status, dtype: int64
space["Status"].value_counts().plot(kind = "pie")

# Чтобы добавить легенду в диаграмму, можем передать параметр legend с аргументом True
space["Status"].value_counts().plot(kind = "pie", legend = True)
```