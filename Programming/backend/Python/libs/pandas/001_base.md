#python #package #pandas

- Введение в pandas: анализ данных на Python - https://khashtamov.com/ru/pandas-introduction/
- https://github.com/paskhaver/pandas-in-action
---
- Pandas — библиотека для анализа данных, основанная на языке программирования Python.
- Pandas превосходно справляется со сложными операциями над большими наборами данных и отличается сжатым синтаксисом.
- В качестве альтернатив использованию библиотеки pandas можно рассматривать визуальное приложение электронных таблиц Excel, статистический язык программирования R и комплект программ SAS.
- Pandas способна импортировать множество разнообразных форматов файлов.
- `DataFrame` — основная структура данных библиотеки pandas. По существу, она представляет собой таблицу данных с несколькими столбцами.
- `Series` представляет собой одномерный маркированный массив. Его можно рассматривать просто как отдельный столбец данных. Каждому значению `Series` в Pandas соответствует метка (`label`) — идентификатор, с помощью которого можно ссылаться на это значение. Объект Series, как основной «строительный блок» библиотеки pandas.
- Обращаться к строкам в `Series` и `DataFrame` можно по номеру строки или метке индекса.
- Можно сортировать `DataFrame` по значениям одного или нескольких столбцов.
- Для извлечения подмножеств данных из `DataFrame` можно использовать логические условия.
- Можно группировать строки DataFrame по значениям какого-либо столбца и затем производить над полученными группами операции агрегирования, например суммирование.
---
#### 🔹 Оптимизация памяти, используемой набором данных 

При импорте набора данных важно обеспечить хранение данных во всех столбцах в наиболее оптимальных типах. «Наилучший» тип данных — тип, потребляющий меньше всего памяти или обеспечивающий наибольшие возможности на практике.Например, целочисленные значения на большинстве компьютеров занимают меньше памяти, чем значения с плавающей точкой, так что, если набор данных содержит только целые числа, лучше импортировать их в виде целочисленных значений, а не значений с плавающей точкой. Еще один пример: если данные содержат даты, лучше всего импортировать их в виде меток даты/времени, а не строковых значений, чтобы можно было производить над ними соответствующие операции.
Важно при оптимизации:
-  преобразовать текстовые значения в столбцах где используется дата в метки даты/времени с помощью параметра `parse_dates`
```python
employees = pd.read_csv(
	"employees.csv", parse_dates = ["Start Date"]
)
```

 - Для просмотра списка столбцов, их типов данных, количества отсутствующих значений и общего объема занимаемой объектом DataFrame оперативной памяти можно вызвать метод `info` и проанализировать набор данных.
```python
employees.info()
# Out
# <class 'pandas.core.frame.DataFrame'>
# RangeIndex: 1001 entries, 0 to 1000
# Data columns (total 6 columns):
# Column Non-Null Count Dtype
# --- ------ -------------- -----
# 0 First Name 933 non-null object
# 1 Gender 854 non-null object
# 2 Start Date 999 non-null datetime64[ns]
# 3 Salary 999 non-null float64
# 4 Mgmt 933 non-null object
# 5 Team 957 non-null object
# dtypes: datetime64[ns](1), float64(1), object(4)
# message usage: 47.0+ KB
```

- для преобразование типов используется метод `astype`, принимает один аргумент — новый тип данных, причем передать ему можно как тип данных, так и строковое значение с его названием
```python
# столбец Mgmt имеет два значени - True и False, их нужно преобразовать в bool
# извлекаем объект Series Mgmt из employees и вызываем его метод astype с аргументом bool
employees["Mgmt"].astype(bool)

# или сразу перезаписываем в DataFrame
employees["Mgmt"] = employees["Mgmt"].astype(bool)
```

- Если в столбце целочисленные значения, но есть отсутсвующие значение (NaN), то все значения будут преобразованы (автоматически) в числа с плавающей точкой и в таком случае если мы преобразуем их в `int` то получим ошибку - `ValueError ( Cannot convert non-finite values (NA or inf) to integer)`. Для этого нужно использовать метод `fillna` который  заменяет пустые значения в объекте Series на переданный ему аргумент.
```python
employees["Salary"] = employees["Salary"].fillna(0).astype(int)
```

- В библиотеке pandas есть особый тип данных — категория (`category`). Он идеально подходит для столбцов, состоящих из небольшого числа уникальных значений (то есть набор значений невелик по сравнению с общим количеством записей в столбце).Несколько распространенных примеров точек данных с ограниченным количеством значений: пол, дни недели, группы крови, планеты и группы населения по уровню доходов. «За кулисами» pandas хранит только одну копию каждого категориального значения, а не все дублирующиеся в разных строках значения.
```python
# Подсчитать число уникальных значений в каждом столбце объекта DataFrame
employees.nunique()
# Out 
# First Name 200
# Gender 2
# Start Date 971
# Salary 995
# Mgmt 2
# Team 10

# Столбцы Gender и Team, подходят для хранения категориальных значений
employees["Gender"] = employees["Gender"].astype("category")
# занимаемой памяти резко сократился, поскольку pandas приходится отслеживать лишь два значения
```
---
#### 🔹 Настройки

Pandas хранит свои настройки в единственном объекте `options`, доступном на верхнем уровне библиотеки. Каждый его параметр принадлежит некоторой категории. Функция `describe_option` возвращает описание параметра, название которого можно передать функции в виде строки.
```python
pd.describe_option("display.max_rows")
```
получить значения можно с помощью функция `get_option`
```python
# Следующие две строки эквивалентны
pd.get_option("display.max_rows")
pd.options.display.max_rows
```
 изменить значение параметра настройки можно с помощью функция `set_option`
```python
 # Следующие две строки эквивалентны
pd.set_option("display.max_rows", 6)
pd.options.display.max_rows = 6
```
Чтобы вернуть параметр в исходное состояние, просто передайте его имя функции `reset_option`
```python
pd.reset_option("display.max_rows")
```

Данный подход изменяет настройки глобально, если же нужно изменить для разового отображения то этого можно добиться с помощью функции `option_context`, доступной на верхнем уровне pandas, объединив ее со встроенным ключевым словом `with` для создания контекстного блока.Контекстный блок можно рассматривать как временную среду выполнения. Функция `option_context` устанавливает временные значения для параметров pandas, действующие, пока выполняется код внутри блока; глобальные настройки при этом не затрагиваются.
```python
with pd.option_context(
	"display.max_columns", 5,
	"display.max_rows", 10,
	"display.precision", 3
):
display(data)
```

##### 🔸`display.max_rows`
Параметр `max_rows` в категории `display`. Он определяет максимальное количество строк, которое выведет pandas, прежде чем та усечет отображаемую часть DataFrame.

##### 🔸`display.max_columns`
Параметр `max_columns` в категории `display`. Он определяет максимальное количество печатаемых столбцов.

##### 🔸`display.precision`
Параметр `display.precision` устанавливает количество цифр, отображаемых после десятичной точки при выводе вещественных чисел. Значение по умолчанию равно 6.Не изменяет исходные значения в DataFrame и влияет только на их представление при выводе.

##### 🔸`display.max_colwidth`
Параметр `display.max_colwidth` определяет максимальное количество символов, которые pandas может вывести, прежде чем обрезать вывод содержимого текстовой ячейки.

##### 🔸`display.chop_threshold`
Параметр `display.chop_threshold` устанавливает предел, ниже которого числа с плавающей точкой будут отображаться как `0`.Не изменяет исходные значения в DataFrame и влияет только на их представление при выводе.

---
#### 🔹 Series

создание объекта 
```python
import pandas as pd

# из списка
ice_cream_flavors = [
	"Chocolate",
	"Vanilla",
	"Strawberry",
	"Rum Raisin",
]

pd.Series(ice_cream_flavors)
# out
# 0 Chocolate
# 1 Vanilla
# 2 Strawberry
# 3 Rum Raisin

# из словаря
calorie_info = {
	"Cereal": 125,
	"Chocolate Bar": 406,
	"Ice Cream Sundae": 342,
}

pd.Series(calorie_info)
# out
# Cereal 125
# Chocolate Bar 406
# Ice Cream Sundae 342

# из кортежа
pd.Series(data = ("Red", "Green", "Blue"))
# out
# 0 Red
# 1 Green
# 2 Blue

# При передачи множества (set) pandas сгенерирует исключение TypeError, поскольку для множеств не определены ни порядок (как у списка), ни связь (как у словаря). А значит, библиотека pandas не может знать, в каком порядке хранить значения множества. Если данные находятся в множестве, их сначала нужно преобразовать к примеру в спиок - list(my_set)
```

Объект `Series()` - принимает несколько параметров:
 - `data` - данные
 - `index` - задает метки индекса для данных (по умолчанию начинаются с 0 и по возрастанию). Можно передавать в параметрах `data` и `index` объекты различных типов, но длины их должны быть одинаковыми, чтобы pandas смогла сопоставить их значения
 - `dtype` - задает тип данных для значений
 - `name`
 - `copy`
 - `fastpath`

#### 🔹 Атрибуты SERIES

Атрибуты возвращают информацию о внутреннем состоянии объекта. Атрибуты открывают доступ к вложенным объектам, возможно обладающим собственной функциональностью.

```python
calorie_info = {
	"Cereal": 125,
	"Chocolate Bar": 406,
	"Ice Cream Sundae": 342,
}
diet = pd.Series(calorie_info)

# values - позволяет обращаться к объекту ndarray, в котором хранятся значения
diet.values # array([125, 406, 342])

# index - возвращает объект Index с метками Series
diet.index 
# Index(['Cereal', 'Chocolate Bar', 'Ice Cream Sundae'],dtype='object')

# dtype - возвращает тип данных значений объекта Series:
diet.dtype # dtype('int64')

# size - возвращает количество значений в объекте Series
diet.size # 3

# shape - возвращает кортеж с размерностями структуры данных библиотеки pandas
diet.share # (3,)

# is_unique - возвращает True, если все значения Series уникальны
diet.is_unique # True

# is_monotonic - возвращает True, если каждое значение объекта Series больше предыдущего или равно ему
pd.Series(data = [1, 3, 6]).is_monotonic # True
```
---
#### 🔹 Методы SERIES

```python
# head - возвращает строки из начала набора данных. Он принимает один аргумент n — число извлекаемых строк
values = range(0, 500, 5)
nums = pd.Series(data = values)
nums.head(3)
# 0 0
# 1 5
# 2 10

#tail - возвращает строки из конца объекта Series

# count - служит для подсчета числа непустых значений
numbers = pd.Series([1, 2, 3, np.nan, 4, 5])
numbers.count() # 5

# sum - суммирует значения объекта Series
numbers.sum() # 15.0
# Большинство математических методов по умолчанию игнорируют отсутствующие значения. Чтобы включить их в расчеты, можно передать аргумент False для параметра skipna
numbers.sum(skipna = False) # nan
# Параметр min_count метода sum задает минимальное количество допустимых значений в Series, которое необходимо pandas для подсчета суммы объекта
numbers.sum(min_count = 3) # 15.0
numbers.sum(min_count = 6) # nan

# product - перемножает все значения объекта Series, может также принимать параметры skipna и min_count 
numbers.product() # 120.0

# cumsum (нарастающая сумма) - возвращает новый объект Series, содержащий скользящую сумму значений. Соответствующая каждому индексу позиция содержит сумму значений, вплоть до значения по этому индексу (включительно). Нарастающая сумма помогает определять, какие значения вносят максимальный вклад в общую сумму
numbers.cumsum()
# 0 1.0
# 1 3.0
# 2 6.0
# 3 NaN
# 4 10.0
# 5 15.0

# pct_change (процентное изменение) - возвращает процентную разницу между последовательными значениями объекта Series. На позиции, соответствующей каждому из индексов, pandas складывает значение с предыдущей позиции и текущее, после чего делит сумму на предыдущее значение. Pandas вычисляет процентную разницу только при наличии на обеих позициях допустимых числовых значений.
numbers.pct_change()
# исходные данные. операция.         выходные данные
# 0 1.0.           nan.               nan   
# 1 2.0.          (2.0 - 1.0) /1.0.   1.00000
# 2 3.0.          (3.0 - 2.0) /2.0    0.50000
# 3 nan.          (4.0 - 3.0) /3.0.   0.00000
# 4 4.0.          (5.0 - 4.0) /4.0.   0.333333
# 5 5.0.          (3.0 - 3.0) /5.0.   0.25000

# mean - возвращает среднее значений объекта Series
numbers.mean() # 3.0

# median - возвращает число, расположенное посередине в отсортированном списке значений Series
numbers.median() # 3.0

# std - возвращает стандартное отклонение (standard deviation) — меру отклонения данных от среднего значения:
numbers.std() # 1.5811388300841898

# max и min служат для извлечения максимального и минимального значений из объекта Series
numbers.max() # 5.0
numbers.min() # 1.0

# describe - получает всю сводную информацию об объекте Series
numbers.describe()
# count 5.000000
# mean 3.000000
# std 1.581139
# min 1.000000
# 25% 2.000000
# 50% 3.000000
# 75% 4.000000
# max 5.000000

# sample - выбирает случайный набор значений из объекта Series
numbers.sample(3)
# 1 2
# 3 4
# 2 3

# unique возвращает NumPy-объект ndarray, содержащий неповторяющиеся значения из объекта Series

# nunique возвращает количество уникальных значений в объекте Series:
```

##### 🔸 Арифметические операции
Библиотека pandas предоставляет и другие способы выполнения арифметических вычислений с Series. Производить арифметические операции над Series в Python можно с помощью стандартных математических операторов:
- `+` - для сложения
- `-` - для вычитания
- `*` - для умножения
- `/` - для деления

```python
s1 = pd.Series(data = [5, np.nan, 15], index = ["A", "B", "C"])
s1 + 3
# A 8.0
# B NaN
# C 18.0

# или так, результат будет тот же
s1.add(3)

s1 - 5
s1.sub(5)
s1.subtract(5)

s1 * 2
s1.mul(2)
s1.multiply(2)

s1 / 2
s1.div(2)
s1.divide(2)
```

##### 🔸 Преобразование даты
При импорте набора данных библиотека pandas определяет самый подходящий тип данных для каждого столбца. Если данные включают даты то они будут преобразованы в строку, но можно указать чтобы они преобразовывались в метку времени.
```python
pd.read_csv("google_stocks.csv", parse_dates = ["Date"]).head()
```

если даты не будет то получаем значение NumPy-объекта NaT (not a time —«не значение времени»).

##### 🔸 Выбор колонок
```python
#параметра usecols функции read_csv можно указать список импортируемых библиотекой pandas столбцов 

pd.read_csv(
	"revolutionary_war.csv",
	index_col = "Start Date",
	parse_dates = ["Start Date"],
	usecols = ["State", "Start Date"],
	squeeze = True
).tail()
```

##### 🔸 Сортировка объектов SERIES
Можно отсортировать объект Series по значениям или индексу, в порядке возрастания или убывания.
```python
pd.read_csv("google_stocks.csv", parse_dates = ["Date"]).head()

# сортируем по значению (от меньшего к большему)
google.sort_values()

# сортировка в порядке убывания
google.sort_values(ascending = False).head()

# значения NaN в конце списка
# Две нижеприведенных строки эквивалентны
google.sort_values()
google.sort_values(na_position = "last")

# значения NaN в начале списка
google.sort_values(na_position = "first")

# удаляет значения NaN только среди значений объекта Series, но не индекса
google.dropna().sort_values()


# сортировка по индексу
# Две нижеприведенные строки эквивалентны
google.sort_index()
google.sort_index(ascending = True)
```

##### 🔸 Минимального и максимального значений
```python
# сортирует значения в возвращаемом объекте Series в порядке убывания
google.sort_values(ascending = False).head()

# для операции выше можно использовать метод nlargest
# Две нижеприведенные строки эквивалентны
google.nlargest(n = 5)
google.nlargest()

# метод nsmallest, он возвращает минимальные значения объекта Series, отсортированные в порядке возрастания
google.nsmallest(n = 5)
google.nsmallest(5)
```

##### 🔸 Перезапись объекта SERIES
При манипуляции и получении данных сами данные в объекте не меняются, если нам нужно модифицировать объект Series, то используется метод `inplace`
```python
# метод вернет None и вывода не будет, но порядок даных измениться в самом обьекте и далее в работе он уже будет измененый 
google.sort_values(inplace = True)
```

> У параметра `inplace` имеется одна особенность. Его название предполагает модификацию/изменение уже существующего объекта вместо создания копии. Но необходимо осознавать, что даже при использовании параметра `inplace` библиотека `pandas` создает копию объекта при каждом вызове метода. Библиотека всегда создает дубликат; параметр `inplace` просто размещает этот новый объект в уже существующую переменную.Таким образом, вопреки расхожему мнению, параметр `inplace` не дает никакого преимущества в смысле производительности.

##### 🔸 Подсчет кол-во значений
`value_counts` - подсчитывающий количество вхождений каждого значения объекта Series. Возвращает новый объект Series. В роли меток индекса выступают значения объекта Series, а в роли значений - их количество в наборе.
```python
pokemon.value_counts()
# Normal 65
# Water 61
# Grass 38
# Psychic 35
# Fire 30

# можно сортироват значения
pokemon.value_counts(ascending = True)

# Для получения частотности всех уникальных значений установите параметр normalize метода value_counts в True
pokemon.value_counts(normalize = True).head()

# Для получения доли каждого типа покемонов в процентах можно умножить объект Series с частотностями на 100
pokemon.value_counts(normalize = True) * 100
# можно округлить значения
(pokemon.value_counts(normalize = True) * 100).round(2)
```

> В подобных ситуациях первостепенное значение имеет целостность данных. Лишний пробел или отличающийся регистр символа — и библиотека pandas распознает два значения как не равные между собой и посчитает их по отдельности

Можно группировать данные по корзинам и подсчитывать их кол-во
```python
# Сгруппируем курсы акций по корзинам начиная с 0 и до 1400
buckets = [0, 200, 400, 600, 800, 1000, 1200, 1400]
google.value_counts(bins = buckets)
# out
# (200.0, 400.0] 1568
# (-0.001, 200.0] 595
# (400.0, 600.0] 575
# (1000.0, 1200.0] 406
# (600.0, 800.0] 380
# (800.0, 1000.0] 207
# круглая скобка означает, что значение не включается в интервал;
# квадратная скобка означает, что значение включается в интервал.

# использовать сортировку
google.value_counts(bins = buckets).sort_index()

# подсчет с пустыми значениями
battles.value_counts(dropna = False).head()
```
---
#### 🔹 Передача объектов  SERIES функциям PYTHON

Можно передавать объекты Series любым встроенным функциям языка Python и получать вполне предсказуемые результаты.
```python
cities = pd.Series(
	data = ["San Francisco", "Los Angeles", "Las Vegas", np.nan]
)

len(cities) # 4
type(cities) # pandas.core.series.Series
list(cities) # ['San Francisco', 'Los Angeles', 'Las Vegas', nan]
dict(cities) # {0: 'San Francisco', 1: 'Los Angeles', 2: 'Las Vegas', 3: nan}

# проверка по индексу
"Las Vegas" in cities # False
2 in cities # True
```
---
#### 🔹 Вызов функции для каждого из значений объекта SERIES

Функции являются объектами первого класса (first-class object) в языке Python, то есть язык обрабатывает их как любой другой тип данных. Все, что можно делать с числом, можно делать и с функцией. Например, следующее:
 - хранить функцию в списке;
- присваивать ключу словаря функцию в качестве значения;
- передавать одну функцию в другую в качестве аргумента;
- возвращать одну функцию из другой.
Важно различать функцию и вызов функции.Функция — это последовательность инструкций, генерирующих какой-либо результат.А вызов функции (function invocation) —это само выполнение инструкций.

Можно вызывать функцию ко всем значениям в объекте Series, для этого есть метод `apply`, вызывающий заданную функцию однократно для каждого значения объекта Series. Этот метод возвращает новый объект Series, состоящий из возвращаемых значений этих вызовов функции. В качестве первого параметра метод `apply` ожидает вызываемую функцию.
```python
# Две нижеприведенные строки эквивалентны
google.apply(func = round)
google.apply(round)
```

Методу `apply` можно передавать и пользовательские функции. Достаточно описать функцию с одним параметром, возвращающую значение, которое pandas должна поместить в агрегированный объект Series.
```python
def single_or_multi(pokemon_type):
	if "/" in pokemon_type:
		return "Multi"
	return "Single"
	
pokemon.apply(single_or_multi)
pokemon.apply(single_or_multi).value_counts()
```
 ---
#### 🔹 Чтение файлов

```python
# чтение из файла csv (файл лежит там же где и скрипт), 
# строки эквивалентны
pd.read_csv(filepath_or_buffer = "pokemon.csv")
pd.read_csv("pokemon.csv")

# указываем какая колонка будет в качестве идентификатора (index)
pd.read_csv("pokemon.csv", index_col = "Pokemon")

# Параметр squeeze приводит к использованию Series вместо DataFrame
pd.read_csv("pokemon.csv", index_col = "Pokemon", squeeze = True)
```
---
#### 🔹 Объект DataFrame

Объект `DataFrame` библиотеки pandas представляет собой двумерную таблицу данных со строками и столбцами. Аналогично объектам `Series` pandas присваивает метку индекса и позицию индекса каждому столбцу. Мы называем `DataFrame` двумерным, поскольку для указания конкретного значения из набора данных необходимы две координаты — строка и столбец.Синтаксис создания экземпляра класса `DataFrame` идентичен синтаксису создания экземпляра `Series`. Обращаемся к классу `DataFrame` и создаем его экземпляр с помощью пары круглых скобок: `pd.DataFrame()`

```python
# создание из ассоциативного массива
city_data = {
	"City": ["New York City", "Paris", "Barcelona", "Rome"],
	"Country": ["United States", "France", "Spain", "Italy"],
	"Population": [8600000, 2141000, 5515000, 2873000]
}

cities = pd.DataFrame(city_data)
# Out
#    City Country Population
# ----------------------------------------------
# 0 New York City United States 8600000
# 1 Paris France 2141000
# 2 Barcelona Spain 5515000
# 3 Rome Italy 2873000

# если понадобится поменять заголовки столбцов и метки индекса местами
# Две строки ниже эквивалентны
cities.transpose()
cities.T
# Out
#       0          1        2        3
# -------------------------------------------------------
# City New York City Paris Barcelona Rome
# Country United States France Spain Italy
# Population 8600000 2141000 5515000 2873000
```

Можно такж, при создании DataFrame, использовать объект `ndarray` библиотеки `NumPy`
```python
random_data = np.random.randint(1, 101, [3, 5])
pd.DataFrame(data = random_data)

# создание меток
row_labels = ["Morning", "Afternoon", "Evening"]
temperatures = pd.DataFrame(
	data = random_data,
	index = row_labels
)
# out
#          0  1  2  3  4
# ------------------------------
# Morning 25 22 80 43 42
# Afternoon 40 89 7 21 25
# Evening 89 71 32 28 39

# Задать названия столбцов можно с помощью параметра columns конструктора
column_labels = (
	"Monday",
	"Tuesday",
	"Wednesday",
	"Thursday",
	"Friday",
)
pd.DataFrame(
	data = random_data,
	index = row_labels,
	columns = column_labels,
)
# Out
#.         Monday Tuesday Wednesday Thursday Friday
# --------------------------------------------------------
# Morning 25 22 80 43 42
# Afternoon 40 89 7 21 25
# Evening 89 71 32 28 39
```

> Многие атрибуты и методы класса `Series` присутствуют и в классе `DataFrame`. Их реализация, впрочем, может отличаться: библиотека `pandas` должна учитывать во втором случае наличие у объекта нескольких столбцов и двух отдельных осей координат. Объект `DataFrame` удобно рассматривать как набор объектов `Series` с общим индексом
---
#### 🔹 Атрибуты DataFrame

```python
city_data = {
	"City": ["New York City", "Paris", "Barcelona", "Rome"],
	"Country": ["United States", "France", "Spain", "Italy"],
	"Population": [8600000, 2141000, 5515000, 2873000]
}

cities = pd.DataFrame(city_data)

# dtypes - атрибут возвращает объект Series со столбцами объекта DataFrame в качестве меток индекса и типами данных столбцов в качестве значений
cities.dtypes

# index - можно обращаться к индексу объекта DataFrame возвращает объект RangeIndex который включает три атрибута: start (нижняя граница, включительно), stop (верхняя граница, не включительно) и step (интервал/шаг между двумя соседними значениями)
cities.index
# out
# RangeIndex(start=0, stop=450, step=1)

# columns - обращение к столбцам, возвращает объект Index когда индекс состоит из текстовых значений

# ndim возвращает размерность объекта pandas. Размерность DataFrame — два

# shape возвращает размеры объекта DataFrame по измерениям в виде кортежа где первое значение - кол-во строк, а второе кол-во столбцов

# size вычисляет общее количество значений в наборе данных. В это число включаются и отсутствующие (пустые) значения
```
---
#### 🔹 Методы DataFrame

##### 🔸 Сортировка
```python
# Сортировка по столбцу - Name
# Две строки ниже эквивалентны
nba.sort_values("Name")
nba.sort_values(by = "Name")
# может принимать параметр ascending (для сортировки по убыванию и по возрастанию)

# можно сортировать по нескольким столбцам, в примере сначала отсортируется по Team, потом по Name 
nba.sort_values(by = ["Team", "Name"])

# если нужно отсортировать один столбец по убыванию, а второй по возрастанию
nba.sort_values(
	by = ["Team", "Salary"], ascending = [True, False]
)

# Сортировка по индексу
# Две строки ниже эквивалентны
nba.sort_index().head()
nba.sort_index(ascending = True).head()

# Объект DataFrame — двумерная структура данных. Ее можно отсортировать еще по одной оси координат — вертикальной.В следующем примере столбцы сортируются в порядке возрастания
# Две строки ниже эквивалентны
nba.sort_index(axis = "columns").head()
nba.sort_index(axis = 1).head()

# set_index - возвращает новый объект DataFrame с заданным столбцом в качестве индекса
# Две строки ниже эквивалентны
nba.set_index(keys = "Name")
nba.set_index("Name")

# reset_index - сбросить индекс (нужно делать перед назначение нового индекса)
nba.reset_index().set_index("Team").head()
```

##### 🔸 Извлечение одного столбцов из объекта DataFrame
К любому столбцу Series можно обратиться как к атрибуту объекта DataFrame.
```python
nba.Salary
# или так, данный вариант удобнее если в столбцах есть пробелы
nba["Salary"]

# извлечение нескольких столбцов, будут извлечены в порядки их следования
nba[["Salary", "Birthday"]]

# select_dtypes - выбора столбцов по типам данных, Он принимает два параметра, include и exclude, аргументы которых могут представлять собой отдельные строковые значения или списки, указывающие типы столбцов, которые следует выбрать или отбросить
nba.select_dtypes(include = "object")
```

##### 🔸 Извлечение строк из объектов DATAFRAME
```python
# loc - атрибут служит для извлечения строки по метке
nba.loc["LeBron James"]
# извлечение нескольких строк
nba.loc[["Kawhi Leonard", "Paul George"]]
# извлечение данных обо всех игроках от Отто Портера до Патрика Беверли
nba.sort_index().loc["Otto Porter":"Patrick Beverley"]
# извлечение данных от конкретного значения до конца
nba.sort_index().loc["Zach Collins":]
# извлечение данных от начала и до конкретного значения
nba.sort_index().loc[:"Zach Collins"]
# извлекаем значение - "Giannis Antetokounmpo", и только столбец "Team"
nba.loc["Giannis Antetokounmpo", "Team"]
# извлекаем несколько столбцов
nba.loc["James Harden", ["Position", "Birthday"]]

# iloc (index location — «место индекса») извлекает строки по позиции индекса
nba.iloc[300]
# извлечем строки от десятой с конца до шестой с конца, не включая последнюю
nba.iloc[-10:-6]
# извлечения десяти первых строк nba с шагом 2
nba.iloc[0:10:2]
# извлечем значение, расположенное на пересечении строки с индексом 57 и столбца с индексом 3
nba.iloc[57, 3]
```

 есть альтернатива, которая работает быстрее - `at` и `iat`
 ```python
 nba.at["Austin Rivers", "Birthday"]
 
 nba.iat[263, 1]
 ```
##### 🔸 Переименование столбцов и строк
```python
nba.columns
# Out Index(['Team', 'Position', 'Birthday', 'Salary'], dtype='object')

# поменяем название столбца Salary на Pay
nba.columns = ["Team", "Position", "Birthday", "Pay"]

# или можно так
nba.rename(columns = { "Salary": "Pay" })

# переименование индекса (в качестве индекса - имена игроков)
nba = nba.rename(
	index = { "Giannis Antetokounmpo": "Greek Freak" }
)
```
---
#### 🔹 ФИЛЬТРАЦИЯ

Извлечение подмножества — самая распространенная операция в анализе данных. Подмножество (subset) — это часть более крупного набора данных, удовлетворяющих какому-либо условию.
Для сравнения всех записей объекта Series с константой необходимо указать объект Series с одной стороны оператора равенства, а константу — с другой, в результате равенства библиотека pandas возвращает объект Series, содержащий булевы значения. Значение True показывает, что по соответствующему индексу действительно располагается значение, а False — что нет. 

Чтобы получить все записи "Maria" из нашего набора данных, необходимо извлечь лишь строки со значением True в вышеприведенных результатах. Для фильтрации строк необходимо указать булев объект Series между квадратными скобками после объекта DataFrame:
```python
marias = employees["First Name"] == "Maria"
employees[marias]

# или так
employees[employees["First Name"] == "Maria"]

# если значение поле уже bool, то его можно просто передавать
employees[employees["Mgmt"]]

high_earners = employees["Salary"] > 100000
employees[high_earners].head()
```

Также поддерживается фильтрация по нескольким условиям
```python
is_female = employees["Gender"] == "Female"
in_biz_dev = employees["Team"] == "Business Dev"

# логическо И
employees[is_female & in_biz_dev].head()

# логическо ИЛИ
employees[is_female | in_biz_dev].head()

# Логическое отрицание (~)
# Все значения True превращаются в False, а все False становятся True
employees[~(employees["Salary"] >= 100000)]
```

##### 🔸 Методы для работы с булевыми значениями
```python
# Равно
employees["Team"] == "Marketing"
employees["Team"].eq("Marketing")

# Не равно
employees["Team"] != "Marketing"
employees["Team"].ne("Marketing")
 
# Меньше чем
employees["Salary"] < 100000
employees["Salary"].lt(100000)
  
# Меньше чем или равно
employees["Salary"] <= 100000
employees["Salary"].le(100000)
   
# Больше чем
employees["Salary"] > 100000
employees["Salary"].gt(100000)
    
# Больше чем или равно
employees["Salary"] >= 100000
employees["Salary"].ge(100000)
```

##### 🔸 Метод `isin`
К примеру  если требуется выбрать сотрудников, относящихся к одной из групп Sales, Legal или Marketing
```python
sales = employees["Team"] == "Sales"
legal = employees["Team"] == "Legal"
mktg = employees["Team"] == "Marketing"
employees[sales | legal | mktg].head()
```
Но данный вариант не оптимален. Лучше будет воспользоваться методом `isin`, который принимает на входе итерируемый объект с элементами (список, кортеж, объект `Series` и т. д.) и возвращает булев объект `Series`. `True` в его записи означает, что pandas нашла значение из строки среди значений итерируемого объекта, а `False` — что нет. А затем можно отфильтровать объект `DataFrame` как обычно, на основе полученного объекта `Series`.
```python
all_star_teams = ["Sales", "Legal", "Marketing"]
on_all_star_teams = employees["Team"].isin(all_star_teams)
employees[on_all_star_teams].head()
```

##### 🔸 Метод `between`
```python
higher_than_80 = employees["Salary"] >= 80000
lower_than_90 = employees["Salary"] < 90000
employees[higher_than_80 & lower_than_90].head()

# реализацию можно переделать с помощью метода between
between_80k_and_90k = employees["Salary"].between(80000, 90000)
employees[between_80k_and_90k].head()

# можно использовать с датами
eighties_folk = employees["Start Date"].between(
	left = "1980-01-01",
	right = "1990-01-01"
)
employees[eighties_folk].head()
```

##### 🔸 Методы `isnull` и `notnull`
Метод `isnull` возвращает булев объект Series, в котором `True` означает, что значение для соответствующей строки отсутствует. Метод `notnull` возвращает обратный этому объект Series, в котором `True` означает наличие значения для соответствующей строки.
```python
employees["Team"].isnull().head()
employees["Team"].notnull().head()

# можно извлечь всех сотрудников, у которых отсутствует значение в столбце Team
no_team = employees["Team"].isnull()
employees[no_team].head()
```

##### 🔸 Обработка пустых значений
Метод `dropna` служит для удаления строк объекта `DataFrame`, содержащих какие-либо значения `NaN`. Неважно, сколько значений отсутствует в строке; этот метод удалит строку даже при наличии в ней одного-единственного значения `NaN`.
```python
employees.dropna()

# Для удаления только строк, в которых отсутствуют все значения, можно передать методу dropna параметр how с аргументом "all"
employees.dropna(how = "all")

# Для удаления строк, в которых отсутствует значение в конкретном столбце, служит параметр subset
employees.dropna(subset = ["Gender"])
employees.dropna(subset = ["Start Date", "Salary"])

# Параметр thresh позволяет задавать минимальное количество непустых значений в строке, при котором библиотека pandas не будет ее удалят
employees.dropna(how = "any", thresh = 4)
```

##### 🔸 Метод `duplicated`
Метод `duplicated` возвращает булев объект `Series`, указывающий на дублирующие значения в столбце. Библиотека pandas возвращает `True` при обнаружении значения, которое ранее уже встречалось ей в этом столбце.
```python
# отмечает первое вхождение "Finance" в столбце Team значением False как недублирующее. А все последующие вхождения "Finance" отмечает как дублирующие (True).
employees["Team"].duplicated()

# Параметр keep метода duplicated указывает, какое из дублирующих значений оставлять. Аргумент по умолчанию, "first", позволяет оставить первое вхождение каждого из дублирующих значений. Следующий код эквивалентен приведенному выше
employees["Team"].duplicated(keep = "first")
```

##### 🔸 Метод `drop_duplicates`
По умолчанию этот метод удаляет строки, в которых все значения совпадают с уже встречавшейся строкой.
```python
employees.drop_duplicates()

# параметр subset со списком столбцов, которые библиотека pandas должна учитывать при определении уникальности строки
employees.drop_duplicates(subset = ["Team"])

# также может принимать параметр keep
employees.drop_duplicates(subset = ["Team"], keep = "last")
# если keep передать False, он исключит все дублирующие строки
```

