#python #matplotlib #package

```bash
pip install matplotlib
```

#### 🔹 `Pyplot`

**Стиль `Pyplot` (функциональный):** подходит для простых и быстрых набросков. Состояние графика хранится неявно.
```python
import matplotlib.pyplot as plt

plt.plot([1, 2, 3], [4, 5, 1])
plt.show()
```
---
#### 🔹 Объектно-ориентированный стиль

 Рекомендуется для сложных проектов. Вы явно создаете объекты «Фигура» (холст) и «Оси» (координатная плоскость).
```python
fig, ax = plt.subplots() # Создаем фигуру и одну область рисования
ax.plot(x, y)            # Рисуем на конкретных осях
```
---
#### 🔹 Настройка элементов графика

Вы можете управлять практически любой деталью: 
- **Заголовки и подписи:** `plt.title()`, `plt.xlabel()`, `plt.ylabel()`.
- **Легенда:** метод `plt.legend()` подписывает ряды данных. Можно менять размер шрифта (`fontsize`) и положение на графике.
- **Цвета и линии:** задаются параметрами `color` (например, 'r' или '#FF0000') и `linestyle` (например, '--').
- **Сетка:** включается командой `plt.grid(True)`
---
#### 🔹 Работа с данными

Matplotlib бесшовно интегрируется с другими библиотеками: 
- **NumPy:** используется для передачи массивов чисел в качестве координат.
- **Pandas:** графики можно строить прямо из объектов DataFrame с помощью метода `.plot()`, так как визуализация в Pandas основана на Matplotlib.
---
#### 🔹 Сохранение результата

Вместо вывода на экран через `plt.show()`, график можно сохранить в файл нужного формата (PNG, PDF, SVG и др.)

```python
plt.savefig('my_plot.png', dpi=300)
```
---