#python #code

Каждый блок данных в программе — это **объект**. У каждого объекта есть идентификатор (`identity`), тип (еще называемый его классом) и значение. Например, при выполнении команды `a = 42` создается объект, представляющий целое число, со значением `42`. Идентификатор объекта — это число, представляющее его местоположение в памяти, `a` — метка, относящаяся к этой конкретной ячейке памяти, хотя метка — не часть самого объекта.
**Тип (класс)** объекта определяет как внутренние данные объекта, так и поддерживаемые им методы. Объект конкретного типа называется ==экземпляром этого типа==. После создания экземпляра его идентификатор не изменяется. Объект называется изменяемым, если его значение может быть изменено. Если значение изменяться не может, то объект называется неизменяемым. Объект, который хранит ссылки на другие объекты, называется ==контейнером==.
Объекты характеризуются их атрибутами. ==Атрибут== — это значение, связанное с объектом, для обращения к которому используется оператор «точка» `(.)`. Атрибут может содержать простое значение данных, например число. Но он может содержать и функцию, которая вызывается для выполнения некоторой операции. Такие функции называются ==методами==

```python
a = 34 # Создать целое число
n = a.numerator # Получить numerator (атрибут)

b = [1, 2, 3] # Создать список
b.append(7) # Добавить новый элемент методом append

# Например, при вычислении выражения a + 10 выполняется метод
a.__add__(10).
```

#### 🔹 Операторы `is` и `is not`

Операторы `is` и `is not` сравнивают идентификаторы двух объектов. `type()` возвращает тип объекта.
```python
# Сравнение двух объектов
def compare(a, b):
	if a is b:
		print('same object')
	if a == b:
		print('same value')
	if type(a) is type(b):
		print('same type')
		
a = [1, 2, 3]
b = [1, 2, 3]

compare(a, a)
# same object
# same value
# same type

compare(a, b)
# same value
# same type

compare(a, [4,5,6])
# same type
```
---
#### 🔹 Подтип

Подтип — это тип, определенный наследованием. Он сохраняет все возможности исходного типа и содержит дополнительные и/или переопределенные методы. 

Пример определения подтипа списка, в который добавлен новый метод.
```python
class mylist(list):
	def removeall(self, val):
		return [i for i in self if i != val]

# Пример
items = mylist([5, 8, 2, 7, 2, 13, 9])
x = items.removeall(2)
print(x) # [5, 8, 7, 13, 9]
```
---
#### 🔹 ПОДСЧЕТ ССЫЛОК И СБОР МУСОРА

Для управления объектами Python использует механизм автоматической сборки мусора. Для всех объектов ведутся счетчики ссылок. Счетчик ссылок увеличивается, когда объект связывается с новым именем или помещается в контейнер (список, кортеж или словарь).
```python
a = 37 # Создает объект со значением 37
b = a # Увеличивает счетчик ссылок для 37
c = []

c.append(b) # Увеличивает счетчик ссылок для 37
```

Счетчик ссылок объекта уменьшается командой `del` и каждый раз, когда ссылка выходит из области видимости или когда переменной присваивается другая ссылка
```python
del a # Уменьшает счетчик ссылок для 37
b = 42 # Уменьшает счетчик ссылок для 37
c[0] = 2.0 # Уменьшает счетчик ссылок для 37
```

==Текущий счетчик ссылок объекта можно получить так==
```python
import sys

a = 37
sys.getrefcount(a) # 7
```

==Когда счетчик ссылок достигает нуля, объект уничтожается сборщиком мусора==

----
#### 🔹 ССЫЛКИ И КОПИИ

При выполнении присваивания в программе (например, `b = a`) создается новая ссылка на a. Может показаться, что для неизменяемых объектов (числа и строки) оно создаст копию a (хотя это и не так). Но для изменяемых объектов (списков и словарей) поведение выглядит иначе

```python
a = [1,2,3,4]
b = a # b содержит ссылку на a

b is a # True

b[2] = -100 # Изменяем элемент в b

a # Обратите внимание: содержимое a тоже изменилось
# [1, 2, -100, 4]
```

В этом примере a и b ссылаются на один объект. Поэтому изменения, вносимые в одну переменную, отражаются на другой. Во избежание этого нужно создать копию объекта вместо новой ссылки

К объектам-контейнерам (спискам и словарям) применяются операции копирования двух типов: ==поверхностного== и ==глубокого==. Поверхностное копирование создает новый объект, но заполняет его ссылками на элементы из исходного. Глубокое копирование создает новый объект и рекурсивно копирует все содержащиеся в нем

**Поверхностное копирование**
```python
a = [1, 2, [3,4]]
b = list(a) # Создание поверхностной копии a

b is a # False

b.append(100) # Присоединение элемента к b
# [1, 2, [3, 4], 100]

a # Обратите внимание: a не изменяется
# [1, 2, [3, 4]]

b[2][0] = -100 # Изменение элемента внутри b
b # [1, 2, [-100, 4], 100]

a # Обратите внимание на изменения в a
# [1, 2, [-100, 4]]
```

**Глубокое копирование** (deep copy)
_Глубокое копирование нужно для создания полной копии объекта, включая все вложенные объекты._ Это важно для изменяемых объектов, таких как списки, чтобы изменения в копии не затрагивали оригинал.
Когда мы вызываем глубокое копирование, происходит следующее:
- Создается новый контейнерный объект (список, словарь и т. д.);
- Для каждого элемента исходного объекта рекурсивно вызывается глубокое копирование:
    - Если элемент является изменяемым объектом (списком, словарем), то создается его копия с рекурсивным копированием вложенных элементов;
    - Если элемент неизменяемый (число, строка) - он просто копируется как есть;
- Копии вложенных объектов вставляются в копию исходного объекта.

```python
import copy

a = [1, 2, [3, 4]]

b = copy.deepcopy(a)

b[2][0] = -100
b # [1, 2, [-100, 4]]

a # Обратите внимание: a не изменяется
# [1, 2, [3, 4]]
```

> В большинстве программ использовать `deepcopy()` не стоит. Копирование объектов — операция медленная и часто лишняя. Оставьте `deepcopy()` для ситуаций, где копирование действительно необходимо. Учтите, что `deepcopy()` не будет работать с объектами, в которых задействовано состояние системы или исполнительной среды (открытые файлы, сетевые подключения, программные потоки, генераторы и т. д.).
---
#### 🔹 ПРЕДСТАВЛЕНИЕ И ВЫВОД ОБЪЕКТОВ

Для получения дополнительной информации используйте функцию `repr()`
```python
d = date(2012, 12, 21)
repr(d)

print(repr(d))

# При форматировании строк к значению можно добавить суффикс !r, чтобы получить результат вызова repr() вместо нормального преобразования в строку
print(f'The date is: {d!r}')
```
---
#### 🔹 ПЕРВОКЛАССНЫЕ ОБЪЕКТЫ

Все объекты в языке Python первоклассные. Это значит, что все объекты, которые могут быть связаны с именем, могут интерпретироваться и как данные. В качестве данных объекты могут сохраняться в переменных, передаваться в аргументах, возвращаться функциями, сравниваться с другими объектами и т. д.

Благодаря этому можно реализовывать данный подход
```python
# Размещение функций или классов в словаре — стандартный прием для устранения сложных команд if-elif-else. Если у вас код следующего вида

if format == 'text':
	formatter = TextFormatter()
elif format == 'csv':
	formatter = CSVFormatter()
elif format == 'html':
	formatter = HTMLFormatter()
else:
	raise RuntimeError('Bad format')
	
# его можно переписать с использованием словаря

_formats = {
	'text': TextFormatter,
	'csv': CSVFormatter,
	'html': HTMLFormatter
}

if format in _formats:
	formatter = _formats[format]()
else:
	raise RuntimeError('Bad format')
```
---
#### 🔹 ИСПОЛЬЗОВАНИЕ NONE

Иногда в программах нужно представить необязательное или отсутствующее значение. `None` отлично подходит для этой цели. None возвращается функциями, которые не возвращают явное значение. `None` также часто используется как значение по умолчанию для необязательных аргументов, чтобы функция могла проверить, передала ли сторона вызова значение для этого аргумента.

```python
# стандартный способ проверки значения на равенство None

if value is None:
	команды
	...
```
---
#### 🔹 ПРОТОКОЛЫ ОБЪЕКТОВ

> Многие возможности Python определяются ==протоколами==. Это магические методы (dunder methods), которые позволяют объектам вести себя как встроенные типы данных.
> Протоколы объектов в Python предоставляют мощный механизм для создания объектов, которые интегрируются с языком на глубоком уровне. Правильное использование этих протоколов позволяет создавать интуитивные и эффективные API, которые ведут себя как встроенные типы Python

##### 🔸 Что такое протоколы объектов
```python
class MyNumber:
    def __init__(self, value):
        self.value = value
    
    # Протокол представления
    def __str__(self):
        return f"MyNumber({self.value})"
    
    def __repr__(self):
        return f"MyNumber({self.value})"
    
    # Арифметический протокол
    def __add__(self, other):
        if isinstance(other, MyNumber):
            return MyNumber(self.value + other.value)
        return MyNumber(self.value + other)
    
    # Протокол сравнения
    def __eq__(self, other):
        if isinstance(other, MyNumber):
            return self.value == other.value
        return self.value == other

# Использование
num1 = MyNumber(5)
num2 = MyNumber(3)

print(num1)           # MyNumber(5)
print(num1 + num2)    # MyNumber(8)
print(num1 == 5)      # True
```

##### 🔸 Жизненный цикл объекта
```python
class LifeCycleDemo:
    def __new__(cls, name):
        print(f"__new__ вызван для {name}")
        # Создание нового экземпляра
        instance = super().__new__(cls)
        return instance
    
    def __init__(self, name):
        print(f"__init__ вызван для {name}")
        self.name = name
    
    def __del__(self):
        print(f"__del__ вызван для {self.name}")

# Демонстрация
obj = LifeCycleDemo("test")
# __new__ вызван для test
# __init__ вызван для test

del obj
# __del__ вызван для test
```

##### 🔸 Базовые арифметические операции
```python
class Vector:
    def __init__(self, x, y):
        self.x = x
        self.y = y
    
    def __str__(self):
        return f"Vector({self.x}, {self.y})"
    
    # Сложение
    def __add__(self, other):
        if isinstance(other, Vector):
            return Vector(self.x + other.x, self.y + other.y)
        return NotImplemented
    
    def __radd__(self, other):
        """Обратное сложение (когда левый операнд не поддерживает операцию)"""
        return self.__add__(other)
    
    def __iadd__(self, other):
        """Сложение с присваиванием +="""
        if isinstance(other, Vector):
            self.x += other.x
            self.y += other.y
            return self
        return NotImplemented
    
    # Вычитание
    def __sub__(self, other):
        if isinstance(other, Vector):
            return Vector(self.x - other.x, self.y - other.y)
        return NotImplemented
    
    # Умножение
    def __mul__(self, scalar):
        if isinstance(scalar, (int, float)):
            return Vector(self.x * scalar, self.y * scalar)
        return NotImplemented
    
    def __rmul__(self, scalar):
        """Обратное умножение"""
        return self.__mul__(scalar)
    
    # Деление
    def __truediv__(self, scalar):
        if isinstance(scalar, (int, float)) and scalar != 0:
            return Vector(self.x / scalar, self.y / scalar)
        return NotImplemented
    
    # Отрицание
    def __neg__(self):
        return Vector(-self.x, -self.y)
    
    # Абсолютное значение
    def __abs__(self):
        return (self.x ** 2 + self.y ** 2) ** 0.5

# Использование
v1 = Vector(3, 4)
v2 = Vector(1, 2)

print(v1 + v2)      # Vector(4, 6)
print(v1 - v2)      # Vector(2, 2)
print(v1 * 2)       # Vector(6, 8)
print(2 * v1)       # Vector(6, 8) - обратное умножение
print(v1 / 2)       # Vector(1.5, 2.0)
print(-v1)          # Vector(-3, -4)
print(abs(v1))      # 5.0

v1 += v2
print(v1)           # Vector(4, 6)
```

##### 🔸 Протоколы сравнения
```python
from functools import total_ordering

@total_ordering  # Автоматически генерирует остальные методы сравнения
class Grade:
    def __init__(self, score):
        self.score = score
    
    def __str__(self):
        return f"Grade({self.score})"
    
    def __eq__(self, other):
        if isinstance(other, Grade):
            return self.score == other.score
        return NotImplemented
    
    def __lt__(self, other):
        if isinstance(other, Grade):
            return self.score < other.score
        return NotImplemented
    
    def __hash__(self):
        """Делает объект хешируемым"""
        return hash(self.score)

# Без @total_ordering нужно было бы реализовать все методы:
class FullGrade:
    def __init__(self, score):
        self.score = score
    
    def __eq__(self, other):
        if isinstance(other, FullGrade):
            return self.score == other.score
        return NotImplemented
    
    def __ne__(self, other):
        result = self.__eq__(other)
        if result is NotImplemented:
            return result
        return not result
    
    def __lt__(self, other):
        if isinstance(other, FullGrade):
            return self.score < other.score
        return NotImplemented
    
    def __le__(self, other):
        if isinstance(other, FullGrade):
            return self.score <= other.score
        return NotImplemented
    
    def __gt__(self, other):
        if isinstance(other, FullGrade):
            return self.score > other.score
        return NotImplemented
    
    def __ge__(self, other):
        if isinstance(other, FullGrade):
            return self.score >= other.score
        return NotImplemented

# Использование
g1 = Grade(85)
g2 = Grade(92)
g3 = Grade(85)

print(g1 == g3)     # True
print(g1 < g2)      # True
print(g2 > g1)      # True (автоматически из @total_ordering)
print(g1 <= g3)     # True (автоматически из @total_ordering)

# Можно использовать в множествах и как ключи словарей
grades = {g1, g2, g3}
print(len(grades))  # 2 (g1 и g3 равны)
```

##### 🔸 Последовательности (sequences)
```python
class CustomList:
    def __init__(self, *items):
        self._items = list(items)
    
    def __len__(self):
        """Длина контейнера"""
        return len(self._items)
    
    def __getitem__(self, index):
        """Получение элемента по индексу"""
        if isinstance(index, slice):
            return CustomList(*self._items[index])
        return self._items[index]
    
    def __setitem__(self, index, value):
        """Установка элемента по индексу"""
        self._items[index] = value
    
    def __delitem__(self, index):
        """Удаление элемента по индексу"""
        del self._items[index]
    
    def __contains__(self, item):
        """Проверка наличия элемента (оператор in)"""
        return item in self._items
    
    def __iter__(self):
        """Итерация по контейнеру"""
        return iter(self._items)
    
    def __reversed__(self):
        """Обратная итерация"""
        return reversed(self._items)
    
    def __str__(self):
        return f"CustomList({', '.join(map(str, self._items))})"
    
    # Дополнительные методы
    def append(self, item):
        self._items.append(item)
    
    def insert(self, index, item):
        self._items.insert(index, item)

# Использование
cl = CustomList(1, 2, 3, 4, 5)

print(len(cl))          # 5
print(cl[2])            # 3
print(cl[1:4])          # CustomList(2, 3, 4)
print(3 in cl)          # True

cl[1] = 10
print(cl)               # CustomList(1, 10, 3, 4, 5)

del cl[0]
print(cl)               # CustomList(10, 3, 4, 5)

# Итерация
for item in cl:
    print(item, end=' ') # 10 3 4 5

print()
# Обратная итерация
for item in reversed(cl):
    print(item, end=' ') # 5 4 3 10
```

##### 🔸 Ассоциативные контейнеры (mappings)
```python
class CustomDict:
    def __init__(self):
        self._data = {}
    
    def __getitem__(self, key):
        return self._data[key]
    
    def __setitem__(self, key, value):
        self._data[key] = value
    
    def __delitem__(self, key):
        del self._data[key]
    
    def __contains__(self, key):
        return key in self._data
    
    def __len__(self):
        return len(self._data)
    
    def __iter__(self):
        """Итерация по ключам"""
        return iter(self._data)
    
    def __str__(self):
        items = ', '.join(f'{k}: {v}' for k, v in self._data.items())
        return f"CustomDict({{{items}}})"
    
    # Методы словаря
    def keys(self):
        return self._data.keys()
    
    def values(self):
        return self._data.values()
    
    def items(self):
        return self._data.items()
    
    def get(self, key, default=None):
        return self._data.get(key, default)

# Использование
cd = CustomDict()
cd['name'] = 'Иван'
cd['age'] = 30

print(cd['name'])       # Иван
print(len(cd))          # 2
print('age' in cd)      # True

for key in cd:
    print(f"{key}: {cd[key]}")

print(cd)               # CustomDict({name: Иван, age: 30})
```

##### 🔸 Callable объекты
```python
class Multiplier:
    def __init__(self, factor):
        self.factor = factor
    
    def __call__(self, value):
        """Делает объект вызываемым"""
        return value * self.factor
    
    def __str__(self):
        return f"Multiplier(×{self.factor})"

# Использование
double = Multiplier(2)
triple = Multiplier(3)

print(double(5))        # 10
print(triple(4))        # 12

# Можно использовать как функцию
numbers = [1, 2, 3, 4, 5]
doubled = list(map(double, numbers))
print(doubled)          # [2, 4, 6, 8, 10]

# Проверка на вызываемость
print(callable(double)) # True
```

##### 🔸 Декоратор как класс
```python
class CountCalls:
    def __init__(self, func):
        self.func = func
        self.count = 0
    
    def __call__(self, *args, **kwargs):
        self.count += 1
        print(f"Функция {self.func.__name__} вызвана {self.count} раз")
        return self.func(*args, **kwargs)
    
    def __str__(self):
        return f"CountCalls({self.func.__name__}, calls={self.count})"

@CountCalls
def greet(name):
    return f"Привет, {name}!"

print(greet("Анна"))    # Функция greet вызвана 1 раз
                        # Привет, Анна!
print(greet("Петр"))    # Функция greet вызвана 2 раз
                        # Привет, Петр!
```

##### 🔸 Протокол with (Контекстный менеджер)
```python
class FileManager:
    def __init__(self, filename, mode):
        self.filename = filename
        self.mode = mode
        self.file = None
    
    def __enter__(self):
        """Вход в контекст"""
        print(f"Открываем файл {self.filename}")
        self.file = open(self.filename, self.mode)
        return self.file
    
    def __exit__(self, exc_type, exc_value, traceback):
        """Выход из контекста"""
        print(f"Закрываем файл {self.filename}")
        if self.file:
            self.file.close()
        
        # Обработка исключений
        if exc_type is not None:
            print(f"Произошла ошибка: {exc_type.__name__}: {exc_value}")
            return False  # Не подавлять исключение
        return True

# Использование
try:
    with FileManager('test.txt', 'w') as f:
        f.write('Hello, World!')
        # raise ValueError("Test error")  # Раскомментировать для теста ошибки
except Exception as e:
    print(f"Обработано исключение: {e}")
```

##### 🔸 Асинхронный контекстный менеджер
```python
import asyncio

class AsyncResource:
    def __init__(self, name):
        self.name = name
        self.connected = False
    
    async def __aenter__(self):
        """Асинхронный вход в контекст"""
        print(f"Подключаемся к {self.name}...")
        await asyncio.sleep(1)  # Имитация асинхронной операции
        self.connected = True
        print(f"Подключено к {self.name}")
        return self
    
    async def __aexit__(self, exc_type, exc_value, traceback):
        """Асинхронный выход из контекста"""
        print(f"Отключаемся от {self.name}...")
        await asyncio.sleep(0.5)
        self.connected = False
        print(f"Отключено от {self.name}")
        return False

async def main():
    async with AsyncResource("База данных") as resource:
        print(f"Работаем с ресурсом: {resource.name}")
        await asyncio.sleep(0.5)

# asyncio.run(main())
```

##### 🔸 Система событий с протоколами (ПРИМЕР)
```python
class Event:
    def __init__(self, name, **kwargs):
        self.name = name
        self.data = kwargs
        self.handled = False
    
    def __str__(self):
        return f"Event({self.name}, {self.data})"
    
    def __bool__(self):
        """True если событие не обработано"""
        return not self.handled

class EventHandler:
    def __init__(self, name, handler_func):
        self.name = name
        self.handler_func = handler_func
        self.call_count = 0
    
    def __call__(self, event):
        """Делает обработчик вызываемым"""
        if event.name == self.name:
            self.call_count += 1
            result = self.handler_func(event)
            event.handled = True
            return result
        return False
    
    def __str__(self):
        return f"EventHandler({self.name}, calls={self.call_count})"

class EventSystem:
    def __init__(self):
        self.handlers = []
    
    def __iadd__(self, handler):
        """Добавление обработчика через +="""
        if isinstance(handler, EventHandler):
            self.handlers.append(handler)
        return self
    
    def __isub__(self, handler):
        """Удаление обработчика через -="""
        if handler in self.handlers:
            self.handlers.remove(handler)
        return self
    
    def __call__(self, event):
        """Обработка события"""
        for handler in self.handlers:
            if handler(event):
                break
        return event.handled
    
    def __len__(self):
        return len(self.handlers)
    
    def __iter__(self):
        return iter(self.handlers)

# Использование
def on_user_login(event):
    print(f"Пользователь {event.data['user']} вошёл в систему")

def on_user_logout(event):
    print(f"Пользователь {event.data['user']} вышел из системы")

# Создание системы событий
event_system = EventSystem()

# Добавление обработчиков
event_system += EventHandler("user_login", on_user_login)
event_system += EventHandler("user_logout", on_user_logout)

# Генерация событий
login_event = Event("user_login", user="Иван", ip="192.168.1.1")
logout_event = Event("user_logout", user="Иван")

print(f"Обработчиков: {len(event_system)}")

# Обработка событий
event_system(login_event)   # Пользователь Иван вошёл в систему
event_system(logout_event)  # Пользователь Иван вышел из системы

print(f"Событие входа обработано: {not bool(login_event)}")
```