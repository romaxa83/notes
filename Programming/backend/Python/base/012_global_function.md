
 all, abs, any, ascii, bin, bool,  breakpoint, bytearray,  bytes, len,  callable, chr,  classmethod,  compile, dict, dir, divmod,  enumerate

#### 🔹 Print
```python
# вывод в удобном json-формате
import json

print(json.dumps(data, ensure_ascii=False, indent=2))
```