#python 

Модули — это файлы Python, содержащие определения и инструкции. Они позволяют организовать код, разделить его на логические части и повторно использовать функции, классы и переменные в разных программах. Имя модуля — это имя файла без расширения.

#### 🔹 Создание модуля

Создадим простой модуль `math_utils.py`:
```python
"""
Модуль с математическими утилитами
"""

# Константы модуля
PI = 3.14159
E = 2.71828

# Переменная модуля
module_version = "1.0.0"

def add(a, b):
    """Сложение двух чисел"""
    return a + b

def multiply(a, b):
    """Умножение двух чисел"""
    return a * b

def circle_area(radius):
    """Вычисление площади круга"""
    return PI * radius * radius

class Calculator:
    """Простой калькулятор"""
    
    def __init__(self):
        self.history = []
    
    def calculate(self, operation, a, b):
        if operation == 'add':
            result = add(a, b)
        elif operation == 'multiply':
            result = multiply(a, b)
        else:
            result = None
        
        self.history.append(f"{operation}({a}, {b}) = {result}")
        return result

# Код, который выполняется при импорте модуля
print(f"Модуль math_utils загружен, версия {module_version}")
```
---
#### 🔹 Импорт модулей

>Исходный код модуля загружается и выполняется только один раз, независимо от числа раз использования import. Последующие команды import связывают имя модуля с его объектом, уже созданным прошлой командой. Кеш всех модулей, загруженных на данный момент, доступен в sys.modules - словаре, связывающем имена модулей с их объектами.
##### 🔸 Импорт всего модуля
```python
import math_utils

# Использование через имя модуля
print(math_utils.add(5, 3))           # 8
print(math_utils.PI)                  # 3.14159
print(math_utils.circle_area(5))      # 78.53975

# Создание объекта класса из модуля
calc = math_utils.Calculator()
result = calc.calculate('add', 10, 20)
print(result)  # 30
```

##### 🔸 Импорт нескольких модулей
```python
import socket, os, re
```
##### 🔸 Импорт с псевдонимом
```python
import math_utils as math

print(math.multiply(4, 7))  # 28
print(math.E)               # 2.71828
```

##### 🔸 Импорт конкретных элементов
```python
from math_utils import add, PI, Calculator

print(add(15, 25))     # 40
print(PI)              # 3.14159

calc = Calculator()
print(calc.calculate('multiply', 6, 7))  # 42
```

##### 🔸 Импорт с псевдонимами
```python
from math_utils import circle_area as area, PI as pi

print(area(3))  # 28.27431
print(pi)       # 3.14159
```

##### 🔸 Импорт всех элементов (не рекомендуется)
```python
from math_utils import *

print(add(1, 2))        # 3
print(multiply(3, 4))   # 12
print(module_version)   # 1.0.0
```

##### 🔸 Импорт в функции
> стоит использовать если у вас есть специализированная функция, которая редко вызывается, размещение зависимостей импортирования функции внутри тела может ускорить загрузку программы. В этом случае важные модули будут загружаться, только если они действительно нужны. 

```python
def f(x):
	import math
	return math.sin(x) + math.cos(x)
```
---
#### 🔹 Стандартные модули

##### 🔸 `os`
Модуль для работы с файловой системой, путями и системными командами.
```python
import os

# Работа с путями
print(os.getcwd())                    # Текущая директория
print(os.path.exists('file.txt'))     # Проверка существования файла
print(os.path.join('folder', 'file.txt'))  # Правильное создание пути

# Информация о файлах и папках
print(os.listdir('.'))               # Список файлов в директории
print(os.path.getsize('file.txt'))   # Размер файла
print(os.path.isfile('file.txt'))    # Это файл?
print(os.path.isdir('folder'))       # Это директория?

# Создание и удаление
os.mkdir('new_folder')               # Создать папку
os.makedirs('path/to/folder')        # Создать путь с подпапками
os.remove('file.txt')                # Удалить файл
os.rmdir('folder')                   # Удалить пустую папку

# Переменные окружения
print(os.environ.get('PATH'))        # Получить переменную окружения
os.environ['MY_VAR'] = 'value'       # Установить переменную
```

##### 🔸 `shutil`
>Модуль `shutil` (shell utilities) предоставляет высокоуровневые операции для работы с файлами и директориями. Он дополняет функциональность модуля `os` более удобными методами для копирования, перемещения, архивирования и других операций с файловой системой.

**Модуль `shutil` предоставляет мощные инструменты для**:
- **Копирования файлов и директорий** с сохранением метаданных
- **Перемещения и переименования** файлов
- **Архивирования и распаковки** данных
- **Поиска исполняемых файлов** в системе
- **Получения информации** о дисковом пространстве
**Основные функции**:
- `copy()`, `copy2()`, `copyfile()` - различные способы копирования
- `copytree()` - копирование дерева директорий
- `move()` - перемещение файлов и директорий
- `rmtree()` - удаление дерева директорий
- `make_archive()`, `unpack_archive()` - работа с архивами
- `which()` - поиск исполняемых файлов
- `disk_usage()` - информация о дисковом пространстве

Полезные скрипты - [[file#🔹 Основные операции с файлами|Основные операции с файлами]], [[file#🔹 Работа с директориями|Работа с директориями]], [[file#🔹 Архивирование и сжатие|Архивирование и сжатие]], [[file#🔹 Поиск исполняемых файлов|Поиск исполняемых файлов]], [[file#🔹 Работа с правами доступа|Работа с правами доступа]], [[file#🔹 Лучшие практики и советы при использования `shutil`|Лучшие практики и советы при использования shutil]]
##### 🔸 `sys`
Доступ к интерпретатору Python и системным настройкам.
```python
import sys

# Информация о системе
print(sys.version)          # Версия Python
print(sys.platform)         # Платформа (linux, win32, darwin)
print(sys.argv)             # Аргументы командной строки

# Пути поиска модулей
print(sys.path)             # Пути поиска модулей
sys.path.append('/my/path') # Добавить путь

# Управление выполнением
sys.exit(0)                 # Завершить программу с кодом
sys.stdout.write('Hello')   # Запись в стандартный вывод
sys.stderr.write('Error')   # Запись в стандартный поток ошибок

# Информация о памяти
print(sys.getsizeof([1, 2, 3]))  # Размер объекта в памяти
```

##### 🔸 `datetime`
```python
from datetime import datetime, date, time, timedelta

# Текущие дата и время
now = datetime.now()
today = date.today()
current_time = datetime.now().time()

print(f"Сейчас: {now}")
print(f"Сегодня: {today}")
print(f"Время: {current_time}")

# Создание конкретных дат
birthday = date(1990, 5, 15)
meeting = datetime(2023, 12, 25, 14, 30, 0)

# Форматирование
print(now.strftime("%d.%m.%Y %H:%M:%S"))  # 15.10.2023 14:30:45
print(today.strftime("%A, %B %d, %Y"))     # Monday, October 15, 2023

# Парсинг строк
date_string = "2023-10-15"
parsed_date = datetime.strptime(date_string, "%Y-%m-%d")

# Арифметические операции
tomorrow = today + timedelta(days=1)
week_ago = now - timedelta(weeks=1)
next_hour = now + timedelta(hours=1)

# Разница между датами
age = today - birthday
print(f"Возраст в днях: {age.days}")
```

##### 🔸 `time`
>Модуль `time` предоставляет функции для работы со временем в Python. Он включает функции для получения текущего времени, форматирования дат, создания задержек и работы с различными временными форматами.

```python
import time
import calendar
from datetime import datetime, timezone

def basic_time_operations():
    """Основы работы с модулем time"""
    
    print("=== Основы модуля time ===")
    
    # 1. Текущее время
    print("1. Получение текущего времени:")
    
    # Unix timestamp (секунды с 1 января 1970)
    current_timestamp = time.time()
    print(f"   Unix timestamp: {current_timestamp}")
    print(f"   Округленный timestamp: {int(current_timestamp)}")
    
    # Время в читаемом формате
    readable_time = time.ctime()
    print(f"   Читаемое время: {readable_time}")
    
    # Время с определенным timestamp
    specific_time = time.ctime(1000000000)
    print(f"   Время для timestamp 1000000000: {specific_time}")
    
    # 2. Структурированное время (struct_time)
    print("\n2. Структурированное время:")
    
    # Локальное время
    local_time = time.localtime()
    print(f"   Локальное время: {local_time}")
    print(f"   Год: {local_time.tm_year}")
    print(f"   Месяц: {local_time.tm_mon}")
    print(f"   День: {local_time.tm_mday}")
    print(f"   Час: {local_time.tm_hour}")
    print(f"   Минута: {local_time.tm_min}")
    print(f"   Секунда: {local_time.tm_sec}")
    print(f"   День недели (0=понедельник): {local_time.tm_wday}")
    print(f"   День в году: {local_time.tm_yday}")
    print(f"   DST (летнее время): {local_time.tm_isdst}")
    
    # UTC время
    utc_time = time.gmtime()
    print(f"\n   UTC время: {utc_time}")
    
    # Преобразование timestamp в struct_time
    timestamp = 1640995200  # 1 января 2022 00:00:00 UTC
    converted_time = time.localtime(timestamp)
    print(f"   Timestamp {timestamp} -> {converted_time}")
    
    # 3. Преобразования между форматами
    print("\n3. Преобразования времени:")
    
    # struct_time -> timestamp
    struct_to_timestamp = time.mktime(local_time)
    print(f"   struct_time -> timestamp: {struct_to_timestamp}")
    
    # struct_time -> строка
    struct_to_string = time.asctime(local_time)
    print(f"   struct_time -> строка: {struct_to_string}")
    
    # timestamp -> строка
    timestamp_to_string = time.ctime(current_timestamp)
    print(f"   timestamp -> строка: {timestamp_to_string}")

basic_time_operations()
```

примеры скриптов - [[time#🔹 Измерение времени выполнения|Измерение времени выполнения]]
##### 🔸 `asyncio`
>Модуль `asyncio` поддерживает параллельные операции ввода/вывода с применением опроса каналов ввода/вывода и цикла событий. Он предназначен для использования в коде, работающем с сетями и распределенными системами.

```python
# эхо-сервер TCP с применением низкоуровневых сокетов

import asyncio
from socket import *

async def echo_server(address):
	loop = asyncio.get_event_loop()
	sock = socket(AF_INET, SOCK_STREAM)
	sock.setsockopt(SOL_SOCKET, SO_REUSEADDR, 1)
	sock.bind(address)
	sock.listen(5)
	sock.setblocking(False)
	print('Server listening at', address)
	with sock:
		while True:
			client, addr = await loop.sock_accept(sock)
			print('Connection from', addr)
			loop.create_task(echo_client(loop, client))

async def echo_client(loop, client):
	with client:
		while True:
			data = await loop.sock_recv(client, 10000)
		if not data:
			break
		await loop.sock_sendall(client, b'Got:' + data)
	print('Connection closed')

if __name__ == '__main__':
	loop = asyncio.get_event_loop()
	loop.create_task(echo_server(('', 25000)))
	loop.run_forever()
	
# Чтобы протестировать этот код, подключитесь к порту 25 000 на вашем компьютере с помощью nc или telnet. Программа должна повторять вводимый вами текст. Если вы подключитесь одновременно из нескольких окон терминала, то увидите, что программа может обрабатывать все подключения одновременно
```

##### 🔸 `configparser`
> используется для чтения файлов .ini и извлечения данных из них

```bash
# config.ini
; Комментарий
[section1]
name1 = value1
name2 = value2

[section2]
; Альтернативный синтаксис
name1: value1
name2: value2
```

```python
import configparser

# Создание парсера и чтение файла

cfg = configparser.ConfigParser()
cfg.read('config.ini')

# Извлечение значений

a = cfg.get('section1', 'name1')
b = cfg.get('section2', 'name2')
```

##### 🔸 `hashlib`
> предоставляет функции для вычисления криптографических хеш-кодов — MD5 и SHA-1

```python
import hashlib

# Основные алгоритмы хеширования
text = "Привет, мир!"
data = text.encode('utf-8')  # Преобразуем в байты

# MD5 (не рекомендуется для безопасности)
md5_hash = hashlib.md5(data)
print(f"MD5: {md5_hash.hexdigest()}")

# SHA-1 (не рекомендуется для безопасности)
sha1_hash = hashlib.sha1(data)
print(f"SHA-1: {sha1_hash.hexdigest()}")

# SHA-256 (рекомендуется)
sha256_hash = hashlib.sha256(data)
print(f"SHA-256: {sha256_hash.hexdigest()}")

# SHA-512
sha512_hash = hashlib.sha512(data)
print(f"SHA-512: {sha512_hash.hexdigest()}")

# Список доступных алгоритмов
print("Доступные алгоритмы:", hashlib.algorithms_available)
print("Гарантированные алгоритмы:", hashlib.algorithms_guaranteed)
```

полезные скрипты: [[security#🔹 Рекомендации по безопасности при хешировании|Рекомендации по безопасности]], [[file#🔹 Хеширование файлов|Хеширование файлов]], [[file#🔹 Контрольные суммы и целостность данных| Контрольные суммы и целостность данных]]

##### 🔸 `io`
> Модуль `io`предоставляет основные возможности для работы с потоками ввода-вывода в Python. Он содержит классы для работы с файлами, строками, байтами и другими типами данных как с потоками. Он особенно полезен для:
- **Обработки данных в памяти** без создания временных файлов
- **Создания буферов** для накопления данных
- **Преобразования** между различными форматами данных
- **Тестирования** кода, работающего с файлами
- **Перенаправления** стандартных потоков ввода-вывода

```python
import io

# Основные типы потоков
print("=== Типы потоков ===")

# Текстовый поток в памяти
text_stream = io.StringIO()
text_stream.write("Привет, ")
text_stream.write("мир!")
print(f"Содержимое StringIO: '{text_stream.getvalue()}'")

# Байтовый поток в памяти
bytes_stream = io.BytesIO()
bytes_stream.write(b"Hello, ")
bytes_stream.write("мир!".encode('utf-8'))
print(f"Содержимое BytesIO: {bytes_stream.getvalue()}")

# Перемещение указателя
text_stream.seek(0)  # В начало
print(f"Чтение из начала: '{text_stream.read(6)}'")

text_stream.seek(7)  # К позиции 7
print(f"Чтение с позиции 7: '{text_stream.read()}'")

# Получение текущей позиции
print(f"Текущая позиция: {text_stream.tell()}")

# Закрытие потоков
text_stream.close()
bytes_stream.close()
```

> полезные скрипты - [[stream#🔹 Обработки данных с использованием потоков|Обработки данных с использованием потоков]], [[stream#🔹 Оптимизация работы с потоками|Оптимизация работы с потоками]]
##### 🔸 `logging`
> `logging` считается стандартом для вывода диагностики программы и отладочных данных в стиле print. Он может использоваться для перенаправления вывода в файл журнала и дает множество параметров конфигурации.

```python
import logging

log = logging.getLogger(__name__)

# Функция, использующая logging
def func(args):
	log.debug("A debugging message")
	log.info("An informational message")
	log.warning("A warning message")
	log.error("An error message")
	log.critical("A critical message")

# Конфигурация вывода в журнал (выполняется один раз при запуске)
if __name__ == '__main__':
	logging.basicConfig(
		level=logging.WARNING,
		filename="output.log"
	)
```
 
##### 🔸 `json`
```python
import json

# Данные Python
data = {
    "name": "Иван",
    "age": 30,
    "skills": ["Python", "JavaScript"],
    "active": True,
    "score": 95.5
}

# Сериализация в JSON
json_string = json.dumps(data, ensure_ascii=False, indent=2)
print(json_string)

# Запись в файл
with open('data.json', 'w', encoding='utf-8') as file:
    json.dump(data, file, ensure_ascii=False, indent=2)

# Чтение из строки
parsed_data = json.loads(json_string)
print(parsed_data["name"])

# Чтение из файла
with open('data.json', 'r', encoding='utf-8') as file:
    loaded_data = json.load(file)
    print(loaded_data)

# Обработка ошибок
try:
    invalid_json = '{"name": "Иван", "age":}'
    json.loads(invalid_json)
except json.JSONDecodeError as e:
    print(f"Ошибка парсинга JSON: {e}")
```

##### 🔸 `re` — Регулярные выражения
```python
import re

text = "Телефон: +7-900-123-45-67, email: user@example.com"

# Поиск
phone_match = re.search(r'\+7-\d{3}-\d{3}-\d{2}-\d{2}', text)
if phone_match:
    print(f"Найден телефон: {phone_match.group()}")

# Поиск всех совпадений
emails = re.findall(r'\b[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Z|a-z]{2,}\b', text)
print(f"Найденные email: {emails}")

# Замена
cleaned_phone = re.sub(r'[^\d+]', '', phone_match.group())
print(f"Очищенный телефон: {cleaned_phone}")

# Разделение строки
data = "яблоко,банан;апельсин:груша"
fruits = re.split(r'[,;:]', data)
print(f"Фрукты: {fruits}")

# Компиляция паттерна для переиспользования
email_pattern = re.compile(r'\b[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Z|a-z]{2,}\b')
matches = email_pattern.findall(text)

# Группы в регулярных выражениях
phone_pattern = r'\+(\d{1})-(\d{3})-(\d{3})-(\d{2})-(\d{2})'
match = re.search(phone_pattern, text)
if match:
    country, code, number1, number2, number3 = match.groups()
    print(f"Код страны: {country}, код: {code}")
```

##### 🔸 `random` — Генерация случайных чисел
```python
import random

# Случайные числа
print(random.random())              # Дробное число от 0 до 1
print(random.randint(1, 10))        # Целое число от 1 до 10
print(random.uniform(1.0, 10.0))    # Дробное число от 1.0 до 10.0
print(random.randrange(0, 100, 5))  # Число от 0 до 100 с шагом 5

# Работа с последовательностями
items = ['apple', 'banana', 'orange', 'grape']
print(random.choice(items))         # Случайный элемент
print(random.choices(items, k=3))   # Случайная выборка с повторениями
print(random.sample(items, 2))      # Случайная выборка без повторений

# Перемешивание
numbers = [1, 2, 3, 4, 5]
random.shuffle(numbers)             # Изменяет исходный список
print(numbers)

# Установка начального значения для воспроизводимости
random.seed(42)
print(random.randint(1, 100))       # Всегда одинаковый результат

# Генерация паролей
import string
password_chars = string.ascii_letters + string.digits + "!@#$%"
password = ''.join(random.choices(password_chars, k=12))
print(f"Сгенерированный пароль: {password}")
```

##### 🔸 `collections` — Специальные типы коллекций
```python
from collections import defaultdict, Counter, deque, namedtuple

# defaultdict - словарь с значениями по умолчанию
dd = defaultdict(list)
dd['fruits'].append('apple')
dd['fruits'].append('banana')
print(dd)  # defaultdict(<class 'list'>, {'fruits': ['apple', 'banana']})

# Counter - подсчет элементов
text = "hello world"
letter_count = Counter(text)
print(letter_count)  # Counter({'l': 3, 'o': 2, 'h': 1, ...})
print(letter_count.most_common(3))  # Самые частые элементы

words = ['apple', 'banana', 'apple', 'orange', 'banana', 'apple']
word_count = Counter(words)
print(word_count['apple'])  # 3

# deque - двусторонняя очередь (быстрые операции с концами)
queue = deque([1, 2, 3])
queue.appendleft(0)    # Добавить в начало
queue.append(4)        # Добавить в конец
print(queue.popleft()) # Удалить из начала
print(queue.pop())     # Удалить из конца

# namedtuple - именованные кортежи
Person = namedtuple('Person', ['name', 'age', 'city'])
person = Person('Анна', 25, 'Москва')
print(person.name)     # Анна
print(person.age)      # 25

# Можно использовать как обычный кортеж
name, age, city = person
print(f"{name} ({age}) из {city}")
```

##### 🔸 `itertools` — Инструменты для итераторов
```python
import itertools

# Бесконечные итераторы
counter = itertools.count(10, 2)  # 10, 12, 14, 16...
# for i, num in enumerate(counter):
#     if i > 5: break
#     print(num)

# cycle - бесконечное повторение
colors = itertools.cycle(['red', 'green', 'blue'])
# for i, color in enumerate(colors):
#     if i > 10: break
#     print(color)

# Итераторы завершения
numbers = [1, 2, 3, 4, 5]
accumulated = list(itertools.accumulate(numbers))  # [1, 3, 6, 10, 15]
print(accumulated)

# Комбинаторика
items = ['A', 'B', 'C']
print(list(itertools.permutations(items, 2)))      # Размещения
print(list(itertools.combinations(items, 2)))      # Сочетания
print(list(itertools.combinations_with_replacement(items, 2)))  # С повторениями

# Группировка
data = [1, 1, 2, 2, 2, 3, 1, 1]
grouped = itertools.groupby(data)
for key, group in grouped:
    print(f"{key}: {list(group)}")

# Объединение итераторов
list1 = [1, 2, 3]
list2 = ['a', 'b', 'c']
chained = itertools.chain(list1, list2)
print(list(chained))  # [1, 2, 3, 'a', 'b', 'c']

# zip_longest - объединение с заполнением
short = [1, 2]
long = ['a', 'b', 'c', 'd']
zipped = itertools.zip_longest(short, long, fillvalue=0)
print(list(zipped))  # [(1, 'a'), (2, 'b'), (0, 'c'), (0, 'd')]
```

##### 🔸 `pathlib` — Современная работа с путями
```python
from pathlib import Path

# Создание путей
current_dir = Path('.')
home_dir = Path.home()
file_path = Path('documents') / 'projects' / 'main.py'

print(f"Текущая директория: {current_dir.resolve()}")
print(f"Домашняя директория: {home_dir}")
print(f"Путь к файлу: {file_path}")

# Информация о пути
print(f"Имя файла: {file_path.name}")           # main.py
print(f"Расширение: {file_path.suffix}")        # .py
print(f"Без расширения: {file_path.stem}")      # main
print(f"Родительская папка: {file_path.parent}") # documents/projects

# Проверки
print(f"Существует: {file_path.exists()}")
print(f"Это файл: {file_path.is_file()}")
print(f"Это директория: {file_path.is_dir()}")

# Операции с файлами
config_path = Path('config.txt')

# Запись в файл
config_path.write_text('debug=True\nport=8000', encoding='utf-8')

# Чтение из файла
content = config_path.read_text(encoding='utf-8')
print(content)

# Перебор файлов в директории
for py_file in Path('.').glob('*.py'):
    print(f"Python файл: {py_file}")

# Рекурсивный поиск
for py_file in Path('.').rglob('*.py'):
    print(f"Python файл (рекурсивно): {py_file}")

# Создание директорий
new_dir = Path('test_folder')
new_dir.mkdir(exist_ok=True)  # exist_ok=True - не ошибка если существует

# Удаление
if config_path.exists():
    config_path.unlink()  # Удалить файл
```

##### 🔸 `csv` 
>модуль предназначен для чтения/записи CSV-файлов (значений, разделенных запятыми). Для использования этого модуля откройте файл и наложите на него дополнительный уровень кодирования/декодирования CSV:

```python
import csv

# Чтение файла CSV в список кортежей
def read_csv_data(filename):
	with open(filename, newline='') as file:
		rows = csv.reader(file)
		# Чтение заголовка, который часто содержится в первой строке
		headers = next(rows)
		# Теперь прочитать остальные данные
		for row in rows:
		# Обработка текущей строки
			...

# Запись данных Python в файл CSV
def write_csv_data(filename, headers, rows):
	with open(filename, 'w', newline='') as file:
		out = csv.writer(file)
		out.writerow(headers)
		out.writerows(rows)

# Часто вместо этого используется вспомогательный метод DictReader(). Он интерпретирует первую строку CSV-файла как набор заголовков и возвращает каждую строку как словарь вместо кортежа.

import csv

def find_nearby(filename):
	with open(filename, newline='') as file:
		rows = csv.DictReader(file)
		for row in rows:
			lat = float(rows['latitude'])
			lon = float(rows['longitude'])
			if close_enough(lat, lon):
				print(row)

```

>Главное преимущество этого модуля в том, что он умеет правильно кодировать/декодировать данные и обрабатывает многие граничные случаи, включая данные в кавычках, специальные символы и другие подробности.
>Этот модуль может использоваться для написания простых сценариев чистки данных или их подготовки к использованию в других программах. Если вы намерены решать задачи анализа данных с данными CSV, рассмотрите возможность использования стороннего пакета, такого как популярная библиотека `pandas`.

 полезный скрипт -  [[stream#🔹 Обработки данных с использованием потоков|Обработки данных с использованием потоков]]
##### 🔸 `urllib` — Работа с URL и HTTP
```python
from urllib.parse import urlparse, urljoin, quote, unquote
from urllib.request import urlopen
import urllib.error

# Парсинг URL
url = "https://example.com:8080/path/to/page?param=value&other=123#section"
parsed = urlparse(url)

print(f"Схема: {parsed.scheme}")      # https
print(f"Домен: {parsed.netloc}")      # example.com:8080
print(f"Путь: {parsed.path}")         # /path/to/page
print(f"Параметры: {parsed.query}")   # param=value&other=123
print(f"Фрагмент: {parsed.fragment}") # section

# Построение URL
base_url = "https://api.example.com/"
endpoint = "users/123/posts"
full_url = urljoin(base_url, endpoint)
print(f"Полный URL: {full_url}")

# Кодирование/декодирование URL
text = "Привет мир!"
encoded = quote(text)
print(f"Закодированный: {encoded}")    # %D0%9F%D1%80%D0%B8%D0%B2%D0%B5%D1%82...
decoded = unquote(encoded)
print(f"Декодированный: {decoded}")    # Привет мир!

# HTTP запросы (базовые)
try:
    response = urlopen('https://httpbin.org/json')
    data = response.read().decode('utf-8')
    print(f"Статус: {response.status}")
    print(f"Данные: {data[:100]}...")
except urllib.error.URLError as e:
    print(f"Ошибка запроса: {e}")
```

##### 🔸 `smtplib` - отправка email
 >реализует клиентскую сторону SMTP, которая обычно используется для отправки сообщений электронной почты. Есть дополнительный функционал для работы с паролями, аутентификацией и другими аспектами
 
```python
import smtplib

fromaddr = "someone@some.com"
toaddrs = ["recipient@other.com" ]
amount = 123.45
msg = f"""From: {fromaddr}
Pay {amount} bitcoin or else. We're watching.
"""

server = smtplib.SMTP('localhost')
serv.sendmail(fromaddr, toaddrs, msg)
serv.quit()
```

##### 🔸 `socket`
> Модуль `socket` предоставляет доступ к интерфейсу BSD socket — стандартному способу сетевого взаимодействия в Unix и других операционных системах. Этот модуль позволяет создавать как клиентские, так и серверные приложения для работы с сетью

```python
import socket
import threading
import time

def basic_socket_concepts():
    """Основные концепции работы с сокетами"""
    
    print("=== Основы сокетов ===")
    
    # 1. Создание сокета
    print("1. Создание сокета:")
    
    # TCP сокет (надежный, с установкой соединения)
    tcp_socket = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    print("   TCP сокет создан")
    
    # UDP сокет (быстрый, без установки соединения)
    udp_socket = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
    print("   UDP сокет создан")
    
    print("\n2. Информация о сокете:")
    print(f"   Семейство адресов TCP: {tcp_socket.family}")
    print(f"   Тип сокета TCP: {tcp_socket.type}")
    print(f"   Семейство адресов UDP: {udp_socket.family}")
    print(f"   Тип сокета UDP: {udp_socket.type}")
    
    # 3. Получение информации о хосте
    print("\n3. Информация о хосте:")
    hostname = socket.gethostname()
    local_ip = socket.gethostbyname(hostname)
    
    print(f"   Имя хоста: {hostname}")
    print(f"   Локальный IP: {local_ip}")
    
    # Получение всех IP адресов хоста
    try:
        host_info = socket.getaddrinfo(hostname, None)
        ips = set()
        for info in host_info:
            ips.add(info[4][0])
        print(f"   Все IP адреса: {', '.join(ips)}")
    except Exception as e:
        print(f"   Ошибка получения адресов: {e}")
    
    # 4. Разрешение доменных имен
    print("\n4. DNS разрешение:")
    try:
        google_ip = socket.gethostbyname('google.com')
        print(f"   google.com -> {google_ip}")
        
        # Обратное разрешение
        hostname_by_ip = socket.gethostbyaddr(google_ip)
        print(f"   {google_ip} -> {hostname_by_ip[0]}")
    except Exception as e:
        print(f"   Ошибка DNS: {e}")
    
    # Закрываем сокеты
    tcp_socket.close()
    udp_socket.close()
    
    print("\n5. Константы сокетов:")
    print(f"   AF_INET (IPv4): {socket.AF_INET}")
    print(f"   AF_INET6 (IPv6): {socket.AF_INET6}")
    print(f"   SOCK_STREAM (TCP): {socket.SOCK_STREAM}")
    print(f"   SOCK_DGRAM (UDP): {socket.SOCK_DGRAM}")

basic_socket_concepts()
```
больше примеров - [[network (socket)#🔹 TCP клиент и сервер|TCP клиент и сервер]], [[network (socket)#🔹 UDP клиент и сервер|UDP клиент и сервер]]
##### 🔸 `subprocess`
> Модуль `subprocess` позволяет запускать новые процессы, подключаться к их потокам ввода/вывода/ошибок и получать коды возврата.

 Типичные применения:
- **Автоматизация задач** и скриптинг
- **Интеграция** с внешними инструментами
- **Системное администрирование**
- **Тестирование** и CI/CD
- **Обработка данных** внешними программами
- **Выполнение системных команд**

```python
import subprocess
import sys
import os
from pathlib import Path

def basic_subprocess_usage():
    """Основы использования subprocess"""
    
    print("=== Основы subprocess ===")
    
    # 1. Простейший способ - subprocess.run()
    print("1. Простой запуск команды:")
    
    try:
        # Запуск команды и получение результата
        result = subprocess.run(['python', '--version'], 
                              capture_output=True, 
                              text=True)
        
        print(f"   Код возврата: {result.returncode}")
        print(f"   Stdout: {result.stdout.strip()}")
        print(f"   Stderr: {result.stderr.strip()}")
        
    except FileNotFoundError:
        print("   Команда 'python' не найдена")
    
    # 2. Запуск команды без захвата вывода
    print("\n2. Запуск без захвата вывода:")
    
    try:
        # Простая команда, которая работает на большинстве систем
        if sys.platform.startswith('win'):
            result = subprocess.run(['echo', 'Привет из subprocess!'], shell=True)
        else:
            result = subprocess.run(['echo', 'Привет из subprocess!'])
        
        print(f"   Команда завершилась с кодом: {result.returncode}")
        
    except Exception as e:
        print(f"   Ошибка: {e}")
    
    # 3. Работа с shell командами
    print("\n3. Shell команды:")
    
    try:
        if sys.platform.startswith('win'):
            # Windows
            result = subprocess.run('dir', shell=True, capture_output=True, text=True)
            print(f"   Найдено элементов в текущей директории")
        else:
            # Unix/Linux/macOS
            result = subprocess.run('ls -la', shell=True, capture_output=True, text=True)
            print(f"   Список файлов получен (первые 200 символов):")
            print(f"   {result.stdout[:200]}")
            
    except Exception as e:
        print(f"   Ошибка shell команды: {e}")
    
    # 4. Проверка успешности выполнения
    print("\n4. Проверка кодов возврата:")
    
    try:
        # Команда, которая должна завершиться с ошибкой
        result = subprocess.run(['python', '-c', 'exit(42)'], 
                              capture_output=True)
        
        print(f"   Код возврата: {result.returncode}")
        
        # Автоматическая проверка успешности
        try:
            subprocess.run(['python', '-c', 'exit(1)'], check=True)
        except subprocess.CalledProcessError as e:
            print(f"   Команда завершилась с ошибкой: код {e.returncode}")
            
    except Exception as e:
        print(f"   Ошибка проверки кодов: {e}")

basic_subprocess_usage()
```

##### 🔸 `tempfile`
> tempfile поддерживает создание временных файлов и каталогов.

```python
import tempfile

# создания временного файла:
with tempfile.TemporaryFile() as f:
	f.write(b'Hello World')
	f.seek(0)
	data = f.read()
	print('Got:', data)
	
# Создать временный каталог можно так:
with tempfile.TemporaryDirectory() as dirname:
	# Использование каталога dirname
	...	
```

Команда with часто используется и для определения области видимости, где будет использоваться файл или директория. В конце блока with файл или директория будет удален

##### 🔸 `textwrap`
>`textwrap` может использоваться для форматирования текста под конкретную ширину терминала. Это довольно специализированный модуль, но он часто может использоваться для очистки выводимого текста при построении отчетов.

```python
# функции wrap() получает текст и переносит его по заданной ширине столбца, функция возвращает список строк

import textwrap

text = """look into my eyes
look into my eyes
the eyes the eyes the eyes
not around the eyes
don't look around the eyes
look into my eyes you're under
"""

wrapped = textwrap.wrap(text, width=81)

print('\n'.join(wrapped))
# Produces:
# look into my eyes look into my eyes the
# eyes the eyes the eyes not around the
# eyes don't look around the eyes look
# into my eyes you're under


# функции indent() может использоваться для расстановки отступов в блоке текста

print(textwrap.indent(text, ' '))
# Produces:
# look into my eyes
# look into my eyes
# the eyes the eyes the eyes
# not around the eyes
# don't look around the eyes
# look into my eyes you're under
```

##### 🔸 `math`
>Модуль `math` предоставляет доступ к математическим функциям, определенным в стандарте C. Это один из самых важных модулей для научных вычислений и математических операций.

```python
import math

def math_constants():
    """Математические константы в модуле math"""
    
    print("=== Математические константы ===")
    
    # Основные математические константы
    constants = [
        ('math.pi', math.pi, 'Число π (пи)'),
        ('math.e', math.e, 'Основание натурального логарифма (число Эйлера)'),
        ('math.tau', math.tau, 'τ = 2π (тау)'),
        ('math.inf', math.inf, 'Положительная бесконечность'),
        ('-math.inf', -math.inf, 'Отрицательная бесконечность'),
        ('math.nan', math.nan, 'Not a Number (не число)')
    ]
    
    print("1. Основные константы:")
    for name, value, description in constants:
        if math.isfinite(value):
            print(f"   {name:<12} = {value:.15f} ({description})")
        else:
            print(f"   {name:<12} = {value} ({description})")
    
    # Проверка специальных значений
    print("\n2. Проверка специальных значений:")
    
    test_values = [math.pi, math.inf, -math.inf, math.nan, 42.0, 0.0]
    
    for value in test_values:
        finite = math.isfinite(value)
        infinite = math.isinf(value)
        nan = math.isnan(value)
        
        print(f"   Значение {str(value):<8}:")
        print(f"     isfinite: {finite}, isinf: {infinite}, isnan: {nan}")
    
    # Практическое применение констант
    print("\n3. Практические применения:")
    
    # Площадь и длина окружности
    radius = 5
    circle_area = math.pi * radius ** 2
    circle_circumference = 2 * math.pi * radius
    
    print(f"   Окружность с радиусом {radius}:")
    print(f"     Площадь: {circle_area:.2f}")
    print(f"     Длина: {circle_circumference:.2f}")
    
    # Экспоненциальный рост
    initial_value = 1000
    growth_rate = 0.05
    time = 10
    final_value = initial_value * math.e ** (growth_rate * time)
    
    print(f"\n   Экспоненциальный рост:")
    print(f"     Начальное значение: {initial_value}")
    print(f"     Коэффициент роста: {growth_rate}")
    print(f"     Время: {time}")
    print(f"     Итоговое значение: {final_value:.2f}")

math_constants()
```

##### 🔸 `functools`
> `functools` — это стандартный набор инструментов Python для работы с функциями «высшего порядка» (которые принимают другие функции или возвращают их)

`@lru_cache` -  это **декоратор для кэширования результатов выполнения функции**.
Что она делает: 
- **Запоминает результат**: Когда функция вызывается в первый раз с определенными аргументами, она выполняется, и результат сохраняется в памяти (в кэше).
-  **Возвращает готовое**: При всех последующих вызовах с теми же аргументами код внутри функции **не выполняется**. Декоратор просто отдает объект из памяти.
```python
@lru_cache
def get_redis() -> Redis:
    print("Создаю подключение к Redis!") # Выполнится только ОДИН раз за все время жизни приложения
    return Redis(host=config.redis.host, port=config.redis.port)
```

`@cache` - (появился в Python 3.9+): Это упрощенная версия `lru_cache(maxsize=None)`. Она работает быстрее и не имеет лимита по размеру. Идеальна для кэширования конфигураций.

`@wraps` (Обязателен для своих декораторов) - Если ты пишешь свой декоратор (например, для проверки прав или логирования), всегда используй `@wraps`.
```python
from functools import wraps

def my_decorator(f):
    @wraps(f) # Сохраняет __name__, __doc__ и сигнатуру
    async def wrapper(*args, **kwargs):
        return await f(*args, **kwargs)
    return wrapper
```

`partial` (Предварительное заполнение аргументов) - Позволяет создать новую функцию из существующей, «зафиксировав» часть её аргументов. Полезно, когда функция требует 5 аргументов, но в конкретном месте 3 из них всегда одинаковые.
```python
from functools import partial

def send_message(broker, queue, message):
    ...

# Создаем функцию специально для регистрации
send_registration = partial(send_message, broker, "user-registered")

# Теперь вызываем коротко
await send_registration(message="hello")
```

`cached_property` - Это гибрид `@property` и `@cache`. Вычисляет значение атрибута один раз при первом обращении и сохраняет его в экземпляре класса.
**Зачем:** Если у тебя есть Pydantic-модель или класс сервиса, где нужно один раз выполнить тяжелое вычисление (например, посчитать хэш) и зафиксировать его.
```python
from functools import cached_property

class BookService:
    @cached_property
    def complex_config(self):
        # Выполнится 1 раз для конкретного объекта service
        return heavy_computation()
```

`singledispatch` (Перегрузка функций) - Позволяет функции вести себя по-разному в зависимости от **типа первого аргумента**. Например, тебе нужно по-разному логировать ошибку, если пришел `str`, `dict` или `Exception`.
```python
from functools import singledispatch

@singledispatch
def process(data):
    print("Неизвестный тип")

@process.register
def _(data: str):
    print(f"Обработка строки: {data}")

@process.register
def _(data: int):
    print(f"Обработка числа: {data}")
```

`reduce` - Применяет функцию к парам элементов списка, сводя его к одному значению. Например, перемножить все числа в списке или последовательно применить цепочку фильтров.

---
#### 🔹 Пакеты

> Пакет — это способ структурирования пространства имен модулей с помощью точечной нотации или же это - иерархический набор модулей. Пакет представляет собой директорию, содержащую файл `__init__.py`  (в исходном состоянии этот файл пуст), но он может и содержать код, выполняющий специфическую инициализацию для конкретного пакета.

Создадим структуру пакета:
```python
mypackage/
    __init__.py
    core/
        __init__.py
        math_operations.py
        string_operations.py
    utils/
        __init__.py
        helpers.py
```

```python
"""
Главный пакет приложения
"""

__version__ = "1.0.0"
__author__ = "Your Name"

# Импортируем основные функции для удобства использования
from .core.math_operations import add, multiply
from .utils.helpers import format_number

# Список того, что будет доступно при импорте *
__all__ = ['add', 'multiply', 'format_number']
```

```python
def add(a, b):
    """Сложение"""
    return a + b

def multiply(a, b):
    """Умножение"""
    return a * b

def divide(a, b):
    """Деление"""
    if b == 0:
        raise ValueError("Деление на ноль невозможно")
    return a / b
```

```python
def format_number(number, decimal_places=2):
    """Форматирование числа"""
    return f"{number:.{decimal_places}f}"

def validate_positive(number):
    """Проверка на положительное число"""
    return number > 0
```

##### 🔸 Использование пакетов
```python
# Импорт модуля из пакета
from mypackage.core import math_operations

print(math_operations.add(5, 3))  # 8

# Импорт конкретной функции
from mypackage.core.math_operations import divide

print(divide(10, 2))  # 5.0

# Импорт из главного пакета
from mypackage import add, multiply, format_number

print(add(1, 2))              # 3
print(format_number(3.14159)) # 3.14

# Импорт всего пакета
import mypackage

print(mypackage.__version__)  # 1.0.0
```
---
#### 🔹 Атрибуты модулей

Каждый модуль имеет встроенные атрибуты:
```python
"""
Пример модуля для демонстрации атрибутов
"""

def show_module_info():
    """Показать информацию о модуле"""
    print(f"Имя модуля: {__name__}")
    print(f"Файл модуля: {__file__}")
    print(f"Документация: {__doc__}")
    
    # Все атрибуты модуля
    print(f"Все атрибуты: {dir()}")

# Проверка, запущен ли модуль как основная программа
if __name__ == "__main__":
    print("Модуль запущен как основная программа")
    show_module_info()
else:
    print("Модуль импортирован")
```
---
#### 🔹 Поиск модулей

Python ищет модули в следующих местах (в порядке приоритета):
```python
import sys

# Показать пути поиска модулей
print("Пути поиска модулей:")
for path in sys.path:
    print(f"  {path}")

# Добавить новый путь для поиска модулей
sys.path.append('/path/to/my/modules')

# Информация о загруженных модулях
print("\nЗагруженные модули:")
for module_name in sorted(sys.modules.keys())[:10]:
    print(f"  {module_name}")
```
---
#### 🔹 Перезагрузка модулей

```python
import importlib

# Импорт модуля
import math_utils

# Перезагрузка модуля (полезно при разработке)
importlib.reload(math_utils)
```
---
#### 🔹 Создание исполняемого модуля

```python
#!/usr/bin/env python3
"""
Модуль-калькулятор, который можно использовать как библиотеку
или запускать как отдельную программу
"""

def add(a, b):
    return a + b

def subtract(a, b):
    return a - b

def multiply(a, b):
    return a * b

def divide(a, b):
    if b == 0:
        raise ValueError("Деление на ноль")
    return a / b

def main():
    """Основная функция для командной строки"""
    print("Простой калькулятор")
    
    try:
        a = float(input("Введите первое число: "))
        operation = input("Введите операцию (+, -, *, /): ")
        b = float(input("Введите второе число: "))
        
        if operation == '+':
            result = add(a, b)
        elif operation == '-':
            result = subtract(a, b)
        elif operation == '*':
            result = multiply(a, b)
        elif operation == '/':
            result = divide(a, b)
        else:
            print("Неизвестная операция")
            return
        
        print(f"Результат: {result}")
    
    except ValueError as e:
        print(f"Ошибка: {e}")
    except KeyboardInterrupt:
        print("\nВыход из программы")

# Запуск основной функции только при прямом выполнении
if __name__ == "__main__":
    main()
```
---
#### 🔹 Лучшие практики работы с модулями

##### 🔸 Структурирование импортов
```python
# Стандартные библиотеки
import os
import sys
from datetime import datetime

# Сторонние библиотеки
import requests
import numpy as np

# Локальные модули
from myproject import config
from myproject.utils import helpers
```

##### 🔸 Использование `all`
```python
"""Модуль с контролируемым экспортом"""

# Публичные функции
def public_function():
    return "Это публичная функция"

def another_public_function():
    return "Еще одна публичная функция"

# Приватная функция
def _private_function():
    return "Это приватная функция"

# Контролируем, что экспортируется при import *
__all__ = ['public_function', 'another_public_function']
```

##### 🔸  Документирование модулей
```python
"""
Модуль для работы с файлами

Этот модуль предоставляет утилиты для чтения, записи
и обработки файлов различных форматов.

Example:
    >>> from documented_module import read_file
    >>> content = read_file('example.txt')
    >>> print(content)

Attributes:
    DEFAULT_ENCODING (str): Кодировка по умолчанию
    MAX_FILE_SIZE (int): Максимальный размер файла в байтах
"""

DEFAULT_ENCODING = 'utf-8'
MAX_FILE_SIZE = 1024 * 1024  # 1MB

def read_file(filename, encoding=DEFAULT_ENCODING):
    """
    Читает содержимое файла
    
    Args:
        filename (str): Путь к файлу
        encoding (str, optional): Кодировка файла
    
    Returns:
        str: Содержимое файла
    
    Raises:
        FileNotFoundError: Если файл не найден
        IOError: Если возникла ошибка чтения
    """
    try:
        with open(filename, 'r', encoding=encoding) as file:
            return file.read()
    except FileNotFoundError:
        raise FileNotFoundError(f"Файл {filename} не найден")
    except IOError as e:
        raise IOError(f"Ошибка чтения файла: {e}")
```
---
#### 🔹 НАЧИНАЙТЕ С ПАКЕТА

Работу над новой программой легко начать с простого файла Python. Например, вы можете написать сценарий `program.py`, который станет отправной точкой. Такой способ неплохо подходит для временных и коротких задач, но со временем ваш сценарий начнет расти и дополняться новой функциональностью. В какой-то момент вы захотите разбить его на несколько файлов и здесь часто начинаются проблемы.==Из-за этого вам стоит взять в привычку начинать работу над всеми программами в форме пакета==. Например, вместо файла с именем `program.py` создайте каталог пакета с именем program:
```bash
program/
	__init__.py
	__main__.py
```

Включите начальный код в файл `__main__.py` и запустите свою программу командой вида `python -m program`. Для добавления нового кода включите в пакет новые файлы и используйте импорт относительно пакета. Файлам можно присвоить любые имена, не беспокоясь о конфликтах с другими пакетами, модулями стандартной библиотеки или кодом ваших коллег

---
