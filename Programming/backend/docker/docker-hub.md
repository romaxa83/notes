#docker 

### 🔹 Создание приватного регистра для образов

публичные образы тянуться с dockerhub, свои образы можно пулить туда, 
но они будут также в публичном доступе (открыты для всех), можно создавать свои приватные реестры для образов

🔸 есть образ который можно локально поднять для реестра (для тестирования при разработке)
docs.docker.com/registry/deploying

REGISTRY=localhost:5000 IMAGE_TAG=master_2 make build

🔸чтоб закрыть реестр аутентификацией нужно сгенерить и прокинуть его в контейнер а также передать в переменную окружения

сгенерировать
```bash
docker run --rm registry:2 htpasswd -Bbn user password > htpasswd
```

сервис
```yml
registry:
        image: registry:2
        environment:
          REGISTRY_AUTH: htpasswd
          REGISTRY_AUTH_HTPASSWD_REALM: Realm
          REGISTRY_AUTH_HTPASSWD_PATH: auth/htpasswd
        ports:
          - "5000:5000"
        volumes:
            - ./htpasswd:/auth/htpasswd
            - registry:/var/lib/registry
```

🔸api для докеровского реестра
/v2/_catalog - выведет все реестры
/v2/name_image/tags/list - выведет теги (версию) для образа name_images