#docker

### 🔹 Install
###### 🔸 Linux

```bash
curl https://get.docker.com > /tmp/install.sh

chmod +x /tmp/install.sh

/tmp/install.sh

# покажет версию
docker version

# покажет детали установки
docker info
```

==Запуск docker без sudo==
```bash
# создадим группу docker (если нету)
sudo groupadd docker

# просмотреть группы
cat /etc/group

# добавляем пользователя в группу
sudo gpasswd -a ${USER} docker

# перезапускаем сервис
sudo service docker restart
```

==проверка сокетов==
```bash
ls -l /Users/rodomanovroman/.colima/default/docker.sock
```
---
### 🔹 Основные понятия
######  ==ОБРАЗ ( Image )==

Docker образ (Docker image) — это легковесный, автономный, исполняемый пакет программного обеспечения, который включает в себя всё необходимое для запуска приложения: код, среду выполнения, системные библиотеки, системные инструменты и настройки.

**Основные характеристики Docker образов**
1. **Неизменяемость**: образы Docker являются неизменяемыми (immutable), т.е. после создания их нельзя изменить.
2. **Многослойность**: образы состоят из слоёв (layers), где каждый слой представляет собой набор изменений в файловой системе.
3. **Базовые образы**: большинство образов создаются на основе других образов, например, образ PHP приложения может быть построен на базе официального образа PHP.
4. **Версионирование**: образы имеют теги, которые используются для версионирования.

**Docker образы** обычно создаются с помощью Dockerfile - текстового файла с инструкциями, описывающими как собрать образ

---
######  ==КОНТЕЙНЕР==

Docker контейнер — это запущенный экземпляр Docker образа. Если Docker образ можно сравнить с шаблоном или чертежом, то контейнер — это действующий экземпляр этого шаблона, активный процесс, созданный из образа.

 **Основные характеристики Docker контейнеров**
1. **Изоляция**: контейнеры работают в изолированном пространстве на хост-системе, но используют ядро операционной системы хоста.
2. **Легковесность**: в отличие от виртуальных машин, контейнеры не требуют полной копии операционной системы для каждого экземпляра.
3. **Быстрый запуск**: контейнеры запускаются за считанные секунды (в отличие от виртуальных машин, которые могут загружаться минутами).
4. **Временное состояние**: по умолчанию, все изменения в контейнере теряются при его остановке, если данные не сохранены в томах (volumes).

**Жизненный цикл Docker контейнера**
1. **Создание**: контейнер создается из образа с помощью команды `docker run`.
2. **Запуск**: контейнер запускается и выполняет указанную в образе команду (CMD или ENTRYPOINT).
3. **Остановка**: контейнер может быть остановлен командой `docker stop` (с сохранением состояния).
4. **Перезапуск**: остановленный контейнер можно перезапустить командой `docker start`.
5. **Удаление**: когда контейнер больше не нужен, его можно удалить командой `docker rm`.

**Отличия контейнера от образа**

| Docker образ                   | Docker контейнер                    |
| ------------------------------ | ----------------------------------- |
| Статичный шаблон               | Запущенный экземпляр образа         |
| Неизменяемый (read-only)       | Может иметь записываемый слой       |
| Строится из Dockerfile         | Создаётся из образа                 |
| Хранится в репозитории образов | Выполняется на Docker-хосте         |
| Не имеет состояния             | Имеет состояние во время выполнения |

---
######  ==Volume==
 
Docker Volume (том) — это механизм для хранения и управления данными в Docker-контейнерах, который позволяет сохранять данные вне жизненного цикла контейнера. Это решает одну из ключевых проблем контейнеров: по умолчанию все данные внутри контейнера временные и исчезают при удалении контейнера.

**Основные характеристики Docker Volumes**
1. **Постоянное хранение данных**: данные сохраняются даже после остановки или удаления контейнера.
2. **Независимость от контейнера**: тома хранятся в области файловой системы, управляемой Docker
3. **Возможность совместного использования**: несколько контейнеров могут использовать один и тот же том.
4. **Производительность**: тома обычно имеют лучшую производительность, чем использование привязок (bind mounts).
5. **Безопасность**: тома могут быть зашифрованы и настроены с определенными правами доступа.

---
### 🔹 Команды в консоли

######  ==РАБОТА С ОБРАЗАМИ==

🔸 покажет все образы 
```bash
docker images
```

🔸удалить image контейнера
```bash
docker rmi <IMAGE_ID>
```

🔸загрузить образ с DockerHub
```bash
docker pull <IMAGE NAME>

# загрузка redis
docker pull redis
```

🔸cоздать образ из DockerFile
```bash
docker build -t test/cowsay-dockerfile
```
> нужно находиться в той же директории
###### ==РАБОТА С КОНТЕЙНЕРАМИ==

🔸 запуск контейнере
> если нужно запустить несколько контейнеров то лучше это делать с помощью docker-compose
```bash
docker run [OPTIONS] IMAGE [COMMAND] [ARG...]

# простой запуск nginx
docker run nginx

# прокинуть в контейнер внешнии папки
docker run -v $PMD:/var/www/html

# запустит контейнер debian и откроет консольное приложении при выходе (exit) контейнер прекратит работу 
docker run -i -t debian /bin/bash 

# запускаем бд
docker run -d --name mysql -p 3306:3306 -e MYSQL_ROOT_PASSWORD=secret -e MYSQL_DATABASE=my_db mysql:5.7
```
-  `-d` или `--detach` - запуск контейнера в фоновом режиме
-  `-p` или `--publish` - проброс портов (например, `-p 8080:80` перенаправляет порт 8080 хоста на порт 80 контейнера)
- `-v` или `--volume` - монтирование томов (например, `-v /host/path:/container/path`), до двоеточия указывается путь к внешней папке,после к папке внутри контейнера
- `--name` - задание имени для контейнера
- `-e` или `--env` - установка переменных окружения
- `--network` - подключение к определенной сети
- `--rm` - автоматическое удаление контейнера после его остановки
-  `-w` - указываем с какой рабочей папкой будет запускаться процесс внутри контейнера

🔸зайти в контейнер
```bash
docker exec -it <CONTAINER ID> bash

# or

docker exec -it <CONTAINER ID> sh
```

🔸 выведет контейнеры
```bash
# запущенные
docker ps

# все
docker ps -a
```

🔸 информация о контейнере
```bash
docker inspect <CONTAINER ID>
docker inspect <NAME>

# так можно фильтровать инфу (по IP)
docker inspect <NAME> | grep IPAddress

# получение конретных данных (в примере получение статуса)
docker inspect -f "{{.State.Status}}" <NAME> 
```
>при инспектировании можно узнать много полезной инфы, к примеру где хранятся логи

🔸 инфа о измененных файлов в рабочем контейнере	
```bash
docker diff <CONTAINER ID>
```
>"A" впереди говорит о создании файла (директории)
   "D" впереди говорит о удалении файла (директории)

🔸 логи в контейнере
```bash
docker logs <CONTAINER ID>
```

> *Докер пишет логи в для каждого контейнера по пути
/var/lib/docker/containers/<hash_container>/<hash_container>-json.log*

🔸остановка контейнера
```bash
docker stop <CONTAINER ID>

# остановить все контейнеры
docker stop $(docker ps -a -q)
```

🔸удалить контейнер
```bash
docker rm <CONTAINER ID>

# удалит все остановленые контейнеры
docker rm -v $(docker ps -aq -f status=exited)
```

🔸 выводит статистику по запущенным контейнерам
```bash
docker stats
```

🔸  превращения контейнера в образ
```bash
docker commit cowsay test/cowsayimage
```
> cowsay - имя контейнера
> cowsayimage - имя для создаваемого образа
> test - репозиторий в котором образ будет сохранен.
>Возвращаемое значение будет id созданного образа
###### ==РАБОТА С VOLUME (ТОМАМИ)==

🔸 выведет хеши подключенных volume
```bash
docker volume ls
```

🔸инспектируем 
```bash
docker volume inspect <VOLUME NAME>
```
> Mountpoint - точка монтирования файлов

🔸удаления всех неиспользуемых Docker томов 
```bash
docker volume prune -f
```

🔸удаления тома
```bash
docker volume rm <VOLUME NAME>
```

###### ==ПОЛЕЗНА ИНФОРМАЦИЯ==
🔸 определения общего объема места, занимаемого всеми Docker томами/Images/Containers
```bash
docker system df
```

###### ==РАБОТА С СЕТЯМИ==

Чтобы контейнеры, принадлежащие одному и тому же приложению и могли найти друг друга, необходимо запустить их в одной сети

```bash
# подключить контейнер к сети
docker network connect

# отключить контейнер от сети
docker network disconnect

# создать сеть
docker network create <NAME>

# параметры сети
docker network inspect <NAME>

# удаяет не используемые сети
docker network prune

# список всех сетей
docker network ls 

# удалить сеть
docker network rm <NAME>
```
---
### 🔹 Контекст

**Docker Context** — это механизм, позволяющий одной и той же команде `docker` в терминале управлять разными средами исполнения (движками Docker). 

По умолчанию Docker-клиент обращается к локальному демону (обычно через `/var/run/docker.sock`). Однако с помощью контекстов вы можете легко переключаться между локальной машиной, удаленными серверами или виртуальными средами вроде Colima

Для чего он нужен?
1. **Управление несколькими средами:** Вы можете одной командой переключить терминал с управления локальными контейнерами на управление контейнерами на рабочем сервере или в облаке.
2. **Работа с Colima/Docker Desktop:** Если у вас установлены разные инструменты (например, стандартный Docker Desktop и Colima), каждый из них создает свой контекст. Это позволяет избежать путаницы в том, где именно запускается контейнер.
3. **Изоляция настроек:** Каждый контекст хранит свои параметры подключения (пути к сокетам, TLS-сертификаты, IP-адреса удаленных хостов), избавляя вас от необходимости каждый раз прописывать переменные окружения вроде `DOCKER_HOST` вручную.

```bash
# посмотреть список всех доступных контекстов (звездочка `*` указывает на активный)
docker context ls

# переключиться на нужный контекст
docker context use <имя>

# узнать, какой контекст активен сейчас
docker context show

# выполнить разовую команду в конкретном контексте, не переключаясь на него полностью
docker --context <имя> ps
```

> ⚠️ **Важно:** Не путайте это понятие с _«контекстом сборки»_ (build context) — папкой с файлами, которую Docker использует при создании образа (команда `docker build`

---
### 🔹 Утилиты

###### 🔸СTOP
утилита для linux, для удобного просмотра запущенных контейнеров в терминале

https://github.com/bcicen/ctop

```bash
# install
sudo wget https://github.com/bcicen/ctop/releases/download/v0.7.2/ctop-0.7.2-linux-amd64 -O /usr/local/bin/ctop
sudo chmod +x /usr/local/bin/ctop
```

==команды в терминале==
```bash
ctop

# выведет активные образы
ctop -a
```

###### 🔸Colima

это платформа для запуска контейнеров, специально разработанная для macOS и Linux, которая обеспечивает простой и эффективный опыт контейнеризации на операционной системе Apple

```bash
# установить с помощью Homebrew
brew install colima

# запустить
colima start
colima stop
colima status
```

- repository - https://github.com/abiosoft/colima
- manual - https://netdevops.ru/chto-takoe-colima.html

---
### 🔹 Interesting

- Postgres/PostGIS in Docker for production - http://fuzzytolerance.info/blog/2018/12/04/Postgres-PostGIS-in-Docker-for-production/
- Введение в Traefik 2.0 - https://habr.com/ru/post/508636/
- Различные связки контейнеров - https://github.com/docker/awesome-compose
- Докеризация веб-приложения Node.js - https://nodejs.org/ru/docs/guides/nodejs-docker-webapp/
 - [Установка и настройка Docker](https://www.8host.com/blog/ustanovka-i-nastrojka-docker/ "Установка и настройка Docker")
- [Создание контейнеризированного приложения Docker](https://www.8host.com/blog/sozdanie-kontejnerizirovannogo-prilozheniya-docker/ "Создание контейнеризированного приложения Docker")

---