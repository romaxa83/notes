#database #sql #postgres #mysql

Команда `ALTER TABLE` используется в SQL для изменения структуры существующих таблиц в базе данных, такие как:
🔸Добавление новых столбцов в таблицу
🔸Удаление существующих столбцов
🔸Изменять тип данных столбца
🔸Добавлять, удалять или изменять ограничения (constraints)
🔸Переименовывать таблицы и столбцы

### 🔹 ALTER TABLE в MySQL

###### 🔸Синтаксис основных операций
```sql
-- Добавление столбца
ALTER TABLE имя_таблицы ADD COLUMN имя_столбца тип_данных [ограничения];

-- Удаление столбца
ALTER TABLE имя_таблицы DROP COLUMN имя_столбца;

-- Изменение типа столбца
ALTER TABLE имя_таблицы MODIFY COLUMN имя_столбца новый_тип [ограничения];

-- Переименование столбца
ALTER TABLE имя_таблицы CHANGE COLUMN старое_имя новое_имя тип_данных [ограничения];

-- Добавление индекса
ALTER TABLE имя_таблицы ADD INDEX название_индекса (столбец1, столбец2, ...);

-- Добавление первичного ключа
ALTER TABLE имя_таблицы ADD PRIMARY KEY (столбец1, столбец2, ...);

-- Добавление внешнего ключа
ALTER TABLE имя_таблицы ADD CONSTRAINT имя_ограниченияFOREIGN KEY (столбец) REFERENCES другая_таблица(столбец);

-- ПРИМЕРЫ

-- Переименование таблицы
ALTER TABLE старое_имя RENAME TO новое_имя;

-- Добавление AUTO_INCREMENT первичного ключа
ALTER TABLE orders ADD COLUMN id INT AUTO_INCREMENT PRIMARY KEY;

-- Добавление индекса FULLTEXT для поиска
ALTER TABLE products ADD FULLTEXT INDEX product_search (name, description);

-- Изменение движка таблицы
ALTER TABLE orders ENGINE = InnoDB;

-- Добавление временной метки
ALTER TABLE orders ADD COLUMN updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP;
```

###### 🔸Особенности MySQL:

1. **Использование MODIFY и CHANGE:**
    - `MODIFY` изменяет тип и/или ограничения столбца
    - `CHANGE` позволяет переименовать столбец и изменить его тип
2. **Транзакции для DDL:** В MySQL с InnoDB многие операции ALTER TABLE блокируют таблицу. В новых версиях появились возможности выполнения некоторых ALTER TABLE операций онлайн.
3. **Типы данных MySQL:** Поддерживает специфичные типы данных, такие как `ENUM`, `SET`, и различные варианты `TEXT`.
4. **Секционирование (Partitioning):**
```sql
ALTER TABLE имя_таблицы PARTITION BY RANGE (столбец) (
	PARTITION p0 VALUES LESS THAN (1000),
	PARTITION p1 VALUES LESS THAN (2000),
	PARTITION p2 VALUES LESS THAN MAXVALUE
);
```

---
### 🔹ALTER TABLE в PostgreSQL

###### 🔸Синтаксис основных операций
```sql
-- Добавление столбца
ALTER TABLE имя_таблицы ADD COLUMN имя_столбца тип_данных [ограничения];

-- Удаление столбца
ALTER TABLE имя_таблицы DROP COLUMN имя_столбца;

-- Изменение типа столбца
ALTER TABLE имя_таблицы ALTER COLUMN имя_столбца TYPE новый_тип USING выражение_преобразования;

-- Переименование столбца
ALTER TABLE имя_таблицы RENAME COLUMN старое_имя TO новое_имя;

-- Добавление ограничения
ALTER TABLE имя_таблицы ADD CONSTRAINT имя_ограничения PRIMARY KEY (столбец);

-- Добавление внешнего ключа
ALTER TABLE имя_таблицы ADD CONSTRAINT имя_ограниченияFOREIGN KEY (столбец) REFERENCES другая_таблица(столбец);

-- Переименование таблицы
ALTER TABLE старое_имя RENAME TO новое_имя;

-- ПРИМЕР

-- Добавление автоинкрементного первичного ключа
ALTER TABLE orders ADD COLUMN id SERIAL PRIMARY KEY;

-- Добавление временной метки с автоматическим обновлением
ALTER TABLE orders ADD COLUMN updated_at TIMESTAMP;
ALTER TABLE orders ALTER COLUMN updated_at SET DEFAULT CURRENT_TIMESTAMP;
CREATE TRIGGER update_orders_timestamp BEFORE UPDATE ON ordersFOR EACH ROW EXECUTE FUNCTION trigger_set_timestamp();

-- Добавление проверочного ограничения
ALTER TABLE users ADD CONSTRAINT check_age CHECK (age >= 18);
-- Изменение последовательности для столбца
ALTER SEQUENCE orders_id_seq RESTART WITH 1000;
```

###### 🔸Особенности PostgreSQL:

1. **Выражение `USING` при изменении типа:** При изменении типа данных столбца можно указать, как преобразовать существующие данные:
```sql
ALTER TABLE users ALTER COLUMN age TYPE integer USING age::integer;
```
2. **Транзакционность DDL:** В PostgreSQL все DDL операции (включая `ALTER TABLE`) являются транзакционными, что позволяет отменить изменения.
3. **Секционирование (Partitioning):**
```sql
-- В PostgreSQL 10+
ALTER TABLE имя_таблицы ATTACH PARTITION имя_секции FOR VALUES FROM (1) TO (100);
```
4. **Наследование таблиц:**
```sql
ALTER TABLE дочерняя_таблица INHERIT родительская_таблица;
```
5. **Настройка хранения:**
```sql
ALTER TABLE имя_таблицы SET (fillfactor=70);
```

---
### 🔹Ключевые различия между MySQL и PostgreSQL

1. **Синтаксис изменения столбцов:**
    - MySQL: `MODIFY COLUMN` или `CHANGE COLUMN`
    - PostgreSQL: `ALTER COLUMN`
2. **Преобразование типов:**
    - PostgreSQL предлагает более гибкий механизм с выражением `USING`
    - MySQL часто требует создания временных столбцов для сложных преобразований
3. **Транзакционность:**
    - PostgreSQL поддерживает транзакционные ALTER TABLE
    - MySQL традиционно не поддерживал транзакционные DDL, хотя в новых версиях есть улучшения
4. **Производительность:**
    - В PostgreSQL многие операции выполняются без полной перезаписи таблицы
    - MySQL часто требует полной перезаписи таблицы для многих операций ALTER TABLE
5. **Добавление ограничений NOT NULL:**
    - PostgreSQL: `ALTER TABLE таблица ALTER COLUMN столбец SET NOT NULL;`
    - MySQL: `ALTER TABLE таблица MODIFY COLUMN столбец тип NOT NULL;`
---
### 🔹Рекомендации по использованию ALTER TABLE

1. **Планируйте изменения заранее:** Особенно в MySQL, где многие ALTER TABLE блокируют таблицу.
2. **Используйте транзакции** в PostgreSQL для безопасных изменений схемы.
3. **Проверяйте нагрузку на сервер** перед выполнением тяжелых операций ALTER TABLE.
4. **Для больших таблиц** рассмотрите пошаговые миграции данных вместо прямого ALTER TABLE.
5. **В MySQL проверьте размер таблицы** перед ALTER TABLE, так как может потребоваться в 2-3 раза больше места во время операции.
6. **В PostgreSQL используйте выражение USING** для безопасного преобразования типов.
7. **Учитывайте репликацию:** ALTER TABLE может вызвать проблемы с репликацией, особенно в смешанных версиях.
---
