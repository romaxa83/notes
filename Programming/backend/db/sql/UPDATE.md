#database #sql

🔹это команда SQL, используемая для изменения существующих данных в таблице базы данных.

*Базовый синтаксис*
```sql
UPDATE users  
SET name=`update_name`, email=new_email, ...  
WHERE id=1;
```

*Можно при обновлении использовать ариф. операции*
```sql
UPDATE orders SET price = price * 2 
```

*Строковые функции* 
```sql
UPDATE clients  
SET full_name = CONCAT(first_name, ' ', last_name);
```

*Условные выражения*
```sql
UPDATE users 
SET rang = CASE 
		WHEN reputation > 300 THEN 'элита'
		WHEN reputation > 100 THEN 'популярный'
		WHEN reputation > 0 THEN 'пользователь'
		ELSE 'нуб' 
	END;   
```

*Использовать под-запросы*
```sql
-- В SET  
UPDATE сотрудники  
SET зарплата = (SELECT AVG(зарплата) FROM сотрудники WHERE отдел = 'IT')  
WHERE должность = 'Стажер';  
  
-- В WHERE  
UPDATE продукты  
SET скидка = 15  
WHERE id IN (SELECT продукт_id FROM сезонные_товары WHERE сезон = 'Зима');```

> ⚠️ Если упустить условие то обновления применится ко всем записям в таблицу