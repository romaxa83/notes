#js

#### 🔹 Command

```bash
# откроет домашнюю страницу пакет
npm home $package
# открывает git-репозиторий
npm repo $package

# обновляет версию npm
npm update npm -g
# показывает список установленных пакетов
npm list --depth=0
# покажет список установленных пакетов, которые требуют обновления.
npm outdated --depth=0

# удаляет установленный пакет по имени.
npm uninstall package
# покажет версию установленного пакета
npm list package
# последнюю версию пакета, которая существует.
npm view package version
```
