#go 

#### 🔹 Install GVM (Go Version Manager)

> @see - https://github.com/moovweb/gvm

```bash
# install fo Ubuntu
bash < <(curl -s -S -L https://raw.githubusercontent.com/moovweb/gvm/master/binscripts/gvm-installer)

sudo apt-get install bison -y
gvm

# установка версий go
gvm install [version] [options]
gvm install go1.16.5 -B

# использование по умолчанию
gvm use go1.16.5 --default

# експортим переменые
[[ -s "$HOME/.gvm/scripts/gvm" ]] && source "$HOME/.gvm/scripts/gvm"
export GOPATH=$HOME/go
export GOBIN=$GOPATH/bin
export PATH=${PATH}:$GOBIN:$GOROOT/bin

# проверка версии
go version
```
---
#### 🔹 Install

> @see - https://losst.pro/ustanovka-go-ubuntu

```bash
wget https://dl.google.com/go/go1.15.2.linux-amd64.tar.gz
sudo tar -C /opt -xzf go1.15.2.linux-amd64.tar.gz
export PATH=$PATH:/opt/go/bin
go version
nano /etc/environment
# в конец строки добавляем /opt/go/bin
```
---
#### 🔹 Переменные среда
##### 🔸 GOROOT

> переменная среды - определяющая путь установки вашего Golang SDK (интерпритатор языка) Полезен когда у вас несколько версий языка.

##### 🔸 GOPATH
переменная среды, к которой  инструменты `Go` обращаются за информацией о местонахождении рабочей области, т.е. где лежат файлы вашего проекта и их зависимости.Бывает глобальный, проектный и модульный. По дефолту это папка src в `GOROOT`, но ее можно менять (ниже описание как это сделать)

Допустим, вместо домашнего каталога вы разместили свой пакет greeting в каталоге `code` в корневом каталоге своего жесткого диска. И теперь вы хотите запустить файл main.go, который зависит от greeting, то мы получим ошибку, т.к. компилятор не может найти пакет `greeting` (он ищет его в подкаталоге go, по умолчанию) для этого нужно настроить компилятор go и передать информацию о нахождении кода

`export GOPATH="/code"`

после этого, при выполнении команды `go run` (как и других средств Go) будет использоваться  рабочая область, определяемая заданным каталогом. Таким образом, библиотека greeting будет найдена, а программа успешно выполнится данный вариант настроит `GOPATH` на время текущей сессии
##### 🔸 GOPROXY

Позволяет указывать свой репозиторий для модулей. Go базируется на системе модулей и на системе зависимостей от этих модулей. Соответственно, бывает, что у вас закрытая локальная сеть или что у вас нет доступа в интернет. И вам нужно эти модули скачать каким-то образом и их использовать внутри вашей сети. Вот таким образом, вы можете поставить goproxy, и goproxy будет лазить в ваш как бы репозиторий и качать оттуда зависимости. Еще очень удобно, если у вас в проекте какие-то, например, статические зашиты зависимости, вы не хотите обновлять библиотеки, вы можете,соответственно, использовать goproxy. Туда сохранить, сложить все библиотеки и, соответственно, использовать это, как ваш локальный репозиторий.

##### 🔸 `GOMAXPROCS`
Переменная среды (и функция `Go`) `GOMAXPROCS` позволяет ограничить количество потоков операционной системы, которые могут одновременно выполнять код `Go` на уровне пользователя. Начиная с версии `Go 1.5`, по умолчанию значение `GOMAXPROCS` равно количеству логических ядер, доступных на UNIX-компьютере

```go
// узнать значение переменной среды GOMAXPROCS
import (
	"fmt"
	"runtime"
)

func getGOMAXPROCS() int {
	return runtime.GOMAXPROCS(0)
}

func main() {
	fmt.Printf("GOMAXPROCS: %d\n", getGOMAXPROCS())
}

// так можно изменить при запуске
export GOMAXPROCS=800; go run maxprocs.go
```

##### 🔸 Вывод переменных окружения для `go`
```bash
go env

# запись в переменую окружения значения
go env -w GO111MODULE=auto
```
---
#### 🔹 Принципы огранизации кода

- Каждая функция должна делать что-то одно (single responsibly)
- связки функций вместе с каким-то объектом, над которым они работают так же отвечают за что-то одно
- Один файл исходников содержит решение конкретной задачи (открыть/закрыть файл, провалидировать поле, обернуть ответ ...)
- Один или несколько файлов исходников, решающих одну задачу, обьединены в package (все про валидацию, все про работу с бд, все про работу с файлами ...)
- Комплект из Package, решающий глобальную бизнес задачу (авторизация, валидация, бд от соединения до ответа ...) обьединяются в модуль, которые мы часто называем - библиотекой или приложением или сервис, в терминологии go это все модули
- ⚠️ Модули могут быть зависимы друг от друга, но это должно быть явно указано

---
#### 🔹 СОЗДАНИЕ МОДУЛЯ

```bash
go mod init <name> # github.com/romaxa83/some_module
go modv tidy # подтянет все зависимости
```
---
#### 🔹 Запуск программы

```bash
# запускает команду, компилирует ее и все связаные пакеты, но весь скомпелированый код будет потерян после завершения программы hello.go должен быть package main

go run hello.go
```
##### 🔸 Build
```bash
# компилирует и сохраняет исполняемый двоичный файл hello.go должен быть package main
go build hello.go
# запуск программы
./hello

# cоздание файла без отладочной информации (бинарник будет легче)
go build -ldflags "-s" hello.go

# если добавить флаг -x , при билде будет выводить доп. информацию
go build -x hello.go

# компиляция для windows

# build_win32:
GOOS=windows GOARCH=386 go build -o my_app32.exe

# build_win64:
GOOS=windows GOARCH=amd64 go build -o my_app64.exe
```

##### 🔸 Tag
> если нам нужно собрать нашу программу в разных режимах, то можно использовать теги при сборке, к примеру у нас есть dev режим, где мы используем другие переменные окружения

в начале файла мы указываем
```go
// +build dev

package main

func init() {
	os.Setenv("APP_ENV", "dev")
	fmt.Println("App is in dev env")
}
```

```bash
# при обычном билде данный файл не попадет в сборку, чтоб он попал нужно при сборке указать тег
go build -tags dev

# если нужно указать несколько тегов
go build -tags "dev debug"
```
##### 🔸 Install
```bash
# компилирует и сохраняет исполняемый двоичный файл, в папке bin, рабочей области go hello.go должен быть package main
go install hello.go

# если у нас структура src->github.com->romaxa83->mypakage->main.go
go install github.com/romaxa83/mypakage
# запуск
./mypakage
```
---
#### 🔹 Форматирование кода
```bash
# рекомендуется использовать что б соответствовал стилевому руководству Go

go fmt
```
---
#### 🔹 Запуск программы из любого места
```bash
# если файл нужно запускать из людого места создаем бинарник

sudo chmod +x hello
sudo cp hello /usr/local/bin

# теперь можно запускать hello из любого места
```
---
#### 🔹 Установка пакетов
```bash
# пример
go get github.com/gobuffalo/flect

# найдет пакет на GitHub и установит его в каталог $GOPATH ($GOPATH/src/github.com/gobuffalo/flect)

# установит млм обновит до последней версии если пакет уже установлен
go get -u github.com/gobuffalo/flect
```
---
#### 🔹 Чтение документации
```
go doc <имя пакета>
```
---
#### 🔹 Хранение зависимостей в одном месте
```bash
# Иногда необходимо хранить все зависимости в одном месте, рядом с файлами проекта это позволяет сделать команда

go mod vendor
# выполняеться после инициализации модуля
```
---
#### 🔹 Package

Инструменты Go ищут код пакетов в специальном каталоге (папке) на вашем компьютере, который называется **РАБОЧЕЙ ОБЛАСТЬЮ**. По умолчанию рабочей областью является каталог с именем go в домашнем каталоге текущего пользователя. Каталог рабочей области содержит три под-каталога:
- `bin` - для хранения откомпилированных двоичных исполняемых программ.
- `pkg` - для хранения откомпилированных двоичных файлов пакетов.
- `src` - для хранения исходного кода Go.Каждый пакета размещается в отдельном под-каталоге. По соглашениям имя под-каталога должно совпадать с именем пакета, в пакете может сохраняться как один так и несколько файлов

данная структура позволяет инструментам Go находить код

```bash
go
|__ bin
|__ pkg
|__ src
    |__greeting
    |  |__greeting.go
    |  |__plugin.go
    |
    |__hi
 	   |__main.go

```

в файлах пакета `greeting` должны начинаться с `package greeting` т.к. они относятся к пакету `greeting` ==также если мы хотим чтоб их функции/переменные/константы были доступны в других пакетах (экспортировались), их следует писать с верхнего регистра==.
Если это исполняемая команда, то в ней обязательно должен быть `main.go` для импорта другого вашего пакета нужно писать путь относительно папки `src`
`/src/gretting` `import "gretting"`

В пакетах могут быть внутренние пакеты
```bash
go
|__ bin
|__ pkg
|__ src
    |__greeting
    |  |__english
    |  |  |__english.go
    |  |__russian
    |  |  |__russian.go
    |  |__greeting.go 	// исходный код пакета
    |  |
    |  |
    |__hi
 	   |__main.go


```

доступ к внутренним пакетам
```go
import "gretting/english"
```

##### 🔸 Соглашение по выбору имен пакетов

- Имя пакета должно быть записано только символами нижнего регистра.
-  Имя следует сокращать, если его смысл очевиден (например, `fmt`)
- По возможности имя должно состоять из одного слова. Если необходимы два слова, они не должны разделяться символами подчеркивания, а второе слово не должно начинаться с буквы верхнего регистра (пример — пакет `strconv`).
- Импортированные имена пакетов могут конфликтовать с именами локальных переменных, поэтому не используйте имя, которое с большой вероятностью может быть выбрано пользователями пакета. (Например, если бы пакет `fmt` назывался `format`, то импорт этого пакета создавал бы риск конфликта с локальной переменной format)	
------

 часто пакеты публикуются в репозитория, типа `github` и для того чтобы у пакетов не было конфликтов пространства имен для иерархии используется части url-адресса где размещен пакет
```bash
go
|__ bin
|__ pkg
|__ src
    |__github.com		// каталог с именем домена
       |__romaxa83		// каталог с именем пользователя
          |__skeleton		// каталог с именем пакета
             |__skeleton.go

# в коде пакет импортируеться
import "github/romaxa83/skeleton"
```

##### 🔸 Документация пакета
```bash
# вывод документации по пакету
go doc fmt

# вывод документации по конкретоной функции пакета
go doc strconv ParseFloat

# вывод документации своих или скачаных пакетов
go doc github.com/headfirstgo/keyboard
go doc github.com/headfirstgo/keyboard GetFloat

```

> чтоб выводилась документация на своих пакетах нужно добавлять комментарии

==При добавлении документирующих комментариев следует соблюдать ряд правил==
- Комментарии должны состоять из полноценных предложений.
- Комментарии для пакетов должны начинаться со слова «Package», за которым следует имя пакета: `// Package mypackage enables widget management`.
- Комментарии для функций должны начинаться с имени функции, которую они описывают: `// MyFunction converts widgets to gizmos`.
- В комментарии также можно включать примеры кода, которые должны снабжаться отступами.
-  Не включайте дополнительные символы для выразительности или форматирования (кроме отступов в примерах кода). Документирующие комментарии будут выводиться в виде простого текста и должны форматироваться соответствующим образом.

> Также можно запустить сервер с документацией

```bash
godoc -http=:6060
# будет доступна по
http://localhost:6060/pkg

# если не установлен, тот устанавливаеться так
go get -v  golang.org/x/tools/cmd/godoc 
```

##### 🔸 Сторонние пакеты
```bash
# при загрузки стороних пакетов через get пакет грузиться в 
~/go/src/github.com/<name_package>

# также он компилируються и зачастую находяться по пути
~/go/pkg/darwin_amd64/github.com/<name_package>

# удалени временых файлов заргуженого пакета
go clean -i -v -x github.com/<name_package>

# удаление пакета
rm -rf ~/go/src/github.com/<name_package>
```
---
#### 🔹 Start project

```bash
# инициализация проекта
go mod init github.com/romaxa83/project_name
# создает файл go.mod - где описаны все зависимости проекта

go mod tidy
# команда добавляет все недостающие модули, необходимые для построения пакетов и зависимостей текущего модуля, и удаляет неиспользуемые модули, которые не предоставляют никаких соответствующих пакетов, также добавляет все недостающие записи в go.sum и удаляет ненужные, модули добавляет в кеш go, который можно посмотреть go env - путь к кешу будет в переменноую GOMODCACHE

go mod vendor
# создаст папку vendor, и установит туда все зависимые пакеты, иначе он их устанавливает в кеш golang
```
---
#### 🔹 Articles

- https://github.com/Konstantin8105/Effective_Go_RU/blob/master/README.md#if
- https://dev-gang.ru/article/golang-osnovnoi-sintaksis-pua4fd0n18/
- ОСНОВЫ - http://golang-book.ru/chapter-13-core-packages.html
- Как структурировать проект на go - https://github.com/golang-standards/project-layout
- Стандартный layout для проектов - github.com/golang-standards/project-layout

---

