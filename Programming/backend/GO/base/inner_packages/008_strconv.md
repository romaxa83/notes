#go #package 

#### 🔹 `ParseFloat()`

```go
// преобразует значение во float
newFloat, err := strconv.ParseFloat(input, 64)

// где input - преобразуемая строка а 64 - кол-во битов точности
```
---
#### 🔹 `Atoi()`

Часто используется для преобразования пользовательского ввода если нужно именно число.
```go
// преобразует значение (строковое) в целое число
counter, err := strconv.Atoi(input)
```
---