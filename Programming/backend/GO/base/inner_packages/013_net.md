#go #package #http-server

#### 🔹 Прослушивать запрос браузера и отвечать на них.

==поднимает сервер на порту 8080==
```go
import "net/http"

err := http.ListenAndServe("localhost:8080", nil)

http.HandleFunc("/hello", viewHandler)
```
---