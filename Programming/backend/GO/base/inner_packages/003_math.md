#go #package

#### 🔹 `Floor()`

Принимает число , округляет в меньшую сторону,  и возвращает его
```go
math.Floor(2.75)	// 2
```
---
#### 🔹 `Rand()`

```go
import "math/rand"

// генерит случайно число от 0 до 99
t := rand.Intn(100)
```

Пример выше буде каждый раз генерить одно и тоже случайное число, чтоб оно было разным используется такая конструкция.
```go
seconds := time.Now().Unix()
rand.Seed(seconds)
target := rand.Intn(100)
```
---
#### 🔹 `Inf()`

Специальное значение, представляющее отрицательную бесконечность

```go
math.Inf(-1)
```

Используется при сравнения для определения максимального числа

```go
func maximum(numbers ...float64) float64 {
	max := math.Inf(-1)
	for _, number := range numbers {
		
		if number > max {
			max = number
		}
	}
}
```
---