#go #package

#### 🔹 `ReadDir()`

Функция предназначенную для чтения содержимого каталога, получает имя каталога и возвращает сегмент значений, по одному для каждого файла или подкаталога в каталоге. Каждое значение в сегменте поддерживает интерфейс `FileInfo`,  который включает метод `Name`, возвращающий имя файла,  и метод `IsDir`, возвращающий значение true для каталогов.

```go
import "io/ioutil"

files, err := ioutil.ReadDir("my_directory")
```
---
#### 🔹 `ReadFile()`

Читает файл целиком
```go
import "io/ioutil"

fileData, err = ioutil.ReadFile(filename)
```
---