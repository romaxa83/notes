#go #package

> Название пакета - cокращенние от `format` форматирование для входных и выходных данных.

#### 🔹 `Println()`

Выводит все переданные арг. (строки), в терминал их значения разделяются пробелом, после вывода переходит на следующую строку
```go
fmt.Println("string1", "string2")
```
---
#### 🔹 `Printf()`

Получает строку и вставляет в нее одно или несколько значений, отформатированных заданным способом, знак `%` рассматривается как начало глагола форматирования части строка которая будет заменена значением в определенном формате

```go
fmt.Printf("The %s cost %d cents each.\n", "gumballs", 23)

// если нужно увидеть скрытые символы используеться %#v
fmt.Printf("%#v %#v %#v", "", "\t", "\n") // "" "\t" "\n"

// форматированым занчениям можно задавать ширину

// первое поле должно имееть ширину 12 символов, для второго ширина не определена
fmt.Printf("%12s | %s\n", "Product", "Cost in Cents")
fmt.Println("-----------------------------")
fmt.Printf("%12s | %2d\n", "Stamps", 50)
fmt.Printf("%12s | %2d\n", "Paper Clips", 5)
fmt.Printf("%12s | %2d\n", "Tape", 99)

// форматирование ввывода дробных частей
%5.3f
// где 5- минимальная ширина всего числа, 3 - ширина дробной части 
%.2f // позволяет взять число с плавающей точкой и округлить до двух чисел в дробной части

fmt.Printf("%.2f\n", 1.8199999999999998)	// 1.82
```
---
#### 🔹 `Sprintf()`

В целом похожа на `Printf()`, но она возвращает отформатированную строку, а не выводит еe.
```go
resultString := fmt.Sprintf("About one-third: %0.2f\n", 1.0/3.0)
fmt.Printf(resultString)
```
---
#### 🔹 `Fprintln()`

Семейство функций `fmt.Fprintln()` , `fmt.Fprint()` и  `fmt.Fprintf()` используются для записи в файлы с помощью `io.Writer`

---
#### 🔹 `Errorf()`

Работает как `Printf()` и `Sprintf()`  только возвращает значение ошибки

---
#### 🔹 Получение ввода из консоли

```go
// в переменой input будет наш ввод
fmt.Print("Enter a number: ")
var input float64
fmt.Scanf("%f", &input)
```
---
#### 🔹 Специальные символы
##### 🔸 `%T`

%T - выведет тип переменной
```go
fmt.Errorf("unsupported type: %T", v)
```

##### 🔸 `%w`

%w - вложенная ошибка
```go
fmt.Errorf("unsupported type: %w", err)
```
---
