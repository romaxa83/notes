#go #package #reflection 

> ==Рефлексия== — это дополнительная функциональность Go, которая позволяет динамически, в процессе выполнения программы, узнавать тип произвольного объекта, а также получать информацию о его структуре. Для работы с рефлексией создан пакет `reflect`

#### 🔹 `TypeOf()`

```go
// принимает данные, и возвращает их тип данных

reflect.TypeOf(42)	// int
```
---