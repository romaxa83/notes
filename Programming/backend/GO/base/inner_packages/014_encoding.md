#go #package

#### 🔹 Json

Преобразование в `json`

```go
import "encoding/json"

// преобразовывает структуру в срез байтов в виде json
json.Marshal()
// применяет отступы
json.MarshalIndent()

// декодирует json - строку в структуру
json.Unmarshal()
```

Также есть пара `Encode()` и `Decode()`, которая выполняет туже работу, отличие от `Marshal()` и `Unmarshal()` в том что последнии работают с отдельными объектами а первые могут работать как с несколькими объектами так и с потоком байтов.

---