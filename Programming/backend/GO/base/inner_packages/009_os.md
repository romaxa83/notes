#go #package

#### 🔹 `Args`

Сегмент строк, представляющих аргументы командной строки, переданные текущей программе при запуске.

```go
fmt.println(os.Args)
```
---
#### 🔹 `Stdout`

`Stdout` означает «стандартный вывод». Оно работает как файл, но любые данные, записанные в него, выводятся в терминале, а не сохраняются на диске. (Такие функции, как `fmt.Println`, `fmt.Printf` и т. д., незаметно для вас записывают данные в os.Stdout.)

---
#### 🔹 `Stdin`

```go
// доступ к стандартному потоку ввода
os.Stdin 
```
---
#### 🔹 Stderr

```go
// доступ к стандартному потоку ошибок
os.Stderr
```
---
#### 🔹 OpenFile

> Открывает файл

```go
os.OpenFile()

// принимает 3 параметра
// 1 - путь к файлу с названием
// 2 - флаг (открываем только для чтения или для записи) подробней в доке `go doc os O_RDONLY`
// 3 - права	

// пример (подробнее в разделе file)

options := os.O_WRONLY | os.O_APPEND | os.O_CREATE
file, err := os.OpenFile("some.txt", options, os.FileMode(0600))
```
---
#### 🔹 Exit

```go
// выход из программы программу
os.Exit(1)
```

> ⚠️ данный выход считается плохой практикой если это не функция `main`, в ней данный выход приемлем.

---
#### 🔹 Проверить наличие файла

```go
_, err := os.Stat(file)
```
---
#### 🔹 Получение полномочий к доступам файла

> Используется функция `os.Stat(file)` которая вернет информацию о файле

```go
package main

import (
	"fmt"
	"os"
)

func main (){
	arguments := os.Args
	if len(arguments) == 1 {
		fmt.Printf("usage: permissions filename\n")
		return
	}

	filename := arguments[1]
	info, _ := os.Stat(filename)
	// здесь получаем права доступа
	mode := info.Mode()
	fmt.Println(filename, "mode is", mode.String()[1:10])
}
```
---
#### 🔹 Работа с переменными окружения

```go
// установить
os.Setenv("FOO", 1)

// получить
os.Getenv("FOO")

// получить переменую, вторым параметром прийдеь bool
// была ли установленна вообще эта переменная
os.LookupEnv("FOO")

// получение все переменых окружения
for _, e := range os.Environ() {
	fmt.Println(e)
}
```
---
#### 🔹 Exec

> Позволяет запускать другие команда

```go
// вызовет браузер firefox
func main() {
	cmd := exec.Command("firefox")

	err := cmd.Run()
	if err != nil {
		log.Fatal(err)
	}
}


// пример команды с аргументами
func main() {
	// команда которая преобразует строку из маленьких символов в большие
	cmd := exec.Command("tr", "a-z", "A-Z")

	// преобразуемая строка
	cmd.Stdin = strings.NewReader("little")

	var out bytes.Buffer
	cmd.Stdout = &out
	err := cmd.Run()
	if err != nil {
		log.Fatal(err)
	}

	fmt.Printf("translated phrase: %q\n", out.String())
}

// еще пример с несколькими параметрами
func main() {
	prg := "echo"

	arg1 := "there"
	arg2 := "are slurms"

	cmd := exec.Command(prg, arg1, arg2)
	stdout, err := cmd.Output()

	if err != nil {
		log.Fatal(err)
	}

	fmt.Printf("stdout: %+v\n", string(stdout))
}

// вызывать можно по цепочке
func main() {
	out, err := exec.Command("ls", "-l").Output()
	if err != nil {
		log.Fatal(err)
	}

	fmt.Printf("stdout: %+v\n", string(out))
}
```
---
#### 🔹 Pipe

> Когда вам нужно запустить какое-то приложение так, чтобы оно висело отдельным процессом и ждало, ввода какого-то вашего другого процесса внутри вашей системы, то можно использовать `pipe`

```go
func main() {
	cmd := exec.Command("cat")
	stdin, err := cmd.StdinPipe()
	if err != nil {
		log.Fatal(err)
	}

	go func() {
		defer stdin.Close()
		io.WriteString(stdin, "an old slurm")
	}()

	out, err := cmd.CombinedOutput()
	if err != nil {
		log.Fatal(err)
	}

	fmt.Printf("stdout: %+v\n", string(out))
}
```
---
