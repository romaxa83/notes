#go 

#### 🔹Interface

 В Go интерфейс определяется как набор методов,  которые должны поддерживаться некоторыми значениями. Таким образом, интерфейс представляет набор действий, выполняемых с использованием типа.

```go
type myInterface interface {
	methodWithoutParametrs()
	methodWithParametr(float 64)
	methodWithReturnValue() string
}
```

Любой тип, который содержит все методы, перечисленные в определении интерфейса, называется поддерживающим этот интерфейс. Тип может содержать и другие методы, но все методы интерфейса с их сигнатурой должны быть реализованы. Тип может поддерживать несколько интерфейсов, а интерфейс может поддерживаться несколькими типами.

Если тип содержит все методы, объявленные в интерфейсе, то может находиться в любом месте, где должен использоваться этот интерфейс, без каких-либо дополнительных объявлений, go автоматически его связывает.

Проще говоря, интерфейс — это абстрактный тип, определяющий множество функций, которые необходимо реализовать для типа, чтобы его можно было считать экземпляром интерфейса.

Главное преимущество, которое мы получаем благодаря наличию и использованию интерфейса, состоит в том, что мы можем передавать переменную типа, реализующего этот интерфейс, любой функции, которая ожидает параметр данного интерфейса

```go
// обьявление типа интерфейса
type myInterface interface {
	methodForInterface()
}

// обьявление типа, поддерживающего интрефейс
type MyType int

// обязательный метод интерфейса
func (m MyType) methodForInterface(){
	fmt.println("interface method")
}

// сообственный метод
func (m MyType) someMethod(){
	fmt.println("some method")
}
```

Конкретный тип определяет не только то, что могут делать его значения (то есть какие методы для них можно вызывать), но и то, чем они являются: он определяет базовый тип, используемый для хранения данных значения.

Типы интерфейсов не описывают, чем значение является: они ничего не говорят о базовом типе или о том, как хранятся его данные. Они только описывают, что значение может делать, то есть какие методы оно содержит.

Интерфейсы можно встраивать в другие интерфейсы, создавая композицию, для реализации интерфейса, нужна реализация всех методов встроенных интерфейсов

```go
type Sender interface {
	send(msg string) error
}

type Caller interface {
	call(number string) error
}

type IPhone interface {
	Caller
	Sender
	myFunc() // собственный метод для данного интерфейса
}
```

##### 🔸 Example
```go
type Whistle string
func (w Whistle) MakeSound(){
	fmt.Println("Tweet")
}

type Horn string
func (w Horn) MakeSound(){
	fmt.Println("Honk")
}

type Robot string
// Robot поддерживает интерфейсNoiseMaker.
func (r Robot) MakeSound() {
	fmt.Println("Beep Boop")
}
// Дополнительный метод.
func (r Robot) Walk() {
	fmt.Println("Powering legs")
}

// Представляет любой тип с методом MakeSound
type NoiseMaker interface {
	MakeSound()
}

func play(n NoiseMaker) {
	n.MakeSound()
	//утверждение типа
	robot, ok := n.(Robot)
	if ok {
		//Вызов метода, определегого для конкретного типа (не для интерфейса)
		robot.Walk()
	}
}

func main() {
	// Объявляется переменная NoiseMaker
	var toy NoiseMaker
	toy = Whistle("Toyco Canary")
	toy.MakeSound()
	play(toy)
	toy = Horn("Toyco Blaster")
	toy.MakeSound()
	play(toy)
	toy = Robot("Botco Ambler")
	toy.MakeSound()
	play(toy)
}
```
---
#### 🔹 Утверждение типа

Если значение конкретного типа присваивается переменной с типом интерфейса (включая параметры функций), то вызывать для него можно только методы, входящие в этот интерфейс, независимо от того, какие еще методы содержит конкретный тип, чтобы можно было это сделать, нужно использовать утверждение типа.

Если у вас имеется значение конкретного типа, присвоенное переменной с типом интерфейса, утверждение типа позволяет получить информацию о конкретном типе.

После значения с типом интерфейса указывается точка,  а за ней — пара круглых скобок с конкретным типом. (А точнее, предполагаемым конкретным типом значения.) пример с Robot и его методом Walk(), выше.

После использования утверждения типа для возвращения к значению конкретного типа вы сможете вызывать для него методы, определенные для этого типа, но не являющиеся частью интерфейса.

Если утверждения типа используются в контексте, в котором ожидаются несколько возвращаемых значений, то у них есть второе (необязательное) возвращаемое значение, которое сообщает, успешно ли сработало утверждение или нет.

Операции утверждения типа помогают выполнить две задачи: 
- они проверяют, соответствует ли значение интерфейса определенному типу
- операции утверждения типа позволяют использовать конкретное значение, хранящееся в интерфейсе, или присвоить его новой переменной.
##### 🔸 часто утверждение типа через switch (называют switch-тайп )
```go
type Sender interface {
	send(msg string) error 
} 

type Email struct {
	email string
}

// реализация интерфейса
func (e *Email) send (msg string) error {
	fmt.Println("Сообщение %s Отправленно на email - %s", msg, e.email)
}

type Phone struct {
	phone string
}

// реализация интерфейса
func (p *Phone) send (msg string) error {
	fmt.Println("Сообщение %s Отправленно на телефон - %s", msg, e.phone)
}

// принимает в аргументы интерфейс
func Notify(s Sender) {
	err := s.send("Notify message")
	if err != nil {
		return
	}

	// утверждение типов, используя switch type
	switch s.(type) {
	case *Email:
		fmt.Println("Сообщение по почте")		
	}
	case *Phone:
		phone := s.(*Phone)
		fmt.Println("Сообщение по телефону %s", phone.phone)		
	}

	fmt.Println("Success send")
}

func main () {
	email := &Email{"test@gmail.com"}
	Notify(email)

	phone := &Phone{"9987"}
	Notify(phone)
}
```
---
#### 🔹 Интерфейс error

Значение ошибки - это любое значение с методом `Error`, который возвращает строку. Тип `error` — это всего лишь интерфейс.

```go
// выглядит так
type error interface {
	Error() string
}
```

можно определять собственные типы и использовать их везде, где требуется значение ошибки или если кроме ошибки нужно отслеживать доп. информацию.
##### 🔸 реализация собственных ошибок
```go
// определяем тип с базовым типом string
type ComedyError string

// реализуем интерфейс error
func (c ComedyError) Error() string {
	return string(c)
}

func main () {
	var err error
	err = ComedyError("Some error")
	fmt.Println(err)
}
```

##### 🔸 если нужно отслеживать доп. данные

```go
// пример по остлеживанию безопасной температуры
type OverheatError float64
funс (o OverheatError) Error() string {
 	return fmt.Sprintf("Overheating by %0.2f degrees!", o)
}

func checkTemperature(actual float64, safe float64) error {
	excess := actual - safe
	if excess > 0 {
		return OverheatError(excess)
	}
	return nil
}

func main() {
	var err error = checkTemperature(121.379, 100.0)
	if err != nil {
		log.Fatal(err)
	}
}

```
---
#### 🔹 Интерфейс Stringer

Пакет `fmt` определяет интерфейс `fmt.Stringer`: чтобы любой тип мог решить, как он должен отображаться при выводе мы можем реализовать свои тип вывода данных.

```go
type CoffeePot string
func (c CoffeePot) String() string {
	return string(c) + " coffee pot"
}

// так вызывать
coffeePot := CoffeePot("LuxBrew")
fmt.Println(coffeePot.String())
```

Многие функции из пакета `fmt` проверяют, поддерживают ли переданные им значения интерфейс `Stringer`, и если поддерживают - вызывают их методы `String`.

```go
// соответственно можно вызывать и так
fmt.Print(coffeePot, "\n")
fmt.Println(coffeePot)
fmt.Printf("%s", coffeePot)
```
---
#### 🔹 Пустой интерфейс

Тип `interface{}` называется пустым интерфейсом, и он используется для передачи значений любого типа, он не содержит методов.

Яркий пример `fmt.Println()`, которое принимает значения любого типа, то что там используется пустой интерфейс можно увидеть по доке `go doc fmt Println`

==Если вы объявите функцию, которая получает параметр с типом пустого интерфейса, то в ее аргументах  могут передаваться значения любого типа==.

```go
func AcceptAnything(thing interface{}) {
}
AcceptAnything("A string")
AcceptAnything(bool)
```

Если имеется значение с типом пустого интерфейса, сделать с ним можно не так уж много. Не пытайтесь вызывать какие-либо методы для значения с типом пустого интерфейса⚠️ Помните: если у вас имеется значение с типом интерфейса, то вызвать для него можно только те методы, которые входят в этот интерфейс. А пустой интерфейс не содержит никаких методов. Это означает, что у значения с типом пустого интерфейса нет методов, которые вы могли бы вызвать, соответсвенно для вызова методов нужно использовать утверждение методов, а .то не всегда удобно

```go
func AcceptAnything(thing interface{}) {
	fmt.Println(thing)
	whistle, ok := thing.(Whistle)
	if ok {
		whistle.MakeSound()
	}
}
```
---
#### 🔹 Создать свой интерфейс

Создаем файл `myInterface.go` примерно с таким содержанием, который поможет работать с геометрическими фигурами

```go
package myInterface

type Shape interface {
	Area() float64
	Perimeter() float64
}
```

**затем в программе используем**
```go
package main

import (
	"fmt"
	"math"
	"github.com/romaxa83/study/interface_test/myInterface"
)

type square struct {
	X float64
}

type circle struct {
	R float64
}

// реализуем методы интерфейса для square
func (s square) Area() float64 {
	return s.X * s.X
}

func (s square) Perimeter() float64 {
	return 4 * s.X
}
// реализуем методы интерфейса для circle
func (s circle) Area() float64 {
	return s.R * s.R * math.Pi
}

func (s circle) Perimeter() float64 {
	return 2 * s.R * math.Pi
}

// метод принимает интерфейс
func Calculate(x myInterface.Shape) {
	_, ok := x.(circle)
	if ok {
		fmt.Println("Is a circle!")
	}

	v, ok := x.(square)
	if ok {
		fmt.Println("Is a square:", v)
	}

	fmt.Println(x.Area())
	fmt.Println(x.Perimeter())

}

// метод помогает различать типы передаваемых функции 
// параметров x . Вся магия происходит благодаря оператору x.(type) 
// который возвращает тип элемента x
func tellInterface(x interface{}) {
	switch v := x.(type) {
	case square:
		fmt.Println("This is a square!")
	case circle:
		fmt.Printf("%v is a circle!\n", v)
	default:
		fmt.Printf("Unknown type %T!\n", v)
	}
}

func main() {
	x := square{X: 10}
	fmt.Println("Perimeter:", x.Perimeter())
	Calculate(x)
	tellInterface(x)
	y := circle{R: 5}
	Calculate(y)
	tellInterface(y)
}
```
---



