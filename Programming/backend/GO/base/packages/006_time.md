#go #package

#### 🔹 Измерение времени программы или функции

```go
start := time.Now()

// some logic
time.Sleep(time.Second)

duration := time.Since(start)
fmt.Println("It took time.Sleep(1)", duration, "to finish.")
```
---
#### 🔹 Текущее время

```go
var now time.Time = time.Now()

// текущий год
var year int = now.Year()

// получение timestamp
var timestamp int = now.Unix()
```
---
#### 🔹 Преобразование

```go
// timestamp преобразует в нормальный формат
time.Unix(1257894000, 1)
```
---
#### 🔹 `Sleep()` 

```go
// Остановка команда на одну секунду
time.Sleep(time.Second)

time.Second // интервал времени равный секунде
time.Nanosecond
time.Microsecond
time.Millisecond
time.Minute
time.Hour
```
---
#### 🔹 `Sub()`

Функция позволяет вычислить разность между  двумя значениями времени
```go
t1 := time.Now()
time.Sleep(time.Second * 4)
t2 := time.Now()
fmt.Println("Time difference:", t2.Sub(t1))
```
---
#### 🔹 `Format()`

Определеное форматирование времени
```go
formatT := time.Now().Format("01 January 2006")
```
---
#### 🔹 Работа с локалями

```go
formatT := t.Format("01 January 2006")
fmt.Println(formatT)
loc, _ := time.LoadLocation("Europe/Paris")
londonTime := t.In(loc)
fmt.Println("Paris:", londonTime)
```
---
#### 🔹 `Parse()`

Функция, используемая для синтаксического анализа строк, содержащих значения времени и даты, называется time.Parse() и принимает два параметра. Первый из них определяет ожидаемый формат анализируемой строки, а второй содержит саму строку, которая должна быть проанализирована. Первый параметр состоит из элементов, принадлежащих списку констант Go, связанных с анализом даты и времени.

Список констант, которые можно использовать для создания формата синтаксического анализа, находится по адресу -  https://golang.org/src/time/format.go

---