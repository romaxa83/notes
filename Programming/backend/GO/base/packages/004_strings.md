#go #package

> перечень всех функций - https://golang.org/pkg/strings/ .

#### 🔹 `Title()`

Принимает строку и преобразовывает каждое первое слово в вверхний регистр
```go
// Hello World
strings.Title("hello world")
```
---
#### 🔹 `Contains()`

Проверяет наличие подстроки
```go
// true
strings.Contains("test", "es")
```
---
#### 🔹 `Count()`

Кол-во элементов в строке
```go
// 2
strings.Count("test", "t")
```
---
#### 🔹 `HasPrefix()` 

```go
// true
strings.HasPrefix("test", "te")
```
---
#### 🔹 `HasSuffix()`

```go
// true
strings.HasSuffix("test", "st")
```
---
#### 🔹 `Index()`

Индекс элемента
```go
// 1
strings.Index("test", "e")
```
---
#### 🔹 `ToLower()`

```go
// "test"
strings.ToLower("TEST")
```
---
#### 🔹 `ToUpper()`

```go
// "TEST"
strings.ToUpper("test")
```
---
#### 🔹 `Join()`

```go
// "a-b"
strings.Join([]string{"a","b"}, "-")
```
---
#### 🔹 `Split()`

Позволяет разбить заданную строку на части, ограниченные строкой-разделителем, возвращает строковый срез

```go
// []string{"a","b","c","d","e"}
strings.Split("a-b-c-d-e", "-"),
```
---
#### 🔹 `Repeat()`

```go
// == "aaaaa"
strings.Repeat("a", 5)
```
---
#### 🔹 `Replace()`

```go
// "bbaa"
strings.Replace("aaaa", "a", "b", 2)
```
---
#### 🔹 Replacer()

Ищет подстроку в строке и заменяет каждое вхождение этой подстроки в другой строке

```go
broken := "G# r#cks!"

// возвращает значение strings.Replacer, настроенное 
// для замены всех «#» в строке на «o».
replacer := strings.NewReplacer("#", "o")
fixed := replacer.Replace(broken)
fmt.Println(fixed)		// Go rocks
```
---
#### 🔹 `TrimSpace()`

Удаляет все символы-пропуски (символы новой строки, табуляции и обычные пробелы) в начале и в конце строки

---
#### 🔹 `Fields()`

Разбивает строку на основе пробельных символов
```go
str := "some my str"
fmt.Println(strings.Fields(str)) // [some my str]
```
---
#### 🔹 `EqualFold()`

Определить, являются ли две строки одинаковыми, несмотря на различия в регистре букв
```go
fmt.Println(strings.EqualFold("Mihalis", "MIHAlis"))	// true
fmt.Println(strings.EqualFold("Mihalis", "MIHAli"))		// false
```
---
#### 🔹 `Compare()`

Сравнивает две строки лексикографически возвращает `0` , если строки идентичны, и `-1` или `1` — если нет

```go
fmt.Println(strings.Compare("test", "test"))	// 0
fmt.Println(strings.Compare("test", "TEST"))	// 1
fmt.Println(strings.Compare("Test", "tEST"))	// -1
```
---