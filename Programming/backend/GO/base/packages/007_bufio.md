#go #package

> Буферизация потока чтения и записи

#### 🔹 Чтение ввода через терминал

```go
// буферизованое средство чтения, для получения 
// текста с клавиатуры
reader := bufio.NewReader(os.Stdin)
// вернет текст до нажатия enter
input, err := reader.ReadString('\n')
fmt.Println(input)
```

`ReadString` - принимает аргумент с символом, отмечающий конец ввода , возвращает байтовый срез

Ввод с клавиатуры всегда будет строкой, даже если ввод был числовой, и если нужно число то сначала очистить лишние символы, а затем конвертировать.
```go
input = strings.TrimSpace(input)
```
---
#### 🔹 `NewScanner`

`bufio.NewScanner()` - принимает 1 параметр - `os.Stdin` (стандартный поток ввода), возвращает `bufio.Scanner` у которой есть метод `Scan()` для построчного чтения из `os.Stdin`  

```go
// пример
var f *os.File
f = os.Stdin
defer f.Close()

scanner := bufio.NewScanner(f)
for scanner.Scan() {
	fmt.Println(">", scanner.Text())
}
```
---