#go #variable 

#### 🔹 Variable

- должна именоваться с буквы
- все объявленые переменные, должны быть использована
- ⚠️ Важно избегать замещения имен при объявлении переменных, чтоб ее имя не совпадало, с именами существующих функций , пакетов т.е. если объявить так переменную `var int int = 12`  то мы переопределим тип `int`, и дальнейшее обращение к типу `int` будет приводить к ошибке т.к. это уже перемена со значение 12 а не тип данных
- переменную нельзя объявить дважды
```go
// обьявляем "a" и "err"
a, err := strconv.ParseFloat("1.23", 64)
// обьявляем "b" и присваеваем "err"
b, err := strconv.ParseFloat("5.63", 64)
```


> для того чтоб переменная была экспортируемой  (могла быть использована в другом пакете) ее нужно писать с большой буквы, это относится как к переменным так и к функциям и типам

##### 🔸 Три способа объявить переменную

• с указанием типа 
	var first string
 	first = "This is first string"
 	
• без явного указание типа (будет указан автоматически)
	var second = "This is second string"
 
• без объявления переменной и указание типа (нужно указывать двоеточие перед присвоением)
	 third := "This is third string"


⚠️ короткое присваивание можно использовать только внутри функций (глобальные переменные только через var), и ее нельзя использовать уже с объявленой переменной

>если переменная объявлен, но ее не присвоено значение, то автоматом будет присвоено нулевое значение
```go
int,float64 = 0
string = ''
boolean = false
```

##### 🔸 Объявление сразу нескольких переменных
```go
var (
    a = 5
    b = 10
    c = 15
)

// or

var length,widht float
length,widht = 1.2, 2.2
```

##### 🔸 Ошибка при объявлении
```go
// выдаст ошибку
qty = 6
// т.к. нужно сначало обьявить переменую, и потом присвоить ей значение
// или использовать консрукцию
qty := 6
// которая являеться короткой формой обьевления и присвоения
```
------
#### 🔹 Константы

> часто используются для глобальных переменных, чтобы все функции в пакете имели к ней доступ, которые нельзя менять чтобы была экспортируемая нужно писать в верхнем регистре

```go
const TriangleSides int = 3

// обьявление сразу нескольких переменых
const (
  StatusActive = "active"
  StatusBan = "ban"
)
```

##### 🔸 `Iota` для констант

Когда в коде появляются статусы, типы ошибок или этапы обработки, быстро появляется ручная нумерация. Потом кто то вставляет новое значение в середину и начинается путаница. `iota` решает это и делает список констант самоподдерживаемым.
`Iota` работает внутри блока `const` и ведет себя как счетчик. В начале каждого `const` блока он сбрасывается в `0`, потом увеличивается на `1` на каждой строке с константой.

Самый частый кейс это перечисления. Пишем первую константу с `iota`, дальше можно не повторять выражение, Go повторит его сам

```go
type Status int

const (
    StatusPending Status = iota
    StatusProcessing
    StatusDone
    StatusFailed
)
```

==Если нужно начать не с нуля, добавляем смещение==
```go
const (
    PriorityLow = iota + 1
    PriorityMed
    PriorityHigh
)
```

Еще полезный паттерн это флаги через битовый сдвиг. Тогда каждое значение это отдельный бит и их можно комбинировать через побитовое или
```go
type Perm uint8

const (
    PermRead Perm = 1 << iota
    PermWrite
    PermExec
)
```

---
#### 🔹 Преобразование переменных

математические операции можно совершать с переменными одного типа, т.е. нельзя умножить `int` на `float`, для этого нужно сначала преобразовать переменную к нужному типу
```go
var price int = 2   // int
float64(price)      // теперь переменая типа float

// cтрока преобразуется в сегмент байтов.
[]byte("Hello, web!")

// преобразование байтов в строку
var msg = string([]byte{72, 101, 108, 108, 111})

// преобразование числа в строку
var numCPU = fmt.Sprintf("%v", 11)

// преобразование строки в инт
var a = strconv.Atomi('33')
```

⚠️ нужно помнить, при изменении с `float` на `int`, вся дробная часть будет отброшена

---
#### 🔹 Пустые идентификаторы

 > Go очень строг для не используемых переменных, вы не можете скомпилировать код с любыми неиспользованными переменными. Некоторые функции возвращают несколько значений, и некоторые значения могут не использоваться. Итак, Go предоставляет «пустой идентификатор» для замены не используемых переменных, после чего код может быть выполнен. Пустой индентификатор применяет нижнее подчеркивание

```go
x, _ := evenOnly(10)
```
---
#### 🔹 Указатели

> Оператор `&`(амперсанд) используется в Go для получения адреса переменной (в памяти компьютера) адрес называется - указателем, т.к. указывает на область в памяти, в которой храниться переменная, адрес можно получить для переменой любого типа

```go
amount := 6

// выводит значение переменой
fmt.Println(amount)   // 6

// выводит адрес переменой
fmt.Println(&amount)  // 0x1040a124
```

> Тип указателя состоит из знака `*` и типа переменной, на которую ссылается указатель. В программе можно объявлять переменные, содержащие указатели.

```go
var myInt int
// обьявление переменой содержащий указатель на int
var myIntPointer *int
// указатель присваевается переменой
myIntPointer = &myInt
fmt.Println(myIntPointer)

// короткое обьявление переменной указателя
var myInt int
myIntPointer := &myInt
fmt.Println(myIntPointer)
```

 Чтобы получить значение переменной, на которую ссылается указатель, поставьте оператор `*` (называется - разыменованием указателя) прямо перед указателем в коде. 
```go
// Например, чтобы получить значение переменной по указателю myIntPointer, введите *myIntPointer

myInt := 4
myIntPointer := &myInt
// выводит указатель
fmt.Println(myIntPointer)  // 0x1040a124
// выводит значение, на которое ссылаеться указатель
fmt.Println(*myIntPointer)  // 4

// Оператор * может использоваться для обновления значения по указателю.

myInt := 4
fmt.Println(myInt)    // 4
myIntPointer := &myInt
*myIntPointer = 8
// Выводится значение переменной, на которую ссылается указатель
fmt.Println(*myIntPointer)    // 8
// Значение переменной выводится напрямую
fmt.Println(myInt)            // 8
```

> Указатели также можно возвращать из функций просто объявите возвращаемый тип функции как тип указателя

```go
// обьявляем , что функция возвращает указатель на float64
func createPointer() *float64 {
  var myFloat = 98.5
  return &myFloat

}

func main() {
  var myFloatPointer *float64 = createPointer()
  fmt.Println(*myFloatPointer)    // 98.5 
}
```

> чтоб лучше запомнить - "Амперсанд" `&` звучит похоже на "Адрес" (ну, и то слово и другое на "А" начинается, по крайней мере)))), поэтому `&x` читается как "адрес переменной X" звёздочка `*` ни на что не похоже на звучит, но может использоваться в двух случаях — перед типом (`var x *MyType` — означает "тип указателя на `MyType`") и перед переменной (`*x = y` — означает "значение по адресу")

Зачем нужны указатели:
- указатели позволяют обмениваться данными по ссылке, а не по значению, особенно между функциями Go
- указатели бывают чрезвычайно полезны, если вы хотите отличить нулевое значение от неприсвоенного
---
#### 🔹 Строки

> Строки - это байтовый срез, предназначенный только для чтения, который может содержать байты любого типа и иметь произвольную длину

```go
// Варианты использование строк
const sLiteral = "\x99\x42\x32\x55\x50\x35\x23\x50\x29\x9c"
fmt.Println(sLiteral)
fmt.Printf("x: %x\n", sLiteral)
fmt.Printf("sLiteral length: %d\n", len(sLiteral))
```
---
#### 🔹 Руны

> **Руны** — это значения типа `int32` , следовательно, тип Go, который используется для представления кодовых пунктов `Unicode`. Строку можно рассматривать как множество рун.

```go
// руна
const r1 = '€'

// выводит как int
fmt.Println("(int32) r1:", r1)
// выводит шестнадцатеричное значение
fmt.Printf("(HEX) r1: %x\n", r1)
// выводит как строку
fmt.Printf("(as a String) r1: %s\n", r1)
// выводит как символ
fmt.Printf("(as a character) r1: %c\n", r1)
```

 > байтовый срез представляет собой набор рун и что вывод байтового среза с помощью `fmt.Println()` может дать не те результаты, которых мы ожидали. Чтобы преобразовать руну в символ, нужно использовать в `fmt.Printf()` символ подстановки  `%c` . Чтобы вывести байтовый срез в виде строки, нужно использовать в `fmt.Printf()` символ подстановки `%s`
 
 ```go
fmt.Println("A string is a collection of runes:", []byte("Mihalis"))
aString := []byte("Mihalis")

for x, y := range aString {
	fmt.Println(x, y)
	fmt.Printf("Char: %c\n", aString[x])
}
fmt.Printf("%s\n", aString)
 ```

---


