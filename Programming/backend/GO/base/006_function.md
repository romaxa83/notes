#go

#### 🔹 Info

Имя должно быть названо в `camelCase / CamelCase`

функция может принимать ноль или более аргументов. Для двух или более последовательных аргументов одного и того же типа вы можете определить тип в конце последнего аргумента (например, `«string»` в примере).

Функция может возвращать ноль или более значений. Если возвращает более одного значения, вам необходимо заключить их в скобки (при указании).

```go
func myFunction(a, b string, c int) (string, string, int){
  x := b
  x := a
  z := c*c

  return x, y, z
}
```

> Значение каждого передоваемого аргумента копируется в соответствующую переменную-параметр, это называеться «передачей по значению», и изменения этого значения, меняет копию а не оригинал как в примере ниже.

```go
func main() {
  amount := 6
  double(amount)
  fmt.Println(amount)   // 6
}
func double(number int) {
  number *= 2
  fmt.Println(number)   // 12
}
```

Чтоб значение изменилось после функции нужно передавать с указателем (подробнее в разделе про указатели)

```go
func main() {
  amount := 6
  double(&amount)
  fmt.Println(amount)   // 12
}
func double(number *int) {
  *number *= 2
}
```

Вы также можете объявить имена возвращаемых переменных. Таким образом, вам не нужно писать их после ключевого слова `return`

```go
func threeTimes(msg string) (tMsg string) {
  tMsg = msg + ", " + msg + ", " + msg
  return
}
```

Вы можете объявить функцию внутри другой функции и выполнить сразу после ее объявления. Такая функция называется анонимной и может получить доступ к области действия своего родителя. В этом случае анонимная функция может получить доступ к переменной `b`, которая находится в области видимости основной функции.

```go
func main() {
  a := 1
  b := 1
  c := func(x int) int {
      b *= 2
      return x * 2
  }(a)

  fmt.Println(a, b, c)
}
```
---
#### 🔹 Возвращение ошибки

```go
func f() (int, error) {
    
    if err {
      // тут возвращаем нулевое значение и ошибку
      return 0, fmt.Errorf("some error")
    }
    // тут возвращаем значение, а ошибку как nil т.к. ее нет  
    return 5, nil
}
```
---
#### 🔹 Spread

> указывается в конце, `param2` - будет сегментом, если не передать параметра будет пустой сегмент

```go
func myFunc(param1 int, param2 ...string) {

}
```
---
#### 🔹 Defer

«Отложенное» утверждение - это специальное утверждение в `Go`. Вы можете вызывать любые функции после оператора `defer`. Функции будут храниться в стеке и будут вызываться после возврата функции вызывающей стороны. Как видно из примера, существует обратный порядок вызовов функций.

Ключевое слово `«defer»` гарантирует, что вызов функции будет выполнен даже в том случае, если вызывающая функция завершится преждевременно. используется только перед функциями и методами, если нужно использовать перед циклом или чем-то другим, это нужно вынести в функцию.

```go
func main() {
 f()
}

func f() (int, error) {
 defer fmt.Println("DEFER 1")
 defer fmt.Println("DEFER 2")
 defer fmt.Println("DEFER 3")
 fmt.Println("BODY")
 return fmt.Println("RETURN")
}

// >>> OUTPUT <<<
// BODY
// RETURN
// DEFER 3
// DEFER 2
// DEFER 1
```
---
#### 🔹 Функция первого класса

В языках программирования с функциями первого класса программа может присвоить функцию переменной, а затем вызвать ее через переменную.

```go
// обычная функция
func sayHi() {
  fmt.Println("Hi")
}

func main() {
  // Объявляется переменная с типом func(), в ней может храниться функция
  var myFunction func()
  // Переменной присваивается функция sayHi
  myFunction = sayHi
  // Вызов функции, хранящейся в переменной.
  myFunction()
}
```

> языки программирования с функциями первого класса также позволяют передавать функции в аргументах других функций

```go
func twice(theFunction func()) {
  theFunction()
  theFunction()
}

func main() {
  twice(sayHi)
  twice(sayHi)
}
```
---
#### 🔹 Функция как тип

Параметры и возвращаемое значение функции являются частью ее типа. Для переменной, в которой хранится функция, должны быть указаны параметры и возвращаемые значения этой функции. В переменной могут храниться только функции, у которых количество и тип параметров сответствуют заданному типу

```go
func sayHi() {
  fmt.Println("Hi")
}

func divide(a int, b int) float64 {
  return float64(a) / float64(b)
}

func main () {
  var greeterFunction func()
  var mathFunction func(int, int) float64
  greeterFunction = sayHi
  mathFunction = divide
  greeterFunction()
  fmt.Println(mathFunction(5, 2))

  // но если присвоить так, это выдаст ошибку
  greeterFunction = divide
  mathFunction = sayHi
}
```

> это же справедливо и для функций, которые передаются в параметрах

```go
func doMath(passedFunction func(int, int)
  result := passedFunction(10, 2)
  fmt.Println(result)
}


func multiply(a int, b int) float64 {
  return float64(a * b)
}

func main() {
  // здесь норм
  doMath(multiply)
  // здесь ошибка
  doMath(sayHi)
}
```
---