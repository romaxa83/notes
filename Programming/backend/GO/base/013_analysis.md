#go 

#### 🔹 LINTER

- `gofmt` - утилита осуществляет проверку кода на соответствие стилю и автоматически его исправляет

- `go vet` - производит проверки списка типичных ошибок, таких как не используемые присваения, ошибки при работе с инструментами синхронизации, некорректный build теги ...

- `staticcheck` - улучшеный вариант `go vet`

- `golint` - производит проверки на основе документов, таких как _Code Review Comments_

- `errcheck` - следит за тем, что б мы не игнорировали ошибки, возвращаемыми функциями

- `goconst` - поиск литералов и чисел, которые встречаются несколько раз, чтоб вынести их в константу

- `fieldalignment` - помогает оптимизировать потребление памяти с помощью порядка определения полей в структуре

> Пакет https://github.com/golangci/golangci-lint обьединяет в себе многие линтеры

> по ссылке находиться bash скрипт, для установки `golangci-lint` 
   https://raw.githubusercontent.com/golangci/golangci-lint/master/install.sh

```bash
# команда для makefile
install:
	@curl -sfL https://raw.githubusercontent.com/golangci/golangci-lint/master/install.sh | sh -s -- -b $(GOPATH)/bin
# проверка всех файлов 
lint:
	@golangci-lint run ./... -v

lint_autofix:
	@GO111MODULE=on $(GOLINT) run ./... -v --fix

# команда выведет все линтеры
golangci-lint linters
```
---
#### 🔹 ПРОФИЛИРОВАНИЕ

==Профилирования== позволяет измерять производительность наших функций и сравнивать разные их реализации, с помощью инструментов профилирования мы можем понять, какие части кода влияют на производительность процессорного времен и на потребляемую память

==ПРИМЕР (используем функцию подсчета чисел фибоначи, для демонстрации возможностей интсрументов профелировщика)==
```go
func fib(n uint) uint {
	if n == 0 {
		return 0
	} else if n == 1 {
		return 1
	} else {
		return fib(n-1) + fib(n-2)
	}
}

func main() {
	// сдесь создается файл для сбора статистики
	fl, err := os.Create("./cpu.pprof")
	if err != nil {
		log.Fatal(err)
	}
	defer fl.Close()
	
	pprof.StartCPUProfile(fl)
	defer pprof.StopCPUProfile()

	n := fib(30)

	fmt.Println(n)
}
```

```bash
# запускаем с подсчетом времени выпролнения команды
time go run main.go

# запускаем инструменты для иследования статистики от профелировщика
go tool pprof cpu.pprof

# после чего мы перейдем в окружение pprof, у которого есть свои команды

# выведет список всех команд
help

# выводит список функций отсортированых по времени потребления процессора
top

# просмотр конкретный листинг функций, где можно понять какие участки выполняются долго
list main.fib

# запускаем веб страницу со статистикой от профелировщика (http://127.0.0.1:8080)
go tool pprof -http=:8080 cpu.pprof
```
---
#### 🔹 ТРАСИРОВЩИК

Трассировщик показывает нам стек вызовов наших функций и время их отработки. В отличие от профилировщика, который, как бы говорит, с некоторой периодичностью, нашей программе: «А скажи мне своё состояние?», – и фиксирует это состояние в статистике. У трассировщика немножко другая схема работы – он изначально говорит нашей программе: «При каких-либо изменениях сообщи мне, что поменялось». В таком случае у трассировщика получается более детальная информация о состоянии нашей системы.

```go
func main() {

	fl, err := os.Create("./trace.out")
	if err != nil {
		log.Fatal(err)
	}
	defer fl.Close()

	trace.Start(fl)
	defer trace.Stop()

	n := fib(40)

	fmt.Println(n)
}
```

```bash
go tool trace trace.out
```
---