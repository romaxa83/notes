#### 🔹 Создание индекса

```
PUT /product?pretty
```
?pretty - указывает чтоб вывод был человеко-понятным

#### 🔹 Получить документ

```
GET /product/default/1

# Чтоб получить только данные (без служебной информации)
GET /product/default/1/_source?pretty

# Чтоб получить определеное поле
GET /product/default/1?_source=name&pretty

# Кол-во документов в ответе
GET /blog/post/_count
{"query": .... }

# выведет все индексы
GET /_cat/indices?

# выведет информацию о диске, и где находятся данные
GET /_cat/allocation?v

# выведет настройки elastic
GET /_nodes

# выведет информацию о работоспособность кластера
GET /_cluster/health

Элементы ответа:
	- status - (red,green,yellow)
		- red - не все шарды доступны для поиска(ответ на запрос будет не полным)
		- yellow - все шарды доступны,но реплики не все доступны(ответ будет    полным но если узел будет потеря данных)
		- green - все отлично шарды и реплики распределены верно
```
---
#### 🔹 Удалить документ (по id)

```
# по id
DELETE /product/default/1

# Удалить документ по другим параметрам нужно построить запрос
POST /product/_delete_by_query
{
	"query": {
		"match": {
			"FIELD": "TEXT" //к примеру "user": "Roman"
		}
	}
}	
```
---