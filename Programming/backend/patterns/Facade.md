**Facade** (Фасад) — это структурный паттерн проектирования, который предоставляет унифицированный интерфейс (или точку доступа) к сложной подсистеме. Основная идея состоит в том, чтобы упростить взаимодействие с системами, состоящими из множества компонентов.
По сути, это "упаковка" сложной системы или группы классов за одним простым интерфейсом, который скрывает от клиента внутренние детали реализации. Удобная аналогия: это как универсальный менеджер в крупном магазине, к которому можно обратиться за любым вопросом. Менеджер общается с разными отделами (склад, бухгалтерия, служба доставки и т.д.), а вам не нужно разбираться, кто за что отвечает.

---
> [В laravel реализация фасада немного отличается](/backend/patterns/laravel/Отличие_паттерн_Facade_от_фасадов_Laravel.md)

#### Плюсы паттерна Facade
* 🔹 **Упрощение интерфейса** — сложная подсистема скрывается за простым интерфейсом.
* 🔹 **Снижение связности** — клиенту не нужно напрямую зависеть от множества компонентов системы.
* 🔹 **Улучшение читаемости кода** — использование фасада улучшает читаемость и понимание кода.
* 🔹 **Инкапсуляция сложностей** — детали реализации остаются скрытыми от остальной системы.
* 🔹 **Поддержка системы** — если в подсистеме произойдут изменения, то это затронет только фасад.

#### Минусы паттерна Facade
* 🔸 **Ограниченная гибкость** — прямой доступ к компонентам подсистемы затруднён, если взаимодействие пройдет только через фасад.
* 🔸 **Риск чрезмерного упрощения** — если чрезмерно упростить интерфейс, могут быть потеряны важные детали.
* 🔸 **Поддержка фасада** — если подсистемы очень динамичны, фасад может стать сложным и утратить своё назначение из-за частых изменений.

#### Когда использовать
* 🔹Когда вы хотите **скрыть сложность подсистемы**.
    - Например, клиенту нужен простой доступ к системе, но он не должен взаимодействовать с её внутренними компонентами. 
* 🔹Когда у вас есть сложная система, состоящая из множества методов и компонентов, которые вызываются в определённых комбинациях.
    - Фасад поможет агрегировать эти вызовы внутри себя. 
* 🔹Когда нужно **разделить слои приложения**.
    - Например, у вас есть бизнес-логика, которая сильно взаимодействует с низкоуровневыми API. Фасад поможет разделить их.

#### Реальные примеры использования

**1. Подсистемы в приложении**:
- Веб-приложение может иметь фасад для работы с подсистемами аутентификации, платежей, логирования или анализа данных.
- Пример: при добавлении нового пользователя в систему фасад взаимодействует с:
    - базой данных,
    - уведомлениями (на email или телефон),
    - подсистемой аналитики.

Пример кода:

``` php
class UserRegistrationFacade
{
    protected $mailer;
    protected $database;
    protected $analytics;

    public function __construct(Mailer $mailer, Database $database, Analytics $analytics)
    {
        $this->mailer = $mailer;
        $this->database = $database;
        $this->analytics = $analytics;
    }

    public function registerUser($userData)
    {
        $this->database->saveUser($userData);
        $this->mailer->sendWelcomeEmail($userData['email']);
        $this->analytics->trackNewUser($userData['id']);
    }
}
```

Теперь вместо вызова всех этих компонентов напрямую, клиент вызывает лишь один метод `registerUser($userData)`.

**2. В библиотеке Guzzle**
- Фасады используются для упрощения интерфейса работы с HTTP-запросами.
Пример:

``` php
use GuzzleHttp\Client;

class SimpleHttpClient
{
    public function get($url)
    {
        $client = new Client();
        return $client->request('GET', $url);
    }
}
```

#### Краткое резюме
- **Плюсы**: скрывает сложность системы, упрощает взаимодействие и делает код чище.
- **Минусы**: чрезмерное упрощение может сокрыть возможности подсистемы.
- **Использование**: когда есть сложные подсистемы, а клиенту нужен простой интерфейс.
