#pattern #architecture #php  #singleton
**Singleton** – это порождающий паттерн проектирования, который гарантирует, что у класса есть только один экземпляр, и предоставляет глобальную точку доступа к этому экземпляру. Singleton удобен, когда нужен единый экземпляр объекта, и это оправдано архитектурными ограничениями. Однако во многих случаях его можно заменить Dependency Injection или использовать DI-контейнеры, что улучшает тестируемость и модульность системы. Всегда оценивайте влияние на архитектуру перед его использованием! ✨

---

#### Ключевые особенности Singleton
* 🔹 **Единый экземпляр**: У класса будет создан только один экземпляр для всей программы.
* 🔹 **Глобальная точка доступа**: Экземпляр класса хранится внутри самого класса и доступен через статический метод.
* 🔹 **Ленивое создание**: Объект создается тогда, когда он впервые требуется.

#### Когда использовать Singleton
* 🔹 **Управление глобальными ресурсами:**
    - Логирование (логгер).
    - Конфигурации приложения (например, Config Manager).
    - Подключения к базе данных (DB Connection).
* 🔹 **Обеспечение уникальности:** Когда объект должен быть строго единым для правильного функционирования приложения. Например:
    - Работа с общим кэшем.
    - Управление пулом подключений (Connection Pool).
* 🔹**Контейнер зависимости:** Хранение сервисов (например, в Laravel контейнерах).

#### Преимущества Singleton
* 🔸 **Гарантия уникальности**: У объекта есть только один экземпляр, что предотвращает проблему дублирования.
* 🔸 **Ленивое создание**: Экземпляр создаётся только при необходимости, что может улучшить производительность.
* 🔸 **Глобальная точка доступа**: Удобно обращаться к Singleton из любой точки приложения.
* 🔸 **Контроль над состоянием**: Центральное управление состоянием единственного объекта.

#### Недостатки Singleton
* 🔸 **Скрытые зависимости**: Использование глобального объекта нарушает принцип явных зависимостей, что создаёт трудности при тестировании.
* 🔸 **Может нарушать SRP (Single Responsibility Principle):** Объект часто совмещает обязанности создания и управления своим состоянием.
* 🔸 **Трудности с тестированием**: Автотестирование становится сложнее, потому что Singleton сохраняет своё состояние между тестовыми случаями и его трудно "замокать".
* 🔸 **Проблемы с многопоточностью**: Невнимательное использование может вести к проблемам с доступом из нескольких потоков.
* 🔸 **Антипаттерн в современном программировании**: Многие архитектуры (в том числе DI-контейнеры) предлагают замену Singletons, так как они чрезмерно связывают код.

#### ❗️ Когда избегать Singleton
* 🔹 **Современные альтернативы:** В современных фреймворках (например, Laravel) используется контейнер зависимостей для предоставления единственного экземпляра. Это предпочтительнее, чем прямой доступ через Singleton.
* 🔹 **Проблемы с тестированием:** Если объект становится глобальным, может быть сложно изменить его поведение в тестах.
* 🔹 **Сложная зависимость:** Если вам нужно расширить функционал через наследование, Singleton затруднит это. Лучше использовать Dependency Injection.

#### Реализация Singleton на PHP

*Вот пример простой реализации Singleton.*
``` php
<?php

class Singleton
{
    // Хранит единственный экземпляр класса
    private static ?Singleton $instance = null;

    // Закрытый конструктор запрещает создание экземпляра извне
    private function __construct()
    {
        // Здесь можно инициализировать ресурсы
    }

    // Метод для получения единственного экземпляра класса
    public static function getInstance(): Singleton
    {
        if (self::$instance === null) {
            self::$instance = new self();
        }
        return self::$instance;
    }

    // Клонирование запрещено
    private function __clone()
    {
    }

    // Сериализация запрещена
    private function __wakeup()
    {
    }

    // Пример метода Singleton
    public function doSomething()
    {
        return "I am the only instance!";
    }
}

// Тестирование Singleton
$first = Singleton::getInstance();
$second = Singleton::getInstance();

// Проверка, что оба объекта одинаковы
var_dump($first === $second); // true
```

**Реальные кейсы использования Singleton**

*Система логирования (ILogger или Log Manager):
Логирование в приложении часто должно быть централизованным. Используя Singleton, можно обеспечить, что только один объект логгера будет писать в файл или систему.*
``` php
   class Logger
   {
       private static ?Logger $instance = null;

       private function __construct()
       {
       }

       public static function getInstance(): Logger
       {
           if (self::$instance === null) {
               self::$instance = new self();
           }
           return self::$instance;
       }

       public function log($message)
       {
           // Логирование
           echo "[LOG]: " . $message . "\n";
       }
   }

   Logger::getInstance()->log("Это первая запись в лог.");
   Logger::getInstance()->log("Еще одна запись в лог.");
```

 *Настройки приложения (Configuration Manager):** Например, класс для работы с конфигурационными параметрами приложения, загружающимися из `.env` или базы данных.*
``` php
   class Config
   {
       private static ?Config $instance = null;
       private array $settings;

       private function __construct()
       {
           // Загружаем настройки из файла
           $this->settings = [
               'db_host' => 'localhost',
               'db_user' => 'root',
               'db_password' => 'secret',
           ];
       }

       public static function getInstance(): Config
       {
           if (self::$instance === null) {
               self::$instance = new self();
           }
           return self::$instance;
       }

       public function get(string $key)
       {
           return $this->settings[$key] ?? null;
       }
   }

   echo Config::getInstance()->get('db_host'); // localhost
```

*Соединение с базой данных (Database Connection Pool): В приложении может существовать только одно соединение с базой данных.*
``` php
   class DatabaseConnection
   {
       private static ?DatabaseConnection $instance = null;
       private \PDO $connection;

       private function __construct()
       {
           $this->connection = new \PDO('mysql:host=localhost;dbname=test', 'username', 'password');
       }

       public static function getInstance(): DatabaseConnection
       {
           if (self::$instance === null) {
               self::$instance = new self();
           }
           return self::$instance;
       }

       public function getConnection(): \PDO
       {
           return $this->connection;
       }
   }

   // Использование
   $db = DatabaseConnection::getInstance()->getConnection();
```

**Кэширование:**
Если приложение использует общий объект для работы с кэш-системами, Singleton станет идеальным решением для обеспечения единственного экземпляра.