#pattern #architecture #php
**Фабричный метод** (Factory Method) — это порождающий паттерн проектирования, который позволяет делегировать создание объектов подклассам. Вместо того чтобы создавать объекты напрямую через конструктор (используя `new`), фабричный метод предоставляет интерфейс для создания объекта, оставляя выбор типа создаваемого объекта за подклассами. Данный паттерн удобен для уменьшения зависимости кода от конкретных классов. Однако его применение требует осознанного подхода, чтобы не переусложнить архитектуру программы. Используйте этот паттерн там, где необходимо повысить гибкость и масштабируемость системы.

---

#### **Ключевые особенности**
* 🔹 **Инкапсуляция создания объектов**: Код, который использует объект, не знает и не зависит от конкретного класса объекта.
* 🔹 **Использование наследования**: Базовый класс определяет интерфейс для создания объекта, а подклассы реализуют этот метод для определения точного типа создаваемого объекта.
* 🔹 **Полиморфизм**: Позволяет использовать полиморфизм в процессе создания объектов, что упрощает поддержку кода и расширяемость.

#### **Когда использовать**
* 🔹 Когда заранее неизвестно, какой конкретный объект нужно создавать (например, тип определяется на основании параметров или конфигурации).
* 🔹 При работе с большим количеством подклассов, создание объектов которых должно быть централизовано.
* 🔹 Если необходимо избежать жесткой привязки к конкретным классам.
* 🔹 В случаях, когда основной класс хочет делегировать ответственность за создание объектов своим дочерним классам.

#### **Преимущества**
* 🔹 **Инкапсуляция логики создания объектов.**
* 🔹 **Улучшенная масштабируемость**: Легко добавлять новые типы объектов без изменения существующего кода.
* 🔹 **Снижение зависимости от конкретных классов.**
* 🔹 Повышение гибкости и тестируемости кода.

#### **Недостатки**
* 🔹 **Усложнение структуры программы**: Требуется большее количество классов.
* 🔹 Может повысить сложность конфигурации и сопровождения, особенно если паттерн используется неправильно.
* 🔹 Если требуется частое создание различных объектов, то фабричный метод может увеличить количество кода и снизить читаемость.

#### ❗️ **Когда избегать**
* 🔹 Если структура программы небольшая, и добавление интерфейса для создания объектов избыточно.
* 🔹 Если классы, которые необходимо создавать, остаются неизменными и их не более одного или двух.
* 🔹 Если создание объектов является очень простым процессом, а переусложнение кода из-за использования паттерна не оправдано.

#### **Реализация на PHP**

*Пример реализации:*
```php
<?php

// Базовый продукт
interface Product 
{
    public function getDescription(): string;
}

// Конкретный продукт 1
class ConcreteProductA implements Product
{
    public function getDescription(): string
    {
        return "Это продукт A.";
    }
}

// Конкретный продукт 2
class ConcreteProductB implements Product
{
    public function getDescription(): string
    {
        return "Это продукт B.";
    }
}

// Фабричный метод
abstract class Creator
{
    // Фабричный метод
    abstract public function factoryMethod(): Product;

    // Общая логика
    public function someOperation(): string
    {
        $product = $this->factoryMethod();
        return "Создан объект: " . $product->getDescription();
    }
}

// Конкретный создатель 1
class ConcreteCreatorA extends Creator
{
    public function factoryMethod(): Product
    {
        return new ConcreteProductA();
    }
}

// Конкретный создатель 2
class ConcreteCreatorB extends Creator
{
    public function factoryMethod(): Product
    {
        return new ConcreteProductB();
    }
}

// Использование
function clientCode(Creator $creator)
{
    echo $creator->someOperation() . PHP_EOL;
}

clientCode(new ConcreteCreatorA()); // Создан объект: Это продукт A.
clientCode(new ConcreteCreatorB()); // Создан объект: Это продукт B.
```

#### **Реальные кейсы использования**
* 🔸 **GUI-библиотеки**: При создании различных UI-компонентов (например, кнопки, окна, текстовые поля). На разных платформах (Windows, macOS и др.) фабричный метод может создавать разные элементы интерфейса.
* 🔸 **Система логирования**: Использование фабричных методов позволяет создавать разные типы логгирования (файловый логгер, логгер базы данных и т. д.).
* 🔸 **Работа с API**: Создание объектов запроса/ответа для разных типов данных в зависимости от входных параметров.
* 🔸 **ORM (Object-Relational Mapping)**: Например, создание разных сущностей/моделей для работы с различными таблицами базы данных.
* 🔸 **Игровая индустрия**: Создание различных типов врагов, уровней, героев.
