#pattern #architecture #php 
**Decorator** (Декоратор) — это структурный паттерн проектирования, который позволяет динамически добавлять объектам новую функциональность, оборачивая их в цепочку классов-декораторов.

---

#### **Основная идея Декоратора**
Вместо того, чтобы наследовать класс и расширять его функциональность, вы создаете новый класс, который "оборачивает" существующий объект. Это позволяет добавлять или изменять поведение объекта без изменения его исходного кода.

#### **Когда использовать Декоратор?**
1. **Необходима гибкость вместо наследования**.
Если создание подклассов для добавления функциональности приводит к огромному числу классов (традиционный подход через наследование).
2. **Не хочется изменять класс напрямую**.
Иногда вы не можете изменять класс (например, сторонняя библиотека), но хотите расширить его функционал.
3. **Динамическое добавление обязанностей**.
Декоратор позволяет "уровневое" добавление функционала — оборачивая объект в несколько "слоев".

#### **Преимущества паттерна Decorator**
- Новый функционал можно добавлять "на лету", без модификации существующего кода.
- Легко комбинировать поведение путем наложения нескольких декораторов.
- Сохраняет принцип открытости/закрытости (_OCP_, Open-Closed Principle).

#### **Недостатки паттерна Decorator**
- Увеличивает сложность кода. Наложение декораторов может быть трудно читаемым и отлаживаемым (когда много "слоёв").
- Декораторы могут нарушить принцип _SRP_ (Single Responsibility Principle): один декоратор может брать на себя слишком много обязанностей.

#### **Реализация Decorator на PHP**
##### **Простая реализация Декоратора**

``` php
<?php

// Базовый интерфейс (или класс)
interface Notifier
{
    public function send(string $message): void;
}

// Реализация основного объекта
class BasicNotifier implements Notifier
{
    public function send(string $message): void
    {
        echo "Sending notification: $message\n";
    }
}

// Базовый класс декоратора (оборачивает Notifier)
class NotifierDecorator implements Notifier
{
    protected Notifier $notifier;

    public function __construct(Notifier $notifier)
    {
        $this->notifier = $notifier;
    }

    public function send(string $message): void
    {
        $this->notifier->send($message);
    }
}

// Конкретные декораторы
class EmailNotifierDecorator extends NotifierDecorator
{
    public function send(string $message): void
    {
        parent::send($message);
        echo "Sending email notification: $message\n";
    }
}

class SMSNotifierDecorator extends NotifierDecorator
{
    public function send(string $message): void
    {
        parent::send($message);
        echo "Sending SMS notification: $message\n";
    }
}

// Использование
$basicNotifier = new BasicNotifier();
$emailNotifier = new EmailNotifierDecorator($basicNotifier);
$smsNotifier = new SMSNotifierDecorator($emailNotifier);

$smsNotifier->send("Hello World!");

// Вывод:
// Sending notification: Hello World!
// Sending email notification: Hello World!
// Sending SMS notification: Hello World!
```

#### **Разбор кода**
1. **Интерфейс `Notifier`**: Определен стандарт, которому следуют все уведомители.
2. **Класс `BasicNotifier`**: Базовая функциональность (отправка простого уведомления).
3. **Декораторы `EmailNotifierDecorator` и `SMSNotifierDecorator`**: Они добавляют дополнительную логику, оборачивая базовый объект `BasicNotifier`.
4. **Динамическое комбинирование**: Декораторы можно комбинировать и накладывать друг на друга (как в цепочке).

#### **Пример 2: Расширение функциональности в системе логирования**
Предположим, у нас есть система логирования, и мы хотим, чтобы базовый логгер мог писать:
- сообщения в файл;
- при необходимости — в консоль;
- опционально — в удаленный сервис.

``` php
<?php

interface Logger
{
    public function log(string $message): void;
}

// Базовая реализация логгера (пишет в текстовый файл)
class FileLogger implements Logger
{
    public function log(string $message): void
    {
        file_put_contents('log.txt', $message . PHP_EOL, FILE_APPEND);
    }
}

// Базовый декоратор
class LoggerDecorator implements Logger
{
    protected Logger $logger;

    public function __construct(Logger $logger)
    {
        $this->logger = $logger;
    }

    public function log(string $message): void
    {
        $this->logger->log($message);
    }
}

// Декоратор для логирования в консоль
class ConsoleLogger extends LoggerDecorator
{
    public function log(string $message): void
    {
        parent::log($message); // сохраняем базовый функционал
        echo "Console log: $message\n";
    }
}

// Декоратор для отправки логов в удаленный сервис
class RemoteLogger extends LoggerDecorator
{
    public function log(string $message): void
    {
        parent::log($message); // сохраняем базовый функционал
        // Имитация отправки в удаленное место
        echo "Sending log to remote server: $message\n";
    }
}

// Использование
$fileLogger = new FileLogger();
$consoleLogger = new ConsoleLogger($fileLogger);
$remoteLogger = new RemoteLogger($consoleLogger);

$remoteLogger->log("Error: Unable to connect!");
 
Console log: Error: Unable to connect!
Sending log to remote server: Error: Unable to connect!
// В файл log.txt также будет записана строка
Error: Unable to connect!
```

#### **Когда использовать Decorator в реальных проектах**
1. **Логирование**: Как в примере выше, вы можете гибко добавлять запись в консоль, удалённый сервер и текстовый файл.
2. **Отправка уведомлений**: Если вы хотите отправлять сообщения несколькими способами (email, SMS, push-уведомления). Каждый новый способ можно внедрить с помощью декоратора.
3. **Форматирование данных**: Например, декораторы для добавления форматирования текста (шрифты, размеры).
4. **Кеширование**: Декоратор может добавлять слой кеширования для доступа к данным без изменения базы или сервисов.

#### **Альтернатива для PHP: Middleware**
В Laravel используется middleware (промежуточное ПО), которое похоже на декораторы, поскольку позволяет "оборачивать" объекты/запросы дополнительной функциональностью.
**Пример использования middleware:**
- Аутентификация запросов.
- Логирование запросов.
- Изменение заголовков ответов.

Middleware работают по принципу цепочки обязанностей, что роднит их с декоратором.

#### **Плюсы и минусы Decorator в реальных проектах**
**Плюсы**:
* 🔹 Декораторы позволяют добавлять новую функциональность без изменения существующего кода.
* 🔹 Сохраняется гибкость — можно динамически включать/выключать слои.
* 🔹 Устраняет необходимость создавать множество подклассов для каждого сочетания функциональностей.

**Минусы**:
* 🔸 Чрезмерное использование может привести к запутанному коду.
* 🔸 Многоуровневая вложенность декораторов усложняет отладку.

---
#### 🔹 Info

 - https://refactoring.guru/ru/design-patterns/decorator

 > Декоратор — это структурный паттерн, который позволяет добавлять объектам новые поведения на лету, помещая их в объекты-обёртки. Декоратор позволяет оборачивать объекты бесчисленное количество раз благодаря тому, что и обёртки, и реальные оборачиваемые объекты имеют общий интерфейс.

==Применимость==:
-  Когда вам нужно добавлять обязанности объектам на лету, незаметно для кода, который их использует.
- Когда нельзя расширить обязанности объекта с помощью наследования

==Теория==
Паттерн имеет один интерфейс (Component), который реализуеться двумя классами ConcreteComponent и Decorator. Класс Decorator имеет экземпляр Component, который может быть экземпляром ConcreteComponent или Decorator. Это позволит размещать несколько декораторов друг над другом, что добавляет новые функциональные возможности к переопределенным методам. Последний тип класса, в шаблоне декоратора, это ConcreteDecorator. Это классы, которые декорируют Component, добавляя ему поведение.

##### 🔸 EXAMPLE

==Пример реализации декоратора при отправки sms==
```php
// создаем интерфейс для отправки sms
intetface SmsSender
{
	public function send($number, $text): void;
}

//класс с реальной логикой для отправки
class SmsRu implementa SmsSender
{
	public function send($number, $text)
	{
		// some logic
	}
}

// класс для логирования отправок
class LoggedSms implements SmsSender
{
	private $next;

	public function __construct(SmsSender $next) {
		$this->next = $next;
	}

	public function send($number, $text)
	{
		logger()->info($text);
		$this->next->send($number, $text);
	}
}

$sms = new LoggedSms(new SmsRu());

// класс LoggedSms являеться декоратором для SmsRu, который попадает в $next, LoggedSms не нарушает работу SmsRu, но при этом добавляет новый функционал (в примере логирование)
```

##### 🔸 EXAMPLE

==Пример на основе торта с ценой и ингридиентами==
```php
interface Cake
{
    public function ingredients(): string;
  
    public function price(): double;
}

// это абстрактный класс, который заставляет 
// каждый конкретный декоратор, расширяющий CakeDecorator, 
// реализовывать свои собственные версии ingredients и price. 
// Обратите внимание, что CakeDecorator получает экземпляр Cake, 
// который делает возможным создание цепочек
abstract class CakeDecorator implements Cake
{
    protected $cake;

    public function __construct(Cake $cake)
    {
        $this->cake = $cake;
    }
}

// Класс SimpleCake является примером класса Component, 
// который можно декорировать. Эта функциональность очень легко расширяема,
// все, что нужно, это создать новый компонент, который называется к примеру ChocolateCake
class SimpleCake implements Cake
{
    public function ingredients(): string
    {
        return 'Простой торт';
    }
    public function price(): double
    {
        return 12.5;
    }
}

// WithWhippedCream — это класс декоратора, 
// который расширяет CakeDecorator. 
// Этот класс использует родительское свойство $cake, 
// чтобы добавить поведение (декорировать) родителю.
class WithWhippedCream extends CakeDecorator
{
    public function ingredients(): string
    {
        return $this->cake->ingredients() + ' со взбитыми сливками';
    }
    public function price(): double
    {
        return $this->cake->price() + 2.5;
    }
}

// Класс WithSprinkles выглядит почти так же, 
// как класс WithWhippedCream. Разница между ними 
// заключается в том, что каждый из них украшает 
// своего родителя по-своему. Оба имеют разную цену и разные ингредиенты
class WithSprinkles extends CakeDecorator
{
    public function ingredients(): string
    {
        return $this->cake->ingredients() + ' с обсыпкой';
    }
    public function price(): double
    {
        return $this->cake->price() + 1.25;
    }
}

// использование
$cake = new SimpleCake();
$cake = new WithWhippedCream($cake);
$cake = new WithSprinkles($cake);
echo $cake->ingredients(); // Простой торт со взбитыми сливками с обсыпкой
echo $cake->price(); // 16.25

// или можно цепочкой
$cake = new WithSprinkles(new WithWhippedCream(new SimpleCake()));
echo $cake->ingredients(); // Простой торт со взбитыми сливками с обсыпкой
echo $cake->price(); // 16.25
```
---

