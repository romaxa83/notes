#pattern #php #architecture
Паттерн **Абстрактная фабрика** относится к группе порождающих паттернов проектирования. Он предоставляет интерфейс для создания семейств взаимосвязанных или зависимых объектов без указания их конкретных классов. Абстрактная фабрика устанавливает способ создания "неконкретных" объектов, обеспечивая большую гибкость при добавлении новых семейств продуктов. Полезен в масштабируемых системах, где требуется создавать семейства объектов с гарантией их совместимости. Его правильное использование значительно улучшает структуру, гибкость и тестируемость кода, однако избыточное использование паттерна может чрезмерно усложнить систему.

---

#### **Ключевые особенности**
* 🔸 **Создание семейств объектов:** Позволяет создавать связанные или зависимые объекты, которые работают совместно.
* 🔸 **Инкапсуляция:** Позволяет скрыть конкретные детали реализации объектов, предоставляя клиенту интерфейс для их создания.
* 🔸 **Принцип "Интерфейс вместо реализации":** Клиентский код оперирует интерфейсами, а не конкретными классами.
* 🔸 **Поддержка продуктовых семейств:** Легкая замена одного семейства объектов другим.

#### **Когда использовать**
* 🔸 Когда системе нужно создавать множество взаимосвязанных объектов (например, компоненты GUI для разных платформ: Windows, macOS).
* 🔸 Когда требуется изолировать процесс создания объектов от их использования.
* 🔸 Если для разных конфигураций или контекстов требуется создавать разные продукты из строго определенного семейства.
* 🔸 Когда нужно обеспечить совместимость между создаваемыми объектами.

#### **Преимущества**
* 🔸 **Высокая степень изоляции кода:** Клиент остается полностью изолирован от конкретных классов.
* 🔸 **Гарантированная совместимость продуктов:** Объекты, созданные одной фабрикой, принадлежат одному семейству и легко взаимодействуют друг с другом.
* 🔸 **Простота в изменении конкретной реализации:** Легко сменить фабрику для создания другого семейства продуктов.
* 🔸 Поддержка **открытости/закрытости (OCP):** Новый тип продуктов или фабрик можно добавить без изменения существующего кода.

#### **Недостатки**
* 🔸 **Усложнение кода:** Добавление новых семейств объектов может потребовать создания множества классов.
* 🔸 Сложность при добавлении нового типа объекта в существующее семейство. Чтобы поддержать новый тип, потребуется изменить интерфейсы всех фабрик и их реализаций.
* 🔸 Может быть избыточным для небольших проектов.

#### **Когда избегать**
* 🔸 Если количество семейств объектов ограничено и новые семейства добавляться не будут.
* 🔸 Если приложение достаточно простое и прямые вызовы конструкторов объектов не усложняют поддержку.
* 🔸 Когда не требуется создание строго совместимых объектов.

#### **Реализация на PHP**

*Пример реализации:*
```php
<?php

// Интерфейсы продуктов
interface Button
{
    public function render(): string;
}

interface Checkbox
{
    public function render(): string;
}

// Конкретные продукты для Windows
class WindowsButton implements Button
{
    public function render(): string
    {
        return "Кнопка Windows";
    }
}

class WindowsCheckbox implements Checkbox
{
    public function render(): string
    {
        return "Чекбокс Windows";
    }
}

// Конкретные продукты для macOS
class MacOSButton implements Button
{
    public function render(): string
    {
        return "Кнопка macOS";
    }
}

class MacOSCheckbox implements Checkbox
{
    public function render(): string
    {
        return "Чекбокс macOS";
    }
}

// Интерфейс фабрики
interface GUIFactory
{
    public function createButton(): Button;
    public function createCheckbox(): Checkbox;
}

// Конкретная фабрика для Windows
class WindowsFactory implements GUIFactory
{
    public function createButton(): Button
    {
        return new WindowsButton();
    }

    public function createCheckbox(): Checkbox
    {
        return new WindowsCheckbox();
    }
}

// Конкретная фабрика для macOS
class MacOSFactory implements GUIFactory
{
    public function createButton(): Button
    {
        return new MacOSButton();
    }

    public function createCheckbox(): Checkbox
    {
        return new MacOSCheckbox();
    }
}

// Клиентский код
function clientCode(GUIFactory $factory)
{
    $button = $factory->createButton();
    $checkbox = $factory->createCheckbox();

    echo $button->render() . PHP_EOL;
    echo $checkbox->render() . PHP_EOL;
}

// Использование
$os = 'Windows'; // Задание ОС — Windows или macOS

if ($os === 'Windows') {
    $factory = new WindowsFactory();
} else {
    $factory = new MacOSFactory();
}

clientCode($factory);
```

*Вывод:*
```
Кнопка Windows
Чекбокс Windows
```

#### **Реальные кейсы использования**
* 🔸 **GUI-библиотеки**: Абстрактная фабрика помогает создавать компоненты UI (кнопки, списки, чекбоксы) для разных платформ с единым интерфейсом.
* 🔸 **Системы для межплатформенной разработки:** Например, приложения, работающие и в macOS, и в Windows, могут содержать фабрики для создания элементов интерфейса, совместимых с каждой платформой.
* 🔸 **Подсистемы взаимодействия с базой данных**: Абстрактная фабрика может создавать разные реализации подключений или запросов для разных СУБД (MySQL, PostgreSQL, SQLite).
* 🔸 **Игровая индустрия:** Создание объектов для различных типов уровней, персонажей, предметов или оружия (например, разные "темы" уровней: космос, джунгли, подводный мир и т.д.).
* 🔸 **Тестирование зависимостей**:  Использование подмен объектов тестовыми реализациями (Mock, Stub) через фабрику.