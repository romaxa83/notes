#algoritm #python #recipes

Подробное обьснения - https://www.youtube.com/watch?v=GqieI_1yh40

Пример ниже показывает саму логику алгоритма, но реализовывать его на python нет смысла, можно использовать метод `sort()`, который будет работать быстрее. Данный алгоритм имеет смысл реализовывать для быстрых "яп". 
```python
a = [1, 4, 10, 11]
b = [2, 3, 3, 4, 8]
c = []

N = len(a)
M = len(b)

i = 0
j = 0

while i < N and j < M:
	if a[i] <= b[j]:
		c.append(a[i])
		i += 1
	else:
		c.appendp(b[j])
		j += 1

# здесь будут добавленны эл., которые не добавлены в цикле
c += a[i:] + b[j:]
# закоментированый код, анолагичен строчке выше
# if i < N:
#	for k in range(i, N):
#		c.append(a[k])
# elif j < M:
#	for k in range(j, M)
#		c.append(b[k])
		
pritn(c)
```