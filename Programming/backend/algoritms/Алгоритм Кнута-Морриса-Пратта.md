#algoritm

подробное объяснение - https://www.youtube.com/watch?v=S2I0covkyMc

**Алгоритм Кнута-Морриса-Пратта (КМП / KMP)** — это эффективный алгоритм поиска подстроки в строке, который позволяет избежать повторных сравнений символов, уже прошедших проверку.

В отличие от «наивного» поиска, который при несовпадении просто сдвигается на один символ, КМП использует информацию о самом паттерне (искомой строке), чтобы «прыгать» сразу на максимально возможное расстояние.

#### 🔹 Основная идея: Префикс-функция

Главный секрет алгоритма — предварительно вычисленная **префикс-функция** (часто обозначается как `π` или `P`)
Для каждой позиции в паттерне мы считаем длину **самого длинного собственного префикса, который одновременно является его суффиксом**.

в массиве  `π` хранятся максимальные длины суффиксов совпадающих с соответсвующими префиксами, первый элемент всегда принимает значения `0`.**Пример для строки `ABCDABD`:**
1. `A` -> 0
2. `AB` -> 0
3. `ABC` -> 0
4. `ABCD` -> 0
5. `ABCDA` -> 1 (префикс `A` совпадает с суффиксом `A`)
6. `ABCDAB` -> 2 (префикс `AB` совпадает с суффиксом `AB`)
7. `ABCDABD` -> 0

==Алгоритм формирования массива  `π`  в реализации на python==
```python
# есть искомая строка
t = "лилила"

# длина массива будет такой же как и длины искомой строки, первый элемент всегда 0
p = [0]*len(t)

# задаем два счетчика
j = 0
j = 1

while i < len(t):
	if t[j] == t[i]:
		p[i] = j+1
		i += 1
		j += 1
	else:
		if j == 0:
			p[i] = 0
			i += 1
		else:
			j = p[j-1] # это ключевой момент данного алгоритма
```

==Второй этап - уже поиск подстроки в строке==
```python
a = "лилилось лилилась"
m = len(t)
n = len(a)

i = 0
j = 0
while i < n:
	if a[i] == t [j]:
		i += 1
		j += 1
		if j == m:
			print("подстрока найдена")
			break
	else:
		if j > 0:
			j = p[j-1]
		else:
			i += 1

if i == n:
	print("подстрока не найдена")	
			
```

==полный вариант==
```python
# Алгоритм Кнута-Морриса-Пратта  
  
text = "лилилось лилилось"  
pattern = "лилила"  
  
def compute_prefix(pattern)->list:  
    p = [0] * len(pattern)  
    j = 0  
    i = 1  
  
    while i < len(pattern):  
        if pattern[j] == pattern[i]:  
            p[i] = j + 1  
            i += 1  
            j += 1  
        else:  
            if j == 0:  
                p[i] = 0  
                i += 1  
            else:  
                j = p[j - 1]  
  
    return p  
  
def kmp_search(text, pattern):  
    m = len(pattern)  
    n = len(text)  
    p = compute_prefix(pattern)  
  
    i = 0  
    j = 0  
    while i < n:  
        if text[i] == pattern[j]:  
            i += 1  
            j += 1  
            if j == m:  
                print("подстрока найдена")  
                break  
        else:  
            if j > 0:  
                j = p[j - 1]  
            else:  
                i += 1  
  
    if i == n:  
        print("подстрока не найдена")  
  
kmp_search(text, pattern)
```

#### 🔹 Сложность алгоритма 

- **Время:**  `𝑂(𝑛+𝑚)`, где 𝑛 — длина текста, 𝑚 — длина паттерна. Это значительно быстрее наивного алгоритма ( 𝑂(𝑛×𝑚) ) в худших случаях.
- **Память:** 𝑂(𝑚) для хранения таблицы префикс-функции.