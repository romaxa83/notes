#algoritm #python #recipes

подробное объяснение - https://www.youtube.com/watch?v=jMWvNTp_wFA

**Сортировка вставками (Insertion Sort)** — это алгоритм, который работает именно так, как большинство людей сортируют карты в руках.
Ты берешь по одному элементу из «неотсортированной» части и вставляешь его на нужное место в «отсортированную».

#### 🔹 Как это работает

- Считаем первый элемент отсортированным.
- Берем следующий элемент (это «ключ»).
- Сравниваем его с элементами слева (которые уже отсортированы).
-  Сдвигаем все элементы, которые больше «ключа», на одну позицию вправо.
-  Вставляем «ключ» в освободившееся место.
-  Повторяем, пока элементы не закончатся.

Данный алгоритм сопоставим , по вычислительной сложности с "сортировкой выбором", но имеет одно ключевое отличие, к пример если есть массив из 4 элементов уже отсортированных и в этот массив добавляется еще два элемента, то не нужно сортировать все элемента занова достаточно на нужно место поставить два новых элемента, т.е. данный метод позволяет нам досортировывать массив при появлении новых элементов, "сортировка выбором" таким свойством не обладает

```python
a = [-3, 5, 8, 0, -8, 1, 10]
N = len(a) # число элементов в списке

for i in range(1, N):
	for j in range(i, 0, -1):
		if a[j] < a[j-1]:
			a[j], a[j-1] = a[j], a[j-1]
		else:
			break

print(a)
```

#### 🔹 Характеристики 

- **Сложность:**
    - В худшем случае: 𝑂(𝑛2) (если массив отсортирован задом наперед).
    - **В лучшем случае: 𝑂(𝑛) **(если массив уже почти отсортирован).
- **Память:** 𝑂(1) — сортировка на месте.
- **Устойчивость:** Да (сохраняет порядок равных элементов). 

#### 🔹 Когда использовать? 

- На **маленьких наборах данных** (до 20–50 элементов) он часто быстрее даже «быстрой сортировки» (QuickSort) из-за простоты.
- Если данные **поступают в реальном времени** (по одному): ты сразу вставляешь новый элемент на место.
- Если массив **уже почти отсортирован** — это один из самых эффективных алгоритмов в мире. 

**Интересный факт:** Стандартная сортировка в Python (`sorted()` и `.sort()`) использует алгоритм **Timsort**, который внутри себя переключается на сортировку вставками, когда встречает короткие отрезки данных. 