#python #recipes #algoritm

Хеш-таблица — это структура данных, которая позволяет находить, добавлять и удалять элементы практически мгновенно. Если массив — это список, который нужно перебирать, то хеш-таблица — это **идеально организованный архив**.

В Python хеш-таблицы реализованы как **словари (`dict`)** и **множества (`set`)**. В JavaScript — как **`Object`** или **`Map`**.

#### 🔹 Как это работает

Представь, что у тебя тысячи книг, и тебе нужно найти книгу по названию.

- **Обычный поиск:** ты идешь по полкам и читаешь каждое название (это долго).
- **Хеш-таблица:** у тебя есть «волшебная формула» (Хеш-функция).
	- Ты даешь формуле название: _"Мастер и Маргарита"_.
	- Формула выдает число: _42_.
	- Ты идешь сразу к полке №42. Там лежит твоя книга.

#### 🔹 Основные понятия

- **Ключ (Key):** то, что мы передаем хеш-функции (например, ID пользователя или его Email).
- **Значение (Value):** данные, которые привязаны к ключу (объект пользователя, настройки и т.д.).
- **Хеш-функция:** алгоритм, который превращает любой входной объект (строку, число) в целое число (индекс в памяти).
- **Коллизия:** ситуация, когда два разных ключа выдали одинаковый индекс. Хорошие хеш-таблицы умеют их разруливать (например, создавая список внутри одной ячейки).

#### 🔹 Почему это круто?

В массиве, чтобы найти элемент, тебе в худшем случае нужно проверить  все элементы.
В хеш-таблице поиск занимает **константное время**. Неважно, 10 у тебя элементов или 10 миллионов — поиск будет одинаково быстрым.
#### 🔹 Примеры в разработке

-  Кэширование (Redis, Memcached):
	Хранение результатов тяжелых запросов. Ключ — текст запроса, Значение — результат из базы.
- **Уникальность (Sets):**  
	Если нужно быстро проверить, заходил ли пользователь на сайт сегодня. Мы просто кладем его ID в хеш-таблицу. Если он уже там есть — мы узнаем об этом мгновенно.
- **Подсчет частоты** 
==Посчитать, сколько раз каждое слово встречается в тексте==
```python
counts = {}
for word in text:
    counts[word] = counts.get(word, 0) + 1
```

#### 🔹 Минусы
-  **Память:** Хеш-таблицы «прожорливы». Чтобы они работали быстро и без коллизий, им нужно много свободного места в оперативной памяти.
-  **Нет порядка:** В классической хеш-таблице элементы лежат хаотично. Если тебе важен порядок (как в списке), придется использовать дополнительные структуры. _P.S. В новых версиях Python словари сохраняют порядок вставки, но это особенность реализации._

---
```
5d4{*jz;Z5Cxg<ZB&'C<
```
