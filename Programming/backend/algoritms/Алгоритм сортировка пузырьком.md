#algoritm #python #recipes

подробное объяснение - https://www.youtube.com/watch?v=5BuCMzKYagg

**Сортировка пузырьком (Bubble Sort)** — это самый простой и наглядный алгоритм сортировки. Свое название он получил потому, что ==при каждом проходе по списку самые «тяжелые» элементы (наибольшие) постепенно «всплывают» в конец, как пузырьки воздуха в воде==.

#### 🔹 Как это работает
-  Идем по списку слева направо.
-  Сравниваем два соседних элемента.
-  Если левый больше правого — **меняем их местами**.
-  Повторяем это для каждой пары до конца списка. После первого такого прохода самое большое число гарантированно окажется в самом конце.
-  Снова проходим по списку, но уже не заглядываем в последний элемент (он на своем месте).
-  Повторяем, пока список не станет отсортированным.

==пример на python==
```python
a = [7, 5, -8, 0, 10, 1]
N = len(a)

for i in range(N-1): # N-1 итераций работы массива
	for j in range(0, N-1-i): # проход по оставшимся не отсортированым парам массива
		if a[j] > a[j+1]:
			a[j], a[j+1] = a[j+1], a[j]

print(a)
```

#### 🔹 Характеристики 

- **Сложность:** 𝑂(𝑛2) в худшем и среднем случае. Это медленно.
- **Память:** 𝑂(1) (сортирует прямо в текущем массиве, дополнительное место не нужно).
- **Устойчивость:** Да (не меняет порядок равных элементов). 

#### 🔹 Когда использовать? 
Почти **никогда** в реальных проектах, так как есть `QuickSort` или `MergeSort`. Пузырек хорош только для обучения программированию и понимания того, как работают циклы и перестановки.