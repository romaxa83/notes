#php #architecture #snippet
**DTO (Data Transfer Object)** – это объект, предназначенный для переноса данных между слоями приложения, компонентами или системами. Основной задачей DTO является упрощение и оптимизация передачи данных. Он чаще всего используется для упаковки данных, которые считываются из одного источника (например, базы данных) и передаются, например, в другой слой системы, JSON API или клиентское приложение. DTO упрощает и структурирует передачу данных, делая архитектуру приложения более понятной и безопасной. Относится к **проектированию архитектуры** приложения и является частью слоёв, которые работают с данными. Обычно DTO связывают с такими подходами, как **многослойная архитектура** или архитектура с разделением ответственности (Separation of Concerns)


---

#### Ключевые особенности DTO:
* 🔹 **Простота структуры**: DTO содержит только свойства (поля) и возможные геттеры/сеттеры, без реализации бизнес-логики.
* 🔹 **Концентрация на данных**: DTO-фокусируется исключительно на передаваемой информации. Отсутствуют функции для обработки этих данных.
* 🔹 **Изоляция слоёв**: DTO позволяет изолировать доменные модели от конкретного слоя приложения (например, пользовательского интерфейса или API), предотвращая попадание ненужных подробностей реализации.
* 🔹 **Может использоваться как "контейнер"**: В одном DTO могут быть объединены сразу несколько объектов или составленные данные из разных систем.

#### Когда использовать DTO:
* 🔹 При необходимости обмена данными между:
    - Сервисами и компонентами в многослойной архитектуре.
    - Приложением и внешними API (входящие и исходящие запросы).
    - Базой данных и бизнес-логикой (особенно при сложной структуре ответа).

* 🔹 Чтобы избежать передачи "сырой" сущности:
    - Например, чтобы исключить внутренние данные моделей (пароли, секреты).
    - Когда доменные объекты содержат бизнес-логику, которая не должна "утекать" во внешний слой.

* 🔹 Когда нужно преобразовать данные в формат, понятный клиенту:
    - Пример: Преобразование `User` из базы данных в JSON, подходящий для API.

* 🔹 В случаях оптимизации:
    - DTO может ограничить количество передаваемых данных и улучшить производительность.

#### Преимущества DTO:
* 🔹 **Изоляция (Separation of Concerns)**: Разделение слоёв приложения и предотвращение утечки внутренней реализации в публичные интерфейсы.
* 🔹 **Обеспечение безопасности**: DTO может исключить конфиденциальные данные перед отправкой, например, в клиент или API.
* 🔹 **Согласованность данных**: DTO хорошо подходит для формирования стандартизированного формата вывода или API.
* 🔹 **Производительность**: DTO позволяет агрегировать только необходимые поля для передачи, исключая избыточные данные.
* 🔹 **Тестируемость**: DTO проще тестировать, так как он является простым структурным объектом без зависимости от бизнес-логики.

### Недостатки DTO:
* 🔸 **Добавление сложностей кода**: Необходимость создания дополнительных объектов, что может усложнить систему и увеличить количество кода.
* 🔸 **Дублирование данных**: Зачастую в DTO происходит копирование данных из доменных объектов, что может быть избыточным.
* 🔸 **Необходимость преобразований**: Требуется точно определить процесс маппинга данных для конвертации между сущностью и DTO.

### ❗️ Когда избегать использования DTO:
* 🔸 В простых проектах или монолитах: Если передача данных между слоями минимальна и неудобства от использования домена невелики.
* 🔸 В случае отсутствия сложной структуры ответа: Например, небольшие CRUD-приложения, где объекты сущностей напрямую передаются как ответы.
* 🔸 Если данные напрямую используются без дополнительных преобразований: Например, для внутреннего использования доменных объектов.

### Реализация на PHP:

🔹 *Пример DTO для передачи данных пользователя*
``` php
class UserDTO
{
    private int $id;
    private string $name;
    private string $email;

    public function __construct(int $id, string $name, string $email)
    {
        $this->id = $id;
        $this->name = $name;
        $this->email = $email;
    }

    public function getId(): int
    {
        return $this->id;
    }

    public function getName(): string
    {
        return $this->name;
    }

    public function getEmail(): string
    {
        return $this->email;
    }
}
```

🔹 *более простой вариант*
``` php
class NoteDto
{
    public string $title;
    public ?string $text;
    public array $tags;

    public static function byArgs(array $data): self
    {
        $self = new self();

        $self->title = $data['title'];
        $self->text = $data['text'] ?? null;
        $self->tags = $data['tags'] ?? [];

        return $self;
    }
}

// use
NoteDto::byArgs($someData)
```

#### Классификация в зависимости от сценариев
DTO можно классифицировать в зависимости от его роли:
* 🔹 **Input DTO**: Используется для обработки входящих данных, например, данных, отправленных клиентом через форму или JSON-запрос.
    - Пример: Валидация запросов API.

* 🔹 **Output DTO**: Используется для ответа клиенту, чтобы выдавать данные в удобном и безопасном формате.
    - Пример: Подготовка данных для возврата из REST API.

* 🔹 **Data Aggregation DTO**: Используется для объединения данных из нескольких источников (например, нескольких моделей или API).
