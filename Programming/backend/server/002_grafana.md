#monitor #grafana

- play.grafana.org (RoMaXa1983_grafana)

#### 🔹  Install

- https://grafana.com/grafana/download

- заходим на сервер, 
- устанавливаем по инструкции
- запускаем grafana
`sudo service grafana-server start`
-  после чего може заходить по порту :3000
- заходим под дефолтными кредами
	login - admin
	pass - admin
-  после чего устанавливаем новый пароль
---
#### 🔹 Установка источника данных

-  переходим в /datasources
-  нажимаем `<Add data source>`
-  выбираем источник

На примере TestData DB (в самом низу списка) нажимаем на плашку -> Save & test -> переходим на вкладку Dashboards ->import -> Dasboards / browse  - где видим наши плашку с тестовами данными `<Simple Streaming Example>`

Добавление row (обьединение несколько графиков в один список) наживмаем `<Add panel>` (иконка вверху столбики с плюсиком)-> выбираем `<Add a new row>`, после чего панели будут в этом списке (row), панели можно перетаскивать из списка в список, переименовывать и удалять списки, при удалении списка панели в нем не удаляются.

---
#### 🔹 Панели

-  готовые решения - https://play.grafana.org/
-  панели для MYSQL - https://grafana.com/grafana/dashboards?dataSource=mysql

- создание/редактирование новых панелей (графиков) - 
`Dashboards -> browse -> <New Dashboard> -> <Add a new panel>`

- вверху справа выбираем тип визуализации
- справа, ниже - настройки для визуализации (графиков)

Вкладка Query
	- Data source - выбираем источник данных

Вкладка Transform (https://grafana.com/docs/grafana/latest/panels/transformations/)
	Add field from calculation
		обьединяет потоки данных (если их более одного) 
		и выводить total, или выролнять другие операции
	Reduce
		работает для таблиц, может выводить данные по типу
		max, min и т.д.

< + Query > - Добовляет новый поток данных в панели

---
#### 🔹  Загрузка стороних дашбордов

- https://grafana.com/grafana/dashboards/
	- выбираем нужный дашборд
	- переходим в него и копируем его id
	- в grafana переходим `Dashboard -> browse -> <Import> ->` вставляем в поле скопированый id `-> <Load>->` Пример для Mysql , в поле MySQL выбираем источник данных для mysql `-> <Import>`

---