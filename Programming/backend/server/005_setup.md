#server 

#### 🔹 Cтартовый сетап Linux-сервера

```bash
# Ubuntu/Debian - fast server bootstrap
sudo apt update && sudo apt -y upgrade

# Base tools
sudo apt -y install curl wget git unzip zip jq ca-certificates gnupg lsb-release \htop iotop ncdu lsof net-tools dnsutils ufw fail2ban tmux vim

# Firewall
sudo ufw default deny incoming
sudo ufw default allow outgoing
sudo ufw allow OpenSSH
sudo ufw --force enable

# SSH hardening (recommended after adding SSH keys)
sudo sed -i 's/^#\?PasswordAuthentication.*/PasswordAuthentication no/' /etc/ssh/sshd_config
sudo sed -i 's/^#\?PermitRootLogin.*/PermitRootLogin no/' /etc/ssh/sshd_config
sudo systemctl restart ssh

# Fail2ban
sudo systemctl enable --now fail2ban

# Time sync
sudo timedatectl set-ntp true
```
---
#### 🔹 Еще вариант

Самая частая причина взлома - открытый SSH и слабые настройки по умолчанию.  
Сделай минимум: обнови пакеты, включи фаервол, запрети root-вход, отключи парольный SSH, включи Fail2ban и авто-апдейты безопасности.  
Это 80% защиты для любого VPS.

```bash
# обновляем пакеты
sudo apt update && sudo apt -y upgrade

# создаем пользователя и добавляем в группу sudo
sudo adduser admin
sudo usermod -aG sudo admin

# Запрет входа под `root` и по обычному паролю, только по SSH-ключу и под созданным пользователем.
sudo sed -i 's/^#\?PermitRootLogin.*/PermitRootLogin no/' /etc/ssh/sshd_config
sudo sed -i 's/^#\?PasswordAuthentication.*/PasswordAuthentication no/' /etc/ssh/sshd_config
sudo systemctl restart ssh

# Фаервол (UFW), Разрешает входящий трафик для SSH (управление), 80 (HTTP) и 443 (HTTPS) портов, все остальные порты закрываем
sudo ufw allow OpenSSH
sudo ufw allow 80,443/tcp
sudo ufw --force enable

# защита для сервера
sudo apt install -y fail2ban unattended-upgrades
sudo systemctl enable --now fail2ban
sudo dpkg-reconfigure -plow unattended-upgrades
```

######  🔸 Дополнительные рекомендации
1. **Смена порта SSH:**  
    По умолчанию SSH работает на 22 порту. Боты в 99% случаев сканируют именно его.
    - Измените в `/etc/ssh/sshd_config` строку `Port 22` на что-то вроде `Port 2244`.
    - Не забудьте сделать `sudo ufw allow 2244/tcp` перед перезагрузкой SSH!
2. **Защита общей памяти (Shared Memory):**  
    Добавьте в `/etc/fstab` строку:  
    `tmpfs /run/shm tmpfs defaults,noexec,nosuid 0 0`  
    Это мешает некоторым видам эксплойтов исполнять вредоносный код в оперативной памяти.
3. **Установка Logwatch:**  
    `sudo apt install logwatch`  
    Эта утилита будет присылать вам на почту ежедневный отчет: кто пытался зайти, какие ошибки возникали, что обновилось.
4. **Двухфакторная аутентификация (2FA):**  
    Если данные на сервере критически важны, можно настроить **Google Authenticator** для SSH. Даже если у кого-то будет ваш ключ, ему понадобится код из приложения на телефоне.

#### 🔹 Настройка  2FA для сервера

- https://habr.com/ru/companies/alexhost/articles/524356/

Для реализации двухфакторной аутентификации (2FA) используется модуль **PAM (Pluggable Authentication Modules)**.

==Установка модуля==
```bash
sudo apt update
sudo apt install libpam-google-authenticator
```

==Настройка для пользователя==
Запустите конфигуратор от имени того пользователя, под которым будете заходить (в вашем случае — `admin`):

```bash
google-authenticator
```

**На вопросы отвечайте так:**
- `Do you want authentication tokens to be time-based?` — **y** (коды по времени).
- **СКАНИРУЙТЕ QR-КОД** в приложении (Google Authenticator, Authy или Яндекс.Ключ) и обязательно сохраните **emergency scratch codes** в надежном месте.
- `Do you want me to update your "/home/admin/.google_authenticator" file?` — **y**.
- `Do you want to disallow multiple uses of the same authentication token?` — **y** (защита от перехвата).
- `By default, a new token is generated every 30 seconds...` — **n** (если нет проблем с синхронизацией времени, лучше оставить стандартное окно).
- `Do you want to enable rate-limiting?` — **y** (защита от перебора).

==Настройка PAM==

Отредактируйте файл конфигурации аутентификации:
```bash
sudo nano /etc/pam.d/sshd
```

Добавьте в самый конец файла строку:
```text
auth required pam_google_authenticator.so
```

> _Если вы хотите, чтобы код запрашивался даже при входе по паролю (хотя вы его отключили), закомментируйте строку `@include common-auth`._

==Настройка SSH-демона==

Теперь нужно заставить SSH запрашивать второй фактор:
```bash
sudo nano /etc/ssh/sshd_config
```

Проверьте или измените следующие параметры:
```text
KbdInteractiveAuthentication yes   # Раньше называлось ChallengeResponseAuthentication
UsePAM yes
```

⚠️ **Важный нюанс:** Если вы используете **SSH-ключи** (как в ваших прошлых шагах), SSH по умолчанию считает аутентификацию завершенной и не запрашивает 2FA. Чтобы он требовал и ключ, и код, добавьте в конец `sshd_config`:
```text
AuthenticationMethods publickey,keyboard-interactive
```

==Перезапуск и проверка==

⚠️ **ВНИМАНИЕ:** Не закрывайте текущую сессию, пока не проверите вход в новом окне!

```bash
sudo systemctl restart ssh
```

> Теперь при входе сервер сначала проверит ваш **SSH-ключ**, а затем попросит **Verification code** из приложения. Без любого из этих компонентов вход станет невозможен.

---