#terminal #tools #monitor #devops #config

- https://codeby.net/threads/ne-xvataet-vozmozhnostej-konsoli-linux-ispolzujte-tmux.67880/
- https://github.com/gpakosz/.tmux

#### 🔹 Установка

```bash
# for ubuntu
sudo apt-get install tmux

#for mac
brew install tmux
```
---
#### 🔹 Пакет менеджер плагинов
- https://github.com/tmux-plugins/tpm

```bash
# Устанавливаем
git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm

# создаем конфигурационный файл
touch ~/.tmux.conf
```
---
#### 🔹Команды

```bash
# запуск менеджера
tmux

# проверяет ативные сессии
tmux ls

# показывает все сессии 
tmux a
```
---
#### 🔹 Горячие клавиши

```
- ctrl+b " - разделить окна по горизонтали
- ctrl+b % - разделить окна по вертикали
- ctrl+b стрелка - переключаться между панелями
- ctrl+b (зажать) стрелка - изменять размер панели
- ctrl+b c - открыть новое окно
- ctrl+b n - переключиться на новое окно
- ctrl+b p - переключиться на предыдущее окно
- ctrl+b w - список окон
- ctrl+b , - переименовать окно
- ctrl+b q - отобразить номер панели
- ctrl+b x - убить панель
- ctrl+b пробел - переключиться раскладку панелей
- ctrl+b { - переместить панель влево
- ctrl+b } - переместить панель вправо
- ctrl+b z - увеличить\уменьшить панель
  
// Копировать
ctrl+b [ - переход в режим копирования
// переходи к нужному разделу
ctrl + пробел - для начала выделения
ctrl + w - скопировать в буфер обмена
ctrl+b ] - вставить из буфера обмена
```
---
#### 🔹 Создание конфигурации

в корне проекта создаем файл `.tmux.conf` после внесения изменений
```bash
tmux source-file ~/.tmux.conf
```

или в самом tmux
```bash
:source-file ~/.tmux.conf
```
---
#### 🔹 Конфиг

```ini
#переключение ctrl+b на  ctrl+a
unbind C-b
set -g prefix C-a
#корректная работы мыши
set -g mouse on
# to enable mouse scroll, see https://github.com/tmux/tmux/issues/145#issuecomment-150736967
bind -n WheelUpPane if-shell -F -t = "#{mouse_any_flag}" "send-keys -M" "if -Ft= '#{pane_in_mode}' 'send-keys -M' 'copy-mode -e'"
# Переключение между панелями одного окна 
# с помощью Alt и стрелок
bind -n M-Left select-pane -L
bind -n M-Right select-pane -R
bind -n M-Up select-pane -U
bind -n M-Down select-pane -D
# Изменить цвет активного окна в панели уведомлений
set-window-option -g window-status-current-bg cyan
# Мониторинг активности. Получать уведомление, если в каком-то из окон 
# произошли изменения (например, что-то было выдано в STDOUT) 
setw -g monitor-activity on
set -g visual-activity on
```
---