#laravel #email #notification

*  🔹  Если при формировании письма, в email определенный строки нужно обернуть в тег, то это можно сделать через класс - **HtmlString**, т.к. если просто добавить теги они ==**не**== будут применены а отобразятся как часть текста. К примеру если у нас в строке есть ссылка

```php
use Illuminate\Notifications\Messages\MailMessage;

$mail = (new MailMessage)
    ->line('Text <a href="some-path">Link</a>')
```

то в email данная строка так и будет выведена. ==**Чтоб вывести именно как ссылку нужно обернуть HtmlString**==

```php
use Illuminate\Notifications\Messages\MailMessage;
use Illuminate\Support\HtmlString;

$mail = (new MailMessage)
    ->line(new HtmlString('Text <a href="some-path">Link</a>'))
```

*  🔹  Для кастомизации встроенных шаблонов для email, их нужно сначала опубликовать (если они еще не опубликованы)
```bash
php artisan vendor:publish --tag=laravel-mail
```
после он буду находиться -  `resources/views/vendor/mail`