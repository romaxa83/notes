#php #laravel 

#### 🔹 Получение данных

```php
// получение значения name
$request->post('name');
$request->input('name');
$request['name'];

// вернет все данные
$request->all();

// вернет все данные, которые описаны в rules (отвалидтрованые данные)
$request->validated();

// получение query параметров
$request->query();

// получении пути
$request->path();

// проверка пути по шаблону
$request->is('admin/*');

// получение url,без query параметров
$request->url();

// получение url,с query параметрами
$request->fullUrl();

// получение метода
$request->isMethod('post');

// получение занчения из массива
$request->input('products.0.name');

```
---
#### 🔹 Фильтрация данных

> Также можно отнаследоватся от use `App\Core\Abstractions\SecureFormRequest` в нем появляется еще один метод `filter` которые позволяет отфильтровать пришедшии данные.

```php
// если пользователь менеджер,удаляем поле first_name
public function filter(array $attributes): array
{
    if($this->user()->isManager()){
        unset($attributes['first_name']);
    }

    return $attributes;
}
```
---
#### 🔹 Хелпер

```php
// получение всех query-параметров
request()->input()

// получение по ключу
request()->input('user_id')
```
---
#### 🔹 Передать данные из кастомного правил в реквест

```php
// в rule
request()->merge(['user' => $model]);

// чекрез реквест получаем
$request['user'];
```
---
