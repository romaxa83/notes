#php #laravel #helpers

 - https://laravel.com/docs/8.x/helpers

#### 🔹 COЗДАНИЕ СОБСТВЕННЫХ ХЕЛПЕРОВ (через composer)

==создаем файл app/Helpers/helpers.php (можно в любом другом месте) где описываем нужную функцию в таком формате==
```php
if (!function_exists('my_helpers')) {
    function my_helpers($data)
    {
        // ...
    }
}
```


==затем в composer.json в секции autoload прописываем путь к файлу с хелперами==
```php
"autoload": {
	"files": [
		"app/Helpers/helpers.php"
	],
	"psr-4": {
		"App\": "app/"
	}
},

```

==запустить команду==
```bash
composer du
```
---
#### 🔹 COЗДАНИЕ СОБСТВЕННЫХ ХЕЛПЕРОВ (через serviceProvider)

==создаем файл app/Providers/HelpersLoaderProvider.php в методе register регистрируем файл с хелперами==
```php
public function register() 
{ 
	$file = app_path('Helpers/helpers.php'); 
	if (file_exists($file)){ 
		require_once($file); 
	} 
}
```

==если в папке несколько файлов с хелперами то подключаем так==
```php
public function register() 
{ 
	foreach (glob(app_path() . '/Helpers/*.php') as $file) { 
		require_once($file); 
	} 
}
```


> регистрируем наш сервис провайдер в приложении в файле `config/app.php` в массиве  `providers` прописываем наш провайдер `App\Providers\HelpersLoaderProvider::class` 

> если создан хелпер в виде класса с методами и пространством имен, то прописываем в файле `config/app.php` в массиве aliases `'Helper' => App\Helpers\Helper::class\` после чего в коде можно использовать через Helper

---
#### 🔹 Проверка пароля

```php
// первый параметр - проверяемый пароль, второй - существующий
password_verify()
```
---
#### 🔹Преобразование многомерного массива в одномерный 

Хелпер `array_dot()` позволяет преобразовывать многомерный массив в одномерный, который использует точечную запись представления вложенностей.

```php
$data = [
    'user' => ['username' => 'truehero'],
    'app' => ['admin' => ['name' => 'truehero'], 'created' => 'now']
];
 
$dot_array = array_dot($array);
// [user.username] => truehero, [app.admin.name] => truehero, [app.created] => now

```
---
#### 🔹 Path

```php
$public_path = public_path(); // /var/www/public

$path = public_path('js/app.js'); // /var/www/public/js/app.js
```
---
#### 🔹 Str

```php
Str::random();

// от переданого кол-ва, переводит слово ао мн. или ед. число
Str::plural('child', count())

// Функция Str::mask может использоваться для замены конфиденциальной информации,
// например номера кредитной карты, на символы-заменители
$creditCard = '4444666644447788';
$hideCreditCard = Str::mask($creditCard, '*', 4, 8);

dd($hideCreditCard); // "4444********7788"

// определяет, содержит ли строка заданное значение (с учетом регистра)
$contains = Str::contains('This is my name', 'my'); // true
```
---
#### 🔹 Получение данных из массива/объекта

```php
data_get($data, $key, ?$defaultValue);
```

 позволяет получить из массива/объекта данные по ключу в формате разделения ключей через точку, принимает 2-основных параметра; `data` - это исходный массив, или объект, `key` - ключ, по которому мы хотим получить данные. и 1-необязательный параметр, это значение по умолчанию, если ничего не было найдено

```php
$data = [
    'users' => [
        ['name' => 'Alex', 'iq' => 100500],
        ['name' => 'Dmitry','iq' => 99]
    ]
];

data_get($data, 'users.0.name'); // Alex

// чтоб получить все значения
data_get($data, 'users.*.iq');
```
---
#### 🔹 abort()

```php
# выбрасывает исключения HttpException, принимает 2 основных аргумента abort($code, $message = ''), где
# $code - http-код ответа, $message - отображаемое сообщение.

abort(403, 'Нет прав');

# есть улучшеная функция abort_if(), которая первым параметром принимает условие

abort_if(true, 404); // выбросит исключение
abort_if(false, 404); // никогда не произойдёт
```
---
#### 🔹 Где находятся хелперы

```php
vendor->laravel->framework->src->Illuminate->Support->helpers.php
```
---
#### 🔹 Session

```php
// доступ к сесси
session();

// проверка на существование локали
session()->has('locale');

// получение локали
session()->get('locale');
```
---
#### 🔹 Request

```php
// получение данных по request
request();
request()->input('user_id'))


```
---
#### 🔹 Env
```php
// получение окружения
App::environment('local')
```
---
#### 🔹 Доп. хелпреры

```php
if (!function_exists('to_bool')) {

    function to_bool($value): ?bool
    {
        return filter_var($value, FILTER_VALIDATE_BOOLEAN,  FILTER_NULL_ON_FAILURE);
    }
}

if (!function_exists('json_to_array')) {
    function json_to_array(?string $jsonString = ''): array
    {
        return json_decode($jsonString, true) ?: [];
    }
}

if (!function_exists('array_to_json')) {
    function array_to_json(array $array, $options = 0): string
    {
        return json_encode($array, $options);
    }
}
```
---