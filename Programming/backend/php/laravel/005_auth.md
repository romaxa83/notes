#php #laravel #auth

#### 🔹 AUTH

Фасад `Auth` (Аутентификация) предназначен для аутентификации он сверяет введенные учетные данные с сохранёнными если это не удается сделать, то показывается `HTTP-код 401`. Авторизация же проверяет разрешено ли вам выполнение определеного действия подробнее про авторизацию в разделе `rbac`. Авторизация всегда происходит после Аутентификации

```php
// залогинить пользователя
// обязательно что бы user имплементировал interface Authenticatable
use Illuminate\Support\Facades\Auth

Auth::login($user);

// разлогинивает пользователя
Auth::logout();

// вернет обьект залогининового пользователя
Auth::user();

// Залогинит пользователя,если в бд найдет его по переданому email и паролю,иначе вернет false
Auth::attempt([
	'email' => $request->get('email'),
	'password' => $request->get('password'),
]);

// проверяет залогининый пользователь или нет (вернет true or false)
Auth::check();

// аутентификация пользователя для одного запроса (если не нужно хранить состояние)
Auth::once()

```
---