#php #laravel #upload

#### 🔹 Загрузка файлов

 > в файле `config/flysystem` находятся настройки куда загружать файлы к примеру грузим в `public` (храниться будут в `storage/app/public`), чтобы ларавел указать эту папку для хранения нужно создать ссылку в `public` на папку `storage/public/app`, используем команду

```bash
php artisan storage:link

// или создать самому
ln -s /app/storage/app/public /app/public/storage
			|						|
	путь к файлу				ссылка
```

```php
// простое сохранение

$request->file('image')->store('uploads', 'public'); , 

// возвращает путь к файлу, где: 
//     public - имя диска которое мы используем из config/flysystem
// 	   uploads - имя директории(где будет распологаться файл) относительно корня файловой системы
```
---

