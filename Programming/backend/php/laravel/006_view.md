#php #laravel #html

#### 🔹 Пагинация

 > В контроллере получаем записи с пагинацией `$post = Post::paginate(2)`; - будет выводить по две записи для пагинации в виде убираем секцию с пагинацией и вставляем `{{ $posts->links() }}`

 ==для изменения верстки пагинации в консоли прописываем==
 ```bash
  php artisan vendor:publish --tag=laravel-pagination
  
  # после чего вид и вендора скопируеться в папку resources\views\rendor\pagination где его можно изменить (по умолчанию используеться дефолтная пагинация)
 ```
---
#### 🔹 Blade

```php
// в виде,в самом вверху указываеться что мы используем шаблон 'layouts.app'
@extends('layouts.app')

// подключает файл(с разметкой)
@include('admin.errors')


@section('content')
//разметка
@endsection

# это разметка встроеться в шаблон(который подключен выше), в место где будет такая конструкция @yield('content'), вместо 'content' может быть любое название(но оно должно быть одинаковым и в @section и в @yield)


// цикл
@foreach($items as $item)
	{{$item -> id}}
	{{$item -> name}}
@endforeach

// роуты для ссылок
<a href="{{ route('categories.create') }}">...</a>

// старое значение для инпутов
<input ... name="name" value="{{ old('name') }}">

// вывод данных
{{ $post->text }}
// если в данных есть html-теги ,чтобы их отрендерить используеться
{!!$post->text!!}

// crsf-токеном для безопастности в форме
@csrf
// или
{{ csrf_field() }}

// Права доступа (через gate)
@can('название_роли',обьект)
@endcan

// проверка авторизован ли пользователь
@if(auth()->user())
// The user is authenticated.
@endif

// альтернатива
@auth
// The user is authenticated.
@endauth

// проверка на гостя
@if(auth()->guest())
// The user is not authenticated.
@endif

// альтернатива
@guest
// The user is not authenticated.
@endguest

// с else
@guest
// The user is not authenticated.
@else
// The user is authenticated.
@endguest


// подключение предстовление при определеном условии
@if($post->hasComments())
    @include('posts.comments')
@endif

// альтернатива
@includeWhen($post->hasComments(), 'posts.comments');


# подключить представление, если оно существует
@if(view()->exists('view-name'))
@include('view-name')
@endif

# альтернатива
@includeIf('view-name')
```
---
#### 🔹 Создание своих  BLADE команд

> нужно зарегистрировать свою команду в `app/Providers/AppServiceProvider.php` в методе `boot()` нужно возвращаем не просто обработанную строку, а синтаксис PHP, выводящий её. после чего почистить кеш шаблонов `php artisan view:clear`

```php
// данная команда переводит строку в верхний регистр
public function boot()
{
    Blade::directive('myupper', function ($string) {
        return "<?php echo strtoupper ($string); ?>";
    });
}

// использовать
<span> @myupper($post->title) </span>
```
---
#### 🔹 @FORELSE

```php
# Упрощаем циклы

@if($users->count() > 0)
    @foreach($users as $user)
        <li>{{ $user->name }}</li>
    @endforeach
@else
    <p>Пользователей не найдено</p>
@endif

// Заменяем на
@forelse ($users as $user)
    <li>{{ $user->name }}</li>
@empty
    <p>Пользователей не найдено</p>
@endforelse
```
---
#### 🔹 @EACH

> Комбинируем цикл и подключение шаблона в одну строку. первый аргумент — представление, которое нужно отрисовать для каждого элемента массива. Второй — массив или коллекиця для перебора. Третий — имя переменной для текущей итерации в представлении. Также можно передать необязательный четвертый параметр — представление, которое будет использовано, если массив пуст.

```php
@foreach ($users as $user)
    @include('components.userdetail', ['user' => $user])
@endforeach

// Заменяем на
@each('components.userdetail', $users, 'user')
// или
@each('components.userdetail', $users, 'user', 'components.usernotfound')
```
---
#### 🔹 @JSON

```php
# передаём массив в шаблон в формате json

<script>
    var users = {!! json_encode($users) !!};
</script>

// Заменяем на
<script>
    var users = @json($users);
</script>
```
---
#### 🔹 @VERBATIM

> Если нужно вывести несколько `javascript-переменных`, то можно обернуть код этой директивой и тогда не нужно будет ставить символ `@`  перед каждым оператором вывода `Blade`

```php
<div class="container">
    Hello, @{{ name }}.
    Date, @{{ date}}.
</div>

// Заменяем на
@verbatim
<div class="container">
    Hello, {{ name }}.
    Date, {{ date}}.
</div>
@endverbatim
```
---
#### 🔹 @ISSET & @EMPTY

```php
# удобные сокращения для проверки

@if(isset($records))
    // $records установлена значением, отличным от NULL
@endif
@if(empty($records))
    // $records пуста
@endif

// Заменяем на
@isset($records)
    // $records установлена значением, отличным от NULL
@endisset
@empty($records)
    // $records пуста
@endempty
```
---
#### 🔹 @PHP

```php
# Используем команду для встраивания «чистого» PHP кода в шаблон

@php
    // используем php в шаблоне
    $val3 = $val1 + $val2;
@endphp
```
---
#### 🔹 @PUSH & @STACK

> Именные стеки. В основном удобно для подключения `javascript-библиотек`, требующихся для дочерних шаблонов.

```php
// пушим в стек в дочернем шаблоне
@push('scripts')
    <script src="/example.js"></script>
@endpush

// отрисовываем из стека в родительском
<head>
    @stack('script')
</head>
```
---
#### 🔹 @INJECT

Внедрение любого класса из сервис-контейнера. Первый аргумент — переменная, куда будет помещен сервис. Второй — имя класса, который вы хотите извлечь

```php
@inject('metrics', 'App\Services\MetricsService')
<div>
    Monthly Revenue: {{ $metrics->monthlyRevenue() }}
</div>
```
---
#### 🔹 @HASSECTION

```php
# Проверяет, есть ли содержимое в данной секции

@hasSection('navigation')
    <div class="pull-right">
        @yield('navigation')
    </div>
    <div class="clearfix"></div>
@endif

```
---
#### 🔹 ERROR

```php
# для вывода ошибок, проверяим их наличие в сессии

@if (session('error'))
<div class="alert alert-danger">{{ session('error') }}</div>
@endif

```
---
#### 🔹 @LOOP

 - https://laravel.com/docs/master/blade#the-loop-variable

```php
# Внутри цикла foreach вы можете проверить является ли текущая запись первой/последней, просто используя переменную $loop.

@foreach ($users as $user)
    @if ($loop->first)
        Это первая итерация.
    @endif
    @if ($loop->last)
        Это последняя итерация.
    @endif
    <p>Это пользователь {{ $user->id }}</p>
@endforeach

# можно использовать в двух уравневом цикле чтоб получить родительский элемент

@foreach ($users as $user)
    @foreach ($user->posts as $post)
        @if ($loop->parent->first)
            This is first iteration of the parent loop.
        @endif
    @endforeach
@endforeach

```
---










