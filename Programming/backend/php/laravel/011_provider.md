#php #laravel 

#### 🔹 ServiceProvider 

> Является центральным местом при начальной загрузке приложений, все основные сервисы Laravel загружаются через сервис-провайдеров

```php
//команда для создания  провайдера
php artisan make:provider MyServiceProvider
```

> метод `boot` - все приложение загруженно, но вида не отредерены,в него можно вставлять логику которую нужно выполнить перед рендером страниц

---
#### 🔹 Прокидываем интерфейс

 > везде можно его инжектить он будет подставлять рабочий класс

```php
protected function bindInterfaces()
{
    $this->app->bind(
        UserServiceInterface::class,	//интерфейс
        UserSimpleService::class			// реализация интерфейса
    );
}	
```
---
