#php #laravel #queue

#### 🔹 Info

> Очередь (`queue`) некий пул задач реализованых по принципу "первый пришел - первый ушел"

> Задача (`job`) - это целостный элемент очереди (очередь может быть пустой или состоять из одной и более задач)

> Воркер (`worker`) - это приложение которое прослушивает очередь и выполняет задачи

---
#### 🔹 Queue

Для реализации очередей в laravel, нужно две таблицы (если использовать database)
- `jobs (php artisan queue:table)`,
- `failed_jobs (php artisan queue:failed-table)`

> создать задачу (создаст папку Jobs с задачами) `php artisan make:job EmailSendJob`

 в классе задачи присутствую трейты 
 - `Dispatchable` - позваляющие ставить в Очередь через статический метод `dispatch`, если нужно выполнить сразу используем `dispatchNow` 
- `InteractsWithQueue` - управление обьектом очереди, удалить очередь, узнать сколько попыток, заново запустить(`realese`) 
- `Queueable` - логика работы самой задачи
- `SerializesModels` - задает поведение для задачи при котором модель переданная в задачу не будет вся передоваться в очередь ,а передаеться ее `id`, и во время запуска задачи автоматически произойдет извлечении из бд

==задать имя очереди (по умолчанию - 'default')==
```php
# в конструкторе задачи
$this->onQueue('name_queue')
```

> также в классе можно указать публичное свойство `public $tries = 3` которое указывает кол-во попыток запустить задачу если она провалилась

> настройки очереди в файле `config/queue.php` по умолчанию задачи выполняются сразу (`sync`) чтоб этого не происходил и очередь работала с бд в .env `QUEUE_CONNECTION=database`

> параметр `'retry_after'` - если задание выполнялось `[retry_after]`сек без удаления, то задача вновь вернется в очередь для повторного запуска

---
#### 🔹 Запустить очередь

```php
# через dispatch (пишет в таблицу)
$job = newBlogAfterCreateJob($data);
$this->dispatch($job);

# или использовать хелпер
dispatch($job);
dispatch_now($job)
# у хелпере можно вызывать по цепочки метода для настройки задачи

# также на событии (или job) можно вызвать статический метод dispatch, и в него передат данные
SomeEvent::dispatch($request->input('body'));
SomeJob::dispatch(someData);

# При запуске можно цепочкой передовать настройки для очереди задержка на 10 мин. (в бд за это отвечает поле available_at)
SomeJob::dispatch(someData)->delay(now()->addMinutes(10));
```

 > Крупная задача может быть разбита на подзадачи (каскад задач), и если одна из задач не будет  выполнена , то последующии задачи также не выполняться, для этого используется статический метод `withChain`, который принимает массив подзадач

```php
# первым выполниться основная задача, а затем подзадачи по очереди

SomeJob::withChain([
	new App\Jobs\FirstSubJob(someData),
	new App\Jobs\SecondSubJob(someData),
])->dispatch(someData);
```
---
#### 🔹 Консольные команды

 > `php artisan queue:work` - запускает процесс обработки задач как демон, все изменения сделаные в коде после запуска приняты не будут, т.е. после апдейта кода потребуется перезапуск команды (меньше потребляет памяти, ⚠️ реком. в продакте)

```php
# сначала выполнит все задачи из очереди queueName1, затем queueName2
php artisan queue:work --queue=queueName1,queueName2


// кол-во попыток выполнения задачиесли она закончилась не удачей
php artisan queue:work  --tries=3; 

// php artisan queue:listen - запускает процесс обработки задач указаной очереди,изменения сделаные в коде будут приняты (много потребляет памяти, реком. в dev)

// мягкий перезапуск демона после того как тот завершит выполняемую задачу
php artisan queue:restart;

// просмотр таблицы проваленых задач
php artisan queue:failed;

// возврат в очередь всех проваленых задач
php artisan queue:retry all;

// возврат проваленой задачи (id=5) в очередь выполнения
php artisan queue:retry 5;

// убрать проваленую задачу с id = 3
php artisan queue:forget 3;

// полностью очистить таблицу от проваленых задач
php artisan queue:flush;
```
---
#### 🔹 HORIZON

Для отслеживание очередей можно использовать пакет horizon
```bash
composer require laravel/horizon
```

> но использовать его можно только если очереди используют redis `QUEUE_CONNECTION=redis`

```php
// публикует конфиги
php artisan horizon:install

// запускает горизонт (доступно по /horizon/dashboard)
php artisan horizon
```
---
#### 🔹Проверка запуска процессов

```bash
ps ax | grep artisan
```

---