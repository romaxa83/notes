#php #laravel #cache

#### 🔹 Basic use

```php
// получение данных из кеша по ключу key
Cache::get(key);

// поместить в кеш данные(value) с ключом(key)
Cache::put(key,value);

// вытянет данные из кеша по ключу(key), если их нет то выполниться анонимная функция(function) которая вернет и запишет в кеш данные
Cache::rememberForever(key,function);

// работает как и Cache::rememberForever,только указываеться еще параметр time - который указывает сколько врмени будит жить кеш
Cache::remember(key,time,function);

// удаляет весь кеш
Cache::flush();
```
--- 
#### 🔹 Tags

> Для того чтобы очищать конкретные данные в кеше при их изменении(но при этом не трогать другие данные) нужно использовать теги

```php
Cache::tags('region')->rememberForever('region_'.$id,function(){
	return $this->region
});

// А потом, при изменении данных их очищаем
Cache::tags('region')->flush();

// Можно задавать несколько тегов через массив
Cache::tags(['region',adverts']);
```
---
#### 🔹 Example

```php
// базовый пример (sleep - для проерки работы)

// 60 * 30 = 30min
$products = \Cache::remember('products', 60 * 30, function() use ($request){
   	sleep(2);
    return Product::all();
});

// и при изменении или обновлении через событие очищаем кеш
```
---