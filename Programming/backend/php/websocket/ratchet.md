#php #package #websocket


- https://www.youtube.com/watch?v=wXCORk5xXPs&list=PLoonZ8wII66je1zXdGrrHgExx6J0WQYKd&index=2
- https://github.com/ratchetphp/Ratchet/ 

#### 🔹 INFO

реализация клиент-клиент (сервер выступает посредником) применяется для чатов создается класс (к примеру ChatSocket) который реализует интервейс `Ratchet\MessageComponentInterface;`, создается консольный сервер который буде прослушивать сокеты и отправлять сообщения всем клиентам

==на стороне клиента js получает сообщения и выводит их==
```js
var conn = new WebSocket('ws://192.168.126.102:8080');

    conn.onopen = function(e) {
       console.log(e);
    };

    conn.onmessage = function(e) {
    	console.log('Получены данные: ' + e.data);
    };

    function send(){
        let data = 'Данные для отправки: ' + Math.random();
        conn.send(data);
        console.log('Отправленно: ' + data);
    }
```

реализация сервер-клиент (из сервера отправляются сообщения на клиент) без настройки `redis` и `nodejs` ставим библиотеку `composer require react/zmq` (перед установкой необходимо установить расширение `zmq` для `php`)

---

 
