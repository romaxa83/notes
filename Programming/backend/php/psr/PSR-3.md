#php #psr 
**[PSR-3](https://www.php-fig.org/psr/psr-3/)** — это стандарт для работы с логированием в PHP, определённый PHP-FIG (PHP Framework Interoperability Group). Этот стандарт описывает интерфейс для создания логгеров, чтобы обеспечить единый подход к логированию в приложениях и библиотеках PHP.

**Основные аспекты PSR-3**

#### 1. **Интерфейс `Psr\Log\LoggerInterface`**
Основу PSR-3 составляет интерфейс `LoggerInterface`, который определяет методы для логирования сообщений различного уровня. Основные методы:
* 🔸 `emergency(string|Stringable $message, array $context = []): void`
* 🔸 `alert(string|Stringable $message, array $context = []): void`
* 🔸 `critical(string|Stringable $message, array $context = []): void`
* 🔸 `error(string|Stringable $message, array $context = []): void`
* 🔸 `warning(string|Stringable $message, array $context = []): void`
* 🔸 `notice(string|Stringable $message, array $context = []): void`
* 🔸 `info(string|Stringable $message, array $context = []): void`
* 🔸 `debug(string|Stringable $message, array $context = []): void`
* 🔸 `log($level, string|Stringable $message, array $context = []): void`

Метод `log($level, string $message, array $context)` является универсальным и принимает уровень логирования в виде строки или перечисленного значения.

#### 2. **Уровни логирования**
Стандарт определяет восемь уровней логирования, соответствующих их важности:
* 🔹 `emergency`: Система недоступна.
* 🔹 `alert`: Необходимы немедленные действия (например, сбой системы).
* 🔹 `critical`: Ошибка, из-за которой система может не работать стабильно.
* 🔹 `error`: Ошибка, которая препятствует выполнению задачи.
* 🔹 `warning`: Потенциальная проблема или предупреждение.
* 🔹 `notice`: Событие, о котором стоит знать, но которое не является критическим.
* 🔹 `info`: Общая информация о ключевых событиях.
* 🔹 `debug`: Отладочная информация.

#### 3. **Контекст**
Методы PSR-3 принимают массив `$context`, который можно использовать для передачи дополнительных данных в логи. Это обеспечивает гибкость и стандартизацию для включения детальной информации о логе.
Например:

``` php
$logger->error('An error occurred', ['exception' => $e]);
```

#### 4. **Общее поведение**
- В сообщениях можно использовать плейсхолдеры, которые подставляются из массива `$context`.
Пример:

``` php
$logger->info('User {username} logged in', ['username' => 'John']);
// Сообщение будет преобразовано в строку
User John logged in
```

Все логгеры, соответствующие PSR-3, **должны правильно обрабатывать плейсхолдеры**.

#### 5. **Расширяемость**
PSR-3 служит базовым интерфейсом, на основе которого можно создавать собственные логгеры или адаптировать сторонние, что гарантирует совместимость.

#### Пример использования
Вокруг PSR-3 построено множество популярных библиотек для работы с логированием. Одной из них является `monolog/monolog`, которая соответствует PSR-3.

Пример логгирования с использованием Monolog:

``` php
use Monolog\Logger;
use Monolog\Handler\StreamHandler;

// Создаем новый логгер
$logger = new Logger('application');

// Устанавливаем хендлер для записи логов в файл
$logger->pushHandler(new StreamHandler('path/to/log_file.log', Logger::WARNING));

// Логируем сообщения
$logger->warning('This is a warning');
$logger->error('This is an error');
```

#### Преимущества PSR-3
1. **Унифицированный интерфейс:** Один стандарт логирования для всех библиотек и фреймворков.
2. **Совместимость:** Позволяет легко переключаться между разными библиотеками, поддерживающими PSR-3.
3. **Гибкость:** Позволяет использовать контекст и адаптировать сообщение к различным ситуациям.