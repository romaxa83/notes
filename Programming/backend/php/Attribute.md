#php #attribute
**Атрибуты** (или аннотации) в PHP — это механизм добавления метаданных к классам, методам, свойствам, параметрам методов или функциям. Метаданные могут быть использованы для реализации определенной логики в программе, например, для валидации, маршрутизации, сериализации, тестирования и других целей. Атрибуты были введены в **PHP 8.0**. Атрибуты — это мощный инструмент в PHP, который позволяет добавлять метаданные к различным структурам кода. Они упрощают реализацию рутинных задач и делают код более декларативным и гибким. Особенно полезны в таких областях, как маршрутизация, валидация данных и автоматизация задач.

---

#### Основные концепции
* 🔹 **Место использования**  
Атрибуты могут применяться к:
   - Классам
   - Методам
   - Свойствам
   - Константам
   - Функциям
   - Параметрам методов или функций

* 🔹 **Синтаксис**
   Атрибут записывается с помощью знака решетки `#` в квадратных скобках:
	 
```php
#[Attribute]
```

* 🔹 **Класс атрибута**
   Атрибуты реализуются в виде классов. Классы атрибутов могут определять логику метаданных, а также требования к их использованию через стандартные флаги.

* 🔹 **Работа через рефлексию**
   Метаданные, заданные атрибутами, доступны в рантайме через рефлексию.

#### Пример базового атрибута

🔹 *Создаем атрибут*
```php
<?php

namespace App\Attributes;

use Attribute;

#[Attribute]
class MyAttribute
{
    public string $value;

    public function __construct(string $value)
    {
        $this->value = $value;
    }
}
```

🔹*Используем атрибут*
```php
<?php

use App\Attributes\MyAttribute;

#[MyAttribute('Some metadata')]
class MyClass
{
}
```

🔹 *Извлекаем атрибут через рефлексию*
```php
<?php

$reflectionClass = new ReflectionClass(MyClass::class);
$attributes = $reflectionClass->getAttributes();

foreach ($attributes as $attribute) {
    $instance = $attribute->newInstance(); // Создаем экземпляр атрибута
    echo $instance->value; // Вывод "Some metadata"
}
```

#### Атрибуты с флагами
Класс атрибута может быть ограничен для использования в конкретных контекстах (например, только для классов или методов). Это настраивается с помощью флагов:

**Доступные флаги**
* 🔸 `Attribute::TARGET_CLASS` — применим к классам.
* 🔸 `Attribute::TARGET_METHOD` — применим к методам.
* 🔸 `Attribute::TARGET_PROPERTY` — применим к свойствам.
* 🔸 `Attribute::TARGET_FUNCTION` — применим к функциям.
* 🔸 `Attribute::TARGET_PARAMETER` — применим к параметрам функции/метода.
* 🔸 `Attribute::TARGET_CLASS_CONSTANT` — применим к константам класса.
* 🔸 `Attribute::IS_REPEATABLE` — атрибут может применяться несколько раз к одному элементу.

#### Пример с флагами

```php
<?php

use Attribute;

#[Attribute(Attribute::TARGET_METHOD | Attribute::IS_REPEATABLE)]
class MyMethodAttribute
{
    public string $description;

    public function __construct(string $description)
    {
        $this->description = $description;
    }
}
```

```php
#[MyMethodAttribute('This is method 1')]
#[MyMethodAttribute('And this is method 2')] // Поскольку IS_REPEATABLE, можно указать ещё раз
function myFunction()
{
}
```

### Использование в разных местах:

🔹 *Для классов*
```php
#[MyAttribute('Class metadata')]
class Example
{
}
```

🔹 *Для методов*
```php
class Example
{
    #[MyAttribute('Method metadata')]
    public function myMethod()
    {
    }
}
```

🔹 *Для свойств*
```php
class Example
{
    #[MyAttribute('Property metadata')]
    public string $property;
}
```

🔹 *Для параметров*
```php
class Example
{
    public function method(#[MyAttribute('Parameter metadata')] string $param)
    {
    }
}
```

#### Извлечение атрибутов через рефлексию
Атрибуты извлекаются с использованием методов рефлексии, таких как:

🔹 **`getAttributes()`** — извлекает атрибуты (как массив объектов `ReflectionAttribute`).
```php
$reflection = new ReflectionClass(MyClass::class);
   $attributes = $reflection->getAttributes();

   foreach ($attributes as $attribute) {
       echo $attribute->getName(); // Имя класса атрибута
       $instance = $attribute->newInstance(); // Экземпляр атрибута
       echo $instance->value; // Использование данных атрибута
   }
```

🔹 **Где применить рефлексию**
   - Классы (`ReflectionClass`)
   - Методы (`ReflectionMethod`)
   - Свойства (`ReflectionProperty`)
   - Параметры методов или функций (`ReflectionParameter`)

#### Пример практического применения атрибутов

🔸 **Маршрутизация**
*Реализация маршрутов с помощью атрибутов*
```php
#[Route('/home', methods: ['GET'])]
class HomeController
{
    #[Route('/index', methods: ['GET'])]
    public function index()
    {
    }
}
```

*Извлечение маршрутов в рантайме*
```php
$reflectionClass = new ReflectionClass(HomeController::class);

$attributes = $reflectionClass->getAttributes(Route::class);

foreach ($attributes as $attribute) {
    $route = $attribute->newInstance();
    echo $route->path . ' (' . implode(', ', $route->methods) . ')';
}
```

🔸 **Валидация**
*Пример валидации свойств*
```php
#[Attribute(Attribute::TARGET_PROPERTY)]
class NotEmpty
{
}

class User
{
    #[NotEmpty]
    public string $name;
}

function validate($object)
{
    $reflectionClass = new ReflectionClass($object);

    foreach ($reflectionClass->getProperties() as $property) {
        $attributes = $property->getAttributes(NotEmpty::class);
        if (!empty($attributes)) {
            $property->setAccessible(true);
            if (empty($property->getValue($object))) {
                throw new Exception("Property {$property->getName()} cannot be empty.");
            }
        }
    }
}
```

#### Преимущества использования
* 🔹 **Чистый и структурированный подход.**: Логика вынесена в метаописания (атрибуты), что позволяет избегать громоздкого кода.
* 🔹 **Декларативное программирование.**: Описываем "что надо сделать", а не "как сделать".
* 🔹 **Повторное использование.**: Атрибуты могут быть универсально использованы в разных частях приложения.
* 🔹 **Понятный интерфейс для разработчиков.**