#php #extensions 

- Setup Step Debugging in PHP with Xdebug 3 and Docker Compose:
	https://matthewsetter.com/setup-step-debugging-php-xdebug3-docker/
- О xdebug:
	https://proglib.io/p/using-xdebug/
------------------------------------------------------------------
#### 🔹 Install

```bash
# for Ubuntu

# Установить Xdebug
sudo apt-get install php-xdebug
# подключаем модуль к php
sudo phpenmod xdebug
# выключить модуль
sudo phpdismod xdebug
# перезагрузить php
sudo service php7.0-fpm restart

```

==редактируем файл xdebug.ini==
```ini
;путь /etc/php/7.0/mods-available

zend_extension=xdebug.so
xdebug.remote_autostart=on
xdebug.remote_enable=on
xdebug.remote_handler="dbgp"
xdebug.remote_host=127.0.0.1
xdebug.remote_port=9001
xdebug.remote_mode=req 
xdebug.idekey="PHPSTORM"
```
---
#### 🔹 Настройка phpstorm

переходим `settings/languages&frameworks/php` в поле `interpreter` выбираем текущую версию `php` затем `settings/languages&frameworks/php/dedug` в поле port выбираем порт указанный в настройках, переходим `settings/languages&frameworks/php/dedug/dbgp proxy` в поля заполняем данные из конфига затем выходим из настроек и вверху(toolbar) возле значка `debuger` выбираем `editconfigurate` добавляем новую конфигурацию, выбираем `php web application` настраиваем сервер с которым будем работать(название `localhost` или название проекта по которому заходим в проект, слеш(`/`) в конце указывает прослушиваем все приложение, можно указывать конкретные страницы)

F8 - переход вниз
F9 - переход к следуещему чек поинт

---
#### 🔹 Настройка красивого дебага

копируем всю информации которую выводит phpinfo(); (вставляем в индексный файл в самом вверху) переходим на сайт https://xdebug.org/wizard.php в вводим поле удаляем пробелы и вставляем инфу по phpinf0() и анализируем его и по приведенной инструкции устанавливаем

---
