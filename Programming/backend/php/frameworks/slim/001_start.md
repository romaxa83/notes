#php #slim #framework


Готовое приложение - https://github.com/gothinkster/slim-php-realworld-example-app

https://habr.com/ru/post/452834/


==создание проекта==
```bash
composer create-project slim/slim-skeleton
```

#### 🔹 Полезные пакеты для разработки
##### 🔸 для конфигов
```bash
 composer required "laminas/laminas-config-aggregator"
```

создаем папку config а ней другие директории
- common - для общих настроек
- dev - для настроек в рабочем окружении
- test - для настроек тестового окружения
---
#### 🔹 Middleware

В Slim мидлевары выполняються с низу вверх, т.е. первым выполниться тот который будет внизу.

При создании миделваров нужну имплементить интерфейс `Psr\Http\Server\MiddlewareInterface` и реализовать метод `process(ServerRequestInterface $request, RequestHandlerInterface $handler)`
где 
- `ServerRequestInterface $request` - запрос от клиента
- `RequestHandlerInterface $handler` - объек который будет содержать следующий мидлевар который идет после него или сам action который мы написали

---