#php #laravel #webpack

#### 🔹 WEBPACK

```bash
# запустить webpack
npm run dev # для разработки
npm run prod # для продакта
```

 если надо скопировать ресурсы в public в файле `webpack.min.js`(в корне проекта) прописываем после `mix`

```js
mix.copy('resource/assets...','public/fonts');
```

- 'resource/assets...' - путь где находяться нужные ресурсы
- 'public/fonts' - путь и папка куда нужно скопировать

##### 🔸 Скомпелировать несколько файлов (css,js) в один

```js
mix.styles([
    'resources/assets/admin/bootstrap/css/bootstrap.min.css',
	......,
	.......
],'public/css/admin.css');
```


- `resources/assets/admin/bootstrap/css/bootstrap.min.css` - путь (относительно корня проекта) где находиться файл
- `public/css/admin.css` - путь и в какой файл будет скомпелирован

> после запуска сборки все файлы в массиве соберутся в один файл `admin.css` для js-файлов вместо `styles` используется `scripts`

---