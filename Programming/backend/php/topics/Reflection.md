#php #reflection
**Рефлексия** — это механизм в PHP, который позволяет анализировать структуру классов, методов, свойств, интерфейсов, функций и других элементов кода **в рантайме**. Она даёт возможность извлекать метаданные, получать или изменять значения свойств, вызывать методы и выполнять многие другие действия. Это мощный инструмент для анализа и модификации структуры программ в рантайме. Однако её использование должно быть хорошо обдуманным и оправданным, чтобы избежать снижения производительности и читабельности кода.

Рефлексия является частью стандартной библиотеки PHP и предоставляется через набор классов в пространстве имен `Reflection`.

---

#### Для чего нужна рефлексия?
* 🔹 **Динамическое управление объектами и классами:** Создание экземпляров классов, вызов методов, изменение свойств в рантайме.
* 🔹 **Работа с метаданными:** Извлечение информации о методах, свойствах, параметрах, типах и т.д.
* 🔹 **Фреймворки и инструменты:** Используется в маршрутизации, ORM, DI-контейнерах, автоматической генерации документации, тестировании и других задачах.
* 🔹 **Анализ кода:** Исследование структуры классов и функций для выполнения действий (например, выполнение методов, привязанных к атрибутам).


#### Основные классы рефлексии
PHP предоставляет множество классов для работы с различными аспектами программного кода:

* 🔸 **`ReflectionClass`** - Для анализа классов
* 🔸 **`ReflectionObject`** - Для анализа конкретного объекта 
* 🔸 **`ReflectionMethod`** - Для анализа методов класса
* 🔸 **`ReflectionProperty`** - Для анализа свойств класса
* 🔸 **`ReflectionFunction`** - Для анализа функций
* 🔸 **`ReflectionParameter`** - Для анализа параметров метода или функции
* 🔸 **`ReflectionType`** - Для анализа типов данных в параметрах или возвращаемом значении
* 🔸 **`ReflectionAttribute`** - Для работы с атрибутами (начиная с PHP 8.0)

#### Базовые примеры рефлексии

🔹 *Работа с классами*
```php
<?php

class TestClass {
    public $publicProp;
    private $privateProp;

    public function __construct($value) {
        $this->privateProp = $value;
    }

    public function testMethod() {
        return "Hello from testMethod";
    }

    private function privateMethod() {
        return "This is private!";
    }
}

$reflectionClass = new ReflectionClass(TestClass::class);

// Получаем информацию о классе
echo $reflectionClass->getName(); // TestClass
echo $reflectionClass->isInstantiable() ? "Can be instantiated" : "Cannot be instantiated";

// Создаём экземпляр объекта через рефлексию
$instance = $reflectionClass->newInstance('Private Value');
print_r($instance);
```

🔹 *Извлечение методов*
```php
<?php

$reflectionMethods = $reflectionClass->getMethods(); // Получить все методы
foreach ($reflectionMethods as $method) {
    echo $method->getName(); // Названия методов
    echo $method->isPublic() ? "Public" : "Not public";
}
```

🔹 *Вызов метода*

```php
<?php

$reflectionMethod = $reflectionClass->getMethod('testMethod');
$result = $reflectionMethod->invoke($instance); // Вызываем метод на экземпляре объекта
echo $result; // Hello from testMethod
```

🔹 *Работа со свойствами*
```php
<?php

$reflectionProperty = $reflectionClass->getProperty('privateProp');
$reflectionProperty->setAccessible(true); // Делаем приватное свойство доступным
echo $reflectionProperty->getValue($instance); // Private Value
```

🔹 *Работа с функциями*
```php
<?php

function testFunction($param1, $param2) {
    return $param1 + $param2;
}

$reflectionFunction = new ReflectionFunction('testFunction');

// Получить информацию о функциях
echo $reflectionFunction->getName(); // testFunction
echo $reflectionFunction->isUserDefined() ? "User defined" : "Built-in";

// Вызов функции через рефлексию
$result = $reflectionFunction->invoke(10, 20); // 30
echo $result;
```

🔹 *Работа с параметрами функций*
```php
<?php

$parameters = $reflectionFunction->getParameters();

// Информация по каждому параметру
foreach ($parameters as $param) {
    echo $param->getName(); // Имя параметра
    echo $param->isOptional() ? "Optional" : "Required"; // Обязательный параметр?
    echo $param->hasType() ? (string)$param->getType() : "No type"; // Тип параметра
}
```

🔹 *Работа с атрибутами (PHP 8+)*
```php
<?php

#[Attribute]
class MyAttribute
{
    public $value;

    public function __construct($value) {
        $this->value = $value;
    }
}

#[MyAttribute('Test Metadata')]
class TestClass {};

$reflection = new ReflectionClass(TestClass::class);
$attributes = $reflection->getAttributes();

foreach ($attributes as $attribute) {
    $instance = $attribute->newInstance();
    echo $instance->value; // Test Metadata
}
```

#### Основные методы классов рефлексии

**`ReflectionClass`**
* 🔸 `getName()` - Возвращает имя класса
* 🔸 `isInstantiable()` - Проверяет, можно ли создать объект этого класса
* 🔸 `getMethod($name)`- Возвращает объект `ReflectionMethod` для указанного метода
* 🔸 `getMethods()` - Возвращает массив объектов `ReflectionMethod`
* 🔸 `getProperty($name)`- Возвращает объект `ReflectionProperty` для указанного свойства
* 🔸 `getProperties()` - Возвращает массив объектов `ReflectionProperty`
* 🔸 `newInstance(...$args)`- Создаёт объект класса
* 🔸 `hasMethod($name)` - Проверяет, существует ли метод 
* 🔸 `getAttributes()` - Возвращает массив `ReflectionAttribute` (PHP 8+)

**`ReflectionMethod`**
* 🔸 `getName()` - Имя метода
* 🔸 `getModifiers()` - Возвращает модификаторы доступа (`public`, `private`, `static`)
* 🔸 `isPublic()` - Проверяет, общедоступный ли метод
* 🔸 `invoke($object, ...$args)` - Вызывает метод на заданном объекте

**`ReflectionProperty`**
* 🔸 `getName()` - Имя свойства
* 🔸 `setAccessible(true)` - Делает приватное/защищённое свойство доступным для модификации
* 🔸 `getValue($object)` - Получает значение свойства объекта
* 🔸 `setValue($object, $value)` - Устанавливает значение свойства

**`ReflectionFunction`**
* 🔸 `getName()` - Имя функции
* 🔸 `isUserDefined()` - Пользовательская функция?
* 🔸 `invoke(...$args)` - Вызывает функцию

#### Преимущества использования рефлексии
* 🔹 **Динамичность:** Возможность изменять поведения программы на основе структуры кода.
* 🔹 **Универсальность:** Используется в популярных фреймворках для маршрутизации, валидации, DI-контейнеров и т.д.
* 🔹 **Гибкость:** Позволяет разрабатывать программы без строгого кодирования метаданных.

#### ❗ Недостатки рефлексии
* 🔹 **Сложность:** Код с использованием рефлексии может быть менее читаемым.
* 🔹 **Замедление производительности:** Рефлексия требует больше времени на выполнение задач по сравнению с прямым 1. вызовом методов или свойств
* 🔹 **Потенциальные проблемы безопасности:** При неправильном управлении можно случайно изменить или раскрыть защищённые/приватные данные. 