#php #architecture
**Позднее статическое связывание (Late Static Binding)** в PHP — это механизм, который позволяет корректно ссылаться на класс, вызвавший метод, при использовании статических методов или наследования. Это полезно, когда мы имеем дело с полиморфизмом, и необходимо, чтобы выполнялся код класса-наследника, а не родительского класса, в случае вызова статических методов или доступа к статическим свойствам.

PHP реализует позднее статическое связывание через ключевое слово `static`. Давайте разберемся подробно.

---

### Механизм работы позднего статического связывания
Когда мы вызываем метод или свойство через `self`, в родительском классе всегда используется **родительский класс**, даже если метод был вызван в классе-наследнике. Однако при использовании `static` вызывается **класс, в котором был произведен вызов метода** (на момент исполнения). Именно этот механизм называется поздним статическим связыванием.

**Ключевое слово `static`** в этом случае ссылается на «поздний» класс, который вызвал метод, а не на класс, в котором этот метод определен.

---

### Пример с использованием `self` и `static`

```php
class ParentClass
{
    public static function whoAmI()
    {
        return __CLASS__; // Всегда возвращает ParentClass
    }

    public static function callWhoAmI()
    {
        return static::whoAmI(); // Позднее статическое связывание
    }
}

class ChildClass extends ParentClass
{
    public static function whoAmI()
    {
        return __CLASS__; // Возвращает ChildClass
    }
}

// Прямой вызов
echo ParentClass::whoAmI(); // Вывод: ParentClass
echo ChildClass::whoAmI(); // Вывод: ChildClass

// Используется позднее статическое связывание
echo ParentClass::callWhoAmI(); // Вывод: ParentClass
echo ChildClass::callWhoAmI(); // Вывод: ChildClass
```

✨ Разница в механизме использования:
1. `self` ссылается на класс, в котором метод определен. Это значит, что в примере выше, при использовании `self::whoAmI()` результат был всегда `ParentClass`, даже если вызван внутри `ChildClass`.
2. `static` использует класс, который сделал вызов метода, то есть в момент выполнения. Поэтому при вызове `ChildClass::callWhoAmI()` результат — `ChildClass`.

---

### Зачем нужно позднее статическое связывание?

Позднее статическое связывание решает проблему, когда в условиях наследования и полиморфизма требуется, чтобы статический метод использовал контекст класса-наследника, а не класса-родителя.

---

### Практическое применение

#### 1. **Статические фабрики (Factory Pattern)**
Позднее статическое связывание полезно для реализации фабричных методов в наследуемых классах. Например, вы хотите, чтобы фабрика возвращала экземпляр класса-наследника.

```php
class ParentClass
{
    public static function create()
    {
        return new static(); // Возвращает экземпляр того класса, который вызвал метод
    }
}

class ChildClass extends ParentClass
{
}

$instance = ChildClass::create();
echo get_class($instance); // Вывод: ChildClass
```

В данном случае, фабрика `create()` вернет экземпляр `ChildClass`, хотя метод `create()` определен в `ParentClass`.

---

#### 2. **Общий код в родительских классах**
Позднее статическое связывание позволяет использовать общий код для всех подклассов, который будет корректно работать, даже если он зависит от информации о классе-наследнике.

##### Пример: сохранение в базу данных
```php
class Model
{
    protected static string $table;

    public static function getTableName()
    {
        return static::$table; // Благодаря static покажет значение из наследника
    }
}

class User extends Model
{
    protected static string $table = 'users';
}

class Product extends Model
{
    protected static string $table = 'products';
}

echo User::getTableName(); // Вывод: users
echo Product::getTableName(); // Вывод: products
```

---

#### 3. **Реализация шаблонного метода**
Позднее статическое связывание может использоваться для реализации шаблонных методов, где дочерние классы могут перегружать метод для выполнения специфической логики.

```php
abstract class Report
{
    public static function generate()
    {
        echo static::getTitle();
        // Перечисление элементов отчета
    }

    abstract protected static function getTitle(): string;
}

class SalesReport extends Report
{
    protected static function getTitle(): string
    {
        return 'Отчет о продажах';
    }
}

class InventoryReport extends Report
{
    protected static function getTitle(): string
    {
        return 'Отчет о запасах';
    }
}

// Генерация отчетов
SalesReport::generate(); // Вывод: Отчет о продажах
InventoryReport::generate(); // Вывод: Отчет о запасах
```

Благодаря позднему статическому связыванию шаблонный метод `generate()` вызывает логически правильный метод `getTitle()` из дочернего класса.

---

### Когда использовать позднее статическое связывание?

Используйте позднее статическое связывание (`static`) в следующих случаях:
1. **Когда пишете код в родительском классе** и хотите, чтобы он динамически работал для классов-наследников на основе их контекста.
2. **Когда вам нужно вернуть экземпляр класса-наследника** или работать с его свойствами в статических методах, используя общий метод в родительском классе.
3. **Для реализации фабрик, репозиториев, ORM-моделей**, где статические методы используют свойства или способы обработки наследников.
4. **В шаблонных методах**, которые организуют логику работы через дочерние классы.

---

### Когда **не стоит** использовать позднее статическое связывание?
1. Если ваш код требует явной привязки к родительскому классу и логика не должна зависеть от наследников — в этом случае лучше использовать `self`.
2. Если приложение становится сложно поддерживать из-за чрезмерной зависимости от статических методов. Иногда вместо использования позднего статического связывания лучше применять зависимости или паттерны инверсии управления (IoC).

---

### Полезно знать: Разница между `static` и `self`

- `self` фиксирует, что вызов относится к **классу, где метод определен**.
- `static` позволяет перенаправить вызов к **классу, который сделал вызов**, что используется для полиморфного поведения.

---

### Заключение

Позднее статическое связывание в PHP нужно использовать в рамках полиморфизма, чтобы динамически выполнять методы или использовать свойства зависимого класса, который делает вызов. Это особенно полезно для фабрик, моделей ORM и общего кода, где классы-наследники могут добавлять собственные особенности или данные.

Если у вас есть конкретный пример, напишите, и я помогу с реализацией. 😊