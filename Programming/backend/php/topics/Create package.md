#php #package 


https://www.youtube.com/watch?v=pDyVjREXSa4&fbclid=IwAR25IBq_mPEIqmb8xmWIBeaDzRmjDsCPcW5fM18ldXVCjFp-HI-zGxEIZKs

----------------------------------
#### 🔹 Создание консольного приложения и загрузка его на Packagist

- создаем папку (проекта) и переходим туда
- touch run
- git init

для консольного приложения в начале файла пишем `#! /usr/bin/env php`  (env указывает что путь к итерпритатор php надо взять из переменой окружения) после нужно сделать файл исполняемым `chmod +x run` (run - название файла) теперь можно запускать через `./run` 

- пишем код

структура проекта папка src - где на будет находиться весь код пакета в ней создаем папку с название проекта

- создаем репозиторий на github
- пушим в него

- composer init

после создание файла `composer.json` прописываем в нем секцию autoload после делаем `composer update`, появится папка vendor и в ней файл `autoload`, который мы подключаем
в `index` файле `require_once "vendor/autoload.php"`

- после создания приложения заходим на packagist
- нажимаем submit , в форму вставляем ссылку с github
- перед этим нуно указать вверсию пакета на гите
```bash
git tag -a v1.0 -m "Version 1.0"
git push --tags
```


- устанавливать composer require romaxa/days:1.0
---