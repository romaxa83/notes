#api #info

#### 🔹 `HTTP`

`Http (HyperText Transfer Protocol)` - проток передачи данных, изначально предназначенный для передачи гипертекстовых документов. Предполагает использование клиент-серверной архитектуры.

##### 🔸 Особенности `HTTP`
- Основой `HTTP` является технология "клиент-сервер", т.е. предполагает существование клиентов и серверов
- Работает на прикладном уровне
- Режим постоянного соединения, т.е. устанавливает `TCP` - сессию
- Не сохраняет своего состояния
- Для индентификации ресурсов использует глобальный `URI`

##### 🔸 Структура`HTTP` сообщения
- Стартовая строка, описывающая запрос или статус (успех или сбой). Это всегда одна строка
- Произвольный набор `HTTP` заголовков, определяющих запрос или описывающий тело сообщения
- Произвольное тело, содержащие пересылаемые с запросом данные (например, содержимое `HTML` - форм) или отправляемый в ответ документ. Наличие тела и его размер стартовой строкой и заголовками `HTTP`

##### 🔸 `URL`
`URL (Uniform Recource Locator)` - "единообразный указатель местонахождения ресурса", адрес ресурса в сети Интернет 

##### 🔸 `URI`
`URI (Uniform Recource Identifier)` - унифицированный (единообразный) идентификатор ресурса. Последовательность символов, идентифицирующая абстрактный и физический ресурс. 

##### 🔸 `URN`
`URI (Uniform Recource Name)` - единообразное название (имя) ресурса.

---
#### 🔹 `HTTPS`

`Http (HyperText Transfer Protocol Secure)` - расширение протокола `HTTP` для поддержки шифрования в целях повышения безопастности. Данные в протоколе `Https` передаются поверх криптографических протоколов `SSL` и `TLS`. В отличие от `Http`   c `TCP` - портом 80, для `Https` по умолчанию используется `TCP`-порт - 443 

##### 🔸 Особенности `HTTPS`
- Требует установки сертификатов открытого(public) и закрытого(private) ключа на сервере
- Сертификат открытого ключа (public) подтверждает принадлежность данного открытого ключа владельцу сайта
- Сертификат открытого (public) ключа и сам ключ посылаются клиенту при установлении соединения
- Закрытый (private) ключ используется для расшифровки сообщений от клиента

##### 🔸 `SSL/TLS`
- `SSL (Secure Sockets Layer)` - криптографический протокол. В 2014 правительство США сообщило об уязвимости в текущей версии протокола, поэтому `SSL` должен быть исключен из работы в пользу `TLS`
- `TLS (Transport Layer Security)` - криптографически проток, обеспечивающий защищенную передачу данных между узлами в сети Интернет

##### 🔸 Особенности `TLS`
- Основан на протоколе `SSL` версии 3.0
- Использует асимметричное шифрование для аутентификации
- Использует симметричное шифрование для конфиденциальности и коды аутентичности сообщение для сохранения целостности данных
- Уровни защиты:
    - шифрование данных - позволяет избежать их перехвата
    - сохранность данных - любое изменение данных фиксируется
    - аутентификация - защищает от перенаправление пользователя

---
#### 🔹 МЕТОДЫ
Последовательность символов указывающая на основную операцию над ресурсом.

- `GET` - получить ресурсы
- `HEAD` - получить данные о ресурсе (тольк его заголовки)
- `POST` - отправка данных, для создания ресурса или для др. целей
- `PUT` - поместить (создать) ресурс (данные по ресурсу должны прийти все (его поля))
- `PATCH` - меняет в ресурсе какуе то часть(не весь), к примеру дату (данные по ресурсу могут прийти не все, то что прийдет то и будет измененно)
- `DELETE` - удалить ресурс
- `OPTION` - Используется для определения возможностей сервре
---
#### 🔹 HEADERS (Заголовки)
строки в `Http`-сообщении, содержащие разделенную двоеточием пару параметр-значение.

Основные группа заголовков:
- General Headers (Основные заголовки) - Должны включаться в любое сообщение клиента и сервера.
- Request Headers (Заголовки запроса) - Используются только в запросах клиента
- Response Headers (Заголовки ответ) - Только для ответа от сервера
- Entity Headers (Заголовки сущности) - Сопровождают каждую сущность сообщения

##### 🔸 **Cache-Control**
Заголовок ответа. Управляет кэшированием ответа на клиентской стороне или прокси-сервере.

```
Cache-Control: public,max-age=3153600
```


- `public,max-age=3153600` - директивы если указать private - только клиент (браузер) может кешировать
- `max-age` - указывает промежуток времени(сек.) на протяжении (86400 - неделя) которого данные являются свежими (не требуются новые запросы) после окончании времени браузер сделает повторный запрос (для проверки актуальных данных), если они есть вернет статус `200` и новый файл, иначе вернет `304` (стоит использовать осторожно так как мы указываем браузеру что файл устарел) 
- `no-store` - запрещает кешировать
- `no-cache` - указывает браузеру сначала проверить ресурс на сервере и если свежей версии нет (`304`) использовать кешированую версию, иначе если (`200`) то использовать новую версию
- `must-revalidate` - используется с `max-age`, похожа на `no-cache` только запрос будет отправляться через время указаное в `max-age`
- `immutable` - способ указать браузеру что файл не измениться
- `stale-while-revalidate` - указывает какое дополнительное время браузер может использовать старый ресурс пока проверяется новая версия
- `stale-if-error` - указывает какое дополнительное время браузер может использовать старый ресурс если с сервера пришла ошибка 5**

##### 🔸 Accept-Language 
Заголовок запроса. Используется клиентом (браузером), чтобы сообщить серверу, на каких языках пользователь предпочитает получать контент. 

**Параметры и синтаксис:**
- **Теги языка (`<language>`)**: Код языка (например, `en`, `ru`) или локаль с указанием региона (например, `en-US`, `ru-RU`).
- **Весовой коэффициент (`q-factor`)**: Параметр `;q=`, указывающий приоритет языка от 0.0 до 1.0 (по умолчанию 1.0).
- **Wildcard (`*`)**: Символ подстановки, означающий «любой другой язык». 

**Пример:** `Accept-Language: ru-RU, ru;q=0.9, en-US;q=0.8, *;q=0.5`  
_(Приоритет: русский (Россия), затем любой русский, затем английский (США), затем любой другой)_.

##### 🔸 Content-Language
Заголовок ответа. Используется сервером, чтобы указать, для какой языковой аудитории предназначен документ. 

**Параметры и особенности:**
- **Список тегов**: Может содержать один или несколько тегов языка через запятую.
- **Назначение**: Указывает на _целевую аудиторию_, а не обязательно на язык самого текста (например, учебник французского для англоговорящих может иметь заголовок `Content-Language: en`).
- **Значение по умолчанию**: Если заголовок отсутствует, считается, что контент предназначен для всех языковых групп. 

**Пример:** `Content-Language: de, en`

##### 🔸 User-Agent
Заголовок запроса. Содержит информацию о программном обеспечении, с по­ мощью которого отправлен запрос. Это может быть браузер, мобильное приложение или любой другой клиент.

Пример: `User-Agent: Mozilla/5.0 (Windows NТ  1О.О; Win64; х64) AppleWebKit/537.36 (KHTML, like Gecko) Сhготе/58.0.3029.110 Safari/537.3`

##### 🔸 Accept
Заголовок запроса. Определяет типы контента, которые клиент готов принять от сервера, например, HTML, JSON, XML и т.д

Пример: `Accept: text/html,application/xhtml+xml,application/xml;q=0.9,image/webp, */* ;q=0.8`

##### 🔸 Content-Type
Заголовок запроса и также ответа. Указывает тип содержимого, отправляемого в теле запроса.

Пример: `Content-Type: application/json`

##### 🔸 Authorization
Заголовок запроса. Используется для передачи информации об аутентификации, например, токен доступа или базовая аутентификация.

Пример: `Authorization: Basic QWxhZGRpbjpvcGVнJHNlc2FtZQ==`

##### 🔸 Status
Заголовок ответа. Содержит код состояния ответа НТТР, который указывает на успех, ошибку или перенаправление запроса.

Пример: `НТТР/1.1 200 ОК`

##### 🔸 Content-Length
Заголовок ответа. Указывает длину содержимого ответа в байтах.

Пример: `Content-Length: 348`

##### 🔸 Date
Общий заголовок. Содержит дату и время, когда запрос был отправлен или ответ получен.

Пример: `Date: Тне, 15 NoY 1994 08:12:31 GMT`

##### 🔸 Connection
Общий заголовок. Определяет, следует ли поддерживать постоянное соединение между клиентом и сервером.

Пример: `Connection: keep-alive`

##### 🔸 Server
Общий заголовок. Cодержит информацию о сервере, на котором обрабатывается запрос.

Пример: `Server: Арасhе/2.4.1 (Unix)`

---
#### 🔹  List of HTTP status codes 

- 200 (For getting,updating information) : For GET methods PUT methods 
- 201 (For сreating ) : For POST
- 204 (For deleting) : For DELETE methods
- 302 Found, 302 Moved Temporarily — запрошенный документ временно доступен по другому URI, указанному в заголовке в поле Location. Этот код может быть использован, например, при управляемом сервером согласовании содержимого. Некоторые[какие?] клиенты некорректно ведут себя при обработке данного кода. Введено в HTTP/1.0.
- 400 Bad Request — сервер обнаружил в запросе клиента синтаксическую ошибку. 
- 401 Unauthorized — для доступа к запрашиваемому ресурсу требуется аутентификация. В заголовке ответ должен содержать поле WWW-Authenticate с перечнем условий аутентификации. Клиент может повторить запрос, включив в заголовок сообщения поле Authorization с требуемыми для аутентификации данными.
- 403 Forbidden — сервер понял запрос, но он отказывается его выполнять из-за ограничений в доступе для клиента к указанному ресурсу. 
- 404 Not Found — Сервер понял запрос, но не нашёл соответствующего ресурса по указанному URL.
- 405 Method Not Allowed — указанный клиентом метод нельзя применить к текущему ресурсу.
- 410 Gone — сервер понял запрос, но данный ресурс удален, и дальнейший запрос по указанному URL будет приводить к такому же результату 
- 422 Unprocessable Entity — сервер успешно принял запрос, может работать с указанным видом данных (например, в теле запроса находится XML-документ, имеющий верный синтаксис), однако имеется какая-то логическая ошибка, из-за которой невозможно произвести операцию над ресурсом.Чаще всего ошибка валидации
- 429 Too Many Requests — клиент попытался отправить слишком много запросов за короткое время.
- 500 Internal Server Error - сервер столкнулся с неожиданной ошибкой, которая помешала ему выполнить запрос.
---
#### 🔹 Идемпотентность

==Идемпотентность== - это забавное слово, которое часто выбивает людей из колеи. Идемпотентность кажется иногда запутанной концепцией, по крайней мере, если судить по академическому определению.

С точки зрения RESTful-сервиса, операция (или вызов сервиса) идемпотентна тогда, когда клиенты могут делать один и тот же вызов неоднократно при одном и том же результате, работая как "сеттер" в языке программирования. Другими словами, создание большого количества идентичных запросов имеет такой же эффект, как и один запрос. Заметьте, что в то время, как идемпотентные операции производят один и тот же результат на сервере (побочные эффекты), ответ сам по себе может не быть тем же самым (например, состояние ресурса может измениться между запросами).

Методы PUT и DELETE по определению идемпотентны. Тем не менее есть один нюанс с методом DELETE. Проблема в том, что успешный DELETE-запрос возвращает статус 200 (OK) или 204 (No Content), но для последующих запросов будет всё время возвращать 404 (Not Found), если только сервис не сконфигурирован так, чтобы "помечать" ресурс как удалённый без его фактического удаления. Как бы то ни было, когда сервис на самом деле удаляет ресурс, следующий вызов не найдёт этот ресурс и вернет 404. Состояние на сервере после каждого вызова DELETE то же самое, но ответы разные.

Методы GET, HEAD, OPTIONS и TRACE определены как безопасные, что также делает их идемпотентными. Прочитайте секцию о безопасности ниже.

---
#### 🔹 Безопасность

Некоторые HTTP-методы (например: HEAD, GET, OPTIONS и TRACE) определены как безопасные, это означает, что они предназначены только для получения информации и не должны изменять состояние сервера. Другими словами, они не должны иметь побочных эффектов, за исключением безобидных эффектов, таких как: логирование, кеширование, показ баннерной рекламы или увеличение веб-счетчика. Созданный произвольный GET-запрос, который не учитывает контекст состояния приложения, следует считать безопасным.

Упрощенно, безопасность означает, что вызов метода не имеет побочных эффектов. Следовательно, такие (безопасные) запросы клиенты могут безопасно совершать неоднократно, не опасаясь изменить состояние сервера. Это означает, что сервисы должны придерживаться определения безопасности для GET, HEAD, OPTIONS и TRACE операций. Не выполнения этого свойства может приводить в заблуждение потребителя сервиса, а также вызвать проблемы для веб-кеширования, поисковых систем и других автоматизированных агентов, которые непреднамеренно будут изменять состояние сервера.

По определению, безопасные операции идемпотентны, так как они приводят к одному и тому же результату на сервере.

Безопасные методы реализованы как операции только для чтения. Однако безопасность не означает, что сервер должен возвращать тот же самый результат каждый раз.

---
#### 🔹 All HTTP Status

1xx: Information
100: Continue
101: Switching Protocols
102: Processing (WebDAV)

2xx: Success
* 200: OK
* 201: Created
202: Accepted
203: Non-Authoritative Information
* 204: No Content - возращаеться после удаления ресурса
205: Reset Content
206: Partial Content
207: Multi-Status (WebDAV)
208: Already Reported (WebDAV)
226: IM Used

3xx: Redirect
300: Multiple Choices
301: Moved Permanently
302: Found
303: See Other
* 304: Not Modified
305: использовать прокси
306: зарезервировано (код использовался только в ранних спецификациях)
307: Temporary Redirect
308: Permanent Redirect (экспериментально)

4xx: Client Error
* 400: Bad Request
* 401: Unauthorized
402: Payment Required
* 403: Forbidden
* 404: Not Found
405: Method Not Allowed
406: Not Acceptable
407: Proxy Authentication Required
408: Request Timeout
* 409: Conflict  - можно использоваться если создается ресурс который уже есть с бд, к примеру пользователь с `email` - который уже есть в бд
410: Gone
411: Length Required
412: Precondition Failed
413: Request Entity Too Large
414: Request-URI Too Long
415: Unsupported Media Type
416: Requested Range Not Satisfiable
417: Expectation Failed
418: I'm a teapot (RFC 2324)
420: Enhance Your Calm (Twitter)
- 422: Unprocessable Entity (WebDAV) - часто используется для ошибок валидации
423: Locked (WebDAV)
424: Failed Dependency (WebDAV)
425: Reserved for WebDAV
426: Upgrade Required
428: Precondition Required
429: Too Many Requests
431: Request Header Fields Too Large
444: No Response (Nginx)
449: Retry With (Microsoft)
450: Blocked by Windows Parental Controls (Microsoft)
451: Unavailable For Legal Reasons
499: Client Closed Request (Nginx)

5xx: Server Error
* 500: Internal Server Error
501: Not Implemented
502: Bad Gateway
503: Service Unavailable
504: Gateway Timeout
505: HTTP Version Not Supported
506: Variant Also Negotiates (Experimental)
507: Insufficient Storage (WebDAV)
508: Loop Detected (WebDAV)
509: Bandwidth Limit Exceeded (Apache)
510: Not Extended
511: Network Authentication Required
598: Network read timeout error
599: Network connect timeout error

---