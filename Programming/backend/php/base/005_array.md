#php #collections 

#### 🔹 Метода по работе с массивами

##### 🔸 Получение элементов

```php
// возвращает текущий эл. массива(на который указывает внутрений указатель)
current($array)

// устанавливает внутренний указатель массива на его первый элемент и возвращает его
reset($array)

// устанавливает внутренний указатель массива на его последний элемент и возвращает его
end()

// перемещает указатель массива вперед на один элемент и возвращает его
next()

// Передвигает внутренний указатель массива на одну позицию назад и возвращает его
prev() 
```

---
#### 🔹 Практические примеры

##### 🔸 Получение первого элемента в цикле
```php
$flag = true;
foreach ($collection as $value)
{
	if ($flag)
        {
		$flag = false;
        }
        else {
                //TODO
             }
}
```

##### 🔸 Удаление из массива пустых значений
```php
array_diff($vproduct_ids, array(''))
```

##### 🔸 Расхождение для многомерных массивов
```php
array_diff(array_map('serialize',$a2), array_map('serialize',$a1))
```

##### 🔸 Пример с многомерным массивом

==ЕСТЬ ТАКОЙ МАССИВ==
```php
$array = [
	0 => [
		'name' => 'admin'
        'description' => 'Администратор'
	],
	1 => [
		'name' => 'user'
        'description' => 'Пользователь'
	],
]
```

==НУЖЕН ТАКОЙ МАССИВ==
```php
$result = [
	'admin' => 'Администратор'
    'user' => 'Пользователь'
]
```

==РЕШЕНИЕ==
```php
 $all_role = ArrayHelper::index($array,'name');
 $result = array_map(function($item){
    return $item['description'];
 }, $all_role);
```
##### 🔸 УБРАТЬ ДУБЛИРУЮЩИЕ МАССИВЫ ИЗ МНОГОМЕРНОГО МАССИВА
```php
private function super_unique($array)
    {
        $result = array_map("unserialize", array_unique(array_map("serialize", $array)));
        foreach ($result as $key => $value) {
            if ( is_array($value) ) {
                $result[$key] = $value;
            }
        }
        return $result;
    }
```
 
---
  
