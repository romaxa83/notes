#php 

#### 🔹 Наследование

##### 🔸 Вызов родительского метода

```php
//  в дочернем классе вызываем родительский класс (который можно дополнить)
parent::назв.метода
```

> от абстрактного класса можно только отнаследоваться, создать объект из него не получиться

> от финального класса нельзя отнаследоваться `final class name_class` финальный метод нельзя переопределить `final public function(){};`


abstract class name_class

в абстрактном классе можно создать абстрактный метод,он пишеться без код
- abstract protected function name();
но его обязательно нужно переопределить(описать его логику) в наследуемом классе


при вызове статического метода(переменной,константы) в класе,используеться
кл.сл. self:: ,но он ссылаеться только на тот метод где был обьявлен(не
наследуеться вниз),если нужно такое наследование используеться кл.сл. static::

---