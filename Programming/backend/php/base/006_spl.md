#php 

#### 🔹 Standard PHP Library (SPL)
- https://habr.com/ru/post/161987/

##### 🔸 SplDoublyLinkedList
есть два принципа извлечения значения из списка: 
 - `FIFO` (First In First Out – первый зашел, первый ушел)
 - `LIFO` (Last In First Out – последний зашел, первый ушел)
 
 С помощью `SplDoublyLinkedList` можно извлекать значения по любому из этих принципов. Следовательно, с его помощью можно легко организовать стек или очередь.

##### 🔸 SplStack
> класс является наследником `SplDoublyLinkedList` и реализует стек - 

```php
$stack = new SplStack();

// добавляем элемент в стек
$stack->push('1'); 
$stack->push('2');
$stack->push('3');

echo $stack->count(); // 3
echo $stack->top(); // 3
echo $stack->bottom(); // 1
echo $stack->serialize(); // i:6;:s:1:"1";:s:1:"2";:s:1:"3";

// извлекаем элементы из стека
echo $stack->pop(); // 3
echo $stack->pop(); // 2
echo $stack->pop(); // 1
```

##### 🔸 SplQueue
> используется для создания очередей

```php
$queue = new SplQueue();

$queue->setIteratorMode(SplQueue::IT_MODE_DELETE);

$queue->enqueue('one');
$queue->enqueue('two');
$queue->enqueue('qwerty');

$queue->dequeue();
$queue->dequeue();

echo $queue->top(); // qwerty
```
---