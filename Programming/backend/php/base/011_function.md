#php 

#### 🔹 Closure

Замыкание(`closure`) - это функция, которая инкапсулирует состояние,
окружавшее ее на момент создания. Инкапсулированное состояние
продолжает существовать внутри замыкания, даже когда окружение
замыкания перестанет существовать.

> ⚠️ Замыкания и анонимные функции - разные понятия. Но в РНР они считаются одним и тем же.

Замыкания в РНР являются объектами.Каждый эк­земпляр замыкания имеет свое внутреннее состояние, доступное че­рез ключевое слово `$this`, так же как внутреннее состояние любого другого объекта в РНР. Также прикреплять состояние можно через метод bindTo();
##### 🔸 Пример:

```php
$closure = function($name) 
{
	return sprintf( 'Hello % s ' , $name );
};
echo $closure("Josh");		//выведет "Hello Josh"
```

Мы можем ссылаться на переменную $closure, по­ тому что значением переменной является замыкание и объект замыкания при реализации использует магиче­ский метод `__invoke()`. РНР находит и вызывает ме­тод `__invoke()` всякий раз, когда пара скобок `()` следу­ет за именем переменной.
##### 🔸 Пример : Прикрепление состояния к замыканию с помощью ключевого слова `use`

```php
function enclosePerson($name) 
{
	return function($doCommand) use ($nаmе) {
		return sprintf(%s, %s, $nаmе, $doCommand);
	};	
};

// Прикрепление строки "Clay" к замыканию
$clay = enclosePerson ('Clay');

// Вызов замыкания
echo $clay('get mе sweet tea!');	//выведет "Clay, get mе sweet tea!"
```

именованная функция `enclosePerson` принимает аргумент `$nаmе` и возвращает 
объект замыкания, который замыкает аргумент `$nаmе`. Возвращаемый объект 
замыкания сохраняет значе­нние аргумента `$nаmе` и вне функции `enclosePerson()`. Переменная `$nаmе` продолжает существовать в замыкании!

---




