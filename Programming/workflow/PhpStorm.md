#tools #php

#### 🔹 Shortcut
* 🔸 *Сигнатура класса*
	Ctrl+F12
*  🔸 *форматирование (выравнивание) кода, строк*
	ctrl + alt + L
* 🔸 *открыть настройки*
	Ctrl + Alt + S 
* 🔸 *курсор на методе или на переменной*
	Alt + shift + 7 - показывает где оно используется
* 🔸 *курсор на подключение к бд*
	Ctrl + Alt + U - диаграмма бд
* 🔸 *курсор на название интерфейса*
	Ctrl + Alt + B - покажет все классы, которые имплементируют данный интерфейс
* 🔸 *выделяет фрагмент где стоит курсор, с каждым нажатием фрагмент увеличивается*
	Ctrl + W
* 🔸 *список всех измененых файлов*
	Ctrl + E
* 🔸 *свернуть/развернуть все методы в классе*
	Ctrl + Shift + +/-
* 🔸 *выделение метода*
	Первое нажатие выделит текст в пределах текущих скобок, следующее будет постепенно расширять выделение
	Ctrl + W (Windows/Linux) или Cmd + W (macOS)
*  🔸 *Генерация (getter,settre,phpDoc...)*
	alt+Ins
* 🔸 *Генерация полей в конструкторе*
	курсор на аргументе alt+enter

#### 🔹 Настройка директорий в проекте
settings->directories

#### 🔹 Добавление Docker
settings -> Build,Execution,Deployment -> Docker

если его нет ,нажимаем плюс

Добавление интерпритатор для php из Docker

settings -> Languages & Frameworks -> PHP
CLI Interpretator ...
+ Docker Compose
в поле server из списка добавляем Docker (если нет создаем , описано выше)
в сервисе выбираем наш сервис с докером (cli - если есть или fpm)

затем в новом окне в поле Lifecycle выбираем один из двух вариантов
если используем php-cli - docker-compose run
если используем php-fpm - docker-compose exec

после нажатие ок , в интерпретаторе появиться php из образа докера,который и выбираем

---
#### 🔹 Настройки Xdebug
settings -> Build,Execution,Deployment -> Debuger

settings -> Languages & Frameworks -> PHP -> Debug
нажимаем Validate
в поле 
Path to create validation script : /home/romaxa/project/auction/api/public - указываем на папку где находиться index.php
Url to validation script : http://127.0.0.1:8081/

еще тут [[xdebug]]

---
#### 🔹 Сравнение файлов

> находимся в одном файле, выбираем View -> CompareWith и выбираем другой файл с которым будем сравнивать

---
#### 🔹 **Плагины**
Laravel Idea - улучшает работу с laravel
CodeGlance - карта кода возле скрола
Mongo Plugin - для работы с монго
.ignore - для файлов .ignore

#### ШРИФТ
Noto Mono (size - 14)

---
> ⚠️ Если IDE зависло (выполняем команду в терминале)
```bash
kill -9 $(pgrep -f phpstorm)
```
---
